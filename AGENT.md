# Takealot 店铺运营数据工具：长期维护档案

最后更新：2026-08-01
项目根目录：`D:\南非店铺数据抓取`
当前维护方式：单 Agent、本机 Windows/PowerShell

## 1. 文件职责

本文件是项目的长期交接与修改记录。任何接手本项目的人员或 Agent，都必须先阅读本文件、`README.md` 和 `docs/PROJECT_STATUS.md`，再开始分析或修改。

## 2. 强制同步更新规则

只要本项目发生有意的项目修改，就必须在同一个任务中同步更新本文件，不允许把更新留到以后。

项目修改包括但不限于：

- 源码、测试、配置、数据库结构或迁移；
- PowerShell、批处理及其他运行脚本；
- 依赖、构建、打包或计划任务配置；
- README、需求、设计、审计、测试报告等项目文档；
- 业务规则、指标口径、安全边界或操作流程。

同步更新至少应完成：

1. 修改顶部“最后更新”日期；
2. 在“修改日志”中新增一条记录；
3. 若项目状态、约束、命令、风险或后续事项发生变化，更新对应章节；
4. 在结束任务前检查 `git diff`，确认项目修改与本文件更新同时存在；
5. 记录实际执行的验证；没有执行的验证必须明确写为“未执行”，不得写成已通过。

数据库采集结果、日志、缓存、备份和日报等正常运行时产物，如果没有被有意纳入版本管理，不单独触发本文件更新；但生成逻辑、格式、目录规则或业务口径发生变化时必须更新。

## 3. 项目目标

这是一个在 Windows 本机运行的 Takealot 店铺运营数据工具，负责：

- 通过官方 Seller API 只读采集 Offer 与 Sales 数据；
- 使用本机 MySQL 8.0 保存全部店铺、指标和竞品历史；
- 生成 HTML、Excel、PNG 日报；
- 提供统一的 Vue 3 + TypeScript 小型 ERP，覆盖经营总览、商品、经营坐标、风险/质量、竞品、报表和用户权限；
- 保留 Streamlit 旧版看板作为临时兼容回退，不作为正式功能入口；
- 更新 NFT102 访客表并生成可追溯核对报告。

## 4. 不可破坏的业务与安全约束

- API 客户端只允许读取操作，不得擅自增加写入、修改或删除店铺数据的请求。
- 真实 API Key 只能放在项目根目录 `.env` 或受控环境变量中；不得进入源码、测试夹具、日志、报表、文档或版本库。
- 日期边界使用南非标准时间（SAST）业务日；不得用本机日期直接替代既定转换。
- ERP 中“最近采集”和竞品历史快照属于记录时刻，必须固定按北京时间（Asia/Shanghai）显示；指标日期仍按 SAST 业务日，两种口径不得混淆。
- `page_views_30_days` 只能称为“近30天浏览量”，不是独立访客数，也不是精确当天流量。
- 商品中心列表的每条商品栏必须直接显示平台 `image_url` 缩略图，图片缺失或加载失败时显示中文“暂无图片”占位；点击商品栏或使用键盘激活时必须在当前视口打开完整商品详情弹窗，不得再把详情重复铺在商品列表下方。弹窗必须保留商品身份、经营指标和唯一的每日销售件数快照，历史区不得再展示流量快照；销售柱图纵轴必须显示整数数字刻度。弹窗支持遮罩、关闭按钮、底部按钮及 Escape 关闭，打开时锁定背景滚动，关闭后恢复原商品栏焦点。
- 经营坐标使用最新近30天浏览量和截至最新可用指标日的近7个自然日下单件数，只表达二维相对位置，不得重新加入四象限区域、明星/潜力/待优化/转化问题分类限制、分位选择、分类筛选卡或十字分界。相对排名只用于图上拉开差异，悬停和明细必须展示真实值。
- 经营坐标只允许绘制同时具备浏览量和下单排名的商品；销量为0保留真实低位，缺失流量或下单指标不得补零或放到坐标中，必须继续保留在“全部商品”区域，并明确显示已定位与缺少坐标数量。
- 经营坐标散点必须使用统一的固定高对比填充色，不得继承按钮默认颜色。散点必须使用无原生等待的自定义悬浮信息卡识别商品，并支持点击复制平台 SKU；复制成功、SKU 缺失或复制失败都必须给出中文反馈。
- 经营坐标必须固定标出横轴“近30天浏览量相对排名”和直立竖排纵轴“近7日下单件数相对排名”；两轴0、25、50、75、100关键排名刻度必须放在图框外并保持高对比，不得与散点重叠，图内可以使用轻量网格辅助读数。悬浮信息卡必须优先展示平台 `image_url` 商品缩略图，并展示平台可售库存、指标截止日期、首次上架/最早记录、最近补货时间和平台明确返回的近30天浏览量；图片必须等比完整显示，图片缺失或加载失败时必须显示中文“暂无图片”占位，不得保留破图图标。信息卡不得被图框裁切，字段标题与数值必须保持清晰可读。
- Vue ERP 全部商品图片入口（商品中心、经营坐标、风险与质量、竞品雷达、运营日报及其弹窗/历史区）不得把平台全分辨率原图直接交给浏览器解码。列表、卡片、表格和小型弹窗图统一使用登录态同源的最长边192像素缩略图；商品中心和风险详情的大图使用最长边640像素缩略图。后端只允许读取`takealot.s3.amazonaws.com/covers_images/`和`media.takealot.com/covers_images/`两个Takealot官方来源，禁止任意重定向，限制源文件字节、像素和允许尺寸，并把转换结果保存为可重建的Git忽略运行缓存。同一缓存键的并发请求必须继续共用同一把锁且只转换一次；锁条目必须按活跃使用者计数，并在成功、失败或异常路径的最后一个使用者退出后移除，不得随历史图片键长期增长。所有图片必须声明固有尺寸并异步解码，长列表继续懒加载；视口外重复卡片应跳过布局/绘制，频繁交互状态不得重新构建未变化卡片的内部虚拟节点。
- 经营坐标“全部商品”必须使用无需横向滚动的自适应卡片覆盖接口返回的全部商品，商品名称及所有数值均允许完整换行，不得截断隐藏。每张卡片必须补齐与悬浮卡一致的商品图片、平台 SKU、近30天浏览量、近7日下单、平台可售库存、首次上架/本库最早记录、最近补货时间和指标截止日期；缺失值必须明确显示，不得因无法进入坐标图而从列表中删除。页面必须提供排序选择器，至少支持按浏览量、下单、库存和商品名称排序，空值在数值降序中排到最后；同时提供平台 SKU 即时搜索，忽略大小写并支持部分匹配，显示当前结果数与商品总数，无匹配时显示中文空态，清空搜索必须恢复全部商品。
- Offers 接口的 `created_at` 是首次上架的优先来源；旧快照缺失时只能标为“本库最早记录”。同一商品后一条平台库存快照高于前一条有效快照时，后一条快照的实际采集时间就是最近补货时间，并记录库存增加数量；该记录不得称为“估算”，没有观察到库存增加时只能显示暂无记录。补货快照采集时间固定按北京时间显示。Offers 接口没有独立近7日流量字段时，不得用近30天浏览量按比例推算或展示近7日流量，也不得把浏览量称为访客数。
- 下单件数图的每日实际值和坐标刻度必须使用整数；不得为了平滑趋势绘制或伪装成实际件数的小数值。
- 相邻30天窗口差值只能称为“30天浏览量窗口净变化”；对于已有历史的商品，不能据此还原精确当天浏览量。
- 只有从全新商品上架第一天开始、并确认此前浏览量为零时，才可能递推每日浏览量；即使如此也仍是浏览量（PV），不是访客数（UV）。
- 缺失流量必须保持缺失，不得补零或伪造。
- Offer 分页必须完整获取后再原子发布快照，任何分页失败都不能发布不完整数据。
- 正式 ERP 默认绑定 `0.0.0.0:8501` 供同一受信任局域网访问；MySQL 仍只允许连接本机 `127.0.0.1`，不得开放数据库端口。公网访问必须另行增加 HTTPS、反向代理和网络访问控制，不得直接暴露 Uvicorn。
- ERP 所有业务接口必须要求登录并在后端执行账号级权限校验。`viewer`、`operator`、`selection`、`admin` 只作为“查看、运营、选品、管理员”四个快速模板；管理员可为每个账号独立增删权限，模板名称不得直接代替接口授权。首个管理员只能从服务器本机 `127.0.0.1` 初始化；密码必须使用带随机盐的 scrypt 哈希保存，浏览器会话必须为可撤销的 HttpOnly、SameSite=Strict Cookie，初始及每次有效使用后的有效期均为7天；同一会话自动续期每1天最多写入一次数据库并同步刷新浏览器 Cookie，避免高频轮询造成持续写入。所有写操作必须校验 CSRF，不得只依赖前端隐藏按钮实现权限。
- 权限至少拆分为店铺经营查看、竞品查看/采集、运营日报查看/待办处理/Excel生成、报表查看/生成、NFT102续写、全量刷新和用户权限管理。选品模板必须能查看及采集竞品、查看运营日报，但不得处理日报待办或生成日报 Excel；运营模板默认拥有除用户管理以外的日常业务权限；管理员模板默认拥有全部权限。现有账号允许在模板基础上逐项覆盖，变更后撤销该账号既有会话；系统必须阻止停用或移除最后一个有效的用户权限管理账号。
- 店铺可见范围必须使用账号—店铺多对多授权，店铺和每账号分配数量均不得写死为6个或2个。普通运营只访问明确勾选的启用店铺；管理员、老板等跨店账号通过独立的“全部店铺（含未来新增）”范围自动取得以后新增店铺，不得依赖角色名称逐店补授权。首个管理员和架构升级前既有账号必须保持全店可见，避免迁移锁出；店铺范围变更必须撤销该账号既有会话。当前未分区的单店业务数据只能登记为唯一“已接入”店铺；预留店铺在采集密钥、业务表`store_id`、计划任务和页面店铺上下文全部完成隔离前必须保持“待接入”，不得把当前店铺数据冒充为新店数据。经营总览、商品、经营坐标、风险、运营日报、全量刷新及既有店铺报表必须拒绝没有已接入店铺授权的账号；竞品雷达和仅含NFT102的报表工作台属于公共模块，只校验账号对应功能权限，不得把店铺分配作为额外前置条件。共用商品缩略图允许拥有店铺、竞品或日报任一查看权限的登录账号读取，但来源白名单、重定向和资源限制保持不变。
- ERP 浏览、筛选和页面切换必须使用 MySQL 数据库级只读事务；只有拥有相应权限的账号点击“刷新全部数据”、采集竞品、处理日报或生成报表时才允许执行对应写流程。每次“刷新全部数据”成功后必须冻结独立的手动日报版本并立即重新核对，不得只更新经营指标。所有非管理员模板账号共享全局1小时刷新冷却；管理员模板账号在保留刷新权限时可以越过冷却，但不得与另一场仍在运行的刷新并发。刷新进行状态、执行账号、最近成功时间和剩余冷却必须由后端统一计算并同步到所有已登录页面，不能用换浏览器绕过；冷却成功时间必须持久化到 MySQL，服务重启后继续有效，失败刷新不启动冷却。
- 运营日报新增定时/手动版本、人工处理、备注或撤销落库后，服务端必须向已登录前端推送变更事件并刷新日报矩阵、待办与全局提醒；断线重连必须补刷新，人工编辑期间不得覆盖未提交输入。完成并验证项目修复后必须运行 `scripts/restart_erp.ps1` 自动重启正式 ERP，并以 `/api/health` 成功作为新代码已经加载的最低验收，不得因后台竞品批次而继续保留旧进程；未完成批次依赖既有断点续爬恢复。
- 运营日报顶部首张汇总卡必须显示“现有商品库存总数”，汇总最新 `OfferCurrent` 中全部现有商品的平台可售库存，并跟随“刷新全部数据”取得的最新 Offers 数据变化，不绑定日报日的次日10:05期末库存。任一现有商品库存缺失时不得补零或展示不完整合计，页面改为显示“—”并标明缺失商品数。
- 运营日报顶部“同周期销量版本差异”必须去掉单位，并统计截至当前日报日仍未处理、带`capture_difference`核对问题的全部历史待办；“库存不平”必须统计同一截止范围内仍未处理、带`stock_continuity`核对问题的全部历史待办；“已确认”仍只统计当前日报日。三个数量不得混用口径。近30天浏览量缺失仍必须保持缺失并在日报矩阵中显示，不得因移除原汇总卡而补零或改变底层口径。
- 运营日报矩阵日期列的“当天订单数”和“平台库存数量”对应单元格必须分别汇总该日报日全部商品的正确采用值，合计只显示数字，不得附加“单”或“件”，且不得随 SKU 搜索、状态筛选或横向滚动位置变化；任一商品的对应值缺失时必须显示“—”和缺失商品数，不得展示部分合计。库存合计优先采用业务日D对应的D+1日早间首次完整成功实采；早间漏爬或不完整时采用D+1当天10:05后的最新完整恢复库存。库存行统一称“平台库存数量（次日实采）”，日期总格必须展示实际库存北京时间；恢复场景还要写明“早间库存漏爬已解决”，不得为已补齐的全部商品重复生成占位。筛选后的全部商品必须一次完整放在同一张横向矩阵中，不得按24列拆成“上一组/下一组”；运营直接使用矩阵底部横向滚动条查看到最后一列。鼠标位于横向滚动条命中区时，普通滚轮必须转换为左右滚动并阻止纵向滚动；离开滚动条后不得改变原有纵向滚动。商品列标题下方只显示 SKU 值，不得添加“平台 SKU”前缀；矩阵内部纵向滑动时商品名称与 SKU 表头必须保持可见，并隔离滚动边界，避免查看较早日期时滚动串到外层页面后失去商品列身份。
- 报表工作台只保留 NFT102 上传、校验、续写和结果下载，不得重新显示“运营日报”页签、截止日期、一键生成 HTML/Excel/PNG 或对应下载卡；侧边栏独立的运营日报模块及核对、合并、Excel 导出流程保持不变。既有离线 HTML/Excel/PNG 导出命令和接口可继续基于当前只读数据集写入 `exports/YYYY-MM-DD/` 以兼容原流程，但不得触发平台接口或数据库写入。
- 异常商品 KPI 必须统计最新可用指标日的去重商品数；异常明细允许同一商品因多种异常保留多行。看板默认显示最新指标日并可手动切换全部历史，Excel、HTML、PNG 默认不得混入历史异常日期。
- 风险与质量中的销量趋势固定比较截至指标日的近3个自然日日均下单件数与近15个自然日日均下单件数：近3日均值较低判为“销量下降”，较高判为“销量上升”，相等不触发；两个窗口均包含指标日，只有系统完整覆盖15日窗口时才判断，已覆盖但无订单的自然日按0件参与平均，历史不足保持未判定，不得补零或重新加入最低销量、单日销量及涨跌百分比门槛。
- 风险与质量页的经营异常必须按运营处理层级展示：`non_buyable`（不可购买）使用暖黄色普通“提示”，其他异常统一使用红色“重点提示”；页面必须支持关键词、提示级别和异常类型组合筛选、结果计数及一键清除。筛选只作用于当前已读取列表，不得改变异常判定、触发采集或写入数据库。每张异常卡片必须支持鼠标点击、键盘回车/空格打开完整详情弹窗，至少复用四象限悬浮卡的商品图片、平台 SKU、平台库存、近30天浏览量、近7日下单、首次上架/本库最早记录及最近补货口径，并增加异常发生日与说明、当前经营数据截止日、平台商品编号、条码、商品状态、售价/建议零售价、转化率、最新日下单金额和有效销售件数。除普通提示级`non_buyable`外，每种异常还必须展示异常发生时的具体触发证据：销量趋势优先显示异常发生日截止的15个自然日每日销量柱状图，并保留近3日/近15日均值及差值；流量转化显示异常时的近30天浏览量、近30天转化率，以及本库截至异常日最多30个自然日的销量总和，覆盖不足30天时必须明确实际记录天数和日期范围；高浏览低转化可同时展示当日判定阈值，低浏览高转化只保留浏览量、销量和转化率三项，不重复展示低浏览边界与高转化边界卡片。疑似断货显示异常时库存、状态和异常日前7日销量，停止更新显示最近采集时间、实际间隔和小时阈值，未知销售状态显示具体未配置状态。销量图和销量汇总必须来自异常发生日对应的历史逐日指标，不得拿当前商品指标冒充历史异常证据；已覆盖的完整销量判定窗口内无订单的日期按0件展示，未完整覆盖的流量异常销量汇总不得补足或伪装成30天。历史异常的发生日与当前经营数据截止日必须分开标注；图片缺失或加载失败显示“暂无图片”。弹窗必须支持遮罩、关闭按钮、底部按钮及 Escape 关闭，打开时锁定背景滚动，关闭后恢复原卡片焦点。
- 运营日报核对周期固定为北京时间当日10:00（含）至次日10:00（不含）；周期内计划10:05触发的早间任务、计划18:00触发的晚间任务、计划次日09:00触发的周期末任务及任意次数的手动刷新必须统一归入同一日报业务日并保留全部不可覆盖版本。例如7月25日10:00至7月26日09:59全部处理7月24日日报；普通HTML/Excel/PNG日报也使用该业务日作为数据截止日期。四个Windows任务必须忽略并发重复实例，并在错过计划时间后尽快补跑。
- 运营日报定时和手动采集都必须具备分层采集保护：HTTP请求对传输异常、429和5xx退避重试；整批失败后必须创建新客户端重试，并至少包含一次`trust_env=False`直连备用以绕过错误系统代理；Windows任务必须把CLI非零退出码向上传递并按5分钟间隔最多重启3次。只有Offers与Sales均完整成功才允许冻结版本，禁止用陈旧缓存伪装本次成功。
- 每次整批尝试必须记录业务日、时段、序号、策略、开始/结束时间、Offers/Sales运行ID、阶段状态和脱敏原因；记录同时进入`logs/takealot-ops.log`与`daily_report_runs.counts`并在页面可读。所有策略耗尽后才允许标记漏爬，最终原因必须明确归类为鉴权、限流、网络/DNS/TLS/代理、接口响应或本地流程异常，且不得泄露API密钥。
- 运营日报“待办处理留痕”必须同时提供“处理日期”和“数据日期”两个独立筛选器：处理日期按`handled_at`转换为北京时间自然日，数据日期使用日报`business_date`；两个条件同时选择时按交集筛选，并显示当前结果数、总记录数和清除入口。筛选只作用于已读取的最近100条留痕，不改变确认、撤销、采集或数据库状态。
- 次日09:00周期末任务固定在10:00周期边界前一小时执行，参与同周期全部成功销量版本比较，但不得替换D+1日早间首次成功库存；更早业务日必须显示“尚未启用”，不得倒标为漏采。仍不增加09:50任务，为分层重试和Windows补跑预留时间。
- 运营日报的每次定时/手动采集、人工候选和最终确认值必须分开持久化，后一次采集不得覆盖前一次版本；人工修改必须记录用户、时间和修改原因，但不要求填写操作备注；确认合并和确认库存连续性差异仍必须填写备注并记录用户、时间。近30天浏览量必须保留全部采集版本、底层存储和页面/Excel展示，但不得参与冲突判断。同一10:00核对周期内只比较全部成功版本的当天订单数（销量），任意两个非空销量不一致时形成同周期销量版本差异并要求人工确认；库存和近30天浏览量变化不得触发版本待办，即使最新销量恢复为早先数值，中间销量差异也不得消失。销量一致时不推送版本待办。库存连续性只允许比较相邻前后两个日报日的稳定正确值；任一方存在未确认销量版本差异时必须暂缓跨日公式。
- 每个自然日早间任务首次完整成功采集的系统库存必须按实际北京时间日期和实际成功时间冻结到独立的`daily_inventory_snapshots`。10:05只是计划触发时间；如因内部重试或Windows任务重启而在更晚时间成功，数据库、页面、版本标签和Excel必须记录实际成功时间，不得仍标成10:05。业务日D的日报各版本统一采用D日整日销量与D+1日库存：正常早间完整快照优先且不可覆盖；若早间漏爬或不完整，则允许D+1当天10:05后的晚间或手动完整成功库存作为恢复值，并随当天更晚的完整成功拉取更新，直到完整早间快照成功。已存在完整库存时不得被后续不完整值降级覆盖。恢复成功后必须清除已补齐商品的`missing_capture`占位，只保留整批失败原因，并在页面与Excel的库存/日期总格写明“早间库存漏爬已解决”、来源及精确到秒的实际北京时间；当天结束仍没有完整恢复值时才保持库存为空。
- 运营日报只有在最终采用值的当天订单数或平台库存为空时，才允许把商品标记为`missing_capture`；近30天浏览量为空、某个批次缺少商品或整批失败但订单与库存已由其他成功版本补齐时，不得给商品打漏爬标识。采集批次失败、商品缺席和字段空值仍必须保留脱敏运行日志与 Excel 说明，用于追查实际失败原因；商品漏爬不算冲突、不要求人工确认、不计入历史待办且不阻止导出。页面“数据完整性说明”中的每条定时/手动采集说明必须显示对应数据日期，默认只读取当前日报日及前2天，并允许用开始、结束日期选择器查询任意历史范围；不得再只显示所选范围内的前8条。
- 运营日报库存异常口径固定为“前一日报日正确库存 - 当前日报日正确订单数 != 当前日报日正确平台仓可售库存”；由于每个业务日D的库存字段是D+1日早间期末库存，该公式按自然日展开必须等价于“D日早间库存 - D日整日销量 != D+1日早间库存”。人工确认值优先，缺少前一日报日库存本身不得红标。早间首次完整成功后，只要公式需要的相邻日报值都已具备且当前没有销量版本差异，公式不平必须立即持久化为库存待办并由登录态事件流推送，不得只在页面动态红标而数据库仍停留“等待晚间”；晚间或手动版本随后产生销量差异时，先切换为销量版本待办，若新值使公式恢复则自动解除库存待办。核对状态固定为“先销量版本、后库存连续性”：确认合并只能解决当日销量版本选择，确认后仍不平必须继续生成库存连续性待办，不得顺带豁免；但版本差异待办保存的人工订单和库存若已使相邻日报日公式相符，必须在同一次保存中自动采用该人工值、写入可撤销确认留痕并关闭整条待办，不得再生成库存待办或要求二次点击“消除差异”。公式仍不相符或暂不可计算时继续保留待处理。库存待办期间新增销量版本差异时先暂停连续性并切回版本待办。已有人工确认基线时，新的版本待办只展示该确认值及确认后出现的新拉取/人工候选销量，旧版本只保留审计。库存连续性可填写原因单独确认为“差异属实”，该动作只关闭待办，不得清除页面或 Excel 的库存红标、库存异常筛选结果和异常统计；撤销“差异属实”后若公式仍不平，必须重新生成原库存待办。“待办处理留痕”必须覆盖人工候选、备注新增/修改/删除、确认合并、确认库存差异及两类撤销，每一步都作为独立记录展示商品、平台SKU、日报日、处理人和中国时间；人工候选允许无操作备注，确认合并和确认库存差异继续强制备注，撤销操作继续填写撤销原因。撤销还必须保留原处理记录并在原记录上展示撤销人、撤销时间和撤销备注。普通备注只允许拥有“处理运营日报待办”权限的账号新增、修改、删除，并关联通用、同周期版本差异或库存连续性；每个日报日必须在页面和 Excel 商品横向表中增加独立备注行，以“（问题类型：备注）”展示在对应商品列。新增、修改、删除均追加审计记录并保留操作人、中国时间及修改前后内容；修改通用备注只填写新的备注正文，删除通用备注无需填写原因；删除问题关联备注仍必须填写删除原因。删除只移除当前展示，不得抹除审计，任何备注操作都不得改变待办状态。存在任一未处理销量版本差异或库存连续性异常时必须阻止运营日报 Excel 导出并返回具体日期和商品。
- 库存连续性待办人工修改后必须使用候选订单和库存覆盖基准值重算公式，并强制互斥处理：公式相符时前端只显示“消除差异”，后端只允许采用人工值并关闭红标；公式仍不相符时前端只显示“确认库存差异”，后端只允许采用人工值、关闭待办但保留库存红标。两个动作不得同时可用或仅靠前端限制。“消除差异”必须单独留痕并可按人工确认值撤销；再次人工修改已确认库存差异的商品时必须重新打开核对状态。
- 人工确认合并必须立即重算本日报日与前一日报日、以及下一日报日与本日报日的库存连续性；只要两端版本均已稳定且新公式不平，就必须恢复`needs_review`、撤销旧库存差异豁免并进入全局待办，不得限定为“确认前原本相符”才触发。若相邻日报日仍有自己的版本差异，只保留版本待办，待确认后再重算。运营继续确认下一日后必须按同样规则向后传播。由确认触发的库存待办必须展示实际采用的人工确认值、来源、确认人、北京时间、确认备注、跨日公式和实际库存。
- 撤销人工确认时不得把受影响的下一日报日移出待办：被撤销日报日保留“已撤销确认待重核”，下一日报日保留“撤销确认后库存待重算”，并展示原确认值、撤销人、北京时间、撤销原因、当前订单/库存和撤销前公式。前一日报日重新确认后必须立即重算；公式不平时转为真实库存连续性待办，公式恢复正确时自动解除受影响待办。
- 看板的导航、按钮、上传提示、字段名、状态和说明必须使用中文；框架自带的英文工具栏应隐藏。平台原始商品名称、店铺代码、商品编码和南非货币符号属于真实业务数据，保持原值，不得擅自翻译或伪造。
- HTML、Excel、PNG 与看板指标必须来自同一指标数据集，避免跨输出口径漂移。
- 项目生成的 Excel 中，所有有内容的单元格必须水平、垂直居中；NFT102 更新副本只对目标工作表应用此规则，其他店铺工作表保持原样。
- 不覆盖用户源文件；NFT102 更新必须输出新副本及核对报告。
- NFT102 网页续写必须以用户本次明确上传的运营最终版为基准，原样归档后再生成；不得自动改用其他模板，也不得覆盖归档基准。
- 竞品采集只允许通过 Playwright 驱动本机 Chrome/Edge 访问公开商品/评论地址和隔离匿名购物车；平台拒绝普通 HTTP 客户端时可以用真实浏览器导航读取公开 JSON，但不得接入竞品卖家后台，不得进入结算或下单；每次库存探测结束后必须尽力清空测试购物车。
- 竞品库存探测只允许点击用户提供链接所对应 PLID 的主商品购买区按钮，严禁点击“You Might Also Like”“What’s Hot”等推荐位商品；进入购物车后必须再次按目标 PLID 定位商品，不得因为购物车中只有一个商品就猜测它是目标竞品。
- 竞品“库存”必须按尺寸、颜色、左右款等每个变体分别记录，标注为当前匿名会话、当前卖家和当前 SKU 的平台仓购物车可售上限，不得称为平台物理总库存；任何快捷数量或自定义数量校验一旦出现 `current stock = N` 明确仓库提示，必须立即把 `N` 作为精确平台仓库存并终止该变体的后续超量测试和二分校验。若页面出现 `Limited to N per customer`，必须额外验证限购数量 `N` 是否能加入购物车：能够加入时库存只能记录“至少 N”，不得把促销限购误写成精确库存，并持久化“每位客户限购 N 件”标记；限购数本身仍无法加入时才可继续在其下方校验真实库存。快捷菜单确认 9 件可加入后必须先验证 10 件：出现 `We currently do not have 10 in stock.` 或 10 件被明确拒绝时记录为精确 9 件；若 `10+` 输入框未能打开/完成输入且页面没有明确缺少 10 件的提示，则仍保守记录“至少 9 件”。只有 10 件明确被接受才继续超量测试和二分。异步加购必须等待购物车状态落库；首次数量菜单也必须等待动画并最多重开3次，仍无数字选项时刷新购物车一次并重新按同一 PLID 定位后再试，绝不复用刷新前控件或回退其他商品。输入框未出现时仍必须检查页面是否存在明确的缺货弹窗，只有未标记 `aria-hidden=true` 的自定义数量输入框可视为已打开，临时更新错误必须在仍锁定同一 PLID 的前提下重新加载并有限重试，营销遮罩只允许定向清理阻挡目标控件的 Braze 元素。本次库存仍未探测成功时，当前库存字段必须保持空值；页面只可在 SKU、卖家和完整变体集合均一致时显示上次成功库存及时间，并明确标记为过期参考，该参考不得回填本次库存、计算库存净流出或参与销量推断。公开接口标记为 `is_leadtime` 的供应商调货/长时效到货变体及当前不可加入购物车的变体必须统一标记“没货”，不计有效平台仓库存，也不得参与库存净流出销量推断。
- 用户勾选库存探测时，任一可购买变体最终以`failed`结束必须保存已经取得的商品、评论和变体快照，但整条链接不得计为采集成功，必须以独立“库存未探测”原因进入待重试且不得触发连接类熔断。每个失败变体必须持久记录可读变体名、SKU、失败阶段和精简技术原因，并把同一摘要写入424响应、运行提示和逐链接结果日志，不得只记录“若干变体未探测”。库存未探测的第一次自动复探前必须先执行1条其他待执行链接，第二次自动复探前必须先执行2条其他待执行链接，总计初次探测加2次自动复探；当轮剩余任务不足以满足对应间隔时不得退化为队尾立即复探，必须保留待重试供人工插队或后续继续，避免单链接永久占住整批。用户主动关闭库存探测、供应商调货和明确不可加购均不是失败，不得进入该复探队列。
- 竞品评论按 PLID 商品维度共享，变体不得重复抓取或重复计数；只有前后快照的变体键、SKU 与卖家集合一致时，才允许比较汇总库存并产生库存净流出信号。
- 竞品雷达经营信号必须按用户选择的北京时间自然日区间只读重算：每个商品使用区间内最旧快照和最新快照比较价格涨跌、库存净变化、新增评论、观察期销量信号和趋势说明，不得继续展示采集写库时基于相邻两次快照保存的旧信号。价格信号固定分为涨价、降价、价格不变、价格不可比或待建立价格基线，并展示区间起始价、最新价和差额；价格、库存、评论结构和变体使用区间内最新快照，详情历史只展示区间原始观测值。库存净变化还必须同时满足首尾变体键、SKU、卖家集合完全一致且两端库存均为精确值；库存不可比时保持缺失，但商品维度新增评论仍按首尾评论数独立计算。
- 竞品页面和只读 API 投影不得展示基于评论率反推的累计销量估算；历史快照中的旧累计估算字段暂时保留以避免破坏既有数据结构，但不得作为运营指标。观察期销量只使用可比库存净流出和新增评论，价格、排名或卖家变化不得直接换算为订单。
- 评论属于商品维度，可能跨卖家或变体；评论分类固定为4–5星好评、3星中评、1–2星差评。竞品评论区必须支持分类与起止日期组合筛选，并支持按日期新旧和评分高低排序；日期缺失记录仅在未限定时间时保留。
- 竞品页面和接口复用正式 ERP 的登录、账号级权限和 `8501` 局域网访问边界；浏览历史使用 MySQL 只读事务，只有拥有“采集竞品”权限的账号明确点击采集按钮或运行 `collect-competitors` 时才允许访问公开接口和写入竞品表。
- 竞品监控链接必须使用 MySQL 持久化清单，不得退回临时多行文本框。拥有竞品采集权限的用户可新增、修改、删除；新增按PLID去重，修改不得改变PLID，删除只停用未来监控并保留历史快照。每次变更必须在同一事务中追加操作用户、北京时间、PLID及修改前后URL审计；从待重试详情发起的人工重试也必须以独立`manual_retry`动作记录操作人、北京时间、PLID和URL。链接操作记录默认关闭且只显示入口，首次打开固定视口弹层时才请求数据，并支持日期区间查询；服务端和前端都必须分页，单页默认20条，不得随历史增长一次加载全部记录，也不得以内联展开推动下方精品雷达长列表重排。监控链接明细默认关闭且只显示汇总，关闭时不创建全部DOM；打开后必须使用固定在视口内的管理弹层，不得以内联展开推动下方精品雷达长列表整体重排，并支持商品名、PLID或完整Takealot链接搜索、翻页和每页20/50/100条选择，只创建当前页链接节点；搜索或切换每页数量后回到第1页。完整链接必须通过正则提取PLID再匹配。重复目标无历史快照时只提示清单已存在；有任意历史快照时提供跳转到竞品最新状态对应商品行的操作，该判断必须查询全历史而不能受当前观察区间限制。
- 采集一个竞品时必须枚举公开商品数据中各变体的主报价与`other_offers`；只有明确包含Takealot商品URL或`PLID`的报价才可形成新的爬取目标，不得从SKU、卖家ID、纯数字`product_id`或价格猜造链接。提交的原始目标无论报价能否反向识别都必须保留在组内；新识别目标按PLID幂等创建，已存在的活跃目标只合并分组、不重复新增或追加当前批次，停用目标重新激活后可重新入队。新增或重新激活的跟卖目标以`auto_discover`审计并追加到活动批次，当前商品结束后参与后续采集；没有活动批次时进入下一次完整清单。监控链接管理按同一原始/跟卖组分页，每组使用默认关闭的下拉入口，关闭时不得创建组内全部链接DOM；展开后必须显示原始及已识别跟卖链接，并优先展示各自已采集的卖家和价格。
- 活动竞品批次中新保存的监控链接必须由服务端登记为自动插队并进入待执行队列队头，不要求用户再点“插队”，且不得中断当前正在执行的商品；当前商品结束后优先探测，连续新增按后进先出排序，页面刷新或断点恢复后仍须保持服务端登记的队头顺序。新增链接的自动插队只执行该新增队列项一次；有采集权限的用户还必须能从监控链接明细指定任一原有链接手动“插队”。手动插队由服务端全局批次状态登记操作者、目标PLID和时间，页面刷新及其他用户页面轮询后仍能取得同一指令；不得中断当前正在执行的商品，必须在当前商品之后额外增加一次优先探测，同时保留该链接在原队列中的位置，队列走到原位置时仍须按原顺序再探测一次。服务端必须在本批保留已接受的自动/手动插队记录；按钮成功后立即显示“已自动插队”或“已插队”并禁用，同一PLID的尚未消费指令必须幂等，不得生成多个优先副本。从“待重试任务”链接操作页发起的“人工重试并插队”必须使用独立`manual_retry`来源；上一条人工重试已经开始执行后允许运营再次发起，尚在待执行队列中时重复点击仍按幂等处理，并在批次运行日志和链接操作审计中保留操作者与时间。尚未消费的自动队头、手动插队和已接受记录必须持久化到`logs/competitor-batch-queue.json`，ERP重启后同一`batch_id`恢复时继续执行；目标开始后才移除对应待执行指令，本批无剩余项完成后删除该运行时文件。目标正是当前商品且本批未曾接受插队时拒绝；没有活动批次时不得暗中启动新批次，必须明确提示当前无可插队批次。运行中新增链接和库存未探测复排只能写入非阻塞进度提示，不得复用批次停止原因或导致当前商品结束后暂停；服务端必须把修复前旧页面误交的上述`paused`事件降级为`progress`并记录原提交事件，不能依赖所有旧标签已经刷新。
- 竞品批次的开始、继续（含自动续爬和刷新恢复）与停止必须只允许用户名`kxx`，不得依据`admin`、`operator`、`selection`等权限模板放权；其他任何账号即使具备管理员模板和`competitors.collect`权限，也必须由后端拒绝批次启停和单链接采集接口，但仍可在具备竞品采集权限时新增监控链接和手动插队。前端按钮可隐藏或禁用，但不得作为唯一权限边界。
- `kxx`在竞品队列运行中必须可以开启或关闭“显示检测浏览器”；切换不得改变当前已经开始的链接，只从下一条任务链接起读取最新值，并须同步保存到本地批次断点。其他账号不得借此取得批次控制权。
- 只要服务端存在活动竞品批次，所有账号的进度条以及成功、待重试、确认失效、已检查和待续爬数字必须统一读取同一份服务端批次状态；非批次持有页面不得混入自己的旧浏览器断点统计或旧错误明细。仅在服务端没有活动批次时，页面才可展示本地断点进度供`kxx`恢复。
- 竞品采集任务开始后，ERP 页面导航不得销毁任务页面状态；切换到其他模块再返回时，必须保留本批链接、采集选项、完成数量、进度及逐条成功/失败结果。
- 竞品批量采集是服务器级单任务：同一时间只允许一个账号、一个浏览器会话持有全局采集权，其他账号或同账号的其他标签页不得并发启动。活动批次内还必须由服务端对当前请求编号持有单链接租约：页面刷新只能用相同请求编号接回；任何不同请求编号在上一条未完成时必须被拒绝且不得覆盖当前PLID或启动第二个Playwright库存会话，被拒绝页面必须停止自己的重复续爬但不得反向暂停服务端现有批次。当前操作者、已检查/总数、成功、待重试、待续爬、当前序号、PLID和“后台浏览器/商品变体/评论/库存浏览器/快照保存”阶段必须由后端统一维护，并至少每2秒同步到所有用户的竞品雷达页面；后台公开数据阶段必须明确说明不会出现可见库存窗口。临时403、网络或服务异常只计入待重试，对应链接成功后移除。页面“确认失效”必须取持久化链接复核状态并合并本批刚确认的PLID，成功不得清空其他已确认失效记录。前端状态只用于展示，最终限制必须由后端校验。共享批次占用期间仍允许有权限用户管理持久化链接；新增项必须进入活动批次待执行队头且不得打断当前商品，修改和删除只影响未来批次。开始/继续按钮在其他批次占用时必须显示操作者、进度、待续爬和当前 PLID。持有页面每10秒发送心跳；页面无当前请求且超过2分钟没有心跳/进度时自动释放。人工停止时若当前商品仍在执行，必须完成该请求的浏览器清理后才释放给下一批，避免后台残留探测与新批次并发。
- 竞品最新状态必须支持商品名称、PLID、完整Takealot链接或卖家关键词、当前库存状态和经营信号的前端组合筛选；粘贴完整链接时必须通过正则提取PLID再匹配。页面显示筛选后数量与商品总数，并提供一键清除；库存状态固定分为“有货”“没货”“未探测”，其中本次未探测但展示上次成功过期参考的商品仍属于“未探测”。筛选只作用于当前已读取列表，不触发平台采集或数据库写入。明细表必须提供翻页和每页20/50/100条选择，只创建当前页对应商品行；筛选或切换每页数量后回到第1页，重复目标“查看对应商品”必须先切到目标所在页再定位。
- 竞品最新状态表中的每个商品必须支持鼠标点击和键盘回车/空格直接打开完整详情弹窗；弹窗必须集中提供价格、库存及过期参考、采集时间、经营信号、评论结构、全部变体库存及说明、库存/评论历史快照、评论分类与起止日期筛选、日期/评分排序和评论全文，并支持遮罩、关闭按钮及 Escape 关闭。上述商品详情不得再重复铺在最新状态表下方，避免运营为查看历史和评论滚动页面及前端维护两套详情交互。“任务爬取详情”内的全部成功与待重试链接也必须支持鼠标和键盘打开对应监控队列操作页；成功与待重试明细在同一折叠区中分组展示，确认失效只保留状态统计与复核入口，不伪装成可重试任务。
- 竞品 Playwright 进程必须在创建后立即纳入生命周期管理；Takealot 预热失败、梯子/代理中断、页面超时、用户停止和请求取消均必须尽力关闭页面上下文、浏览器和 Playwright 驱动，不得遗留 Chrome/Node 进程。只有Takealot公开API或商品页403、真实导航失败、429或Takealot 5xx可返回503并计入连接类失败；页面内容暂时无法稳定判定必须单独返回409保留重试，不得触发断网暂停。404疑似失效、409页面不确定、单条浏览器连接失败和5xx等普通可恢复失败按任务数指数退避：三次自动重试前分别至少执行1、2、4条其他待执行链接；当轮任务不足以满足对应间隔时不得退化为队尾立即重试，必须保留待重试供人工插队或后续继续。明确失败索引不得因同一PLID曾有成功结果而从续爬队列消失，插队尝试成功也必须清除该索引已有失败状态。ERP批量采集连续两条连接类失败后暂停剩余链接并保留已完成结果，避免断网期间继续空跑；暂停原因必须记录这两条PLID、浏览器连接失败或HTTP状态及各自原因，新增链接、插队和库存轮后复探普通提示不得参与计数或进入停止原因。
- ERP竞品批次的隐藏公开数据浏览器必须在服务器单任务和单链接租约内串行复用，不得并发共享页面；最多连续处理25条后主动回收。网络失败、浏览器异常、请求取消或服务退出必须立即作废并关闭，下一条重新预热。从第二条链接起，每次公开数据浏览器租约前必须由服务端统一随机冷却5至10秒，浏览器重建、页面刷新、旧前端或直接接口请求均不得绕过；首次预热成功后等待4至7秒，变体请求间等待3至6秒，额外评论分页间等待2至5秒。403、429、超时和5xx的原退避重试及10分钟VPN自动恢复不得缩短。可见匿名库存浏览器继续逐链接隔离启动、使用独立购物车并完整关闭，不得跨商品复用。前端正常链接间只保留1秒界面队列交接，不作为业务限速边界；服务端链接租约、随机冷却和浏览器清理完成才是进入下一条的真实边界。
- Takealot 商品公开数据首次或疑似失效时，单次 404 不得直接删除或确认链接失效。必须在同一 Playwright 浏览器中用最近成功采集的不同 PLID 作为正常对照：`404, Page Not found`页面即使包含`main h1`也必须识别为不存在页；正常商品页应同时利用商品页标题和非空商品标题识别，不得只等待一个可见标题。目标或对照内容不确定、目标仍为商品页或没有可用对照时均不得形成永久判断；只有目标明确为404页且对照页正常才累计一次有效证据。有效证据至少间隔10分钟，累计3次才首次确认失效；复核状态、次数、对照 PLID 和时间必须持久化。已经持久确认为失效的链接在后续批次中只需一次公开商品数据复核：若再次返回404，立即维持确认失效并作为本批终态，不再重复三次页面与对照复核；403、导航失败、429、5xx等连接异常不算完成复核，仍保留重试；任意一次成功采集必须恢复正常。
- 竞品断点续爬必须以当前前端批次为边界，持久保存冻结顺序、已检查索引、失败索引、确认失效索引、成功结果及其原始URL、结构化失败PLID/URL/原因、运行状态及当前链接请求编号；恢复时从最早失败项或首个未执行项往后，只跳过本批已成功 PLID和已确认失效项，疑似失效必须保留重试。不得用数据库旧快照判断跳过。采集中整页刷新后必须自动回到竞品雷达并继续，不要求运营再次点击；前端以服务端批次编号与本地断点批次编号是否一致区分本人刷新与其他批次，同一标签页保留客户端编号并自动重连。若服务端仍有当前链接请求，恢复队列必须先把该链接放到首位并用原请求编号复用、等待原探测任务完成，之后才允许回到最早失败项，禁止刷新前后的两个队列并发争用Playwright。只有其他批次或其他标签页才应提示占用，不能把刷新后的本人误判为其他操作者。人工停止和已经结束的批次不得自动启动。模块切换和 ERP 重启不得丢失未完成批次；服务端活动批次中的新增队尾项必须合并到同一本地断点，监控清单后续修改或删除不得篡改本批已冻结队列。新批次仍允许复核已确认失效链接，但其本次公开商品数据再次返回404时必须一次复核直接终态；服务端必须把批次开始、自动恢复、暂停、人工停止、完成及逐链接开始/结果/异常写入 `logs/competitor-collection.log`，且不得记录密码或会话密钥。
- 竞品自动续爬只适用于不可抗力连接类暂停：Takealot公开API或商品页403、导航失败、429和5xx都必须归为可恢复连接异常；单链接允许有限退避重试，任一后续成功必须清零连续失败次数。连续2条连接类失败后必须持久保存下次自动尝试时间并每10分钟从失败/未完成项继续；VPN恢复后自动继续，仍为403或同类失败则重新安排10分钟。倒计时要逐秒可见，刷新后继续，关闭页面再打开时若已到期则立即尝试；其他批次占用时顺延一轮。人工停止、权限/链接校验问题、确认失效和正常完成不得触发自动重启，人工提前继续成功后必须清除原计划。
- 竞品监控链接新增和修改必须由前端与后端双重校验 URL 格式、Takealot 域名和PLID；发现错误时不得写库或请求外部接口。修改现有项必须保持原PLID，不同商品通过删除旧项后新增处理。
- 正式运行数据库固定为本机 MySQL 8.0、`mysql+pymysql` 同步驱动和 `utf8mb4`；应用不得用 root 账号运行，不得自动回退读取 SQLite。旧 `data/takealot.db` 只允许作为一次性迁移源、历史留档和隔离测试夹具。
- MySQL 密码只能存在于被 Git 忽略的 `.env` 或进程环境中，不得进入源码、命令输出、日志、测试、报表、文档或版本库；备份调用必须通过进程环境传递密码，不得把密码拼进命令参数。

## 5. 关键目录

- `src/takealot_ops/`：业务源码
- `src/takealot_ops/competitors/`：竞品公开接口、库存探测、估算、持久化和本机 API
- `src/takealot_ops/erp/`：统一 ERP 的只读数据投影、动作接口和静态前端服务
- `frontend/competitor/`：统一 Vue 3 + TypeScript ERP 源码及生产构建（沿用竞品目录名）
- `tests/`：单元、集成和端到端测试
- `config/`：业务规则与程序设置
- `scripts/`：计划任务和 NFT102 辅助脚本
- `docs/`：需求、实施、审计、测试和项目状态文档
- `data/`：原始响应、NFT102 基准及已退役 SQLite 迁移源；正式结构化数据位于本机 MySQL
- `exports/`、`outputs/`：生成结果
- `backups/`、`logs/`：运行备份与日志

## 6. 常用验证命令

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check src tests
.\.venv\Scripts\python.exe -m mypy src
.\.venv\Scripts\python.exe -m takealot_ops.cli verify
Set-Location .\frontend\competitor; npm.cmd run build
```

验证范围应与修改风险匹配；修改指标、导出、数据库或采集逻辑时，不能只运行无关的单个测试。

## 7. 当前状态与已知边界

- 数据接口、MySQL 存储、指标计算、异常识别、HTML/Excel/PNG 导出、统一 Vue ERP 和日常运行工具已经完成。
- 项目已使用 CodeGraph 1.5.0 建立本地 AI 代码图谱并接入 Codex MCP；排除可重建前端`dist`产物后的初始图谱包含110个文件、2801个节点、8211条关系和62个FastAPI路由。`.codegraph/`只提交内部忽略规则，索引数据库与日志保留在本机；AI分析架构、调用链和改动影响时优先按`docs/CODEGRAPH_AI_GUIDE.md`进行图谱查询，但图谱不替代业务规则、测试、数据库核对、浏览器验收和`/api/health`。
- 统一 ERP 已增加 MySQL 用户、可撤销会话、四个权限模板、账号级自定义功能权限和无上限账号—店铺多对多范围，正式监听地址为 `0.0.0.0:8501`；首个管理员需在服务器本机打开 `http://127.0.0.1:8501` 初始化，之后同事使用服务器局域网 IP 登录。现有单店数据自动登记为`current`已接入店铺；以后新增店铺可先进入权限目录并分配账号，但只有完成数据分区与采集接入后才能标记已接入。
- ERP 登录会话采用7天滑动有效期：登录后有效7天，到期前的有效请求把数据库到期时间和浏览器 HttpOnly Cookie 同步顺延到最近使用后的7天；同一会话每1天最多续期一次。连续7天未使用、主动退出或账号权限、模板、密码、启用状态变化时仍立即失效。
- 用户权限页使用四张无推荐、无单独高亮的权限模板卡；现有账号改为连续边框卡片，可逐项勾选功能权限和店铺范围并立即保存。店铺目录不设数量上限，区分已接入与待接入；“全部店铺（含未来新增）”独立于模板，适用于管理员和老板账号，普通运营可按实际负责范围分配任意数量店铺。登录后顶栏必须显示账号可访问店铺的当前查看选择器；在真正完成逐店数据分区和查询上下文前，选择任何非`current`或未接入店铺时，只有店铺数据模块显示安全占位，不得挂载店铺业务页面或沿用当前店铺数据，竞品雷达和NFT102等公共模块仍按功能权限开放。0店铺账号登录时应自动进入第一个已开放的公共模块；不同运营人员通过各自账号登录，不提供管理员无留痕冒充账号切换。选品模板只显示竞品雷达和运营日报导航，可发起竞品采集但不能处理日报待办。前后端权限回归必须同时覆盖模板默认值、账号自定义覆盖、依赖权限自动补齐、旧账号列升级与全店范围回填、未来店铺动态纳入、0店铺公共模块放行、限定账号隔离和最后一个权限管理员保护。
- 统一 ERP 已包含 NFT102 续写，可校验并归档运营最终版、识别连续下一日期、调用既有只读采集脚本并提供新表格下载。
- ERP 侧边栏已显示最近成功采集时间和最新指标日期，并提供完整流程手动刷新按钮；每次手动刷新成功后都会冻结独立版本并与当前北京时间10:00核对周期内全部版本比较。Windows运营日报任务默认时间为中国时间10:05、18:00、次日09:00和18:30。
- 合成数据验收已完成；真实店铺只读联调状态以 `docs/PROJECT_STATUS.md` 的最新记录为准。
- 当前官方 Offer 数据提供近30天滚动浏览量，没有可确认的精确每日产品访客数接口。
- 正式运行已切换到本机 MySQL 8.0；旧 SQLite 的12张表共4204行已全量迁移并逐表核对一致，ERP、采集、指标、竞品、报表、完整性检查和每日备份均读取或写入 MySQL。全部备份产物必须写入 `D:\南非店铺数据抓取\backups\`，不得在C盘另建备份目录；MySQL数据目录中的活动binlog是运行源，不算备份副本。完整备份固定使用 `mysqldump --single-transaction`，成功后压缩为本机 `.sql.gz`，生成 JSON 清单与 SHA-256 并重新读取全包校验；新格式按14天全留、90天每日、1年每周、7年每月分层保留，旧 `.sql` 不自动删除。binlog 使用独立 `takealot_backup@localhost` 账号通过 `mysqlbinlog --raw --stop-never` 连续复制到 `backups\binlog\`，闭合文件每日校验并保留35天；专用账号启用后完整备份清单必须记录对应binlog文件和位置。当前按用户要求不做异地备份，不得把本机备份描述为可防整机或物理硬盘损坏；未完成隔离库导入和binlog重放前不得宣称PITR已验证。
- 竞品观察最小闭环已完成：公开商品/评论采集已由普通 HTTP 请求切换为 Playwright 复用一个本机浏览器会话逐地址导航，采集器、ERP 接口和 CLI 全链路使用异步调用；支持多链接逐条采集、单条失败隔离、全部变体枚举及独立库存、PLID 共享完整评论、历史快照和观察期信号。累计销量估算已从运营页面和只读 API 投影移除。该页面现为统一 Vue ERP 内的正式模块，与店铺经营页面共用 `8501` 和 MySQL。
- 竞品采集会从全部变体公开报价中提取明确可识别的Takealot跟卖URL/PLID，提交原链接始终作为分组基准；新目标按PLID幂等写入、审计并追加到活动批次，已有目标不会重复入队。监控链接弹层按原始/跟卖关系折叠成组，只有展开组时才创建成员链接；经营信号同时按所选北京时间区间首尾快照给出涨价、降价、价格不变或不可比证据。
- 竞品雷达的商品级展示继续读取商品快照主图；每个非默认变体必须从该变体公开详情的 gallery 单独保存 `image_url`，详情当前变体表和每个历史快照都按快照 ID 展示当时所有变体的独立名称、库存、限购标记和图片。变体名称只能展示颜色、尺寸等可读值，不得输出 API 字典或 JSON 字符串。默认款没有独立变体图片时必须回退到所属商品快照主图；颜色、尺寸等非默认旧变体缺图时必须显示“暂无图片”，不得拿商品主图冒充；任何图片加载失败也不得保留破图图标。
- 经营坐标已移除分位切换、四象限统计/筛选、区域名称和十字分界，只保留统一蓝色商品散点与25/50/75轻量网格；竞品采集页在模块导航切换期间保持任务状态，返回后继续显示原进度和结果。
- 经营坐标悬浮卡已加入平台商品缩略图、平台库存、首次上架/本库最早记录、平台库存增加对应的最近补货时间、平台明确返回的近30天浏览量和指标截止日期；图片缺失或加载失败时显示“暂无图片”。坐标页图片经登录态同源接口转换为最长边192像素JPEG并保存在`data/runtime-cache/product-thumbnails-192/`，卡片使用懒加载、离屏内容可见性和Vue记忆化，避免复制/悬停引起404张卡片深度重渲染。两轴关键数字位于图框外且不被散点覆盖，“全部商品”已改为无需横向滚动的自适应完整卡片，支持按浏览量、下单、平台库存和商品名称排序，并支持平台 SKU 部分搜索；独立近7日流量因接口未提供而不再推算或展示。MySQL Offer 当前表和历史快照表已增加可空 `created_at` 字段。
- 当前11个竞品链接保留在本机 MySQL；重新采集后已有13条商品快照、14条变体结果和425条共享去重评论，`PLID70540744` 与 `PLID99275672` 最新精确平台仓购物车上限分别为4和10。
- 运营日报模块已接入统一 Vue ERP：北京时间每日10:00至次日10:00为一个核对周期，MySQL 保存早间/晚间定时版本、周期内全部手动刷新版本、人工候选、最终确认、操作审计和18:30待办快照；计划任务仍在10:05/18:00触发，但每个版本以最终完整成功的实际北京时间落库和展示，重试或任务重启后的成功不得伪装成计划时刻。每个自然日早间首次完整成功库存作为优先且不可覆盖的固定快照；早间漏爬或不完整时，业务日D日报改用D+1当天10:05后的最新完整恢复库存。页面不提供日报业务日期选择器，按10:00边界自动读取当前周期对应日报日。主视图和Excel保留原表商品横向布局，页面加载最近30个有数据日报日并纵向堆叠，每日固定显示近30天浏览量、当天订单数、平台库存数量（次日实采）、备注四行；表格视窗一次最多完整显示4日，打开时自动定位到最新日期，可上下滚动历史；左侧固定“指标”列宽170px，“日期”列必须从170px紧邻固定，不得留下可见缝隙。“当天访客数”已完全移除。近30天浏览量继续保存和展示但不参与合并，也不参与漏爬判定；只有最终订单或库存为空才标记商品漏爬。同周期版本待办只比较销量，销量一致不推送；确认正确销量后才按相邻日报行的次日实采库存计算连续性。版本待办仅显示确认基线与本次新销量版本，库存待办仅显示两个正确日报值及公式。顶部手动次数仅统计当前周期。库存差异确认为“差异属实”后只关闭待办，页面矩阵、库存筛选、异常统计和 Excel 继续保留红色事实标识；如果确认合并后继续确认库存差异，页面与 Excel 的同一备注单元格必须同时显示“（确认：合并备注）”和“（库存差异已确认：差异备注）”。页面“待办处理留痕”集中显示最近100次人工候选、备注增改删、确认合并、库存确认及两类撤销，每一步独立展示商品、SKU、数据日期、操作人和北京时间；人工候选可无操作备注，确认合并和库存确认必须填写备注，撤销操作继续填写原因；撤销独立留痕且原处理记录保留撤销关联，有效确认可直接撤销，库存确认撤销后公式仍不平会恢复待办；留痕可按北京时间处理日期和日报数据日期独立或组合筛选。拥有“处理运营日报待办”权限的账号可在备注行或待办卡新增、修改、删除备注，删除时也必须填写原因，当前内容按问题类型在对应商品列括号展示，所有操作保留不可删除的审计历史且不改变待办。业务日D所需的D+1日库存只有在早间及当天后续拉取均无完整值时才保持为空；早晚计划时间未到时显示等待，不提前计为漏爬。
- 运营日报定时版本现扩展为早间、晚间和次日09:00周期末三次采集；周期末版本仍归入10:00结束前的同一业务日并参与全部非空销量比较，但库存固定沿用D+1日早间首次成功实采快照。该任务自北京时间2026-07-30起启用，更早日报显示“尚未启用”且不产生漏采问题。
- 2026-08-01 起，早间库存恢复规则已正式落地。早间漏爬/不完整时，10:05后最新完整成功库存会回填`daily_inventory_snapshots`与该日报日所有成功观察；页面和 Excel 库存/日期总格集中显示恢复说明及实际北京时间，已补齐商品不再逐项占位。正常早间完整快照仍优先且不可覆盖，09:00周期末库存仍不用于恢复。
- 运营日报实时更新使用同源、需登录且需“查看运营日报”权限的服务端事件流；前端在事件到达和断线重连时重新获取待办、矩阵与全局提醒，人工编辑期间延后应用。正式服务修复后统一由 `scripts/restart_erp.ps1` 精确识别项目监听进程、重启并检查 `/api/health`，stdout/stderr 保存在 `logs/`。
- 运营日报“人工修改”首次打开时，近30天浏览量、当天订单数和平台仓库存必须以当前采用数据作为输入框初始值，并在字段旁持续展示当前基准；已有人工候选时输入框回填上次人工值，基准仍保持不变，方便运营只调整需要修改的数字。
- 运营日报人工修改的数值输入必须同时兼容浏览器返回的字符串和数字；不得对数字直接调用字符串方法导致提交在请求前中断。弹窗保存失败时必须保留当前编辑内容，并在弹窗内直接显示错误原因。
- 运营日报“确认合并”和“撤销上次确认”等人工操作不得静默禁用提交按钮：必填备注为空时仍允许点击，并在弹窗内给出具体提示并聚焦备注输入框；请求发出后必须显示与动作对应的处理中状态，在完成前锁定取消、遮罩关闭和重复提交。成功提示必须在日报重新加载后继续可见，接口失败则保留弹窗与已填内容供重试。
- 运营日报全部采集入口已启用分层保护：请求级网络/429/5xx重试、整批新会话重试、`trust_env=False`直连备用和Windows任务每5分钟最多3次失败重启。每次整批尝试及最终脱敏根因进入文件日志与日报运行JSON，页面按时段提供折叠记录；只有Offers与Sales完整成功才冻结版本。
- 运营日报宽表只能在矩阵内部横向滚动，不得撑宽日报根网格或让下方真实差异板块超出视口；真实差异列表固定单列，每条商品占满一行，右侧“人工修改、确认合并、加备注”操作始终保持可见并允许响应式换行。
- 运营日报每张人工核对待办卡和待办处理留痕卡都必须支持点击定位商品：点击卡片非操作控件区域时，必须把对应平台 SKU 自动填入上方搜索框、切换为“全部”筛选并只显示对应商品，随后把日报矩阵纵向滚动到该记录自己的数据日期并突出日期起始行；卡片内人工修改、确认合并、备注、确认库存差异和撤销按钮不得触发该定位。目标日期不在当前读取范围内时必须明确提示，不得伪装定位成功。两类卡片以及由其打开的操作/备注弹窗必须在商品名称和SKU身份区展示平台商品图，图片缺失或加载失败显示“暂无图片”；日报横向数据表不得加入图片，避免破坏纵向对比布局。
- 运营日报顶部“历史未合并”提醒只统计当前日报日之前的待办，必须明确标注“不含当前日报日”，并同时展示当前日报日待办数以及下方完整待办合计，避免历史数量与历史加当前总数看似不一致。
- 竞品公开评论区已支持评价分类、起止日期组合筛选和日期/评分排序，并显示筛选结果数与评论总数。
- 竞品批量链接输入已支持编译器式错误定位：非 Takealot 域名、格式无效或缺少 PLID 时自动回跳并红色选中第一条错误链接，同时显示错误行号、原因和完整链接。
- 竞品采集已增加代理断线保护：公开浏览器启动会有限重试，所有启动阶段对象提前登记并在异常/取消路径强制回收；ERP只把真实导航失败、429和Takealot 5xx返回为HTTP 503并计入断网暂停，页面复核不确定返回409保留重试。一次历史遗留进程清理共移除了57个项目Playwright Node驱动和274个Chrome子进程，真实浏览器启停检查前后进程数保持不变。
- 竞品雷达已增加批次级断点续爬：当前链接、采集选项、成功结果、失败/已检查位置和停止原因写入浏览器本地缓存；刷新或重启后可从最早失败项继续，后方穿插的本批成功 PLID 自动跳过，未执行项继续排队。开始新采集会覆盖旧批次，数据库历史不会导致当日链接被误跳。
- 竞品 404 已增加分层复核状态：首次或疑似失效链接只有在目标明确404且同浏览器正常商品对照可用时，才按至少间隔10分钟累计证据，3次后首次确认失效；MySQL保存疑似/确认状态和复核审计，前端展示复核队列。历史已确认失效链接在后续批次中若公开商品数据再次返回404，按一次复核立即维持确认失效并成为本批终态，不再重复三次页面与对照检查；网络或服务异常仍重试，后续采集成功自动恢复正常。
- 当前是用户要求的最小模块；原独立工具中的关键词/类目排名、卖家上新、Buy Box 历史占有率和六工作表 Excel 尚未迁入，后续只能在保持现有销量口径与批量架构的前提下逐项增加。

## 8. 修改日志

| 日期 | 修改摘要 | 主要文件 | 验证与结论 |
|---|---|---|---|
| 2026-08-01 | 扩展竞品雷达跟卖与价格信号：商品采集枚举全部变体的主报价和`other_offers`，仅将明确含Takealot URL/PLID的报价按PLID幂等加入监控清单；原始链接始终保留，同组已有活跃目标不重复入队，新建/重新激活目标以`auto_discover`审计并追加到活动批次。监控链接管理按原始/跟卖关系折叠为下拉组，关闭组不挂载成员，展开后显示成员卖家和已采集价格。经营信号按所选北京时间区间最旧/最新快照新增涨价、降价、价格不变、不可比与价格差额，并纳入经营信号筛选和详情证据 | `src/takealot_ops/competitors/`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/storage/`、`frontend/competitor/`、相关测试、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 专项`57 passed`，完整回归`347 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过；正式MySQL无损增加并索引`offer_group_plid`，既有目标未回填数为0。正式ERP经`restart_erp.ps1`重启，`/api/health`返回`ok`，首页及`index-Ch3-QiuU.js`、`index-BVvSO-xH.css`均HTTP 200。按既定偏好未执行浏览器自动化；无法明确识别的公开报价不猜造链接 |
| 2026-08-01 | 修复早间库存漏爬后整页商品占位：正常早间完整快照继续优先且不可覆盖；早间漏爬/不完整时，改用D+1当天10:05后的最新完整成功库存补齐并持续更新，完整值不被后续不完整拉取降级。页面与Excel库存行改为“次日实采”，库存/日期总格集中注明“早间库存漏爬已解决”、来源和精确北京时间；商品值补齐后不再逐项标漏爬，原整批失败原因继续保留。另将`Cancelled by Takealot - DC Stock Inquiry`归为排除的取消单，消除误报质量失败 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/storage/models.py`、`frontend/competitor/`、`config/sale_status_rules.yaml`、测试、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报/指标专项`63 passed`，完整回归`341 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过；正式ERP重启后`/api/health`返回`ok`并加载`index-DaCTkt0c.js`、`index-CNA7XVFk.css`。北京时间2026-08-01 11:16:03手动完整拉取成功，业务日2026-07-31的库存快照410条、空值0、合计582，商品级漏爬0；早间HTTP 403仍保留为整批说明，2026-07-28以来未知销售状态质量事件为0。按既定偏好只使用HTTP、数据库、测试和构建验收，未执行浏览器自动化 |
| 2026-07-31 | 加固竞品待重试调度与人工重试留痕：普通可恢复失败的三次自动重试前分别间隔1、2、4条其他待执行任务，库存未探测的两次自动复探前分别间隔1条和2条；当轮剩余任务不足时保留待重试，不再退化为队尾立即重试。任务爬取详情内全部成功/待重试链接均可打开监控队列操作页；待重试来源提供“人工重试并插队”，服务端用独立`manual_retry`来源记录批次操作者/时间，并在MySQL链接操作审计追加同一动作；尚未消费的重复请求幂等，上一条开始后允许再次人工重试 | `frontend/competitor/src/retryQueue.ts`、`tests/retryQueue.test.ts`、`package.json`、`src/pages/CompetitorsPage.vue`、`api.ts`、`types.ts`、`styles.css`、前端构建产物、`src/takealot_ops/competitors/batch.py`、`src/takealot_ops/erp/web.py`、`tests/unit/test_competitor_batch.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 指数退避Node单测`3 passed`，批次与ERP Web专项`37 passed`，完整工作区回归`340 passed`；仅含本次竞品改动的独立暂存快照回归`320 passed`，Ruff、Mypy（50个源码文件）及Vue TypeScript/生产构建通过，独立构建资源为`index-DdFQXC6f.js`与`index-Di9cvrHx.css`（两次回归均只有1条第三方TestClient弃用提示）。数据库`verify`通过。正式ERP已通过`restart_erp.ps1`重启为PID 2276，`/api/health`返回`ok`，首页及当前完整工作区资源`index-Byk7B3lK.js`与`index-vzGieKaS.css`均为HTTP 200。按既定偏好未执行浏览器自动化 |
| 2026-07-31 | 优化竞品雷达日常交互：“链接复核状态”默认收起，仅保留疑似/确认数量与展开按钮，展开时才挂载完整复核表；疑似/确认失效行可直接点击，即使没有成功快照也能打开当前 PLID 的独立监控队列操作页。点击“竞品最新状态”商品行继续打开原详情弹窗，并在弹窗中显示同一套修改、删除、活动批次插队和重新加入能力。批次逐商品结果统一收进一个“任务爬取详情”折叠区，展开后内部按成功任务与待重试任务分组 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-BeGvTnHg.js`与`index-2mbfkEtn.css`；静态断言确认旧复制链接逻辑已移除、复核状态行与最新状态行均可进入队列增删改/插队/重新加入入口，且成功/待重试共用一个详情展开区，`git diff --check`通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`，首页及最终JS/CSS均为HTTP 200。按既定偏好未执行浏览器自动化或浏览器交互验收；本次纯前端与文档调整未运行Python测试 |
| 2026-07-31 | 降低竞品采集整体频率并统一由服务端执行：第一条直接启动，从第二条起每条公开数据浏览器租约前随机冷却5至10秒，网络失败后重建浏览器也不能绕过；首次预热后随机等待4至7秒、变体请求间3至6秒、额外评论页间2至5秒。前端1秒只作界面交接，403/429/超时/5xx退避、10分钟VPN恢复、最多25条浏览器串行复用、可见库存浏览器逐条隔离均保持不变 | `src/takealot_ops/erp/web.py`、`src/takealot_ops/competitors/api.py`、`tests/unit/test_erp_web.py`、`tests/unit/test_competitors.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 节流/竞品专项`25 passed`、完整回归`338 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）及数据库`verify`通过。正式ERP已重启为PID 2780，`/api/health`返回`ok`且首页为HTTP 200；原326条竞品批次仍保持人工暂停断点，已完成75条、待继续251条，未擅自恢复。按既定偏好未执行浏览器自动化 |
| 2026-07-31 | 修正运营日报顶部异常卡时间口径：“同周期销量版本差异”和“库存不平”分别汇总截至日报日仍未处理的历史版本、库存待办，版本卡去掉“个商品”单位，“已确认”继续只算当日；数据完整性说明每条补充数据日期，默认近3天并支持任意起止日期查询，所选范围不再截断前8条 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 新增默认近3天、自定义范围、每条日期及倒置范围回归；日报及ERP接口专项`61 passed`（1条第三方弃用提示），Ruff、Mypy、Vue TypeScript检查与生产构建通过，最终资源为`index-2tpE0X7g.js`与`index-D2CN5B-7.css`；数据库`verify`确认2026-07-31无数据质量事件，`git diff --check`通过（仅现有CRLF提示）。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`，首页和两项最终资源均为HTTP 200。按既定偏好未执行浏览器自动化 |
| 2026-07-30 | 修复普通待重试项不会在本轮队尾自动执行：404疑似失效、409页面不确定、单条连接失败及5xx等可恢复失败在完成其余链接后自动重试1次，仍失败才保留给后续继续；库存未探测继续使用原有最多3次规则。续爬队列现在优先保留明确失败索引，即使同一PLID曾有成功/插队结果也不会被成功集合误过滤；任一插队尝试成功会同步清除该索引旧失败状态 | `frontend/competitor/src/pages/CompetitorsPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日志还原确认2026-07-27批次在21:00以`pending=2`提前结束，`PLID101534220`与`PLID99141891`直到21:03人工继续后才于21:04复核，根因为已入队索引阻止失败项追加到队尾。Vue TypeScript检查与生产构建通过，最终资源为`index-DFuKfcmV.js`与`index-Dch9i85E.css`；`takealot_ops.cli verify`通过，2026-07-30无数据质量事件；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`，首页和两项资源均为HTTP 200。当前326条批次的用户手动暂停断点保留。按既定偏好未执行浏览器自动化；本次纯前端调整未运行Python测试 |
| 2026-07-30 | 为“监控链接管理”弹层增加每页20/50/100条选择，继续只创建当前页链接节点；搜索或切换每页数量时自动回到第1页，页数随筛选结果和所选数量重新计算。链接操作记录仍保持服务端每页20条，避免历史审计增长后一次加载全部记录 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-7CuIbBd1.js`与`index-Dch9i85E.css`；`takealot_ops.cli verify`通过，2026-07-30无数据质量事件；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`，首页和两项最终资源均为HTTP 200。当前326条竞品批次由用户手动暂停，停止前已开始的`PLID70540744`在重启前保存成功，断点保留。按既定偏好未执行浏览器自动化；本次纯前端调整未运行Python测试 |
| 2026-07-30 | 优化竞品雷达长列表负担：监控链接管理与链接操作记录均改为固定视口弹层，关闭时不创建明细DOM；管理清单每页最多渲染20条，操作记录首次打开才请求且服务端每页20条。竞品最新状态新增上一页/下一页及每页20/50/100条选择，只创建当前页商品行；重复目标跳转会先切到目标所在页。监控链接与竞品最新状态两个SKU搜索框均支持粘贴完整Takealot链接，并通过正则提取PLID后匹配 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-D9qMDTbm.js`与`index-CvxMsO8o.css`；`takealot_ops.cli verify`通过，2026-07-30无数据质量事件；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`，首页和两项最终资源均为HTTP 200。活动竞品批次因重启窗口于321/325触发保护暂停，18:26:06从保留队列自动恢复并继续处理原PLID，未新建批次或从头重跑。按既定偏好未执行浏览器自动化；本次前端调整未运行Python测试 |
| 2026-07-30 | 补齐竞品采集暂停与库存未探测原因留痕：日志确认12:40和17:16新增链接提示、16:26 `PLID93033479`库存复探提示均由修复前旧标签页误写为`paused`，并非真实连接类熔断；当前构建已把两类信息隔离为非阻塞运行提示，服务端也会把旧页误交的这两类`paused`降级为`progress`、保持批次活动并记录`submitted_event=paused`，不再依赖所有旧标签已刷新。库存探测现在按“清理购物车/打开商品页/等待购买区/加购/打开购物车/校验库存”记录失败阶段，归类常见浏览器网络错误并把变体、SKU和原因同时写入持久库存说明、424响应、页面运行提示与逐链接日志；真实库只读核对确认本次`PLID93033479`为默认款SKU`206198754`打开购物车时`ERR_CONNECTION_CLOSED`。真正连续两条连接类失败暂停时记录两个PLID、连接/HTTP状态和各自原因，新增、插队和库存复探提示不参与计数 | `src/takealot_ops/competitors/stock.py`、`service.py`、`batch.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/pages/CompetitorsPage.vue`、对应测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 库存/采集专项`47 passed`、批次/ERP Web专项`35 passed`；完整回归`334 passed`（仅1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP已重启为PID 27880，`/api/health`返回`ok`并加载`index-CW_IBU77.js`与`index-9vYMQDyp.css`；未使用浏览器自动化 |
| 2026-07-30 | 继续压缩竞品可见库存窗口之间50至70秒的准备等待：日志确认队列交接本身约5秒，主要耗时来自ERP每条链接重复启动隐藏公开数据浏览器，以及变体/评论成功请求间的长随机等待。现改为服务器单任务内串行复用隐藏公开数据浏览器，最多25条主动回收，网络/浏览器异常、请求取消和服务退出立即关闭并在下一条重建；正常预热、变体和评论节流缩短，403/429/超时/5xx退避保持不变；前端正常队列交接由5秒降为1秒。可见匿名库存浏览器仍逐链接隔离，不改变库存、限购、未探测复排和VPN恢复口径 | `src/takealot_ops/competitors/api.py`、`service.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/pages/CompetitorsPage.vue`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 新增ERP连续链接复用同一公开浏览器、网络失败立即回收、下一条重建和服务退出关闭专项；竞品/ERP定向`44 passed`、完整回归`331 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过。正式ERP已重启为PID 30220，`/api/health`返回`ok`并加载`index-DZxR9VZZ.js`与`index-9vYMQDyp.css`；运行批次第215条首次预热约7.3秒，第216条记录“正在复用后台数据浏览器”，从链接开始到库存浏览器阶段约0.9秒。重启前已打开的旧标签仍保留原5秒交接，刷新加载新资源后才使用1秒交接 |
| 2026-07-30 | 修复竞品可见库存窗口之间偶发长时间无反馈及重复续爬冲突：将运行中新增链接、库存未探测复排等普通提示从批次停止原因中分离，424未探测会真正完成普通队列后再复探，不再在当前商品结束后误暂停。服务端为活动批次当前请求增加单链接租约，相同请求编号允许刷新接回，不同请求在上一条未完成时返回423且不能覆盖当前PLID；重复页面收到租约冲突后只停止自己的续爬，不计商品失败、不暂停现有服务端批次。逐链接新增“后台数据浏览器、商品与变体、全部评论、库存探测浏览器、保存快照、疑似失效复核”阶段并写入共享状态和轮转日志；所有账号每2秒显示同一阶段，后台公开数据阶段明确说明不会出现可见库存窗口 | `src/takealot_ops/competitors/batch.py`、`service.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`pages/CompetitorsPage.vue`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日志定位确认正常链接结果到下一条开始固定约5秒；14:07一次424被普通提示误写为`paused`，14:11手动继续与自动继续重叠后产生`TargetClosedError`。新增租约、同请求接回、阶段顺序及HTTP 423专项；定向`54 passed`、完整回归`330 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载`index-jqtg049k.js`与`index-9vYMQDyp.css`，进程PID 13268；原活动批次继续运行，新日志已依次写出后台浏览器、商品与变体、全部评论和库存浏览器阶段 |
| 2026-07-30 | 调整竞品活动批次队列、控制与共享显示边界：新增监控链接由服务端登记为“已自动插队”并放入待执行队头，当前商品不中断，连续新增后进先出，刷新/断点恢复继续按服务端顺序；手动插队仍是额外执行一次并保留原位置，成功后本批按钮显示“已插队”并禁用，重复请求幂等，不产生多个优先副本。未消费自动队头、手动插队和已接受记录写入`logs/competitor-batch-queue.json`，ERP重启后同批恢复继续执行，目标开始后移除待执行项，无剩余项完成后清理。批次启停和直接采集只允许用户名`kxx`；其他账号即使使用管理员模板也由后端拒绝，但具备竞品采集权限时仍可新增链接和手动插队。`kxx`运行中可切换“显示检测浏览器”，从下一条链接生效。活动批次期间所有账号的进度及各统计统一使用服务端状态 | `src/takealot_ops/competitors/batch.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/App.vue`、`api.ts`、`pages/CompetitorsPage.vue`、`tests/unit/test_competitor_batch.py`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日志核对确认旧版按钮曾在11:59:37和11:59:40对同一PLID重复提交两次，随后服务重启前未执行，解释了“点击无用”的观感；新版以批次已接受记录锁定按钮、服务端幂等和运行时持久队列修复。重启恢复专项覆盖自动队头、手动插队、已接受记录恢复及目标开始后仅移除待执行项；队列/ERP Web专项`30 passed`、完整回归`328 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP已重启，`/api/health`返回`ok`并加载`index-CzZai1s7.js`与`index-9vYMQDyp.css`，进程PID 24244、工作集157.9MB；运行中批次从第77条继续，持久队列文件已生成并绑定当前`batch_id`，当前无未消费插队项 |
| 2026-07-30 | 将店铺范围与公共模块功能权限彻底分离：竞品雷达及其查看、采集、批次、目标管理接口不再要求已接入店铺；仅含NFT102的报表工作台同样只校验功能权限；共用商品缩略图接受店铺、竞品或日报任一查看权限。经营总览、商品、经营坐标、风险、运营日报、全量刷新及既有店铺报表继续要求已接入店铺。前端仅在店铺数据模块显示无店铺/待接入占位，0店铺账号登录后自动进入第一个已开放公共模块 | `src/takealot_ops/erp/web.py`、`frontend/competitor/src/App.vue`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 正式账号`pxg`只读核对为启用、已有竞品查看/采集等功能权限、`all_stores=false`且0个店铺，未改账号数据。新增0店铺公共模块回归覆盖竞品查看/采集、共用缩略图、NFT102放行和店铺数据403隔离；ERP Web文件`20 passed`、完整回归`326 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过。正式ERP已重启并加载`index-H_hYjjNj.js`与`index-CA6_R_Rk.css`，健康、首页、JS、CSS及认证状态接口均为HTTP 200；构建JS包含0店铺提示和公共模块放行文案。按既定偏好未执行浏览器自动化 |
| 2026-07-30 | 将已验证但此前仅在隔离分支的店铺授权基础正式整合并部署到当前ERP：用户权限页新增店铺目录、预留/重命名/停用入口、账号逐店勾选和“全部店铺（含未来新增）”；后端新增账号—店铺多对多关系，管理员/老板全店账号自动纳入未来新店，限定运营只读取明确授权店铺。登录后顶栏新增账号范围内的“当前查看店铺”选择器；选择待接入店铺时只显示安全占位并保留管理员前往权限配置的入口，不挂载业务页面、不沿用当前店铺数据。当前单店数据登记为唯一`current`已接入店铺，只分配待接入店铺的账号不能读取现有业务接口；店铺数和每账号分配数均不设硬编码上限 | `src/takealot_ops/erp/auth.py`、`web.py`、`src/takealot_ops/storage/models.py`、`migrations.py`、`frontend/competitor/src/App.vue`、`api.ts`、`types.ts`、`pages/UsersPage.vue`、权限/ERP Web测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 店铺授权/迁移/ERP Web专项`22 passed`、完整回归`325 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式变更前一致性备份`takealot-20260730-025306-812858.sql.gz`校验通过；MySQL无损新增`erp_stores`、`erp_user_stores`及`erp_users.store_access_all`，3个旧账号全部保留全店范围。首次内联迁移核对受PowerShell标准输入编码影响写入了乱码店名，随即以Unicode转义修正并复核为“当前店铺”，其他数据未受影响。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载`index-DkGT1Ka-.js`与`index-FHzvI_VO.css`；首页、JS、CSS及认证状态接口均为HTTP 200，构建JS包含顶栏店铺选择、待接入安全占位、店铺目录、账号店铺范围、全店动态范围和`/api/auth/stores`。按既定偏好未执行浏览器自动化 |
| 2026-07-30 | 为竞品库存“未探测”增加轮后自动复探，并加入人工链接插队：可购买变体库存探测失败时仍保存商品/评论/变体快照，但接口返回独立424并进入待重试，不计断网连续失败；先完成普通链接，再在队尾自动复探两次，连续3次仍失败才保留待重试。监控链接明细新增“插队”按钮，服务端统一登记优先目标，当前商品不中断；插队只额外增加当前商品之后的一次优先探测，原队列位置不删除，走到原位置时仍按原顺序再探测。无活动批次或目标正在执行时拒绝 | `src/takealot_ops/competitors/service.py`、`batch.py`、两个竞品API入口、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`pages/CompetitorsPage.vue`、`styles.css`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 库存失败持久化/424分类、批次优先目标登记与开始即消费、无活动批次拒绝、正式ERP插队接口专项已覆盖；竞品/批次/ERP Web定向`52 passed`、完整回归`324 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过；插队保留原队列位置调整后，队列/ERP Web专项`26 passed`并重新通过Vue TypeScript/生产构建，最终资源为`index-DqpC6BG9.js`与`index-CdWcgCiW.css`。部署前原326条批次已把待重试由9降至0并形成10条确认失效终态；正式ERP通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终资源，最终复核进程PID 31876、工作集149.4MB、私有内存474.7MB、341个句柄，项目Playwright/Chrome/Node残留为0 |
| 2026-07-30 | 将长期确认失效链接改为一次复核终态：首次或疑似失效仍须按至少10分钟间隔累计3次有效页面与正常商品对照证据；历史已持久确认为失效的链接在后续批次中若公开商品数据再次返回404，立即维持确认失效并终止本批该链接，不再重复三次交叉复核，同时保留原复核次数和对照证据。403、导航失败、429、5xx等连接异常仍进入待重试，任意成功采集仍恢复正常 | `src/takealot_ops/competitors/repository.py`、`src/takealot_ops/competitors/service.py`、`tests/unit/test_competitors.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 历史确认失效专项覆盖再次404不调用页面交叉复核、直接返回不可重试终态、保留原3次计数和对照PLID；竞品/存储/ERP Web定向`42 passed`，完整回归`320 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`和`git diff --check`通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`；新进程PID 17580，启动工作集150.2MB、私有内存475.5MB、345个句柄，排除项目CodeGraph服务后Playwright/Chrome/Node残留为0 |
| 2026-07-29 | 修复商品缩略图缓存键锁的长期内存积累：同一图片并发首次请求继续共用一把锁并只下载、转换一次；锁表改为记录活跃使用人数，成功、下载失败、转换异常或等待退出时均在`finally`递减，最后一个使用者退出后立即删除键，不再按历史图片数量永久保留锁对象 | `src/takealot_ops/erp/product_images.py`、`tests/unit/test_product_images.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 新增同键双线程并发、失败释放和100个不同失败键不滞留三类回归，缩略图专项`18 passed`；完整回归`319 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`和`git diff --check`通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`；新进程PID 54676，启动基线工作集150.1MB、私有内存475.4MB、341个句柄，项目Playwright/Chrome/Node残留为0 |
| 2026-07-29 | 优化竞品链接管理与操作留痕性能：315条监控链接默认归纳为总数、已有历史、待首次采集三项汇总，收起时不创建明细DOM；展开后支持名称/PLID/URL搜索并固定每页最多20条。操作留痕改为仅在展开时加载，并由服务端按北京时间日期条件统计总数、每页20条分页返回，避免一次查询和渲染整个日期区间 | `src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`types.ts`、`pages/CompetitorsPage.vue`、`styles.css`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 定向`23 passed`、完整回归`316 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、Vue TypeScript/生产构建、数据库`verify`和`git diff --check`通过。正式ERP加载`index-Cb7sjgnP.js`与`index-BKyAdKPP.css`，`/api/health`返回`ok`；在第221条成功后切换，切换窗口刚启动的第230条没有结果并继续保留在待续爬队列，原326条批次恢复后已继续到第269条，待重试由11降至9。按既定偏好未执行浏览器自动化 |
| 2026-07-29 | 增加纯本机D盘binlog连续归档：所有完整备份和binlog副本统一进入`D:\南非店铺数据抓取\backups\`，归档进程使用`mysqlbinlog --raw --stop-never`从最新共有文件续接，断线每分钟重启；闭合文件每天校验并生成SHA-256，当前写入文件不删除，归档保留35天。新增专用低权限账号安全配置脚本、状态/预检/维护CLI和完整备份binlog坐标清单；管理员密码只在本机隐藏输入，随机备份密码仅写入Git忽略`.env`。任务计划注册被Windows拒绝时自动降级为当前用户登录启动守护循环 | `src/takealot_ops/binlog_archive.py`、`src/takealot_ops/scheduler.py`、`src/takealot_ops/settings.py`、`src/takealot_ops/cli.py`、`scripts/configure_binlog_backup.ps1`、`scripts/install_binlog_archive_task.ps1`、两个守护循环脚本、权限SQL样例、相关测试、`.env.example`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | binlog/备份专项`20 passed`；当前含并行竞品改动的完整工作区回归`316 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）及四个PowerShell脚本语法检查通过。正式专用账号和登录启动项已配置，D盘归档4份共72520481字节、同步差距0字节，3份闭合文件校验通过；完整备份`takealot-20260729-103411-686192.sql.gz`二次校验通过并记录起点`DESKTOP-NTRMANG-bin.000004:72519292`。隔离恢复演练仍未执行，不宣称PITR已验证 |
| 2026-07-29 | 将竞品链接输入改为MySQL持久化清单与正式增删改查：新增目标在活动批次中自动追加到服务端队尾并由持有页面合并进本地断点，修改/删除只影响后续批次且删除保留历史；所有用户新增、修改、删除在同一事务中追加操作人、时间、PLID及修改前后URL审计，审计区默认收起并按北京时间日期区间查询。重复PLID无快照时只提示清单已存在，有全历史快照时提示已纳入每日采集并可清除筛选、恢复完整观察区间、滚动聚焦到最新状态对应商品行 | `src/takealot_ops/storage/models.py`、`src/takealot_ops/competitors/batch.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`types.ts`、`pages/CompetitorsPage.vue`、`styles.css`、批次与ERP Web测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 定向`23 passed`、完整回归`309 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、Vue TypeScript/生产构建、数据库`verify`及备份二次校验通过。正式变更前备份`backups/takealot-20260729-101244-504064.sql.gz`，SHA-256为`97113ed49165234d069e87af20d737595a47941eb2cb32b583932f78ef24056f`；正式库无损新增`competitor_target_audits`，315条活跃目标保持不变且均有历史快照，审计从0行开始。正式ERP加载`index-CFjwfbsX.js`与`index-BvzlmWYk.css`，`/api/health`返回`ok`；部署等待活动商品完成后切换，原325条批次从第270条继续，没有替换为315条新清单。按既定偏好未执行浏览器自动化 |
| 2026-07-29 | 将 MySQL 备份升级为纯本机的可校验备份：一致性转储成功后使用 gzip 压缩，生成 JSON 清单和 SHA-256 文件，并重新读取完整压缩包核对哈希、大小、转储头及完成标记；失败自动清理半成品。新增手动备份和已有备份校验 CLI，新格式按最近14天全留、90天每日、1年每周、7年每月分层保留，升级前 `.sql` 保持不动。文档明确本机备份无法防整机、同一物理硬盘或勒索软件同时损坏 | `src/takealot_ops/scheduler.py`、`src/takealot_ops/cli.py`、`tests/unit/test_scheduler.py`、`tests/unit/test_cli.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 正式备份 `takealot-20260729-094041-751835.sql.gz` 生成并二次校验通过，原始8133063字节、压缩1593468字节、SHA-256为`6b872eefe18b9f5a091179829aa58717414075757d736c231bd7e259fefb65dd`；备份/CLI专项`7 passed`、全量回归`306 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`和`git diff --check`通过；正式ERP已由`restart_erp.ps1`重启，`/api/health`返回`ok` |
| 2026-07-29 | 增强竞品变体快照口径：识别 `Limited to N per customer` 后实际验证限购数量，能够加入时只记录“至少 N”并保留每位客户限购标记；变体名称清洗 API 嵌套对象，数据库按快照保存每个变体自己的 gallery 图片和限购证据，详情接口返回所选区间全部变体记录，前端当前表按最新快照展示、历史时间线按每个快照展开当时所有变体库存和独立图片。默认款缺少独立变体图时回退到所属商品快照主图，非默认旧变体缺图才显示“暂无图片”，不复用统一主图冒充 | `src/takealot_ops/competitors/`、`src/takealot_ops/storage/`、两个竞品 FastAPI 入口、`frontend/competitor/src/pages/CompetitorsPage.vue`、`styles.css`、`types.ts`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 限购/变体/存储专项`48 passed`，完整回归`307 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（49个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式库变更前备份`backups/takealot-20260729-092903-317721.sql`，无损新增`competitor_variant_snapshots.image_url`与`customer_purchase_limit`并核对存在；正式库读取核对1,879条变体快照，其中1,332条默认款/默认变体均成功取得所属快照主图。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载`index-Baqn0fSf.js`与`index-D96eLly7.css`。按既定偏好使用测试、构建、HTTP与数据库验收，未执行浏览器自动化；独立变体图片与限购标记从下一次成功采集开始写入，旧快照保留真实缺失，默认款按所属快照主图补足 |
| 2026-07-29 | 为运营日报增加次日09:00周期末定时拉取：新`pre_close`版本仍落在北京时间10:00结束前的同一核对周期，保存不可覆盖的订单版本并参与全部成功版本销量差异判断；库存继续固定采用D+1日早间首次成功实采快照。旧日报通过启用边界显示“尚未启用”，不会倒标漏采。Windows安装脚本扩展为10:05、18:00、次日09:00和18:30四任务 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/cli.py`、`scripts/install_scheduled_task.ps1`、`frontend/competitor/`、相关测试、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报/CLI/调度专项`47 passed`，完整回归`301 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`、PowerShell语法、Vue TypeScript/生产构建和`git diff --check`通过；正式任务“Takealot 运营日报周期末更新”已安装，下一次运行2026-07-30 09:00，命令为`daily-report-run --slot pre_close`，具备错过补跑、忽略并发及每5分钟最多重启3次。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载`index-BFEvJvXP.js`与`index-CQYdr_QY.css`；按既定偏好使用HTTP、任务配置、测试与构建验收，未执行浏览器自动化 |
| 2026-07-29 | 修正竞品批次失败统计口径：临时403、网络和服务异常不再按错误明细累计显示为“失败”，统一展示当前待重试队列，单条后续成功即从待重试计数移除；确认失效改为持久化链接复核记录与本批新增终态PLID的去重结果，独立显示并长期保留，不会因其他链接成功而清零。共享批次和本地结果区同步使用“待重试 / 确认失效 / 待续爬”文案 | `frontend/competitor/src/pages/CompetitorsPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-DVtFd7v-.js`与`index-OqwYQsKy.css`，`git diff --check`通过；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终JS。Chrome管理员正式页在活动批次中确认成功由65增至68、待重试由184降至181，同时确认失效保持10、待续爬257；本地结果区显示“待重试181个”和“确认失效10个，长期保留”。ERP页面无自身控制台错误，仅有1条Chrome扩展历史`Failed to fetch` |
| 2026-07-29 | 增强竞品采集在VPN临时中断与页面刷新时的恢复能力：Takealot公开API和商品页403改为可重试连接异常，单链接有限退避后若恢复则继续，持续403与导航失败/429/5xx一样由前端连续2条熔断并进入10分钟循环自动续爬；刷新同一活动批次时先用服务端当前索引和原请求编号接回正在执行的链接，等待原探测任务后再回到最早失败项，避免刷新前后两个队列并发争用Playwright | `src/takealot_ops/competitors/api.py`、`frontend/competitor/src/pages/CompetitorsPage.vue`、`tests/unit/test_competitors.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 竞品/批次/ERP Web专项`41 passed`，完整回归`299 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过，最终资源为`index-D-CAcaJY.js`与`index-OqwYQsKy.css`。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终资源；Chrome正式页从倒计时自动续爬第31条，执行中刷新后日志以同一请求编号记录`link_reused`，原请求完成后再进入第41条，无新的并发`TargetClosedError`，控制台错误为0。403恢复/持续失败使用隔离模拟回归，未为验收主动关闭VPN |
| 2026-07-29 | 精简“低浏览高转化”异常详情：删除“低浏览边界”和“高转化边界”两张阈值卡，同时把结论和剩余字段提示改为直接描述近30天浏览量、当前已记录销量及近30天转化率；后台分位阈值和异常判定逻辑保持不变 | `frontend/competitor/src/pages/RisksPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查和生产构建通过，最终资源为`index-m9IpzB9V.js`、`index-OqwYQsKy.css`；正式ERP已重启，`/api/health`返回`ok`并加载最终资源。按用户要求未使用浏览器自动化 |
| 2026-07-29 | 整理竞品雷达筛选器排版：搜索、库存和经营信号保持为第一行，日期选择改为独立的整行“观察区间”区域，当前区间与结果数集中在左侧，开始日期、结束日期和重算按钮在右侧成组对齐；窄屏自动改为双列或单列，避免起止日期、摘要与按钮被网格拆散 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`AGENT.md` | Vue TypeScript 检查与生产构建通过，最终资源为 `index-FWH4o_hS.js` 与 `index-OqwYQsKy.css`；正式 ERP 已通过 `restart_erp.ps1` 重启，`/api/health` 返回 `ok`。Chrome 管理员正式页确认桌面端日期组整行对齐，820像素窄屏下两个日期并排、操作按钮整行显示，页面控制台错误为0 |
| 2026-07-29 | 细化风险异常证据展示：销量上升/下降弹窗在均值判定数据之前直接展示截至异常日的15天每日销量柱状图，每根柱显示日期与整数销量；高浏览低转化、低浏览高转化同时展示异常时近30天浏览量、近30天转化率和本库销量汇总。本库销量窗口最多取异常日截止30个自然日，覆盖不足时明确显示实际记录天数、总销量和起止日期，不把不足30天的数据标成“近30天销量”。所有新增销量证据由历史逐日指标生成，不读取当前商品值 | `src/takealot_ops/erp/service.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/RisksPage.vue`、`frontend/competitor/src/styles.css`、`tests/integration/test_erp_service.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | ERP风险投影专项`7 passed`，完整回归`296 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`和前端生产构建通过；正式库只读核对确认销量异常返回15个日期点，历史流量异常按实际1至2个已记录自然日显示销量汇总。正式ERP已重启，`/api/health`返回`ok`并加载当前工作区联合构建资源`index-DHdrcRla.js`、`index-Ban6En3f.css`。按用户要求未使用浏览器自动化；因工作区同时存在非本任务的竞品页布局源码及联合构建产物，提交时仅纳入本任务源码、测试和文档，不覆盖或混入该并行修改 |
| 2026-07-29 | 为竞品雷达增加北京时间观察日期区间，并把经营信号改为只读查询时按每个商品区间内最旧与最新快照重算：库存净变化、净流出/补货、新增评论、观察期销量区间和趋势说明不再复用采集时保存的相邻快照旧值；最新状态、详情变体和原始历史同步限制在所选区间。库存比较继续要求首尾变体键、SKU、卖家集合一致且两端库存精确，库存不可比时新增评论仍可独立计算 | `src/takealot_ops/competitors/service.py`、两个竞品 FastAPI 入口、`frontend/competitor/src/api.ts`、`types.ts`、`pages/CompetitorsPage.vue`、竞品存储测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 竞品专项`20 passed`、完整回归`295 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过。正式MySQL只读核对可用区间为2026-07-23至2026-07-29：全区间315个商品净流出合计219，切换至7月28至29日后合计15。正式ERP通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载`index-DtkHQDH2.js`；Chrome管理员页确认日期默认值、315个结果、区间说明和详情实际比较时间，`PLID100499473`在7月28至29日为单快照“待建立基线”，切回全区间后重算为“库存净流出（待验证）3”，控制台错误为0 |
| 2026-07-29 | 将风险与质量的异常详情改为按类型展示可核对的触发证据，普通提示级“不可购买”除外：销量下降/上升显示近3日与近15日日均、两组差值；高浏览低转化和低浏览高转化显示异常时实际浏览量/转化率及当日分位阈值；疑似断货显示异常时库存、状态与异常日前7日下单；数据停止更新显示最近Offer采集时间、实际间隔和小时阈值；未知销售状态显示具体未配置状态。触发数据随异常落库，历史弹窗不使用当前指标冒充当时证据 | `src/takealot_ops/metrics/service.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/RisksPage.vue`、`frontend/competitor/src/styles.css`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 指标专项`29 passed`，完整回归`294 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`、前端构建和`git diff --check`通过。正式库变更前备份`backups/takealot-20260729-034508-830579.sql`，随后重算2026-07-15至2026-07-29共3676条指标；浏览器核验销量趋势、流量转化、疑似断货、未知销售状态均显示对应触发证据，普通提示级“不可购买”不显示证据区，控制台无错误；正式ERP`/api/health`返回`ok`并加载`index-uL9M1NO3.js`、`index-I5qlwevJ.css` |
| 2026-07-29 | 优化运营日报宽表鼠标滚轮交互：仅当鼠标位于矩阵底部横向滚动条命中区时，把普通滚轮的纵向增量转换为横向位移并阻止矩阵或外层页面上下移动；离开滚动条后仍保持正常纵向滚动。兼容像素、行和整页三种滚轮增量模式，并在滚动条提示中明确该操作 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-CE-0tzhZ.js`与`index-Bk5hsAmF.css`；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`且首页加载最终资源。按全局偏好未执行浏览器调试或浏览器交互验收 |
| 2026-07-29 | 重定义风险与质量的销量趋势：截至指标日的近3个自然日日均下单件数低于近15日均值时判为“销量下降”，高于时判为“销量上升”，均值相等不触发；两个窗口均包含指标日，只有完整覆盖15日窗口才判断，已覆盖的空销量日按0件参与平均，历史不足保持未判定，并移除旧的单日对前7日、最低销量和涨跌百分比门槛。同步更新异常详情字段、中文标签、Excel列、配置、测试和业务文档 | `config/anomaly_rules.yaml`、`src/takealot_ops/metrics/service.py`、`src/takealot_ops/dashboard/labels.py`、`src/takealot_ops/exports/excel.py`、相关测试、`README.md`、`docs/PROJECT_STATUS.md`、需求/实施文档、`AGENT.md` | 指标/风险接口/报表专项`55 passed`，补充完整窗口保护后指标专项`23 passed`，风险中文与Excel均值列专项`23 passed`；完整回归`294 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）、数据库`verify`和`git diff --check`通过。正式库变更前备份`backups/takealot-20260729-032427-466971.sql`，随后按新规则重算2026-07-15至2026-07-29共3676条指标；7月29日为首个完整15日窗口，得到销量下降59条、销量上升21条，更早日期旧趋势记录清零；异常详情已核对保存短/长窗口天数和两组日均值。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok` |
| 2026-07-29 | 为项目接入CodeGraph本地AI代码图谱：安装1.5.0命令行并配置Codex MCP，初始化Python/Vue/TypeScript/FastAPI关系索引；新增项目级`codegraph.json`排除可重建前端构建产物，新增AI查询指南，并要求后续AI在架构、调用链和影响分析时优先取得图谱上下文但仍执行原有验证 | `.codegraph/.gitignore`、`codegraph.json`、`AGENTS.md`、`docs/CODEGRAPH_AI_GUIDE.md`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md`、本机Codex配置 | CodeGraph状态检查通过：最终索引110个文件、2801个节点、8211条关系、62个FastAPI路由且无读取错误；初次唯一错误来自工作区已删除的旧`dist`文件，加入项目排除规则后重建索引消除，并以实际查询、影响分析和受影响测试检查验证。未运行项目代码测试、未重启ERP、未调用Takealot接口或写入MySQL（本次只修改AI开发辅助配置与文档） |
| 2026-07-29 | 修复运营日报早间首次成功采集已能计算出跨日报日库存不平时，只显示红标却仍持久化为“等待晚间”、因而没有进入待办、全局提醒和实时推送的问题：库存连续性不平现在立即持久化为待核对，后续晚间或手动版本差异仍按既定优先级处理，公式恢复相符时自动关闭库存待办；同时移除商品宽表的“上一组/下一组”和24列翻组，筛选后的全部商品改为同一矩阵并通过底部横向滚动条查看到底 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 变更前备份`backups/takealot-20260729-025047-299056.sql`；按新规则重算业务日2026-07-28共更新6条，正式库从0条持久化待办恢复为6条库存待办和6条全局提醒。日报专项`35 passed`、完整回归`292 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（49个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过，最终资源为`index-BcZhPrfB.js`与`index-DV4nRhtd.css`。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终资源；Chrome管理员正式页确认人工核对待办6、库存不平6、409个商品列全部渲染、上一组/下一组按钮均为0，横向滚动从0到最大63663并显示最后一个SKU `9902396433169`，控制台错误为0 |
| 2026-07-29 | 精简运营日报宽表显示：商品列表头的 SKU 只保留实际值，不再添加“平台 SKU”前缀；日期列的订单总数和库存总数只显示纯数字，不再附加“单”或“件”。缺失合计继续显示“—（缺 N 个）”，商品没有 SKU 时继续回退显示 Offer ID | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终资源为`index-_AKtrecH.js`与`index-xEvgLKe8.css`；完整回归`291 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（49个源码文件）和数据库`verify`通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回HTTP 200与`ok`并加载最终资源；Chrome管理员正式页确认SKU前缀数量为0，订单合计显示`20/26/23/21`，库存合计显示`497/470/452/447`，合计单位数量为0，控制台错误为0 |
| 2026-07-29 | 优化运营日报纵向矩阵：内部视窗由5日收紧为完整4日，商品名与平台 SKU 表头保持吸顶且滚动不再串到外层页面；日期列的订单和次日早间实采库存改为按当日全部商品汇总，不受搜索、筛选或24列翻组影响，任一商品缺失则显示“—（缺 N 个）”而不展示部分总数。汇总单元格按反馈移除“合计”字样与加粗小字样式，统一为表格正文的13px宋体正常字重；同时阻止首屏加载期间的实时刷新竞态把默认最新日期位置顶回最早日期 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整回归`291 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（49个源码文件）、数据库`verify`及Vue TypeScript/生产构建通过，最终资源为`index-D981wOwG.js`与`index-D9AiXaYd.css`。正式库只读核对最近5日订单总数为6/20/26/23/21、完整库存总数为—/497/470/452/447，7月24日库存404个商品均缺失。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终资源；Chrome管理员正式页确认视窗内容高度736px、恰好可见7月25日至28日4个日期且默认定位最新日期，汇总显示`21 单`和`447 件`，缺失日显示`—（缺 404 个）`，日期汇总在翻到第2组商品后保持不变；汇总字体为13px宋体/正常字重，SKU表头为`sticky`，滚动边界为`contain`，控制台错误为0 |
| 2026-07-29 | 将7天滑动登录会话的自动续期间隔由5分钟调整为1天：正常会话距离上次续期未满1天时不再写入 MySQL 或刷新 Cookie，超过1天后的下一次有效请求才把到期时间顺延至最近使用后的7天；仍有效的旧12小时等短期会话继续在下一次访问时立即升级 | `src/takealot_ops/erp/auth.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`docs/requirements/takealot-operations-dashboard-prd.md`、`AGENT.md` | 新增未满1天不续期、超过1天后续期的边界回归；专项`1 passed`，完整回归`291 passed`（1条第三方TestClient弃用提示），Ruff和Mypy（49个源码文件）通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回HTTP 200与`ok` |
| 2026-07-29 | 将 ERP 登录会话由固定12小时改为7天滑动有效期：登录后先获得7天，到期前继续使用会同步顺延 MySQL 会话记录与 HttpOnly Cookie 至最近使用后的7天；为避免前端2秒轮询持续写库，续期最多每5分钟执行一次。主动退出及管理员修改账号权限、模板、密码或启用状态时继续立即撤销旧会话；正式切换时仍有效的旧12小时会话在下一次有效请求时立即升级，无需统一重新登录 | `src/takealot_ops/erp/auth.py`、`src/takealot_ops/erp/web.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`docs/requirements/takealot-operations-dashboard-prd.md`、`AGENT.md` | 新增登录初始`Max-Age=604800`、旧短期会话即时升级、认证恢复续期、5分钟内不重复续期及普通受保护接口续期回归；专项`1 passed`，完整回归`291 passed`（1条第三方TestClient弃用提示），Ruff和Mypy（49个源码文件）通过。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回HTTP 200与`ok` |
| 2026-07-28 | 将商品图片优化扩展到整个Vue ERP：新增前端统一缩略图URL工具，把商品中心、经营坐标、风险与质量、竞品雷达、运营日报及其弹窗/历史区共15个图片入口全部改为登录态同源缩略图；列表/卡片/表格/小型头图使用192像素，商品与风险详情大图使用640像素。后端缩略图缓存升级为有界多尺寸，白名单补充竞品使用的`media.takealot.com/covers_images/`，禁止任意重定向并继续限制源文件字节与像素；图片统一声明固有尺寸、异步解码，长列表懒加载和跳过离屏布局/绘制，重复商品/风险/竞品项使用Vue记忆化 | `src/takealot_ops/erp/product_images.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/productImages.ts`、`frontend/competitor/src/pages/ProductsPage.vue`、`RisksPage.vue`、`QuadrantsPage.vue`、`CompetitorsPage.vue`、`DailyReportPage.vue`、`frontend/competitor/src/styles.css`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、PRD、`AGENT.md` | 真实竞品原图成功转换为192×192/6318字节和640×640/37746字节；相关专项`30 passed`，完整回归`290 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（49个源码文件）、数据库`verify`、Vue TypeScript/生产构建及`git diff --check`通过，最终资源为`index-D3x0U7_L.js`与`index-Ua9Eevfy.css`。正式ERP通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终资源。Chrome正式页逐页验收：经营坐标404张、商品中心404张、竞品雷达315张、运营日报17张图片均只走本机缩略图接口，平台原图直连均为0；商品与风险详情640像素图片真实加载，竞品详情6张图片均为本机192像素缩略图；经营坐标热态复制响应303毫秒且SKU反馈正确，全部页面验收过程控制台错误为0 |
| 2026-07-28 | 修复经营坐标复制SKU时整页卡顿和原图解码占满内存：新增登录态同源商品缩略图接口，只允许读取Takealot官方S3商品图并限制源文件字节/像素，首次访问转换为最长边192像素JPEG后保存到可重建运行缓存；前端悬浮卡和全部商品卡统一使用缩略图，卡片增加懒加载、离屏内容可见性和Vue记忆化，复制/悬停状态不再深度重建404张卡片 | `src/takealot_ops/erp/product_images.py`、`src/takealot_ops/erp/web.py`、`pyproject.toml`、`frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、相关测试、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、PRD、`AGENT.md` | 修复前正式页为404张卡、11219个DOM节点，首屏21张800–2000像素原图估算解码264659200字节；20张真实原图抽样中位1029348字节，337个唯一原图预计压缩传输约330.8MB。真实1054512字节样本转换为4589字节、192×192 JPEG。完整回归`282 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（49个源码文件）、数据库`verify`和Vue TypeScript/生产构建通过，最终资源为`index-D6mfjIcF.js`与`index-bi2iVTs3.css`。正式ERP自动重启后`/api/health`返回`ok`并加载最终资源；Chrome正式页首屏9张缩略图估算解码1327104字节、直接S3图片0张、卡片`content-visibility:auto`，复制SKU热态响应292毫秒且反馈SKU正确，悬浮卡图片192×192并位于视口内，控制台错误为0。运行中的竞品批次从311/325继续推进到312/325，未从头重跑 |
| 2026-07-28 | 取消运营日报“人工修改”的操作备注要求：弹窗只保留数值和修改原因，不再显示或校验备注输入；接口允许省略/留空`note`并把空值保存为真正缺失，同时继续记录修改前后值、原因、操作人和时间。确认合并与确认库存差异的备注校验保持不变；已有无备注人工候选仍会默认作为可选确认来源 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、前端构建产物、`AGENT.md` | 日报/ERP Web专项`48 passed`、完整回归`274 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）及Vue TypeScript/生产构建通过，构建为`index-Doe_URNJ.js`与`index-DRb8Zimp.css`。回归明确覆盖人工修改省略`note`成功、空备注保存为缺失、确认合并和确认库存差异空备注仍返回422。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`且首页引用最终构建；Chrome管理员运营日报页加载正常、控制台错误为0，正式当前无待办卡可打开，未为验收写入演练数据 |
| 2026-07-28 | 将运营日报计划时刻与实际成功采集时间彻底分开：10:05/18:00只作为早间/晚间任务的计划触发时间；数据库原有`captured_at`继续记录最终完整成功时间，页面状态明确显示“实际成功：北京时间”，版本名称改为“早间/晚间采集（实际月-日 时:分）”，确认来源不再称固定时刻值。若内部重试或Windows任务第三次重启后才成功，早间库存快照、页面和Excel均按实际成功时间解释，库存行改称“次日早间实采”，不得伪装成10:05 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 模拟北京时间10:37才成功的早间版本断言其标签和`captured_at`均为实际时间；日报与CLI专项`35 passed`、完整回归`273 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）及Vue TypeScript/生产构建通过，构建为`index-CrAvJSjr.js`与`index-CFov2sN-.css` |
| 2026-07-28 | 为竞品雷达增加网络类暂停后的10分钟循环自动续爬：连续2条真实连接失败或Takealot临时服务错误时保存失败/未完成断点和下次尝试时间，提示卡显示逐秒倒计时；到期自动继续，同类失败再次暂停则重新安排10分钟。刷新继续倒计时，关闭后重开且已到期会立即尝试；其他批次占用时顺延。人工停止、权限/链接校验问题、确认失效和正常完成不自动重启，运营可在倒计时期间手动提前继续 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript生产构建、竞品批次/请求复用专项`7 passed`和`git diff --check`通过，生成`index-CrAvJSjr.js`与`index-CFov2sN-.css`；正式8501健康检查为HTTP 200并已返回该JS/CSS资源，构建产物确认包含断点版本5、`autoResumeAt`、10分钟循环调度及倒计时文案。未重启服务，原325条批次继续从293/325推进并开始第294条 |
| 2026-07-28 | 将运营日报顶部“商品”数量卡改为“现有商品库存总数”：接口改为汇总最新 `OfferCurrent` 中全部现有商品的平台可售库存，随“刷新全部数据”更新且不绑定日报固定早间快照；只在全部现有商品库存完整时返回合计，任一库存缺失则返回空值并单独返回缺失商品数，前端显示“—”和缺失提示，避免把缺失误算成0或展示不完整合计 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、前端构建产物、`AGENT.md` | 日报专项`33 passed`、完整回归`273 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）及Vue TypeScript/生产构建通过，构建为`index-7OBuf3N7.js`与`index-CNIZEL3N.css`。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`且首页引用最终构建；Chrome管理员真实运营日报页确认首卡显示“现有商品库存总数 450”，与正式库最新Offer库存汇总一致，控制台错误为0 |
| 2026-07-28 | 为竞品雷达所有商品栏补齐图片：链接复核队列读取该PLID最近一次成功快照，精品最新状态直接显示当前快照，详情弹窗、各变体库存和每条历史快照分别显示对应快照主图；变体当前复用所属商品快照主图。图片为空或加载失败统一切换为“暂无图片”。同时约束竞品主容器和弹窗最小宽度，窄屏只允许变体表内部横向滚动，不再撑宽页面 | `src/takealot_ops/competitors/service.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、`tests/integration/test_competitor_storage.py`、`README.md`、`docs/PROJECT_STATUS.md`、前端构建产物、`AGENT.md` | 竞品存储专项`19 passed`、完整回归`272 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过，最终构建为`index-U6KZ6xfg.js`与`index-BwaRAGe5.css`。正式库315个竞品的1232条历史快照均有图片地址；10条从未成功采集的链接复核记录按真实缺失显示占位。隔离浏览器验证有效图、破图和空图兜底，并确认详情头图、变体图和2条历史图全部存在；390×844窄屏页面与弹窗均无整体横向溢出，变体表仅在内部滚动。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载最终JS/CSS；运行中的325条竞品批次从断点继续推进，重启后日志已开始第269条，未从头重复 |
| 2026-07-28 | 精简商品中心详情历史：移除近30天浏览量快照，只保留每日销售件数柱图；销售图补充明确的整数数字纵轴、“件数”轴标题和起止指标日期，0销量不再用最小高度伪装为正销量。商品指标卡中的近30天浏览量继续保留，本次不改变接口和数据口径 | `frontend/competitor/src/pages/ProductsPage.vue`、`frontend/competitor/src/styles.css`、`README.md`、`docs/PROJECT_STATUS.md`、`docs/requirements/takealot-operations-dashboard-prd.md`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终生成`index-U6KZ6xfg.js`与`index-BwaRAGe5.css`；正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`且首页引用最终哈希资源。Chrome管理员页以真实商品`WOVIBO RGB Gaming Light Bars`验收：销售快照标题1处，两个旧流量快照标题均为0处，10根历史柱的纵轴显示“件数”及整数刻度`1、0`，控制台错误为0 |
| 2026-07-28 | 优化经营页面文案：经营总览“有销量商品”改为“今日售出商品”，明确该数量统计最新指标日下单件数大于0的去重商品；商品中心主标题由“从商品列表快速下钻到销售与流量”改为“店铺商品明细表”。本次只调整展示文案，不改变数据接口和统计逻辑 | `frontend/competitor/src/pages/OverviewPage.vue`、`frontend/competitor/src/pages/ProductsPage.vue`、`docs/requirements/takealot-operations-dashboard-prd.md`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，生成`index-B2M9J-MS.js`并复用`index-CUFb8OMg.css`；正式 ERP 已通过`restart_erp.ps1`重启，`/api/health`返回`ok`。Chrome管理员页面确认“今日售出商品”和“店铺商品明细表”各唯一显示1处，两个旧文案均为0处；运行中的325条竞品批次在重启后保留断点并继续显示共享进度 |
| 2026-07-28 | 修复运营账号在竞品雷达点击“开始采集”后只转圈或没有提示：共享批次占用时不再禁用开始/继续按钮，点击后在操作按钮正下方显示带感叹号、橙色描边、阴影和入场动画的提示卡，明确列出当前操作者、已检查/总数、待续爬和当前PLID，不再依赖页面顶部提示；持有页面逐秒显示当前序号、PLID与等待时长，超过90秒说明可能仍在枚举变体、探测库存或网络重试。修正刷新恢复误判：服务端活动批次与本地断点批次编号一致时，当前标签页使用保留的客户端编号和请求编号自动接回，不再把本人管理员账号提示为占用者；其他批次或其他标签页仍由前后端拦截 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript生产构建、竞品批次/请求复用专项`7 passed`与`git diff --check`通过，生成`index-Cr1jvPx5.js`与`index-CNIZEL3N.css`；正式8501健康检查为HTTP 200并已返回该JS/CSS资源。修复过程未停止或重启服务；管理员刷新前的当前请求已完成第291条，本地断点保留，刷新加载新前端后可从剩余项目自动接回 |
| 2026-07-28 | 修正运营日报顶部历史待办数量的表达：历史提醒继续只统计当前日报日之前的数据，但明确标注“不含当前日报日”，并同时展示当前日报日待办数与下方完整待办合计。为人工核对待办、待办处理留痕及其操作/备注弹窗补充平台商品图；商品图优先读取当前 Offer，缺失时回退最近历史快照，加载失败显示“暂无图片”，横向日报表保持无图片。待办与留痕卡均可点击后自动搜索平台 SKU 并跳到该记录的数据日期 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`32 passed`、完整回归`272 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）、Vue TypeScript/生产构建、数据库`verify`和`git diff --check`通过；正式ERP自动重启并由`/api/health`确认加载新构建。Chrome管理员真实页面确认当前2个待办分别为2026-07-26历史1个和2026-07-27当前日报日1个，顶部显示历史1个、当前另有1个、下方合计2个；2张待办卡和8条留痕均显示已成功加载的商品图片，表格图片为0。点击2026-07-26留痕后搜索框自动填入`9902331724932`并突出2026-07-26日期起始行，控制台错误为0 |
| 2026-07-28 | 为未授权功能增加统一可见反馈：全部模块保留在侧边栏并对无查看权限项标记“未开放”，点击锁定模块仍停留当前页面并提示“当前账号该功能权限未开放”；无刷新、竞品采集、运营日报待办/Excel或NFT102权限时，相关主操作保持可点击，调用前显示同一提示且不发起业务请求。原有后端逐接口权限校验继续保留 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/pages/DailyReportPage.vue`、`frontend/competitor/src/pages/ReportsPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过。隔离SQLite查看账号浏览器实测：报表工作台与用户权限显示“未开放”；点击用户权限、刷新全部数据和竞品开始采集均出现精确文案“当前账号该功能权限未开放”，页面未切换、刷新/采集未启动。隔离服务和测试数据库已删除，正式8501及其竞品批次未中断 |
| 2026-07-28 | 从报表工作台移除运营日报导出功能：删除“运营日报”页签、截止日期、一键生成 HTML/Excel/PNG 按钮和下载卡，工作台改为直接进入 NFT102 上传续写；导航权限改按 NFT102 续写权限显示，并清理导出卡废弃样式。侧边栏独立的运营日报核对、合并和 Excel 导出保持不变，既有离线报表命令和接口继续用于兼容流程 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/ReportsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整回归`272 passed`（1条第三方TestClient弃用提示），Vue TypeScript检查与生产构建通过；本次构建输出`index-DQOX2tEo.js`与`index-RBhOCzdV.css`，正式页面最终加载当前工作区合并构建`index-Db5NYyWW.js`与`index-CgrbBiEU.css`。正式ERP自动重启且`/api/health`为HTTP 200；Chrome管理员真实页面确认报表工作台只显示 NFT102 续写，无运营日报页签、截止日期、生成按钮或下载卡；独立运营日报仍可打开，控制台错误为0 |
| 2026-07-28 | 将商品中心改为图片列表与唯一详情弹窗：每条商品栏直接展示平台图片，缺失或加载失败显示“暂无图片”；点击或键盘激活商品栏后在当前视口打开包含商品身份、6项经营指标及销售/近30天浏览量历史的详情弹窗，原列表下方不再重复铺详情；弹窗支持遮罩、关闭按钮、底部按钮、Escape关闭、背景滚动锁定及焦点恢复 | `frontend/competitor/src/pages/ProductsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整回归`272 passed`（1条第三方TestClient弃用提示），Vue TypeScript检查与生产构建通过，构建为`index-DoVUn5yK.js`与`index-B0U_YRcR.css`；正式ERP自动重启且`/api/health`为HTTP 200。Chrome管理员页以真实404个商品验收：404条均有图片或占位区域、页面下方旧详情为0、弹窗唯一1个且含6项指标/2组历史图；Escape关闭恢复原商品焦点。390×844窄屏文档宽390px、弹窗宽374px且单列图片299px，无横向溢出，控制台错误为0 |
| 2026-07-28 | 为经营坐标“全部商品”增加平台 SKU 搜索框：输入完整或部分 SKU 即时筛选商品卡片，匹配忽略大小写并继续使用当前排序；页面显示“当前结果数 / 商品总数”，无匹配时显示带搜索词的中文空态，清空输入恢复全部商品。搜索框与排序器在窄屏下纵向完整排列，不引入横向滚动 | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整`npm run build`通过，生产构建为`index-D7wU8XET.js`与`index-BOOGTxdy.css`。隔离ERP以4个商品验证小写部分搜索`sku-c`只保留`SKU-C`对应卡片且计数为1/4，无匹配`missing-999`显示0/4及中文空态，键盘清空恢复4/4；390×844窄屏下搜索框与排序器均宽311像素、完整位于视口内，商品卡片和整页无横向溢出，控制台错误为0。正式ERP已通过`restart_erp.ps1`重启，`/api/health`返回`ok`并加载上述哈希资源 |
| 2026-07-28 | 为运营日报增加登录态服务端事件流：定时/手动采集及人工处理落库后自动推送前端刷新日报矩阵、待办与全局提醒，断线重连补刷新，编辑弹窗打开时延后更新。新增安全的正式ERP重启脚本，完成修复后精确停止项目监听进程、隐藏启动并等待健康检查。按用户要求备份后彻底删除两件演练商品在日报库存、观察、处理、审计及截止快照中的137条关联数据 | `src/takealot_ops/erp/daily_report_live.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/DailyReportPage.vue`、`scripts/restart_erp.ps1`、相关测试与文档 | 事件专项与ERP Web专项`17 passed`、完整回归`272 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（48个源码文件）、Vue TypeScript/生产构建、PowerShell语法和数据库`verify`通过；正式库删除后所有直接引用和截止快照JSON引用均为0，删除前备份为`backups/takealot-20260728-054911-517635.sql`。正式ERP已自动重启并通过健康检查，Chrome管理员页加载重启后的正式构建，两件演练SKU均为0处且控制台无错误 |
| 2026-07-28 | 收口版本差异人工修改流程：人工订单和库存保存后若相邻日报日库存公式已经相符，系统在同一次操作中自动采用人工值、生成可撤销的确认留痕并直接关闭整条待办，不再转成库存不平待办或要求再次点击“消除差异”；公式仍不相符或缺少计算条件时继续保留待处理，独立库存连续性待办原有的互斥处理规则不变 | `src/takealot_ops/erp/daily_report.py`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 新增版本差异人工值公式相符后自动关闭、无库存待办、确认留痕可见的回归；日报专项`32 passed`、完整回归`269 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（47个源码文件）、数据库`verify`和`git diff --check`通过 |
| 2026-07-28 | 修正运营日报库存比销量挂早一天：业务日D的全部定时/手动版本统一使用D日整日销量与D+1日10:05期末库存，18:00和手动刷新库存不代替早间期末快照；连续性公式保持“前一日报日库存－当前日报日销量＝当前日报日库存”，按自然日展开即“D日早间库存－D日整日销量＝D+1日早间库存”。页面与Excel库存行明确标注“次日10:05期末”，缺少实际快照时记录所缺日期并保持空值；历史回填同步改接下一日库存并更新运行统计 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`31 passed`、完整回归`268 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（47个源码文件）、Vue TypeScript检查与生产构建、数据库完整性检查通过；构建为`index-56JT_Qy9.js`与`index-CbMVIdk4.css`。正式库先备份`backups/takealot-20260728-040343-062804.sql`，再改接3646条观察并重算662条状态，幂等复跑0变更。当前仅剩2个真实库存异常及2个既有演练待办；真实404个商品的两个区间均403/404公式成立，发生销量的商品分别16/16、15/15全部成立 |
| 2026-07-28 | 修复库存连续性人工调整的易误判路径：纯库存待办二次打开人工修改时，订单数重新采用当前正确销量，不再暗中沿用旧人工候选销量；仍有销量版本差异时明确提示先确认合并，稳定后才显示互斥的库存处理按钮，库存动作遇到状态变化会自动刷新待办。同步将通用备注调整为修改时只填写新正文、删除时无需填写原因，仍完整保留被删除内容、操作人和时间审计；问题关联备注删除原因继续必填 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报与ERP Web专项`44 passed`，完整回归`267 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（47个源码文件）、Vue TypeScript检查与生产构建通过 |
| 2026-07-28 | 完善运营日报待办卡的一键定位：点击非操作区域后先按平台SKU筛出商品，再把页面带回日报矩阵，并将矩阵内部纵向滚动到该待办的数据日期；日期起始行持续描边突出，成功提示同时包含SKU和日期，日期不在当前读取范围时给出明确说明。搜索框继续自动填入、聚焦和选中，但不再把最终视点停在搜索框 | `frontend/competitor/src/pages/DailyReportPage.vue`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建、完整回归`267 passed`、`git diff --check`通过 |
| 2026-07-28 | 修复正式服务未重启时新前端读取旧登录响应导致管理员空白页：旧响应缺少`permissions`时按原账号模板临时补齐，后端重启后继续使用真实账号级权限；权限判断对缺失数组安全降级。新增根级页面错误边界和登录恢复提示，登录响应不兼容、接口连接失败或页面脚本异常时明确说明后台任务不受影响，并提供重新连接或重新加载入口，不再渲染空白页 | `frontend/competitor/src/api.ts`、`frontend/competitor/src/permissions.ts`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/AppErrorBoundary.vue`、`frontend/competitor/src/main.ts`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，正式8501无需重启即加载兼容构建。Chrome原空白管理员标签刷新后恢复八个模块、管理员模板标识和完整竞品页面；同一竞品批次继续运行，验收时页面为65/325，未中断后台采集 |
| 2026-07-28 | 将查看、运营、选品、管理员改为四个等权的快速权限模板，移除运营模板的单独高亮和推荐文案；管理员可在现有账号卡片中直接逐项增删权限，账号四边框和内部横向分隔线保持连续。新增选品模板，默认可查看和采集竞品雷达、查看运营日报，但不能处理日报待办或生成日报 Excel。前后端均按账号实际权限控制导航、按钮和接口，模板切换可重置默认值，自定义权限会撤销账号既有会话，并继续保护最后一个有效用户权限管理账号 | `src/takealot_ops/erp/permissions.py`、`src/takealot_ops/erp/auth.py`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/storage/models.py`、`src/takealot_ops/storage/migrations.py`、`frontend/competitor/src/permissions.ts`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/UsersPage.vue`、相关页面、测试、构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 权限专项`17 passed`，完整回归`267 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（47个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过。隔离浏览器实测四个模板等权展示且无“运营员”或推荐文案；选品账号只显示竞品雷达与运营日报，竞品采集可用，日报待办操作不可见且Excel导出禁用；现有账号卡片四边均为连续1px边框，页面无横向溢出。正式8501因另一个竞品批次仍在运行，未重启以免中断采集 |
| 2026-07-28 | 将库存连续性待办拆成互斥的两种处理：人工修改后的候选订单/库存使公式相符时只显示并只允许“消除差异”，采用人工值并去除红标；公式仍不相符时只显示并只允许“确认库存差异”，采用人工值但保留异常红标。两个后端接口均重新计算并拒绝错误动作；消除差异单独留痕并可按确认值撤销，人工再次修改会重新打开既有库存差异确认 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报业务专项`30 passed`、新增库存差异接口专项`1 passed`，当前工作区完整回归`267 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（47个源码文件）、Vue TypeScript检查与生产构建、数据库`verify`和`git diff --check`通过 |
| 2026-07-28 | 将“待办处理留痕”从确认结果扩展为最近100次全部人工操作：人工候选、备注新增/修改/删除、确认合并、确认库存差异及两类撤销均独立成行，明确展示操作人、北京时间和操作备注；撤销记录单独留痕，同时原处理记录保留撤销关联。删除备注改为必须填写删除原因，留痕同时展示被删除内容与删除原因 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报与ERP Web专项`42 passed`，完整回归`262 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript检查与生产构建、数据库`verify`和`git diff --check`通过；Chrome管理员页面确认新留痕说明正常显示且控制台无错误，正式库当前无历史卡片数据，卡片字段由接口和组件构建测试覆盖 |
| 2026-07-28 | 将经营坐标“全部商品”从1280像素宽表改为自适应商品卡片：商品名称与全部字段完整换行展示，桌面和窄屏均无需左右滑动；保留接口返回的全部商品及图片、平台SKU、近30天浏览量、近7日下单、平台库存、首次上架/最早记录、最近补货和数据截止日期。新增“排序方式”选择器，支持按浏览量、下单、库存降序及商品名称排序，数值缺失项排在末尾 | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整`npm run build`通过，生产构建为`index-BJ-SYJpb.js`与`index--eLrgrPs.css`。隔离ERP浏览器验收为4个商品、4张卡片，每张卡片均含全部字段且宽表DOM为0；默认浏览量降序将缺失流量商品排末尾，下单降序顺序为0、2、1、3，名称排序为0、1、2、3。1280像素桌面与390×844窄屏均无整页横向溢出，窄屏卡片宽311像素、排序器完整位于视口内，控制台错误为0；`git diff --check`通过 |
| 2026-07-28 | 修复管理员“用户权限”页面的排版和角色表达：标题说明恢复正常纵向对齐，新增三级角色权限矩阵并明确“运营员”为运营日报完整功能角色；表单和用户表改为响应式布局，窄屏仅在表格内部滚动。权限回归扩展到运营员的日报查看、人工修改、确认与撤销、备注增改删、库存差异确认与撤销、刷新状态、Excel 生成和下载，同时确认查看员所有日报写接口继续返回403 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/UsersPage.vue`、`tests/unit/test_erp_web.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 用户权限/日报接口专项`3 passed`、ERP Web完整专项`13 passed`、全量回归`262 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过。管理员正式页面在1024像素视口实测整页无横向溢出；隔离运营员登录后可见日报刷新入口和人工修改、确认合并、单独加备注等全部待办操作，且无用户权限导航 |
| 2026-07-28 | 将“经营四象限”改为纯“经营坐标”：导航和页面移除分位选择、五类统计/筛选卡、四象限名称、分类配色和十字分界，仅用统一蓝色散点展示近30天浏览量排名与近7日下单排名。0/25/50/75/100刻度移到图框外的高对比数字标记中，图内仅保留轻量网格；无法形成真实二维坐标的商品不补0，继续保留在下表。全部商品表从5列扩充为8列，补齐商品图片、平台SKU、浏览量、下单、平台库存、首次上架/最早记录、最近补货和数据截止日期 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，最终构建为`index-DKOuedvY.js`与`index-CyfhFolz.css`。隔离ERP数据验收为4个商品、3个进入坐标、1个缺少浏览量但仍在完整表；四象限控件和分界DOM均为0，两轴10个刻度逐一与散点计算无重叠，8个表头和4行商品全部存在。390×844窄屏无整页横向溢出，宽表仅在表格内部滚动；控制台错误为0。正式Chrome标签页当时正被另一任务控制，未抢占，正式库只读数据未改动 |
| 2026-07-28 | 收紧运营日报商品漏爬口径：只有最终采用的订单数或库存为空才标记漏爬，近30天浏览量为空、缺少某一批次但已由其他版本补齐均不再标记；采集失败原因继续保留在运行日志和 Excel 说明中。补齐“确认合并后再确认库存差异”的备注汇总，页面与 Excel 同时展示合并备注和差异备注；同时把固定“日期”列的左偏移从188px校正为“指标”列实际宽度170px，消除两列间缝隙 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`29 passed`、完整回归`262 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript检查与生产构建和`git diff --check`通过；Chrome管理员登录态在横向滚动640px后实测“指标”列宽170px、日期列`left:170px`、两列边界间距0px，页面控制台错误为0 |
| 2026-07-28 | 为运营日报“待办处理留痕”新增“处理日期”和“数据日期”两个筛选器：处理日期按北京时间从处理时间生成，数据日期使用日报业务日；支持独立筛选、交集筛选、结果数/总数提示、无匹配空态和一键清除，留痕数据与撤销能力不受影响 | `frontend/competitor/src/pages/DailyReportPage.vue`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过；Chrome管理员登录态实测23条留痕按处理日期筛为2条，组合筛选保持2条，清除后恢复23条，数据日期可独立选择，页面控制台错误为0；`git diff --check`通过 |
| 2026-07-27 | 区分库存异常事实与待办处理状态：运营确认“差异属实”后只关闭待办，日报矩阵、库存异常筛选、顶部统计和 Excel 库存单元格继续保留红标。新增“待办处理留痕”，集中展示确认合并与库存差异确认涉及的商品、平台SKU、日报日、公式或采用来源、处理人、北京时间、备注和撤销结果；当前有效记录可直接撤销，撤销库存差异确认后若公式仍不平会恢复原待办，原处理与撤销均保留不可删除审计。历史商品即使已不在当前商品表，也从当时采集快照恢复名称和SKU，避免留痕退化为内部Offer ID | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整回归`259 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过。Chrome管理员会话在`DEMO-STOCK-001`实测：确认后待办2→1但库存不平仍为1，矩阵库存单元格保持`stock-value-mismatch`红标，留痕显示SKU、10−2=8/实际9、操作人/时间/备注及撤销按钮；从留痕撤销后待办恢复、处理记录保留撤销原因且不可重复撤销，页面控制台错误为0，最终恢复原演练待办状态 |
| 2026-07-27 | 修复运营日报“确认合并”和“撤销上次确认”弹窗偶发无反馈：必填备注为空时按钮不再静默禁用，点击后直接提示缺少内容并聚焦输入框；提交期间显示“正在合并/正在撤销”和具体重算说明，锁定取消、遮罩关闭及重复提交；完成后先刷新日报再保留成功提示，接口失败继续保留弹窗和输入内容 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-dxMJDNHy.js`与`index-DE7gUhEj.css`；日报后端/API专项`38 passed`（1条第三方TestClient弃用提示）。Chrome管理员会话在`DEMO-REVIEW-001`实测空备注分别显示合并与撤销提示，真实撤销和确认过程中按钮及弹窗显示明确进度，完成后成功提示保持可见；最终已恢复原先“前后日报库存不平”演练待办 |
| 2026-07-27 | 补齐撤销确认后的待办传播：撤销某日报日的人工确认时，该日报日继续显示“已撤销确认待重核”，受影响的下一日报日不再被移出待办，而是显示“撤销确认后库存待重算”，完整展示原确认库存、撤销人/时间/原因、当前订单/库存和撤销前公式；重新确认后若仍不平自动转为真实库存连续性待办，恢复相符则自动解除，并同步重开或关闭18:30持久待办快照 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`26 passed`、日报接口专项`12 passed`（1条第三方TestClient弃用提示）、Ruff与Mypy通过；Vue TypeScript检查与生产构建通过，构建为`index-B0jB2M7c.js`与`index-CiPSi-lr.css`。合成回归覆盖撤销后保留下一日待办、审计完整、重新确认后转真实库存冲突，以及重新确认恢复相符后自动解除两条路径 |
| 2026-07-27 | 为运营日报人工核对待办增加一键定位：点击待办卡片的非操作区域，自动把对应平台 SKU 填入上方搜索框、切回“全部”筛选、定位并选中输入框，使日报矩阵只保留该商品；卡片内人工修改、确认合并、备注、库存确认和撤销按钮继续独立工作，不触发搜索 | `frontend/competitor/src/pages/DailyReportPage.vue`、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-BypZCzMD.js`与`index-DPc9V3Om.css`；日报专项`25 passed`。Chrome正式管理员会话点击库存连续性演练待办后，搜索框自动填入并选中`DEMO-STOCK-001`，筛选结果由406个商品收敛为1个，对应矩阵只显示该SKU；清空搜索后点击卡片内“人工修改”只打开编辑弹窗，搜索框保持为空 |
| 2026-07-27 | 为风险与质量页的每张异常商品卡片增加完整详情弹窗：卡片支持鼠标点击和键盘回车/空格打开，弹窗复用四象限同口径的商品图片、SKU、库存、近30天浏览量、近7日下单、首次上架和最近补货信息，并扩展异常发生日/说明、当前经营数据截止日、平台商品编号、条码、状态、售价/建议零售价、转化率、最新日下单金额及有效销售件数；支持图片失败占位、遮罩/关闭按钮/底部按钮/Escape关闭、背景滚动锁定、焦点回到原卡片和窄屏布局。风险接口只读补齐当前商品经营上下文，不改变异常规则 | `src/takealot_ops/erp/service.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/RisksPage.vue`、`frontend/competitor/src/styles.css`、`tests/integration/test_erp_service.py`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 风险接口专项`6 passed`、完整回归`257 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、`takealot_ops.cli verify`、Vue TypeScript/生产构建和`git diff --check`通过；构建产物为`index-BypZCzMD.js`和`index-DPc9V3Om.css`。未执行浏览器交互测试 |
| 2026-07-27 | 修复运营日报人工修改点击“确认保存”无响应：数字输入框经 Vue 转成数值后不再直接调用字符串 `trim()`，统一规范化后解析；提交失败时弹窗保持打开并就地显示错误，保存按钮明确为提交按钮 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-DE5aTQPr.js`与`index-BX6r0Ti_.css`；日报专项`25 passed`。Chrome正式管理员会话实际把演练商品人工候选保存为浏览量121、订单3、库存7并生成审计，页面无`trim is not a function`；随后清除本次验证候选与审计，恢复“销量版本有差异”初始待办 |
| 2026-07-27 | 优化风险与质量页的经营异常提示：仅“不可购买”保留暖黄色普通提示，其余异常统一改为红色重点提示卡片；新增商品编号/异常名称/说明关键词、提示级别和异常类型组合筛选，实时显示结果数并可一键清除，空结果提供明确说明；筛选只处理当前已读取数据，不改变后台异常规则 | `frontend/competitor/src/pages/RisksPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，产物为`index-DE5aTQPr.js`和`index-BX6r0Ti_.css`；构建资源确认包含“重点提示”、提示级别/异常类型筛选和红色/暖黄色卡片样式。未执行浏览器交互测试 |
| 2026-07-27 | 优化运营日报人工修改弹窗：首次打开时三个数值字段不再为空，自动带入当前采用的近30天浏览量、当天订单数和平台仓库存；每个字段旁固定显示当前基准供对照。已有人工候选时继续回填上次人工值，支持在原修改基础上再次调整 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-o5D-1g43.js`与`index-Kcm1KEML.css`；日报专项`25 passed`。Chrome使用正式管理员会话实测演练商品首次打开时输入框与基准均为浏览量121、订单3、库存8，取消弹窗且未写入数据 |
| 2026-07-27 | 为经营四象限散点悬浮卡增加平台商品缩略图：复用接口已有 `image_url`，图片在固定方框内等比完整显示；地址缺失或加载失败时切换为“暂无图片”中文占位。同步放宽桌面悬浮卡宽度并压缩窄屏图片尺寸，保留上下及左右边缘自动避让 | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-Ch3p3vt9.js`与`index-CkrbEXhv.css`。真实四象限接口的171个已分类商品全部带图片；Chrome悬浮普通、最右侧和顶部商品点均成功加载图片，普通图片自然尺寸2000×2000，悬浮卡宽388px且三种点位均未超出视口，顶部/右侧自动避让生效，页面控制台错误为0。当前真实分类商品没有缺图样本，因此“暂无图片”路径由前端错误处理逻辑与生产构建覆盖，未在正式数据中触发 |
| 2026-07-27 | 修正运营日报销量与库存的日期口径：同一10:00周期的早间、晚间和全部手动版本只比较当天订单数，销量任一非空值不同才推送“销量版本有差异”，销量一致时库存或浏览量变化均不产生版本待办。新增`daily_inventory_snapshots`，每个自然日10:05首次成功库存按实际北京时间日期冻结；次日核对前一业务日时所有版本只读取该日10:05快照，次日实时库存不再写回前一日。缺少对应快照时库存保持为空并记录明确原因。提供幂等历史迁移与状态重算，页面和Excel库存行明确标注“当日10:05”，前端待办说明同步新口径 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/storage/models.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`25 passed`、完整回归`256 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建、数据库`verify`和`git diff --check`通过；构建为`index-CHLJsJYv.js`与`index-BByEpJJF.css`。正式库先备份`backups/takealot-20260727-091838-658035.sql`，迁移建立2026-07-26的404条完整10:05库存快照、改接2020条观察库存并重算172条状态；再从备份恢复2026-07-27原始10:05库存404条，当前两日各404条且全部非空，幂等复跑0变更。7月24/25无对应10:05快照而保持缺失，7月26为171就绪、233浏览量缺失，现有销量版本/库存连续性待办均为0。恢复用暂存表因受限账号无`DROP`权限已清空为0行，留待管理员维护窗口删除，不被应用读取。正式8501已重启加载新构建并通过健康检查 |
| 2026-07-27 | 将确认合并备注接入日报备注单元格：当前有效的确认备注自动以“（确认：备注内容）”显示在对应商品和日报日的页面备注行，并与通用/版本/库存独立备注并列；Excel“运营日报”备注行使用同一顺序和文案。确认被撤销后不再作为当前有效备注显示，但原确认及撤销原因继续保留在审计和确认记录中 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报专项`22 passed`、完整回归`253 passed`，Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建和`git diff --check`通过；构建为`index-CIOy7gpt.js`与`index-95F0euDu.css`。Excel回归确认备注单元格为“（确认：采用晚间库存值） （库存：平台临时调仓）” |
| 2026-07-27 | 修复运营日报人工候选值只能改一次的交互：第二次打开“人工修改”会回填上次候选值、原因和备注，允许继续覆盖当前候选；每次修改仍追加前后值审计。为确认合并增加安全撤销：已确认商品在日期备注行的“备注与确认记录”或确认后新问题待办卡中可填写原因撤销，原确认不删除而追加`confirmation_reverted`审计，当前值恢复为“已撤销确认待重核”；撤销期间下一日报库存连续性暂停，重新确认后再按新值重算并恢复必要待办 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 日报后端/API专项`34 passed`、完整回归`253 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建、数据库`verify`和`git diff --check`通过；构建为`index-CnnDzjzd.js`与`index-Dh3v91pf.css`。正式8501已重启加载新接口，管理员页面只读验收已有人工候选值会完整回填库存1、订单0、浏览量167、原因和备注“00”，页面控制台错误为0，未提交正式修改；正式库当前已确认项为0，因此撤销弹窗由隔离数据库/API回归覆盖 |
| 2026-07-27 | 将运营日报待办卡的笼统“数据有差异”改为随真实问题变化的具体标识：同周期版本问题根据差异字段显示“订单版本有差异”“库存版本有差异”或“订单与库存版本有差异”，已有确认基线后出现新版本时增加“确认后”前缀；库存连续性问题显示“前后日报库存不平”，由人工确认传播触发时显示“人工确认后库存不平”。版本问题使用紫色、库存连续性使用红色，防御性兼容两类同时存在时使用橙色复合标识 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-Dv8yL-Bf.js`与`index-nBr53C_q.css`。管理员正式页面只读验收现有11张待办：7张显示紫色“库存版本有差异”，4张显示红色“前后日报库存不平”，不再出现“数据有差异”；页面控制台错误为0，未执行任何人工确认或数据写入 |
| 2026-07-27 | 修复运营日报备注相关弹窗确认按钮“有文案但不可见”：弹窗通用按钮的白色背景此前以更高选择器优先级覆盖了绿色确认按钮，而文字仍为白色，形成白底白字。现将普通按钮固定为白底深色文字，并为`.action-button`显式恢复深绿色背景、白色文字和同色边框；新增备注、修改备注及共用该操作区的其他确认弹窗均使用同一稳定样式 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`AGENT.md` | Vue TypeScript检查与生产构建通过，构建为`index-DteZ9s4-.js`与`index-DZZhlVJN.css`。管理员正式页面只读打开备注管理和新增备注弹窗，确认按钮文字为“确认保存”，计算样式为深绿色背景`rgb(30, 93, 67)`、白色文字`rgb(255, 255, 255)`、字号12.16px；取消按钮为白底深色文字，关闭弹窗后页面控制台错误为0，未提交正式备注 |
| 2026-07-27 | 将运营日报备注完善为可新增、修改、删除的正式数据能力：运营员和管理员均可操作，修改和删除只改变当前有效备注，不改变待办状态；新增、修改、删除全部追加审计并保留创建人、修改人、中国时间、修改前后内容，删除不抹除历史。页面和 Excel 商品横向主表的每个日期块由三行扩展为“近30天浏览量、当天订单数、平台库存数量、备注”四行，备注按通用、版本、库存类型以括号显示在对应商品列；点击备注行可集中管理该商品当天的全部备注，待办卡也提供修改和删除入口 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 备注后端/API专项`32 passed`、完整回归`251 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（46个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过；构建为`index-CNi_Hv21.js`与`index-Bju8Wamd.css`。管理员正式页面只读验收显示3个日期各4行、72个备注管理单元格、11个“单独加备注”待办入口；既有备注以括号显示，备注行高52px，矩阵横向滚动限制在内部，页面宽1905px等于视口，控制台错误为0 |
| 2026-07-27 | 增加跨用户运营互斥与状态同步：竞品雷达改为服务器级单批次，按账号和浏览器会话锁定，所有用户每2秒同步操作者、进度、成功/失败/待续爬和当前PLID；同账号其他标签页也不能重复启动，停止后等待当前浏览器探测清理再释放。右上角“刷新全部数据”增加MySQL持久化的全局1小时冷却，任何成功刷新都会让全部非管理员进入同一倒计时，管理员可越过冷却但不能与正在运行的刷新并发；刷新人、运行态、最近成功和倒计时同步到全部页面，失败刷新不启动冷却 | `src/takealot_ops/competitors/batch.py`、`src/takealot_ops/erp/coordination.py`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/storage/models.py`、`frontend/competitor/`、相关测试及文档 | 新增批次跨账号/跨标签页冲突、进度同步、停止清理后释放、刷新全局冷却、管理员豁免、失败不冷却及重启后冷却保持测试；专项`19 passed`、完整回归`251 passed`（均只有1条第三方TestClient弃用提示），Ruff、改动后端模块Mypy和Vue TypeScript/生产构建通过。隔离浏览器已验证全员采集状态、进度和冷却倒计时；正式8501在外部更新流程重启后已加载新接口，MySQL冷却表存在、健康检查HTTP 200，且运行批次已产生全员计数字段和心跳，本轮未再重启服务 |
| 2026-07-27 | 将运营日报核对改为严格的两阶段状态机：先确认每个日报日自己的订单/库存版本，前后任一日报日版本未定时暂停库存连续性；版本确认后立即用两个稳定正确值重算，本日或下一日报日仍不平才继续推送库存待办。“确认合并”不再顺带豁免库存差异。库存待办期间出现新拉取差异时切回版本待办；已有确认基线时只展示“上次人工确认值”和确认后新版本，重新确认后再计算连续性。历史状态重算工具同步支持按新优先级升降待办 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 新增“前一日版本未定暂缓跨日”“当前日版本差异优先”“确认后仍不平继续库存待办”“确认基线对比晚间新值”和“重算不改变漏爬状态”回归；日报后端/API专项`30 passed`、完整回归`245 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（45个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过，构建为`index-DO2WWTFH.js`与`index-BoAMRhmL.css`。正式库操作前备份`backups/takealot-20260727-074255-086244.sql`；重算撤下5张因相邻版本未定而提前生成的库存待办，最终为11张卡（7个版本差异、4个库存连续性）。迁移预演误改的171条漏爬状态已按审计恢复并增加防回归测试，第二次重算0处变化。正式8501已加载新构建；管理员页面确认两类问题没有同卡并存，7个“确认合并”、4个“确认库存差异”和11个“单独加备注”与问题类型一一对应，页面无横向溢出、控制台错误为0 |
| 2026-07-27 | 拆分运营日报待办的两类核对规则和证据：同一10:00周期内全部定时/手动版本只检查订单与库存是否变化，不再在版本之间套用库存连续性公式；库存连续性只比较相邻的前一日报日与当前日报日。待办卡分别展示“同周期版本差异”和“前后日报日库存连续性”的相关版本值或跨日公式。完善“单独加备注”为按问题类型关联的追加式历史，保留操作人和中国时间，不覆盖旧记录、不改变待办状态，管理员与运营员沿用现有操作权限 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/types.ts`、`frontend/competitor/src/pages/DailyReportPage.vue`、`tests/unit/test_daily_report.py`、`tests/unit/test_erp_web.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 合成专项验证同周期库存变化只产生版本差异、相邻日报日不平只产生库存连续性问题；连续追加两条不同关联备注后待办仍为`needs_review`且两条历史、操作人均完整保留。日报后端/API专项`28 passed`、完整回归`243 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（45个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过；构建生成`index-CBgWtVzF.js`与`index-pf7R51fi.css`并重启正式8501加载。管理员真实页面只读验收为16张待办、7个同周期差异区、16个跨日库存区、2条既有独立备注历史；备注弹窗可选择关联问题并明确“不覆盖历史”，页面宽1905px等于视口、控制台错误为0，未写入正式数据 |
| 2026-07-27 | 为人工确认合并增加库存冲突向后传播闭环：确认值改变前一日报日库存后立即比较下一日报日在确认前后的公式，只有“原本相符、确认后新冲突”才重新打开下一日待办、恢复库存红标和18:30快照；下一日继续确认时按同一规则向后传播。新增审计事件保存触发确认、确认前后库存公式和受影响日报日原状态；待办卡完整展示同日报日各版本订单/库存、跨日报日公式、触发确认来源/操作人/时间/备注以及原确认记录 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/`、`tests/unit/test_daily_report.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 合成链路验证首日确认库存9→10后自动重开第二日，第二日再确认8→9后继续重开第三日，触发上下文与全局待办计数正确；运营日报专项`17 passed`、完整回归`242 passed`（1条第三方TestClient弃用提示）、Ruff、Mypy（45个源码文件）、Vue TypeScript/生产构建和数据库`verify`通过。构建生成`index-CZGXHHDY.js`与`index-BM6iLQQu.css`并由正式8501加载；管理员页面现有16张待办、7处同日报日版本证据及16处库存公式证据正常显示，页面宽1905px小于1920px视口，ERP自身控制台错误为0（Chrome扩展有1条无关`Failed to fetch`）。正式库尚无确认后新增冲突，因此触发提示区当前为0，未为验收写入合成数据 |
| 2026-07-27 | 修复竞品批量采集因整页刷新或前端重新加载而无提示停止：浏览器断点新增批次运行态、当前索引和稳定请求编号；运行中刷新会自动回到竞品雷达并从当前链接恢复，人工停止、网络暂停和正常结束不会误触发。服务端增加进程内请求幂等协调，页面重连复用同一探测任务/结果，并把批次生命周期及逐链接开始、结果、复用和异常写入独立轮转日志 | `src/takealot_ops/competitors/batch.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/api.ts`、`frontend/competitor/src/pages/CompetitorsPage.vue`、相关测试、构建产物及文档 | 新增协调器取消/重连、完成结果复用、轮转日志和正式接口批次元数据回归；专项`13 passed`、完整回归`241 passed`（均只有1条第三方TestClient弃用提示），Ruff、改动后端模块Mypy和Vue TypeScript/生产构建通过。隔离浏览器以3条链接验证第2条请求中刷新：产生`auto_resume`，该请求收到2次HTTP连接但后台实际探测1次，最终3/3完成；正式8501已加载新构建并通过健康检查 |
| 2026-07-27 | 修正运营日报人工核对区的名称和周期说明：原“跨日期人工核对/跨日期待办”实际同时包含同一日报日多次拉取差异和前后日报日库存连续性差异，统一改为“人工核对待办”。顶部手动刷新次数明确标为当前周期；每张历史待办卡在折叠摘要直接列出自身日报日对应周期的全部版本名称和采集时间，避免把当前周期手动版本误认为应出现在历史卡片中 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，生成`index-CAINRnOq.js`和`index-BUZqH8yz.css`并由正式8501页面直接加载。正式库及管理员页面确认当前共16个待办：7月25日10张卡均显示10:05/18:00两个定时版本，7月26日6张卡均显示10:05定时/14:13手动刷新两个版本；顶部明确显示“当前周期手动刷新：已核对1次”，页面文档宽1905px小于1920px视口。ERP应用自身控制台错误为0（Chrome扩展有2条无关`Failed to fetch`） |
| 2026-07-27 | 将“暂缺近30天浏览量”汇总卡恢复为与其他KPI一致的上下结构：上方显示字段名，下方显示大号数量并用小号“个商品”补充单位，统计口径不变 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，生成`index-rtFQ5wkV.js`和`index-8rVndEna.css`；已登录管理员真实页面显示上方“暂缺近30天浏览量”、下方“233 个商品”，字段名与数值均为块级上下排列，数值23px、单位11px；ERP页面无自身控制台错误（Chrome扩展有1条无关`Failed to fetch`） |
| 2026-07-27 | 为竞品最新状态增加运营筛选器：支持按商品名称、PLID或卖家关键词搜索，按有货/没货/未探测库存状态和动态经营信号组合筛选，实时显示结果数/总数并可一键清除；过期库存参考继续归类为本次“未探测”，空结果提供明确提示，筛选不触发采集或数据库写入 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查及生产构建通过，产物为`index-C4iZSJEn.js`和`index-CE4pY0JC.css`，正式8501健康与首页均为HTTP 200并直接提供新JS。浏览器以4个隔离合成商品验证：卖家关键词、未探测库存、指定经营信号分别筛出正确的1/4，冲突组合显示0/4及空状态，清除后恢复4/4；筛选条位于容器内且页面无横向溢出 |
| 2026-07-27 | 将运营日报顶部容易误解的“漏爬（自动补缺）”汇总改为直接陈述近30天浏览量缺失商品数，按“数量 + 个商品暂缺近30天浏览量”展示；数字只统计当前日报中`page_views_30_days`为空的商品，不增加“等待后续版本补齐”提示 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，生成`index-C4iZSJEn.js`和`index-CE4pY0JC.css`；已登录管理员真实页面显示“233 个商品暂缺近30天浏览量”，旧“漏爬（自动补缺）”标题不再出现，控制台错误为0 |
| 2026-07-27 | 将右上角“刷新全部数据”接入运营日报版本核对：每次手动刷新通过与定时任务相同的完整采集保护后，冻结独立`manual`版本；日报按北京时间10:00至次日10:00归集同一周期，比较周期内全部成功版本的订单和库存，历史中间差异不会被最新值覆盖，漏爬及浏览量变化仍不算冲突。页面显示手动刷新成功/失败次数、每个待办的全部采集版本，并支持采用本周期最新拉取值确认 | `src/takealot_ops/dashboard/refresh.py`、`src/takealot_ops/cli.py`、`src/takealot_ops/erp/daily_report.py`、`frontend/competitor/`、测试与文档 | 完整回归`237 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、数据库`verify`和8501健康检查通过。合成测试确认同周期“早间+2次手动”即使最新恢复原值仍保留订单/库存待办，纯浏览量变化不重开。管理员真实页面显示10:00周期说明、手动刷新核对次数和10张待办的全部版本入口，无日报日期框、无横向溢出、控制台错误为0；未点击正式刷新按钮，避免额外调用店铺接口 |
| 2026-07-27 | 将竞品商品弹窗升级为唯一完整详情入口：把原页面下方的经营信号、评论结构、全部变体库存与说明、库存/评论历史快照、评论分类、起止日期、日期/评分排序及评论全文一比一迁入弹窗，并删除表格下方重复详情；弹窗扩大为1180px上限，固定顶部商品标题和底部操作栏，长内容只在弹窗内滚动 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，正式8501健康检查HTTP 200并直接加载新构建；使用隔离合成数据在真实浏览器验证页面下方历史/评论标题均为0、弹窗内为唯一1份，4个历史时间点、19条评论和完整变体说明正确显示；差评筛出2/19条，2026年7月日期筛出1/19条，最早排序首条为2025-04-19评论；1280×720视口无横向溢出、弹窗内部滚动正常、顶部/底部操作保持可见、Escape恢复页面滚动，控制台错误为0 |
| 2026-07-27 | 移除运营日报顶部“日报业务日期”选择器，页面固定自动读取前一个已完成的SAST业务日；纵向历史由最近7个扩展为最近30个有数据日，表格内部增加上下滚动，视窗高度固定为表头加5日×3行并在加载后定位最新日期 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/DailyReportPage.vue`、`src/takealot_ops/erp/daily_report.py`、测试与文档 | 完整回归`235 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、数据库`verify`和8501健康检查通过。管理员真实页面确认日期选择器不存在、表格数据行38px、最大高度634px（64px表头+15行）；正式库当前3个日期共9行，尚未达到触发滚动的第6日，控制台错误为0 |
| 2026-07-27 | 按运营反馈修正经营四象限可读性：待优化散点与文字统一为棕色；纠正分界标签对应关系，竖向线显示浏览量分界及真实值、横向线显示下单分界及真实值；纵轴标题改为直立竖排，两轴补充0/25/50/75/100关键排名刻度，并约束四象限子区块最小宽度避免窄屏横向溢出 | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过；真实ERP桌面及390像素窄屏验收，待优化散点和文字计算色均为`rgb(154, 104, 16)`，分界标签分别显示“浏览量分界 61”“下单分界 1”，纵轴为`vertical-rl/upright`，两轴共10个关键刻度，窄屏文档宽度不再超过视口，控制台错误为0 |
| 2026-07-27 | 将运营日报人工核对改为截至所选日期的跨日期待办，不再随当前业务日期切换而隐藏历史未处理项；除早晚订单/库存差异外，“前一日库存-当天订单!=当天库存”的库存连续性异常也进入待办并阻止导出。运营员与管理员均可按待办自身日期人工修改、确认合并、加备注或单独确认库存差异；最终确认备注同时作为库存异常处理依据 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/`、测试与文档 | 完整回归`234 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、数据库`verify`和8501健康检查通过。正式库回算新增4条库存连续性待办，7月25日跨日期待办由6条增至10条；变更前备份`backups/takealot-20260727-040934-476906.sql`。管理员真实页面在7月26日视图显示10张7月25日待办卡，每张均有人工修改、确认合并、加备注，控制台错误为0 |
| 2026-07-27 | 为竞品雷达增加商品详情弹窗：点击最新状态中的任一商品即可在当前视口查看价格、平台仓库存/过期参考、采集时间、评论结构、库存净流出、新增评论、观察期判断和各变体SKU/卖家/价格/库存；支持键盘回车/空格打开、遮罩/关闭按钮/Escape关闭和窄屏自适应，原完整历史与评论筛选继续保留 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、前端构建产物、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript检查与生产构建通过，当前构建产物包含`index-Bbelt9Yu.js`和`index-DWyisYSa.css`且ERP已直接加载新资源，健康检查HTTP 200；使用隔离合成竞品数据在真实浏览器完成商品行点击验收，弹窗唯一打开且关键字段/2个变体正确显示，1280×720视口无横向溢出、背景滚动锁定、Escape关闭后恢复，控制台错误为0 |
| 2026-07-27 | 为运营日报早晚采集增加分层恢复与根因追踪：HTTP传输异常与429/5xx退避重试；整批依次走标准接口、新会话和忽略系统代理的直连备用；Windows任务正确向上传递CLI退出码，失败后每5分钟最多重启3次。每次尝试记录序号、策略、时点、阶段状态、Offers/Sales运行ID和脱敏原因，成功恢复及最终失败均写入日志、`daily_report_runs.counts`和页面折叠记录；所有路径失败才标记漏爬，禁止陈旧缓存冒充成功 | `src/takealot_ops/api/`、`collectors/`、`scheduler.py`、`cli.py`、`erp/daily_report.py`、`scripts/install_scheduled_task.ps1`、`frontend/competitor/`、测试与文档 | 专项`60 passed`；完整回归`232 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、PowerShell语法和任务设置核验通过。三条正式任务均为`RestartCount=3`、`RestartInterval=PT5M`且传递退出码；正式API直连备用只读探测成功返回商品，8501健康检查HTTP 200，新构建无控制台错误 |
| 2026-07-27 | 按运营反馈移除不够准确的竞品“累计销量估算”：最新状态列表删除该列和2%–5%评论率说明，Vue类型与只读API投影同步删除；历史快照中的旧字段暂时保留以兼容既有数据库，但不再作为运营指标。评论数、库存净流出、新增评论和观察期信号继续保留 | `src/takealot_ops/competitors/service.py`、`frontend/competitor/`、`tests/integration/test_competitor_storage.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 竞品存储/API专项`2 passed`（1条第三方TestClient弃用提示），Ruff、Mypy和Vue TypeScript/生产构建通过；完整回归为`225 passed, 4 failed`，4个失败均为现有运营日报测试替身未接收调度器新增的`trust_env`参数，与本次竞品展示修改无关 |
| 2026-07-27 | 降低竞品库存偶发“未探测”：首次打开购物车数量菜单也等待动画并最多重开3次，仍无数字选项时刷新购物车一次、重新按同一PLID定位后再试；最终失败不伪造当前库存，只有SKU、卖家和完整变体集合一致时，页面显示上次成功库存及北京时间并明确标为过期参考，该值不回填当前库存、不参与库存净流出或销量推断 | `src/takealot_ops/competitors/stock.py`、`src/takealot_ops/competitors/service.py`、`frontend/competitor/`、相关测试及文档 | 专项`23 passed`（1条第三方TestClient弃用提示）；完整回归`229 passed`（同一提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建和`takealot_ops.cli verify`通过。真实MySQL只读验证`PLID95565512`当前仍为本次未探测、页面参考值为上次成功精确9件（原始记录时间2026-07-25 17:06:43），库存净流出保持空值 |
| 2026-07-27 | 调整运营日报早晚合并口径：近30天浏览量继续完整采集、底层存储并在页面和Excel展示，但退出冲突判定；只有当天订单数或平台库存的早晚非空值不同才进入待合并。无差异商品自动采用晚间优先值并可直接导出，页面“待处理/只处理真实差异”只列真实冲突，移除无差异批量合并入口。正式库在备份后按新规则重算历史状态 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、测试与文档 | 完整回归`226 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）和Vue TypeScript/生产构建通过。正式库保留`page_views_30_days`列和历史值，105条纯浏览量差异由`needs_review`改为`ready`，7月25日只剩6条订单/库存真实差异，全局待合并数由575降为6；操作前备份`backups/takealot-20260727-031320-112143.sql` |
| 2026-07-27 | 收紧竞品库存“精确9件”判定：9件可加入后先验证10件；即使`10+`输入框未出现，也会读取页面弹窗，出现`We currently do not have 10 in stock.`或10件被明确拒绝时记录为精确9件；单纯打不开输入框且没有明确提示时仍保守记录“至少9件”，不会误把界面故障当成库存证据 | `src/takealot_ops/competitors/stock.py`、`tests/unit/test_competitor_stock.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 库存专项`18 passed`；完整回归`225 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）和`takealot_ops.cli verify`通过，数据库为2026-07-27无质量事件 |
| 2026-07-27 | 修复运营日报“只处理真实差异”板块右侧不可见：日报根网格使用`minmax(0, 1fr)`约束宽表最小内容宽度，所有直接子板块限制为容器宽度；差异列表覆盖竞品评论区遗留的同名双列规则，固定为单列，卡片内部列可收缩且操作按钮可换行 | `frontend/competitor/src/pages/DailyReportPage.vue`、前端构建产物、`AGENT.md` | Vue TypeScript与生产构建、`git diff --check`通过；真实1905px视口由页面滚动宽2154px恢复为1905px，差异面板宽1332px、单条卡片宽1290px，右侧3个按钮全部位于视口内，控制台无错误 |
| 2026-07-27 | 修复竞品404复核被误报为梯子断线：显式识别Takealot的`404, Page Not found`页面，正常商品使用页面标题和非空商品标题识别，不再依赖`main h1`在8秒内可见；页面内容不确定改为独立409并保留重试，只有真实导航失败、429或5xx才计入前端连续连接失败。旧版浏览器断点的误导暂停文案会自动迁移为可继续重试提示 | `src/takealot_ops/competitors/api.py`、`service.py`、两个FastAPI入口、`frontend/competitor/src/pages/CompetitorsPage.vue`、测试与文档 | 专项`26 passed`，完整回归`220 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建和`takealot_ops.cli verify`通过。真实无写入验证`PLID95799466`为API 404/页面not-found，正常对照`PLID70540744`为product |
| 2026-07-27 | 为竞品 404 增加保守复核闭环：API 404 与网络错误分离，同一 Playwright 浏览器使用最近成功的不同商品做页面对照；只有目标页为空且对照正常才累计证据，至少间隔10分钟、累计3次才确认失效，后续成功自动恢复正常。新增 MySQL 链接健康表和只读复核接口，前端显示疑似/确认状态、次数、对照商品及北京时间；断点续爬继续重试疑似项，只在当前批次跳过确认失效项，重新开始仍可人工复核 | `src/takealot_ops/competitors/`、`src/takealot_ops/storage/models.py`、两个 FastAPI 入口、`frontend/competitor/`、测试与文档 | 专项测试`20 passed`；完整回归`214 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建和`takealot_ops.cli verify`通过。真实MySQL无损创建链接健康表且初始0条；ERP重启健康检查为HTTP 200，已登录Chrome确认竞品雷达加载新JS资源且新构建控制台错误为0 |
| 2026-07-27 | 重做运营日报可读性：主表保留商品横向、把最近7个有数据业务日按三行日期块纵向堆叠，便于沿商品列对比；页面和Excel彻底移除“当天访客数”，统一显示近30天浏览量、当天订单数、平台库存数量。漏爬原因改为折叠详情，商品表头用细色条代替重复标签，增加日期/销量/库存/缺失图例；计划执行时间未到显示等待，不再误报漏爬 | `src/takealot_ops/erp/daily_report.py`、`frontend/competitor/`、测试与文档 | 完整回归`214 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、数据库`verify`和`git diff --check`通过。真实页面显示7月24/25日共6行纵向矩阵、404商品分17组，无“当天访客数”、无控制台错误；真实Excel为2个工作表、404商品列、三项指标、公式错误0。正式8501服务已重启并加载新构建 |
| 2026-07-26 | 将运营日报的漏爬与真实冲突彻底分离：整批失败、商品缺席或字段空值统一自动采用“晚间优先、早间补缺”，不要求确认、不进历史待办、不阻止导出；定时采集失败持久化脱敏原因，页面每日展示根因，Excel增加“漏爬说明”。页面和Excel按用户原工作簿还原商品横向、日期四行、亮黄表头、浅蓝日期行和浅橙销量提示；当天访客数因接口无精确值显示`—`。备份后将7月24日仅有晚间版本的404个旧误标状态改为`missing_capture`并补录当天早间接口鉴权失败原因 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/cli.py`、`src/takealot_ops/storage/models.py`、`frontend/competitor/`、测试与文档 | 完整回归`207 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建和`takealot_ops.cli verify`通过；真实页面核验404个漏爬、0个冲突、0个库存不平，SKU `9902245237368`显示1028/—/2/28且无红标，翻组/搜索正常、控制台无错误；真实Excel2个工作表、404商品列、公式错误0；变更前备份`backups/takealot-20260725-172842-234772.sql` |
| 2026-07-25 | 修正运营日报业务日期：执行日10:05/18:00/18:30统一处理前一个已完成的SAST业务日，普通HTML/Excel/PNG也按该业务日导出；ERP运营日报使用独立的前一日默认日期，销量单元格由易与异常混淆的橙红色改为绿色，库存错误仍单独使用红色；备份后将本次误归7月25日的1个晚间批次、404个商品状态、1条审计和1条截止快照整体改标到7月24日 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/scheduler.py`、`src/takealot_ops/cli.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/DailyReportPage.vue`、测试与文档 | 完整回归`205 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、`takealot_ops.cli verify`和`git diff --check`通过；修正后7月24日为404商品/1个晚间批次，7月25日为空，已重新生成7月24日三种普通日报并重启ERP验证HTTP 200；修正前备份为`backups/takealot-20260725-160005-211994.sql` |
| 2026-07-25 | 为竞品批量采集增加可持久断点续爬：按批次记录链接顺序、已检查/失败索引、成功结果、采集选项和停止原因；页面刷新、模块切换或 ERP 重启后显示“继续失败/未完成”，从最早失败或首个未执行位置恢复，并跳过其后已经成功的本批 PLID；链接 PLID 或顺序变化时拒绝误用旧断点，开始新采集明确重置批次 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、构建产物及文档 | `203 passed`；Ruff、mypy strict、CLI `verify`、Vue TypeScript/生产构建及 `git diff --check` 通过；Chrome 实测手动暂停显示 `0/4`、待续爬 `4`，模块切换和整页刷新后均恢复断点，“继续失败/未完成”可重新启动，控制台无错误 |
| 2026-07-25 | 修复梯子/代理中断导致竞品采集浏览器成批泄漏：Playwright、browser、context、page 创建后立即登记，预热失败、超时及取消时强制按层回收；Takealot 首页预热增加有限重试，临时连接错误使用可识别的 HTTP 503 中文提示；前端把本机断连/5xx与永久商品错误区分，连续两条连接类失败后暂停剩余链接并保留进度 | `src/takealot_ops/competitors/api.py`、`src/takealot_ops/competitors/service.py`、两个竞品采集接口、`frontend/competitor/`、测试与文档 | 清理历史遗留的57个本项目Playwright Node驱动和274个Chrome子进程后，物理内存占用从87.9%降至69.1%、可用虚拟内存从1.5GB回升至10.6GB；专项测试覆盖代理失败清理、取消清理、可重试分类和503接口，真实Chrome启停前后项目进程数未增长；完整回归`203 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建、2026-07-25数据库校验和`git diff --check`通过 |
| 2026-07-24 | 新增可替代人工横向表格的运营日报闭环：MySQL冻结10:05早间和18:00晚间商品订单/平台仓库存/近30天浏览量，人工候选与系统值并存，确认和修改强制备注并写入审计；无差异可批量合并，差异逐项选源，确认后新系统值变化会重新待办；库存按前一日库存减当天订单核对并支持带原因取消红标；18:30保存跨天待办快照，全局提醒历史未确认项，未合并时阻止旧表布局Excel导出并定位日期/SKU，全部确认后自动本地导出；Windows任务改为10:05/18:00/18:30三时点 | `src/takealot_ops/erp/daily_report.py`、`src/takealot_ops/storage/models.py`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/cli.py`、`frontend/competitor/`、`scripts/install_scheduled_task.ps1`、测试与文档 | 完整回归 `199 passed`（1条第三方TestClient弃用提示），日报接口/Excel专项11项通过；Ruff、Mypy（44个源码文件）、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 通过；真实MySQL无损新增5张日报表，三个计划任务下一次分别为2026-07-25 10:05/18:00/18:30；正式 `8501` 页面确认新导航、空白起始状态、禁用导出和控制台无新错误 |
| 2026-07-24 | 完成 Hermes 留下的竞品 Playwright 异步迁移并修复明确库存提示仍被继续探测：公开商品/评论由普通 HTTP 客户端改为复用本机 Chrome/Edge 导航公开 JSON，采集器、ERP 接口和 CLI 同步改为异步链路；快捷菜单和自定义数量统一把 `current stock = N` 设为最高优先级，数量控件显示成功后保留3秒提示结算窗口，一旦读到数字立即返回精确库存，不再等待、翻倍或二分；恢复原先被临时跳过的9个异步库存安全测试 | `src/takealot_ops/competitors/`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/cli.py`、`tests/unit/test_competitor_stock.py`、`tests/unit/test_competitors.py`、前端停止采集交互及文档 | 新增异步回归测试覆盖“Qty 9先显示、稍后出现current stock=4”、首次超量即命中后不再探测及命中后无额外等待；完整回归 `193 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy（43个源码文件）、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 全部通过，数据库检查为2026-07-24无质量事件 |
| 2026-07-24 | 将 ERP 首个管理员、管理员新增账号和重置密码的最短密码长度统一由12位调整为8位；后端仍统一使用 scrypt 随机盐哈希，前端表单、提示和运行文档同步更新 | `src/takealot_ops/erp/auth.py`、`frontend/competitor/src/pages/LoginPage.vue`、`frontend/competitor/src/pages/UsersPage.vue`、测试与文档 | 后端专项验证7位密码返回422、8位密码可完成初始化；完整回归 `190 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy（43个源码文件）和 Vue TypeScript/生产构建通过 |
| 2026-07-24 | 为局域网共享 ERP 增加完整用户权限并将正式服务改为 `0.0.0.0:8501`：新增 MySQL 用户/会话表、scrypt 密码哈希、12小时可撤销 HttpOnly 会话、SameSite=Strict Cookie、写请求 CSRF、5分钟登录失败限流和首个管理员仅本机初始化；后端强制执行查看员/运营员/管理员三级角色，Vue 增加登录页、账号状态、退出和管理员“用户权限”模块，查看员的刷新、采集、上传和生成操作同时由前端禁用；MySQL 保持仅本机连接 | `src/takealot_ops/erp/auth.py`、`src/takealot_ops/erp/web.py`、`src/takealot_ops/storage/models.py`、`src/takealot_ops/settings.py`、`src/takealot_ops/dashboard/launcher.py`、`frontend/competitor/`、配置、测试与文档 | 完整回归 `190 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy（43个源码文件）、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 通过；专项接口验证远程不能抢占首个管理员、未登录返回401、查看员写操作和用户列表返回403、缺少 CSRF 返回403、唯一管理员不可停用或降级 |
| 2026-07-24 | 增强竞品雷达批量链接校验：采集前逐行检查 URL 格式、Takealot 域名和 PLID，发现错误后停止外部请求，自动聚焦输入框、滚动并选中第一条错误链接；输入框使用红色边框、红色选区和三次脉冲提示，下方编译器式诊断条显示行号、具体原因及完整链接，用户开始修改后立即清除错误状态 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、构建产物及文档 | Vue TypeScript/生产构建、Ruff、Mypy、`takealot_ops.cli verify` 通过；完整回归 `188 passed`（1条第三方 TestClient 弃用提示）。浏览器以第12行为 Mercado Libre 链接验证：输入框获得焦点、内部滚动 `170px`、完整错误链接被精确选中，`aria-invalid=true`，红色边框和脉冲动画生效，诊断条明确显示“第 12 行 / 不是 Takealot 商品链接 / 完整链接”；修改输入后诊断条、错误样式和“失败 1 个”旧状态均立即清除，运行中的 `8501` 已提供新 JS/CSS 资源 |
| 2026-07-24 | 增强竞品雷达公开评论区：现有好评/中评/差评可与开始、结束日期叠加筛选，兼容平台 `DD Mon YYYY` 和既有 ISO 日期；增加最新评论、最早评论、评分高到低、评分低到高四种展示排序，显示当前结果数/总评论数并支持一键清除时间；无日期记录在限定时间后自动排除，移动端控件改为纵向全宽布局；评论卡键加入评论人和展示序号，修复同日同标题评论在排序后重复或残留 | `frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、文档 | Vue TypeScript/生产构建、Ruff、Mypy、`takealot_ops.cli verify` 通过；完整回归 `188 passed`（1条第三方 TestClient 弃用提示）。浏览器以65条真实评论验证日期边界 `2024-03-18` 至 `2026-05-18`，最早/最新排序首尾正确，2026年日期与差评组合筛出3条，清除时间恢复6条差评，评分高低排序首条分别为5星和1星，显示数量与实际卡片数均为65 |
| 2026-07-24 | 移除经营四象限的近7日流量推算：Offers 接口没有独立近7日流量字段，因此后端不再按近30天浏览量比例生成 `page_views_7_day_estimate`，前端悬浮卡删除该卡片和估算说明，只保留平台明确返回的近30天浏览量；增加接口防回归断言并同步业务文档 | `src/takealot_ops/erp/service.py`、`frontend/competitor/`、测试与文档 | 完整回归 `188 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 通过；运行中接口确认 `page_views_7_day_estimate` 已不存在而 `page_views_30_days` 保留，浏览器悬浮卡确认无“近7日流量”文字且仍显示近30天浏览量 |
| 2026-07-24 | 修正经营四象限悬浮卡的展示与补货口径：信息卡移出散点图裁切容器，并按点位上下半区自动选择展开方向，避免向上弹出时被遮挡；字段标题提高字号、字重和明暗对比；补货信息改为读取完整 Offer 历史快照，后一条有效平台库存高于前一条时直接记录该快照的北京时间及增加数量，不再称为估算 | `src/takealot_ops/metrics/service.py`、`src/takealot_ops/erp/service.py`、`frontend/competitor/`、测试与文档 | 后端专项测试 `27 passed`；完整回归 `188 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 通过。真实 MySQL 接口确认库存增加商品返回北京时间及增加量，例如 `9902340483776` 为 `2026-07-24 10:10`、较前次 `+10`；浏览器确认顶部商品点改为向下展开，信息卡超出图框仍完整可见，字段标题计算样式为 84% 白色、10.08px、600字重 |
| 2026-07-23 | 增强经营四象限商品时效信息：持久化 Offers 接口 `created_at` 作为平台首次上架时间，旧历史回退为本库最早完整记录；按相邻有效平台库存上升估算最近补货日期和增加量；按近30天日均乘7提供明确标注“估算”的7日流量参考；悬浮卡新增库存、指标截止日期及上述时间信息，图框外增加固定横纵轴说明；现有 MySQL 两张 Offer 表无损增加可空 `created_at` | `src/takealot_ops/domain.py`、`src/takealot_ops/storage/`、`src/takealot_ops/erp/service.py`、`frontend/competitor/`、测试与文档 | 完整回归 `188 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy、Vue TypeScript/生产构建和 `takealot_ops.cli verify` 通过；MySQL 架构升级成功。真实接口返回397个商品，其中旧历史397个暂按本库最早记录显示、3个商品识别到库存上升补货估算；浏览器确认横纵轴及悬浮卡的库存、7日流量参考、首次记录、补货估算和截止日期均可见，ERP 健康检查通过 |
| 2026-07-23 | 修复统一 ERP 两项页面状态：经营四象限不再把十字线固定在50%，改用接口返回的浏览量与下单排名分界同步移动两条线、轴标签及中心交点；竞品雷达通过 Vue `KeepAlive` 在模块导航时保留正在运行的采集实例、输入、进度和结果，并在采集中明确提示可安全切页 | `frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/pages/CompetitorsPage.vue`、`frontend/competitor/src/styles.css`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | Vue TypeScript 检查及生产构建通过；完整回归 `187 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy、`takealot_ops.cli verify` 通过。浏览器真实数据实测标准分位交点约为50%/31%，宽松分位横向移动至约25%，严格分位移动至约75%/75%；竞品页本地校验状态在切到经营总览再返回后输入与结果均保留，控制台错误为0 |
| 2026-07-23 | 修复两个有货竞品被标记“未探测”：等待主商品异步加购完成，数量菜单关闭动画增加等待和重开；按 `aria-hidden` 排除未启用的数量输入节点，绕过目标商品卡链接对已核验输入框的指针拦截；定向清理 Braze 营销遮罩，购物车临时更新失败时重新加载并有限重试，自定义数量被平台接受且编辑器收起时通过 `Qty: N` 确认结果 | `src/takealot_ops/competitors/stock.py`、`tests/unit/test_competitor_stock.py`、`README.md`、`docs/PROJECT_STATUS.md`、`AGENT.md` | 完整回归 `187 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 通过。真实隔离匿名购物车取得 `PLID70540744=4`、`PLID99275672=10`，均为精确值并已重新采集写入 MySQL；当前数据库为11个目标、13条商品快照、14条变体结果、425条共享去重评论，测试购物车已清理 |
| 2026-07-23 | 修复竞品库存探测误点推荐商品：不再遍历页面全部 `Add to Cart`，只在当前目标 PLID 商品页的 `main aside` 主购买区接受唯一按钮；商品页 URL 必须匹配目标 PLID，进入购物车后也必须再次找到同一 PLID 的商品行，移除“购物车只有一个数量控件就视为目标”的危险回退；补充防回归测试；保留11个链接并清空修复前受影响的重新采集结果 | `src/takealot_ops/competitors/stock.py`、`tests/unit/test_competitor_stock.py`、`README.md`、`docs/PROJECT_STATUS.md` | 完整回归 `181 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 通过。4个真实商品页逐一核对：3个可购买页的主购买区各仅1个按钮，推荐区分别有22、27、7个按钮；不可购买护膝页主购买区为0且不会回退推荐位。真实隔离购物车以 `PLID95526981` 跑通同 PLID 二次核对并取得精确库存90，测试购物车已清理；ERP 8501 已重启，健康检查通过且竞品快照为0 |
| 2026-07-23 | 正式数据库由 SQLite 全面切换为本机 MySQL 8.0：新增 PyMySQL、MySQL 默认配置和本机/同步驱动校验，建立受限应用账号和 `utf8mb4` 数据库；增加空目标保护的一次性全表迁移命令、MySQL 数据库级只读 ERP 会话、`CHECK TABLE` 完整性检查及 `mysqldump --single-transaction` 每日备份；前端数据库状态与运行文档同步改为 MySQL，SQLite 只保留为退役迁移源和测试夹具 | `src/takealot_ops/settings.py`、`src/takealot_ops/storage/`、`src/takealot_ops/scheduler.py`、`src/takealot_ops/erp/`、`frontend/competitor/`、配置、测试与文档 | 旧 SQLite 的12张表4204行迁入 MySQL 并逐表数量完全一致；MySQL `CHECK TABLE`、只读事务拒绝 UPDATE、ERP 五个核心接口、HTML/Excel/PNG 读取和1,041,068字节 SQL 备份通过；完整回归 `177 passed`（1条第三方 TestClient 弃用提示），Ruff、Mypy、`takealot_ops.cli verify` 和 Vue 生产构建通过 |
| 2026-07-23 | 增加竞品变体级库存：递归枚举同一 PLID 下尺寸、颜色等选择器组合，在一个隔离浏览器会话中逐个变体探测并把 SKU、卖家、价格、库存写入独立 SQLite 明细表；不可购买或长时效变体显示“没货”；评论继续按 PLID 只抓取、去重一次；前后变体集合一致才比较汇总库存；Vue 竞品详情新增“各变体库存”表 | `src/takealot_ops/competitors/`、`src/takealot_ops/storage/models.py`、`src/takealot_ops/erp/web.py`、`frontend/competitor/`、测试与文档 | 完整回归 `172 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 和 Vue 生产构建通过。真实 `PLID96909926` 识别 Right Hand、Left Hand 两个变体，分别保存标识、价格与“没货”状态；数据库共18条商品快照、2条新变体明细、192条共享去重评论 |
| 2026-07-23 | 提升竞品有效库存口径：快捷菜单接受9件后自动进入自定义数量，优先解析 `current stock = N`，否则以超量测试和二分校验求精确平台仓上限；公开接口 `is_leadtime` 的供应商调货/长时效到货商品直接标记“没货”，排除出库存净流出销量推断；前端为没货状态增加红色标签 | `src/takealot_ops/competitors/`、`frontend/competitor/`、测试与文档 | 重点测试 `13 passed`，完整回归 `171 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 和 Vue 生产构建通过。真实隔离购物车已跑通100、54拒绝后在31测试中解析出 `current stock = 13`；4个首批竞品重新采集成功，最新值为2个没货、精确库存6和1，共17条快照/192条评论；测试购物车已清理。ERP 已重启，健康和竞品接口均为 HTTP 200 |
| 2026-07-23 | 修正经营四象限当前悬停/选中的单个“待优化”散点过黑：四类散点改为显式固定高对比填充色，“待优化”使用亮金色；散点关闭浏览器原生按钮外观，并在悬停、焦点、按下和复制状态显式保持原填充色，避免交互状态叠加后近黑 | `frontend/competitor/src/styles.css`、`README.md`、`docs/PROJECT_STATUS.md` | Vue TypeScript 检查及生产构建通过，生成 `index-CBLF9BE9.css`；运行中的 `8501` 返回 HTTP 200 并已提供该资源，构建 CSS 已确认包含原生外观关闭、亮金色和按下状态固定填充；`git diff --check` 通过（仅现有 CRLF 提示） |
| 2026-07-23 | 将经营四象限浏览器原生延迟提示替换为即时自定义 ERP 信息卡：鼠标和键盘焦点进入即显示分类、商品名、平台 SKU、近30天浏览量与近7日下单，自动避让图表边缘；保留点击复制 SKU | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、文档 | Vue TypeScript 检查及生产构建通过；原生 `title` 已移除，浮层使用80毫秒入场动画；运行中 `8501` 返回 HTTP 200 并已提供新 JS/CSS 哈希资源 |
| 2026-07-23 | 统一 ERP 记录时间显示：SQLite 返回的无偏移时间按 UTC 解析，“最近采集”和竞品历史快照固定转换为北京时间（Asia/Shanghai），并将“本机记录时间”改为“北京时间”；SAST 指标日期口径保持不变 | `frontend/competitor/src/time.ts`、`frontend/competitor/src/App.vue`、`frontend/competitor/src/pages/CompetitorsPage.vue`、文档 | Vue TypeScript 检查及生产构建通过；实际样例 `2026-07-23T04:30:26` 转换为 `07/23 12:30`；运行中 `8501` 返回 HTTP 200 并已提供新哈希静态资源 |
| 2026-07-23 | 改善经营四象限散点识别与运营操作：散点放大并增加悬停/键盘焦点状态，提示中显示商品和平台 SKU，点击直接复制平台 SKU；成功、SKU 缺失及复制失败均显示中文反馈 | `frontend/competitor/src/pages/QuadrantsPage.vue`、`frontend/competitor/src/styles.css`、`README.md`、`docs/PROJECT_STATUS.md` | Vue TypeScript 检查及生产构建通过；完整回归 `170 passed`（1条第三方 TestClient 弃用提示）；新构建使用哈希静态资源，运行中的 `8501` 刷新页面后加载新交互 |
| 2026-07-23 | 将原 Streamlit 南非店铺看板迁移为统一 Vue 3 + TypeScript 小型 ERP：新增经营总览、商品中心、经营四象限、风险与质量、竞品雷达、报表及 NFT102 六个模块；新增只读 ERP 数据投影和 FastAPI 动作接口，正式 `dashboard` 启动入口改为单端口 `8501`，保留 `dashboard-legacy` 回退；空数据库保持可打开且不被读取页面创建 | `src/takealot_ops/erp/`、`frontend/competitor/`、`src/takealot_ops/dashboard/launcher.py`、`src/takealot_ops/cli.py`、测试与文档 | 完整测试 `170 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 和 Vue 生产构建通过；真实 `8501` 健康、静态页、总览、四象限及竞品接口均为 HTTP 200，读取397个商品/4个竞品，`8502` 已释放；浏览器逐页及390像素窄屏检查无控制台错误 |
| 2026-07-23 | 合并原 Node 竞品库存/评论/销量工具的最小闭环并原生接入当前项目：新增竞品目标、快照和去重评论表，公开商品与评论采集、隔离匿名购物车库存探测、2%–5%评论率累计销量区间、跨快照库存/新增评论信号、批量 CLI、本机 FastAPI，以及 Vue 3 + TypeScript 竞品中心；现有 Streamlit 增加“竞品观察”入口并由启动器同时管理两个回环服务 | `src/takealot_ops/competitors/`、`frontend/competitor/`、`src/takealot_ops/storage/models.py`、`src/takealot_ops/dashboard/`、`src/takealot_ops/cli.py`、测试与文档 | 完整测试 `164 passed`（1条第三方 TestClient 弃用提示）；Ruff、Mypy、`takealot_ops.cli verify` 和 Vue 生产构建通过；4个真实链接全部保存，共5条快照/192条评论，库存结果为精确4、两个至少9、一个未探测 |
| 2026-07-23 | 将完整开发历史快进合并到 `main`，并把 `main` 设为项目后续开发与交付基线；原开发分支暂时保留，未删除 | `AGENT.md`、`docs/PROJECT_STATUS.md` | `main` 从初始提交快进包含开发分支全部44个提交；本次仅调整分支与交接文档，未修改运行代码 |
| 2026-07-23 | 统一异常口径：看板总览、Excel 和 HTML/PNG 总览均统计最新指标日去重异常商品；看板异常页默认仅显示最新日并提供“全部历史”切换；Excel、HTML/PNG 异常明细仅保留最新指标日，同时说明同一商品可有多条异常类型记录 | `src/takealot_ops/metrics/service.py`、`src/takealot_ops/dashboard/app.py`、`src/takealot_ops/exports/excel.py`、`src/takealot_ops/exports/html.py`、相关测试与文档 | 重点测试 `49 passed`；完整测试 `158 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；真实数据核对为最新日289个商品/299条记录，历史累计340个商品/986条记录；已重新生成并核对2026-07-23的HTML、Excel、PNG |
| 2026-07-23 | 导出中心新增“一键导出全部报表”：按页面截止日期先检查 SQLite 完整性，再从当前本地数据生成 HTML、Excel、PNG；不调用平台接口，并对所有已生成文件提供页面直接下载 | `src/takealot_ops/dashboard/app.py`、`tests/e2e/test_dashboard_smoke.py`、`README.md`、`docs/PROJECT_STATUS.md` | 导出与页面重点测试 `28 passed`；完整测试 `155 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；浏览器确认按钮、只读说明及三个下载入口均正常显示，未手动触发真实导出 |
| 2026-07-23 | 重做经营四象限对比：销售维度由单日改为近7日下单件数，销量分界只从正销量商品计算且保持整数，坐标改为相对排名以避免极端值压缩，并增加四色区域背景和真实阈值说明；单品分析及店铺总览的下单件数图移除小数移动平均线并固定整数刻度 | `src/takealot_ops/metrics/service.py`、`src/takealot_ops/dashboard/charts.py`、`src/takealot_ops/dashboard/app.py`、`src/takealot_ops/dashboard/labels.py`、相关测试与文档 | 重点测试 `45 passed`；完整测试 `155 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；真实数据验证得到明星27、转化问题34、潜力19、待优化40、未分类277，浏览器确认页面阈值与分类数量正确 |
| 2026-07-22 | 新增看板数据新鲜度展示和“立即刷新看板数据”按钮；手动刷新复用完整 `daily-run` 且不向页面泄露子进程输出；每日 Windows 自动任务默认改为中国时间 10:10；计划任务脚本改用兼容 Windows PowerShell 5.1 的 ASCII 源码并在运行时生成中文任务名 | `src/takealot_ops/dashboard/app.py`、`src/takealot_ops/dashboard/refresh.py`、`scripts/install_scheduled_task.ps1`、相关测试与文档 | 重点测试 `32 passed`；完整测试 `154 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；系统任务“Takealot 店铺数据每日更新”安装成功，下一次运行 2026-07-23 10:10；看板重启后健康检查 200 |
| 2026-07-22 | 修复运行中的旧看板未重新载入新增模块；将页面固定控件、业务字段、状态和说明统一为中文，隐藏框架与图表英文工具栏，并把网页上传上限固定为100兆字节 | `src/takealot_ops/dashboard/app.py`、`src/takealot_ops/dashboard/labels.py`、`src/takealot_ops/dashboard/charts.py`、`src/takealot_ops/dashboard/launcher.py`、`src/takealot_ops/settings.py`、`src/takealot_ops/nft102_portal.py`、相关测试与文档 | 中文界面重点测试 `39 passed`；完整测试 `148 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；浏览器确认新模块可见，顶部、图表、表格和上传控件无可见英文 |
| 2026-07-22 | 新增 NFT102 前端续写流程：上传运营最终版、校验并原样归档、识别下一日期、一键调用既有生成脚本、下载新 Excel 与核对说明；上传基准和生成结果均不覆盖；连续续写时替换旧日期后缀，避免文件名逐日增长 | `src/takealot_ops/nft102_portal.py`、`src/takealot_ops/dashboard/app.py`、`scripts/update_nft102_daily.ps1`、`tests/unit/test_nft102_portal.py`、`tests/unit/test_nft102.py`、`README.md`、`docs/PROJECT_STATUS.md` | 重点测试 `34 passed`；完整测试 `147 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过 |
| 2026-07-22 | 统一 Excel 输出对齐规则：运营日报全部有内容单元格水平、垂直居中；NFT102 仅居中目标工作表并保持其他店铺工作表不变 | `src/takealot_ops/exports/excel.py`、`scripts/write_nft102_workbook.py`、`tests/integration/test_excel_export.py`、`tests/unit/test_nft102.py` | 完整测试 `138 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；9 个日报工作表完成样式与视觉检查，公式错误扫描为 0 |
| 2026-07-22 | 补齐 Excel 异常商品表的中文导出映射；`high_views_low_conversion`、`suspected_stockout`、`stale_offer_snapshot` 的异常类型和说明不再回退为英文 | `src/takealot_ops/exports/excel.py`、`tests/integration/test_excel_export.py` | 完整测试 `137 passed`；Ruff、Mypy、`takealot_ops.cli verify` 通过；修正版 Excel 的未翻译异常字段扫描为 0，公式错误扫描为 0 |
| 2026-07-22 | 建立项目长期维护档案及 Codex 自动入口；确立任何项目修改必须同步更新 `AGENT.md` 的规则 | `AGENT.md`、`AGENTS.md` | 文档规则检查；未运行代码测试（本次未修改运行代码） |
