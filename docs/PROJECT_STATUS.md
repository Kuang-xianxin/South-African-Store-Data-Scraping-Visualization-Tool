# Takealot 运营数据工具：单 Agent 交接状态

更新时间：2026-08-15
主分支：`main`
功能代码基线：当前 `main` 工作区
工作区模式：项目根目录单 Agent

## 当前结论

- 统一 Vue ERP 已新增独立“异常商品”模块，当前侧边栏共10个模块。五类入口严格分开：动销突然中断、`not_buyable`有库存、`disabled_by_takealot`有库存、`disabled_by_seller`有库存和可售有库存滞销；滞销选择器固定为4/7/10/15/20/30天并显示实际连续未动销天数。突然停销要求前7个完整业务日至少5天有单、合计至少7件且紧邻停销前一天有单，随后连续3个完整业务日为0；只有成功Sales批次核验且已经结束的SAST日才可作为0，缺失和日内数据不补0。三种不可售状态不合并，滞销只纳入`buyable`且当前现货大于0的商品。该模块使用独立`/api/erp/anomaly-products`本地只读投影，没有恢复或改写旧“风险与质量”前端及后台规则。
- 每张异常商品卡片使用PLID交接到竞品雷达现有自有链接详情弹窗，继续复用同一详情接口和类目、卖家报价、价格/库存/评论、自有销量及近30天浏览量数据；没有竞品查看权限时沿用统一未开放提示。正式MySQL只读投影截至2026-08-14：突然停销0个，`not_buyable`有库存4个，平台禁售有库存0个，卖家禁售有库存0个；滞销4/7/10/15/20/30天分别95/81/67/58/46/0个。零命中类型仍保留独立入口，不会隐藏或混入其他组。
- 本轮完整Python`668 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、全部前端`159 passed`；Ruff、72个源码文件Mypy、Vue TypeScript/生产构建、CodeGraph影响检查、六店数据库`verify`与差异检查通过，构建仅有既有大包提示，前端全测仅出现既有Vite关闭阶段依赖扫描提示。正式ERP经`restart_erp.ps1`重启（启动器PID 33708、监听PID 4400），本机与局域网`192.168.110.180`健康及首页均HTTP 200，`index-2Z1ieT_g.js`/`index-D6jyzOtD.css`线上线下字节数和SHA-256一致；未登录新异常接口返回401。发布前09:00批次为同一`scheduled-20260815-a0f5522fd2fd`、1328目标、修订8、354次尝试/351条结果、错误3/终态2、活动索引354，调度断点与队列文件SHA-256在重启后即时完全一致；随后沿原断点推进至355次尝试/352条结果和活动索引355，未新建、归零、停止或恢复批次。本次未执行登录态浏览器验收，未人工发起Takealot上游请求或正式业务写入。
- 竞品雷达所有可打开商品详情的小卡现共用具体类目展示：公开商品采集把Takealot `breadcrumbs.items`完整层级与类目ID结构化保存到快照，个人监控池、个人/共享类型库、真正竞品及自有商品跟卖卡片打开详情后，标题区显示最近一条非空类目路径和末级类目ID。详情接口继续按PLID只读本机MySQL，不因打开弹窗访问平台；升级前历史快照没有类目时明确提示后续成功采集补齐，不根据标题、来源或URL猜测。
- 本轮完整Python`661 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、全部前端`155 passed`；Ruff、71个源码文件Mypy、Vue TypeScript/生产构建、CodeGraph影响检查、数据库`verify`与差异检查通过。正式MySQL已新增可空`category_path` JSON列，19,002条旧快照未回填；原09:00批次续跑后的首条新成功快照`PLID53395543`真实保存`DIY Tools & Machinery › Hand Tools › Pliers`及末级类目ID`27426`，按PLID详情投影原样返回。正式ERP经`restart_erp.ps1`重启（启动器PID 23820、监听PID 1376），本机/局域网健康与首页均HTTP 200，`index-pLjzug6Q.js`/`index-C7yJZmcj.css`线上线下字节数和SHA-256一致。重启前后立即保持同一`scheduled-20260815-a0f5522fd2fd`、1328目标、修订8、266次尝试/263条结果、错误3/失败1/终态2、活动索引266和相同断点哈希，随后沿原断点推进至267次尝试/264条结果及活动索引267；未执行登录态浏览器验收，也未人工发起Takealot上游请求。
- 竞品详情三/四联图左侧已从重叠区域拆为指标说明栏、纵轴刻度栏和绘图区：说明栏从x=12到分隔线x=108，纵轴刻度右对齐到x=158，折线与网格从x=170开始；长指标名“近30天浏览量”、说明、数值刻度和首个折线点不再互相遮挡。四联图392单位总高、三联图346单位总高、共用时间轴、指针/键盘交互及全部数据口径保持不变。
- 竞品自有Seller Offer的逐次观察现已在每次完整刷新时，把`page_views_30_days`与同次价格、精确库存和标题一起不可变保存；第四张流量图只使用时间戳完全相同的观察，不再读取每日唯一快照或吸附最近节点。正式MySQL迁移后共54,507条逐次观察，其中54,095条有同次Seller流量采集证据；唯一无法举证的是`current`店北京时间2026-08-12 16:15整批412条，继续保持缺口。截图样例`current / PLID102576284 / Offer 238002441`原有38个库存观察点，已恢复37个同刻流量值，仅16:15这一点缺失；因此历史不只剩18:00，但无法证明的被覆盖日内值仍不能追回或猜补。公开竞品库存探测继续不调用认证Seller Offers。
- 本轮完整Python`663 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、全部前端`155 passed`；Ruff、71个源码文件Mypy、数据库`verify`、Vue TypeScript/生产构建及差异检查通过，前端仅有既有Vite关闭阶段依赖扫描提示，构建仅有既有大包提示。迁移前备份`backups/takealot-20260815-102921-588374.sql.gz`经清单、gzip内容和SHA-256校验通过（`18d585fc53e8453cbafcda1940a67d5e46a552a7fff025d88302f3477f843c91`）；正式ERP经`restart_erp.ps1`重启（启动器PID 5572、监听PID 29232），本机与局域网`192.168.110.180`健康及首页均HTTP 200，`index-B3TBmXKD.js`/`index-C7yJZmcj.css`线上线下字节数和SHA-256一致。由`kxx`手动暂停的09:00批次在重启前后即时保持同一`scheduled-20260815-a0f5522fd2fd`、1328目标、修订8、323次尝试/318条结果、错误5/失败3/终态2和两个断点文件SHA-256完全一致；最终核对时其既有`last_resumed_by=kxx`显式恢复状态已生效，沿原批次推进至324次尝试/319条结果，没有归零或新建批次，本任务未调用停止或恢复接口。未执行登录态浏览器验收，也未人工发起Takealot上游请求。
- 统一 Vue ERP 已删除“运营日报”前端板块：侧边栏、模块路由、页面组件、全局未处理提醒、日报事件订阅、前端日报 API/类型和用户权限配置项均已移除，旧 `#module=daily-report` 会按无效模块回退；新增独立异常商品后当前侧边栏共10个模块。日报后端接口、MySQL数据、权限键、10:05/18:00/次日09:00/18:30计划任务、CLI与Excel导出继续保留，本次没有删除或修改正式日报数据。
- 本次导航/新鲜度/日期定向`11 passed`、全部前端`153 passed`，Vue TypeScript与生产构建通过；前端源码及生产包中的日报页面/模块/API/类型残留均为0，旧哈希仅保留在负向回退测试中。正式ERP经`restart_erp.ps1`重启（启动器PID 8560、监听PID 8308），本机与局域网`192.168.110.180`健康及首页均HTTP 200；线上`index-0b_A8tz7.js`/`index-D0FuuKTj.css`与本地字节数和SHA-256一致。发布前后09:00批次保持同一`scheduled-20260815-a0f5522fd2fd`、1328个目标、运行修订6、48次尝试、44条结果、4个错误、1个终态、活动索引46及相同断点文件SHA-256，未新建、归零或覆盖断点。本次纯前端删除未运行Python回归，也未执行登录态浏览器验收、Takealot上游请求或正式日报数据写入。
- 竞品详情弹窗已针对“固定详情框与全部折线同时可见”收紧桌面布局：四联图绘图区由按面板固定126单位步长改为四图专用88单位步长、总高392，普通竞品三联图使用102单位步长、总高346；桌面窗口高度不超过900像素时，仅该详情弹窗同步收紧页头、商品图、固定详情、图表说明和页脚间距。价格、库存、评论与自有Seller API近30天浏览量仍共用时间轴和指针，数据、缺失点、标题变更及交互口径均未改变；窄屏或更低窗口继续允许滚动。
- 折线布局与交互定向`11 passed`、全部前端`153 passed`，Vue TypeScript与生产构建通过；全测退出码0，仅关闭阶段出现既有Vite依赖扫描提示，构建仅有既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 22356、监听PID 38252），本机与局域网`192.168.110.180`健康及首页均HTTP 200；线上`index-CAUFADhA.js`/`index-Pi6x8r9G.css`与本地字节数和SHA-256一致。发布前后09:00批次保持同一`scheduled-20260815-a0f5522fd2fd`、1328个目标、运行修订6、35次尝试、31条结果、3个失败、1个终态及活动索引31，未新建、归零或覆盖断点；本次纯前端布局调整未运行Python回归，也未执行登录态浏览器验收、Takealot上游请求或正式业务写入。
- 已修复竞品雷达快速切换店铺时，顶栏已经显示“全部店铺·6店合并”但自有商品仍停在T C STORE的竞态。正式MySQL只读复现确认T C STORE恰为182个自有PLID，6店合并实际为930个，截图中的182不是六店缺数，而是先发出的单店慢请求在后发的六店请求之后回写。前端现在取消旧请求并以请求序号、范围和当前店铺代码拒绝迟到响应，切店只读取自有商品与当前范围自动目标，范围结果有界缓存且竞品页保持挂载；大列表改用浅层响应容器并记忆化当前页卡片。后端新增只读`/api/competitors/own-store`窄投影，列表跳过详情专用历史、评论和变体DataFrame，Seller API观察点查询下推到所选店铺，并用SQL反连接排除与observation同一时刻的重复baseline。正式同库测量中，T C STORE切换由约7.453秒降至0.929秒，6店合并由约10.284秒降至4.111秒；首次完整列表的6店读取为5.635秒，切回已缓存范围无需再次查询，398个真正竞品及182/930个自有PLID均保持不变。
- 本轮完整Python`660 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、全部前端`151 passed`；全仓Ruff、71个源码文件Mypy、Vue TypeScript/生产构建及任务差异检查通过，构建仅有既有大包提示，前端全测退出码0并仅在关闭阶段出现既有Vite依赖扫描提示。正式ERP经`restart_erp.ps1`重启（启动器PID 12944、监听PID 31140），本机与局域网`192.168.110.180`健康及首页均HTTP 200；线上`index-BqcX47iC.js`/`index-DIXwwZGB.css`与本地SHA-256一致。发布前后09:00批次保持同一`scheduled-20260815-a0f5522fd2fd`、1328个目标、运行修订6、31次尝试、28条结果、2个失败和1个终态，活动索引31未被新建、归零或覆盖；未人为启停批次、调用Takealot上游或执行登录态浏览器验收。
- 09:00共享批次不会再在常规队列耗尽时把“待重试”直接留在已结束状态。失败和未执行项现在继续绑定原`batch_id`、冻结顺序、成功结果与确认失效终态，服务端持久等待至少10分钟后自动各重试一次，最多3轮；等待期间所有管理员看到同一倒计时且`kxx`仍可停止，ERP重启会恢复等待和队列。达到上限仍有未解决项时，页面投影“继续待重试（N）”并隐藏“开始采集”，显式继续仍复用原批次且跳过已成功和已确认失效链接。升级前已经落成`completed_with_pending`且仍有轮次预算的旧断点会在ERP启动时迁移到同一延时续爬流程。
- 本轮自动批次专项`13 passed`、竞品批次注册表`19 passed`、ERP Web相关`3 passed`、完整Python`657 passed`（仅1条既有Starlette TestClient/httpx弃用提示），前端批次控件`4 passed`、全部前端`145 passed`；全仓Ruff、71个源码文件Mypy、六店`verify --date 2026-08-14 --all-stores`、Vue TypeScript/生产构建及`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 26804、监听PID 44624），健康、首页及`index-Be2IRVdA.js`/`index-ibPsEKc6.css`均HTTP 200且线上线下SHA-256一致。升级前真实断点`scheduled-20260814-c96042b2d728`为1325个目标、1108成功、13待重试、204终态；重启后保持同一批次和既有结果，自动第1轮使待重试13→10、终态204→207，第2轮使10→1、207→216，第3轮最终使待重试归0、终态增至217，并于00:55:45正常`completed`，全程没有人工点击继续、另建批次或重采既有成功/终态链接。本次按既定偏好未执行登录态浏览器验收。
- 自有商品跟卖分区的“自有 Offer 最新状态”和“库存状态”现按同一个“店铺代码 + Offer ID”的当前`OfferCurrent`联合筛选：`not_buyable / buyable / disabled_by_takealot / disabled_by_seller`任一状态都只能与该Offer自己的当前总库存组合，A店Disabled与B店Buyable且有货不会再跨店拼成命中；真正竞品库存筛选保持原PLID公开库存口径。正式库本次只读投影共919个自有PLID，旧跨Offer组合会让`disabled_by_seller + 有货`命中171个PLID，而同一当前Offer实际同时满足的为0个。详情店铺切换、卡片状态徽标、Disabled继续进入`followers_only`跟卖监控以及历史区间价格/库存/趋势口径均未改变。
- 联合筛选专项前端`4 passed`、后端`1 passed`，完整Python`652 passed`、全部前端`136 passed`，全仓Ruff、70个源码文件Mypy、Vue TypeScript与生产构建通过。正式ERP经`restart_erp.ps1`重启（启动器PID 39788、监听PID 29952），健康、首页及`index-BVR0_ZFR.js`/`index-Br_Zg7qC.css`均HTTP 200且线上线下SHA-256一致；同一09:00批次`scheduled-20260814-c96042b2d728`、运行修订5在重启后从尝试388/结果386推进到389/387，失败1和终态1不变，确认原断点继续。
- 店铺级最新状态专项与当前完整回归均通过：正式只读样例`PLID99418745`精确返回YeboShop=`buyable`、VeldBox=`disabled_by_takealot`、VoltTech ZA=`disabled_by_seller`；完整Python`652 passed`、全部前端`136 passed`，全仓Ruff、70个源码文件Mypy、Vue TypeScript/生产构建通过。正式ERP经`restart_erp.ps1`重启（启动器PID 2580、监听PID 34836），健康、首页及`index-BfY_tyRq.js`/`index-Br_Zg7qC.css`均HTTP 200且线上线下SHA-256一致。09:00批次保持`scheduled-20260814-c96042b2d728`、1318个目标、运行修订5，重启后尝试331→332、结果328→329，失败2和终态1不变，确认同一断点继续推进；本任务未执行登录态浏览器验收、Takealot上游请求或正式业务写入。
- 侧边栏“最近采集 / 最新指标”现已无需手工刷新浏览器：登录页面可见时每15秒读取一次既有本机只读`/api/erp/freshness`，页面从后台恢复可见时立即重读，隐藏页暂停定时请求；短暂服务异常保留最后一次成功值，切换店铺先清除旧店状态且并发迟到响应不会覆盖新店，不再闪回“暂无”或串店。运营日报前端事件订阅已随模块删除。该链路不调用Takealot、不启动采集任务且不写业务数据。专项`4 passed`、全部前端`132 passed`，Vue TypeScript与生产构建通过；全测退出码0并仅出现既有Vite关闭阶段依赖扫描提示，构建仅有既有大包提示。正式ERP经`restart_erp.ps1`最终重启（启动器PID 41160、监听PID 20288），本机/局域网健康、首页及`index-CzYIaC6w.js`/`index-Bce6tZDy.css`均HTTP 200且线上线下SHA-256一致；09:00批次保持同一`scheduled-20260814-c96042b2d728`为running、1316个目标、运行修订5，部署核对期间已尝试60→65、结果59→64、终态仍1且失败仍0，重启后继续处理索引65而未新建批次或归零。本次未执行登录态浏览器验收、Takealot上游请求或正式业务数据写入。
- 侧边栏现有10个模块入口使用稳定 `#module=<模块键>` 真实链接：右键使用浏览器原生“在新标签页中打开”，Ctrl/Cmd/中键也保留默认行为；普通左键仍在当前 Vue 单页内切换并保留竞品雷达 `KeepAlive` 实例。地址栏同步当前模块，刷新或直接打开链接会恢复目标模块；旧 `#module=daily-report` 已失效并回退到当前账号可用模块。锁定模块和直接地址仍按登录账号的功能权限与店铺范围回退，未放宽后端门禁。该导航能力最初的导航/会话/日期定向 `10 passed`、全部前端 `128 passed`、完整 Python `652 passed`（仅1条既有 Starlette TestClient/httpx 弃用提示）；全仓 Ruff、70个源码文件 Mypy、六店数据库校验、Vue TypeScript、独立生产构建和 `git diff --check` 通过，构建仅有既有大包提示。正式 ERP 经 `restart_erp.ps1` 重启（启动器 PID 32240、监听 PID 29992），本机/局域网健康、首页及 `index-C7rV6ZZI.js`/`index-DBLjq_Kw.css` 均 HTTP 200 且线上线下 SHA-256 一致；09:00批次保持同一 `scheduled-20260813-1f8369cd93d0` 为 running，重启前后未新建批次或从头重置。本次未执行登录态浏览器验收、Takealot/模型/Seller Portal 上游请求或正式业务数据写入。
- 竞品雷达的跟卖状态现统一为互斥的“现在被跟卖 / 曾经被跟卖 / 未发现跟卖”，真正竞品、自有商品和个人监控池共用同一判定：所选区间末次快照仍有非自有报价才算“现在”，区间内有发现日期但末次已无才算“曾经”，两者都没有才算“未发现”，旧“有被跟卖”入口已移除。自有链接详情同时删除重复的“SELLER API REFRESH / 自有 Offer 最新完整刷新”逐Offer面板；Seller API手动/定时完整刷新、当前值、不可变历史及卖家连续对比台中的自有报价均保留。个人池与页面契约`12 passed`、全部前端`125 passed`、跟卖历史定向后端`1 passed`、当前完整Python`652 passed`（仅1条既有Starlette TestClient/httpx弃用提示），Vue TypeScript/生产构建及`git diff --check`通过，构建仅有既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 19916、监听PID 5944），本机/局域网健康、首页及`index-DRc4Emq2.js`/`index-DTUfKiSk.css`均HTTP 200且线上线下SHA-256一致；09:00批次保持同一`scheduled-20260813-1f8369cd93d0`运行，重启后结果89→90、当前索引85→86，未新建批次或从头重跑。本次未执行登录态浏览器验收、Takealot上游请求或正式业务数据写入。
- 搜索定位主标题评分已升级为`evidence-title-v2`纯标题质量分：总分只由当前标题相对冻结商品/查询证据的五项匹配构成——图文身份一致性25、商品类型表达20、平台已验证搜索词覆盖25、有依据的卖点与事实表达20、标题可读性与重复10。自然排名、首页同类占比、竞争商品数、价格、库存、广告位和平台扩展顺序全部退出总分，仅保留为独立搜索表现证据；回归锁定同一标题与冻结证据下任意改变自然位和首页相关性都不会改变总分或分项，删减标题自身支持词才会降分。历史`evidence-title-v1`记录读取时仅用原分项本地换算，不调用模型、Takealot或写库并显示兼容标记；用户本轮附件中的旧49分样例投影为78分“稳健”，原自然位2/25和首页相关性2.1/15不再进入明细。定向评分`2 passed`、搜索定位后端`137 passed`、完整Python`649 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、页面契约`24 passed`、全部前端`119 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/生产构建和`git diff --check`通过，构建仅有既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 10804、监听PID 12028），本机/局域网健康与首页均HTTP 200，线上`index-BvVjtGBI.js`/`index-aBjeLbMa.css`和本地SHA-256一致，未登录搜索定位接口401；09:00竞品批次保持同一`scheduled-20260813-1f8369cd93d0`运行，尝试44→47、结果48→51、失败仍0且未从头重置或人工启停。本次没有调用模型、Takealot搜索/补全或Seller Portal，没有重写历史评分或正式业务数据，也未执行登录态浏览器验收。
- 已修复管理员手动停止当日09:00自动批次后无法从服务端断点继续的问题。根因是停止状态只持久化了“当日不再自动启动”，页面却只有浏览器本地人工断点继续入口；ERP重启后共享批次内存状态还会回到空闲，导致“开始采集”可能重新读取清单并创建人工新批。现在服务端从自动批次日志投影完整停止态，新增仅`kxx`可调用的显式继续接口：复用原`batch_id`、统计、结果和冻结目标索引，按原顺序重建失败及未执行队列，跳过本批成功和确认失效项，且不改写已处理触发日；运行修订号同时隔离停止前迟到的当前链接结果。管理员页面显示专用“继续 09:00 自动批次（N）”，存在该断点时隐藏新开始、浏览器本地继续和接管入口，函数层也拒绝绕过。正式现存断点`scheduled-20260812-1b90ddf4cf9d`只读验证为停止态：1279个目标、已检查1232、成功1038、待重试4、确认失效195、失败及未执行合计51；恢复队列最前索引为94、1126、1127、1128、1232……，说明保留原冻结顺序而非从头装载。定向后端`13 passed`、完整Python`648 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、断点控件`4 passed`、全部前端`119 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/独立生产构建及`git diff --check`通过，前端全测结束时一次既有Vite关闭阶段依赖扫描提示不影响退出码，独立构建随后通过且仅有既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 39420、监听PID 36144），本机/局域网健康与首页均HTTP 200，线上`index-BwADRp7M.js`/`index-VVQzBmtK.css`和本地SHA-256一致；重启前后断点文件时间、长度及SHA-256完全未变，仍停止且可续51条、`last_resumed_at`为空。为保留运营控制，本次未实际点击继续、未执行登录态浏览器验收、Takealot上游请求或正式业务写入。
- 搜索定位页首“图文融合搜索定位”已完成紧凑化：原九条等权长列表改为主模型→跨厂商备用服务链，以及扫描边界、探索预算、请求节流三张卡片；南非市场/南非英语/本地客户语境、视觉事实仅来自图片和普通查看只读本地继续常驻。平台扩展入选口径与六级词根来源收进默认折叠的“词根与平台扩展规则”，原始返回不直接入选、最多4个后续词组根词、标题根词最低覆盖、第4级依赖真实搜索结果页和地域语境非实测搜索量仍明确保留。页面契约`23 passed`、全部前端`116 passed`；全测退出码0，结束时仅一次既有Vite关闭阶段依赖扫描提示，独立Vue TypeScript/生产构建随后通过且仅有既有大包提示。源码与生产包均命中新结构，五组旧标题为0。正式ERP经`restart_erp.ps1`重启（启动器PID 41132、监听PID 12156），本机/局域网健康、首页和`index-Ci94DRzu.js`/`index-C7sEwSDz.css`均HTTP 200且线上线下SHA-256一致。09:00批次保持`scheduled-20260812-1b90ddf4cf9d`运行，尝试752→762、结果675→685，失败1/终止81未变化且未人工启停。`git diff --check`通过；本次未运行Python回归、登录态浏览器验收、模型或Takealot请求、Seller Portal和正式业务写入。
- 已修复PLID102110267明明生成`sofa`词根及相关平台扩展、最终搜索词却只剩`chair → chairs`的问题。正式MySQL只读核对最新完成分析ID45：根词实际包含`corduroy lazy sofa`、`sofa chair`、`lazy sofa chair`、`corduroy lazy sofa chair`和`sofa`；平台已把`corduroy lazy sofa chair`、`sofa chairs`、`sofas`及`sofa bed`判为同品相关。缺失发生在二次适配评分：模型排除项`rigid frame sofa`的尾词`sofa`不在第一条窄形态`floor chair`中，旧逻辑便错误把所有含`sofa`候选清零；这与相关性闸门和模型其他正向类型`folding sofa bed`互相矛盾。现在排除项尾词会与全部模型支持商品类型和直接别名核对：完整`rigid frame sofa`仍拒绝，但正向身份已支持时不再封杀`sofas`、`sofa chair`、`lazy sofa`或`sofa bed`，`sofa cover(s)`等配件改指向继续阻断。以分析ID45已保存模型结构和平台扩展快照离线复演，最终平台候选已包含`corduroy lazy sofa chair`、`sofa chairs`和`sofa bed`路径；没有访问Takealot。新增直接评分与完整候选选择2项回归，搜索定位专项`136 passed`、完整Python`647 passed`（仅1条既有Starlette TestClient/httpx弃用提示）；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`及`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 18864、监听PID 35308），监听进程晚于本次服务源码，本机/局域网健康和首页均HTTP 200，线上`index-SRkDvF94.js`/`index-Bipjozzd.css`与本地SHA-256一致，未登录根词扩展库401。09:00竞品批次保持同一`scheduled-20260812-1b90ddf4cf9d`运行，重启前后尝试715→721、结果639→645，失败1/终止80未变化且未人工启停。本次未重新分析该商品族、未调用模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据；旧分析仍保留，运营需显式重新分析后才产生新搜索结果。

- 关键词流量页已删除“已选变化/基线节点”独立效果汇总卡，不再展示观察进度、30天浏览量变更前后值、净变化、百分比以及前后趋势斜率/速度变化。3/7/14/30天窗口选择、所选节点驱动的趋势图前后区间、节点竖线与高亮、标题关键词时间线和后端比较数据继续保留；采集、计算、接口与数据库均未改变。关键词页面契约`3 passed`、折线交互`6 passed`、全部前端`115 passed`，Vue TypeScript、独立生产构建、源码/生产包残留核对和`git diff --check`通过；全测退出码0，结束时仅有一次既有Vite关闭阶段依赖扫描提示，独立构建随后通过。正式ERP经`restart_erp.ps1`重启（启动器PID 42552、监听PID 38968），本机/局域网健康、首页和`index-SRkDvF94.js`/`index-Bipjozzd.css`均HTTP 200且线上线下SHA-256一致。09:00批次保持原`scheduled-20260812-1b90ddf4cf9d`并从662次尝试/587条结果继续到665/590，失败1和终止79未变化，未从头重跑或人工启停。未运行Python回归、登录态浏览器验收、上游请求或正式业务写入。

- 已修复搜索定位打开旧分析时的整页错误`Cannot read properties of undefined (reading 'trim')`。根因是新版词根/词组汇总直接读取`root_expansion_checks[].root.trim()`，而旧分析只保存`seed`或更早的`input_state/shopper_root`，并非后台任务失败。现在统一通过历史兼容器按`root → seed → input_state → shopper_root`读取，非字符串/数字、空白和全部缺失均安全处理；汇总、词根标签、词组判断、后续根词说明和扩展列表键均不再直接访问缺失`root`。旧记录四类回归与页面契约共`26 passed`，全部前端`114 passed`，Vue TypeScript和独立生产构建通过；全测退出码0，结束时仅有既有Vite测试服务器关闭提示，独立构建仅有既有大包体积提示。正式ERP经`restart_erp.ps1`重启（启动器PID 33432、监听PID 36320），本机和`192.168.110.180:8501`健康/首页均HTTP 200；线上`index-Bu6Xr1VX.js`/`index-dN1mjF5S.css`与本地SHA-256一致，正式JS包含安全历史占位，未登录根词扩展库401。09:00竞品批次保持同一`scheduled-20260812-1b90ddf4cf9d`运行，重启前后尝试461→463、结果430→432，失败1和终止30均未变化，未从头重跑且未人工启停。本次为前端历史读取兼容修复，未重新运行Python回归，未调用模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据，未执行登录态浏览器验收。

- 搜索定位现已把“词根必须是单词”的限制改为单词或2至5词商品词组：模型生成的`compressed sofa`、`lazy sofa`、`floor chair`保持完整，主标题确定性拆词优先产出已有同品/别名及含商品身份的2至4词短语，再保留少量单词发现入口。初始最多观察14个核心根词和1个相邻需求根词；平台返回的多词扩展只有先通过商品上下文闸门，才可在总计20个根词观察内选最多4个继续扩展一层。平台原始第1至5项仍全量保序审计，但不会盲选：核心项须保持同品身份，相邻项须命中结构化买家任务中的替代商品族并从相邻需求根词进入；`corduroy jacket`、`lazy susan`、`foldable table`、`modular psu`、`floor lamp`和`sofa cover`回归均为不相关，不发搜索页请求、不评分且不进入蓝海。逐商品详情新增根词/词组、相关/不相关理由及是否继续作词组根词的筛选审计；全店共享区明确改名为“平台原始词根/词组扩展库”，原始返回不代表当前商品入选。历史分析中的根词扩展检查允许只有`seed`、`input_state`或`shopper_root`而没有新版`root`；前端已按兼容顺序读取，全部缺失也只显示安全占位，不再因`.trim()`读取空字段导致整页错误。搜索定位后端`134 passed`、完整Python`645 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、页面契约`22 passed`、全部前端`110 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/生产构建及`git diff --check`通过，构建仅有既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 6972、监听PID 19876），监听进程启动时间晚于本次后端文件；本机与`192.168.110.180:8501`健康/首页均HTTP 200，线上`index-C5oIpUFb.js`/`index-DCksB2_n.css`与本地SHA-256一致且生产JS包含逐商品筛选、不会盲选、原始库和词组继续扩展文案，未登录根词扩展库401。09:00竞品批次保持同一`scheduled-20260812-1b90ddf4cf9d`运行，重启前后尝试数410→429、结果393→408，未从头重跑且本次未人工启停。未调用真实模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据，未执行登录态浏览器验收；既有分析属于历史证据，须重新运行该商品才会产生本版筛选结果。

- 搜索定位的每张搜索词卡已删除两类重复长说明：候选生成理由，以及“完整第一页自然商品中……”开始的S/A命中词、受控别名、策略判定和判级边界段落。卡片仍保留语义等级、首页S/A/C+I结果、S级同品占比、首页S级命中数、来源、扫描状态、页/行/列与跨页自然排名、供给规模和采集时间；未请求搜索页时继续明确显示“未验证”。后端完整证据、S/A/C+I判级、同类率、候选筛选、搜索请求和排名解析均未改变。页面契约`22 passed`、全部前端`110 passed`，Vue TypeScript与生产构建通过，构建为`index-C5oIpUFb.js`/`index-DCksB2_n.css`，仅有既有大包体积提示。正式ERP经`restart_erp.ps1`重启（启动器PID 20916、监听PID 3644），本机与局域网健康/首页均HTTP 200，线上资源与本地SHA-256一致，正式JS中三段被删除文案命中数为0，未登录搜索定位状态接口401。本次未运行Python回归，未执行登录态浏览器验收，未调用模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据。

- 已修复共享类型库自有商品点击“查看商品详情”后页面无弹层、滚动条消失的问题。根因是卡片对象来自个人池PLID小投影，而详情选择器只查询主列表投影：打开标记已为真并锁住`body`，但选中对象为空使弹层无法渲染。现在详情选择覆盖主列表与个人池小投影的合并集合，整页刷新也保留合并集合内的当前选择；滚动锁只在商品对象存在且详情实际可显示，或其他真实弹层打开时启用，个人类型库弹层也纳入同一锁状态，不再直接覆写。专项`6 passed`、全部前端`110 passed`，Vue TypeScript/生产构建及`git diff --check`通过；构建仅有既有大包体积提示。正式ERP经`restart_erp.ps1`重启（启动器PID 32084、监听PID 12872），本机与`192.168.110.180:8501`健康和首页均HTTP 200，线上`index-DacHoz03.js`/`index-DaSh8b9p.css`与本地SHA-256一致，正式包保留“共享类型库/查看商品详情”入口。同期上一项红框长说明删除仍在正式包生效，观察区间与重算控件保留。本次为前端状态修复，未运行Python回归，未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 竞品雷达主列表已删除筛选区与自有/真正竞品商品列表之间的整段计算方法说明；商品/店铺、库存、跟卖、经营信号、排序、北京时间观察区间、起止日期和“按区间重算”均保留，重算、筛选、排序、库存观察和权限逻辑未改。页面契约专项`5 passed`、全部前端`108 passed`，Vue TypeScript/生产构建及`git diff --check`通过；构建仅有既有大包体积提示。正式ERP经`restart_erp.ps1`重启（启动器PID 16128、监听PID 39568），本机与`192.168.110.180:8501`健康和首页均HTTP 200，线上`index-CWp8gBdj.js`/`index-DYbVcEjg.css`与本地SHA-256一致；正式JS不含被删除说明的首尾文案，仍包含“观察区间（北京时间）”和“按区间重算”。本次为前端展示精简，未运行Python回归，未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 个人监控池的小投影和从池内卡片打开的商品详情现固定使用当前账号全部可访问、启用且已接入的店铺，不再受顶栏“当前查看店铺”缩小；详情中的卖家连续对比、报价历史、自有销量及近30天浏览量也保持这一账号全店范围，而从下方普通自有商品工作区打开的详情仍遵循顶栏当前/全部/运营店铺范围。该规则取代下方旧版按当前单店提示切换范围的说明。共享自有商品与账号授权店铺无交集时明确显示“无权查看店铺详情”，并隐藏店铺名称、图片、商品名、价格、库存及Seller API私有详情；有权但当前Offer未返回时显示“店铺详情暂不可用”，来源未知的共享PLID显示“商品详情暂不可用”，三者均不再误报“等待首次采集”。“新增链接默认归类”同时列出本人创建的库和共享给当前账号且权限为`edit`的库，`read`只读库仍不可选；撤销分享、降为只读或删除库会让引用它的默认设置立即失效并要求重新选择。正式库只读核对`PLID102576284`当前存在于`current`与`store-05`的Offer，相关接收账号均有对应店铺授权，确认原卡片是顶栏范围误缩小而非真实无权。个人池后端`6 passed`、前端`10 passed`、区间投影`5 passed`，完整Python`632 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、全部前端`108 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/生产构建及`git diff --check`通过。全部前端测试结束时出现一次既有Vite依赖扫描关闭提示但退出码为0，独立生产构建通过。正式ERP经`restart_erp.ps1`重启（启动器PID 29928、监听PID 36988），本机与`192.168.110.180:8501`健康、首页均HTTP 200，线上`index-CEsg9Vm9.js`/`index-C392gayN.css`与本地SHA-256一致，未登录个人池接口401；09:00批次保持`scheduled-20260812-1b90ddf4cf9d`运行，本次未手工启停。未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 竞品区间销售与补货件数现会跳过所有不可用观察点，而不是让任一坏点拖垮整段：先按卖家、SKU、变体和条件库存范围分组，再过滤缺失与非精确库存；每个范围至少两个精确点才参与，单点范围直接忽略，各范围内部连接剩余精确点并分别累计净下降/净上升，范围之间不相减。过滤后只要至少一组仍有可比点就汇总结果，整个区间没有任何同范围精确点对时才保持数据不足；发生库存变化但后点价格缺失时件数保留、金额为空。商品级周期指标、详情卖家报价、真正竞品、自有商品和个人监控池共用该口径。正式库只读对比`2026-07-23..2026-08-12`的363个真正竞品，较上一版严格规则新增147个可计算结果；`PLID99746466`共有23个快照，其中3个非精确点、3个可比较范围和2个单点范围，跳过后得到售出19件、补货3件、销售观察额R 14,601、补货货值R 2,097、周转R 16,698。原缺失样本`PLID90036965`继续得到售出9件、补货4件。后端区间算法`5 passed`、竞品存储集成`14 passed`、完整Python`636 passed`（仅1条既有Starlette TestClient/httpx弃用提示），详情报价专项`12 passed`、全部前端`108 passed`；目标Ruff/Mypy、Vue TypeScript/生产构建及`git diff --check`通过。前端全测关闭测试服务器时仍出现一次既有Vite依赖扫描提示但退出码为0，独立构建仅有既有大包体积提示。正式ERP经`restart_erp.ps1`重启（启动器PID 20824、监听PID 13420），本机与`192.168.110.180:8501`健康、首页均HTTP 200，线上`index-C3s2BwPm.js`/`index-DYbVcEjg.css`与本地SHA-256一致且生产JS包含新跳过说明。本次只读正式库复现未写业务数据，未请求Takealot上游，未执行登录态浏览器验收。

- 搜索定位已把词根来源、S/A/C+I语义关系和蓝海资格拆成三层。当前六级来源按人工确认商品事实→图文融合模型预测→主标题确定性拆词→搜索结果页反向学习→相邻需求模型→图题交叉验证排序，但来源不再直接决定等级：S表示同品名称/直接别名或首页同品多数；当前官方标题连续出现且与已验证商品主体一致的`lazy sofa`等短语即使被融合模型遗漏，也会确定性补入S术语集合、首页同品率和直接竞品计数。A必须有结构化买家任务和替代商品族，并由第一页标题满足A级结果至少3个、A级占比至少50%、S+A支持占比至少70%；配件、碎片页和无关结果合并为C/I。蓝海作为第二闸门只允许S/A，且必须有Takealot完整根词扩展、事实安全、目标自然位不晚于72、首页扣除目标后直接同品不超过2个；因此`lazy sofa`可成为“S级蓝海”，不必错误判A级，`compressed sofa`未出现在当前标题且没有人工确认时仍被高风险事实门槛阻断。当前第一页证据只含自然结果标题，不含逐商品图片和类目，边界案例保守归C/I；旧分析无新语义证据时隐藏旧蓝海结论并要求重跑。专项后端`122 passed`、完整Python`631 passed`、页面契约`20 passed`、全部前端`106 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/生产构建及`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 36492、监听PID 9612），本机与`192.168.110.180:8501`健康/首页均HTTP 200，线上`index-wrvg6Hb5.js`/`index-tHq26kf7.css`和本地SHA-256一致，未登录根词扩展库401。Python仅1条既有Starlette TestClient/httpx弃用提示；本次未调用真实模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据，未执行登录态浏览器验收。

- 已建立与 Takealot 搜索定位隔离的 Google 搜索框南非区域补全证据库。采集固定为无账号 Cookie 的 `client=chrome&hl=en&gl=za`，用全新会话分别请求 `google.com/complete/search` 与 `google.co.za/complete/search`；两个入口的有序建议必须完全一致才会更新 `google_search_autocomplete_current`，每次成功、失败或不一致均写入不可变的 `google_search_autocomplete_captures` 并保留原始载荷、请求 URL、入口和契约版本。首个词根`camping tent`曾成功写入采集1和15条有序建议，后因用户确认单根补全不满足“完整全部热词”目标，已按精确ID删除该可信记录及采集审计；正式库当前`google_search_autocomplete_current=0`、`google_search_autocomplete_captures=0`，代码与空表结构暂时保留。专项`9 passed`、完整 Python`615 passed`，全仓 Ruff、69个源码文件 Mypy、六店`verify --date 2026-08-12 --all-stores`通过。正式 ERP 经`restart_erp.ps1`重启（启动器PID 34544、监听PID 20828），本机/局域网健康和首页均HTTP 200，线上`index-BshGo3wN.js`/`index-BXNYaSbP.css`与本地SHA-256一致。该库当前不接入搜索定位评分、标题建议或自动调度；补全顺序不是搜索量、全国热度、销量或排名保证。当前证据使用 ZA 区域参数和南非域名，但未由南非出口 IP 复核，因此不得扩大表述为“南非境内所有用户的绝对结果”。

- 搜索定位完整词根现按用户最终确认的六级来源排序：1人工确认商品事实、2图文融合模型预测、3主标题确定性拆词、4搜索结果页反向学习、5相邻需求模型词根、6图题交叉验证词。人工事实继续与模型种子分离；同一根词保存全部来源和原始短语，并由最高优先来源取得主归属，重复搜索词也使用同一规则。初始核心根词按第1、2、3、6类的相对顺序装入，当前官方标题最多8个有效根词仍保留最低覆盖；第4类必须先有真实搜索结果页才能运行，一旦形成合格学习种子，条件式补救先用结果页反向学习，再回退既有次优根词扩展；第5类保留1个相邻需求机会根词。两个模型阶段的全部生成文本字段现强制回传南非市场、南非英语和南非本地客户习惯三项结构化标记，任一缺失或错误即拒绝该模型输出；提示版本升级后旧地域契约缓存不再复用。该约束固定预测语境但不冒充实测搜索量或客户调查。搜索定位后端`131 passed`、完整Python`642 passed`（仅1条既有Starlette TestClient/httpx弃用提示）、页面契约`21 passed`、全部前端`108 passed`；全仓Ruff、69个源码文件Mypy、六店`verify --date 2026-08-12 --all-stores`、Vue TypeScript/生产构建及`git diff --check`通过。全部前端测试退出码为0，结束时仅有一次既有Vite依赖扫描关闭提示，独立生产构建通过且仅保留既有大包提示。正式ERP经`restart_erp.ps1`重启（启动器PID 13068、监听PID 26772），本机与`192.168.110.180:8501`健康和首页均HTTP 200，线上`index-C3s2BwPm.js`/`index-DYbVcEjg.css`与本地SHA-256一致，未登录根词扩展库401；09:00竞品批次保持`scheduled-20260812-1b90ddf4cf9d`运行，活动索引由重启前321继续推进到327，未从头重跑。本次未调用真实模型、Takealot根词扩展/搜索或Seller Portal，未写正式业务数据，未执行登录态浏览器验收。

- 竞品雷达已消除“补货”和“库存数量不变”的商品级冲突。所选北京时间区间内周期补货量大于0即进入“补货”，周期销售件数大于0继续进入“库存减少”；“库存数量不变”现在只允许周期售出件数与补货件数都完整、两项均为0且库存可比的商品命中。只要发生过补货或下降，即使期初与期末库存相等、或卡内某个报价自身数量不变，也不会再把整件商品归为不变；只有两项周期动作字段都缺失的旧数据，才在没有补货/减少信号时回退首尾或报价不变。正式库只读重算`2026-08-01..2026-08-12`共1272张真正竞品/自有商品卡，旧前端会把其中46张周期补货量大于0的商品同时归入不变；例如`PLID101174888`售出3件、补货3件、首尾净变化0，`PLID95949886`售出0件、补货3件且某报价不变，均已由新规则排除“不变”并归入“补货”。定向经营信号/个人池`18 passed`、全部前端`106 passed`、既有后端区间库存算法`3 passed`，Vue TypeScript/生产构建和`git diff --check`通过；全量前端测试退出码为0，测试服务器关闭时仍出现一次瞬时Vite依赖扫描提示，独立生产构建随后通过。正式ERP经`restart_erp.ps1`重启（启动器PID 14028、监听PID 38976），本机/局域网健康和首页均HTTP 200，线上`index-B5u0xojE.js`/`index-DG0hUl6-.css`与本地SHA-256一致。本次只读正式库复现未写业务数据，未运行完整Python回归，未执行登录态浏览器验收或Takealot上游请求。

- 竞品雷达已统一“库存减少”的筛选与排序口径：所选北京时间区间内周期销售件数大于0即进入该信号，再按“区间内售出件数”降序/升序排列；中途补货不会用期末净库存把已观察到的下降段抵消。正式库只读复现中，电视支架`PLID98301520`区间售出50件、补货73件、首尾库存增加23件，旧逻辑因只看首尾净流出把它排除；红光商品`PLID93069837`售出25件、首尾净流出1件，故旧页面只留下红光。修复后50件会排在25件前。卖家连续对比台同时新增“区间内补货件数”，与售出件数并列，分别累计同报价相邻精确库存的上升段和下降段；中间缺失、非精确或报价范围变化时两项都显示数据不足。定向前端`30 passed`、全部前端`103 passed`、既有后端区间库存算法`3 passed`；全测后Vue TypeScript/生产构建及`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 37160、监听PID 20624），本机/局域网健康和首页均HTTP 200，线上`index-DhIaBK4E.js`/`index-DG0hUl6-.css`与本地SHA-256一致且线上包包含两项区间件数文案。全部前端测试退出码为0，测试服务器关闭时出现一次瞬时Vite依赖扫描提示，独立生产构建随后通过。本次为前端口径和展示修复，未运行完整Python回归，未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 搜索定位的新分析已由“逐字补全路径”切换为“完整根词扩展”。服务器从当前标题确定性提取最多8个有效完整根词，再补充模型单词根；`Corduroy Lazy Sofa Chair - Foldable Multi-Functional Seat for Home - Navy Blue`会得到`corduroy / lazy / sofa / chair / foldable / seat`，所以`lazy`不再因模型遗漏而缺席。模型多词种子只归一到完整根词，不再请求`lazy s`、`rgb l`等中间输入。新证据渠道为`takealot_root_expansion`，旧`takealot_autocomplete_path`只作历史兼容；共享库新增`/api/erp/search-ranking/root-expansion-library`，按根词分别保留平台第1至5项，隐藏历史残缺状态，查看仍只读本地MySQL。根词扩展未改变核心与蓝海门槛：`lazy`被查不代表平台会返回`lazy sofa`，更不代表自动形成蓝海；相邻需求仍保存模型原始机会短语用于事实安全核对，并要求目标实际命中、前72自然位及首页扣己后直接同类不超过2个。定向搜索定位后端`104 passed`、页面契约`20 passed`、完整Python`604 passed`、全部前端`101 passed`；Ruff、68个源码文件Mypy、六店数据库校验、Vue TypeScript/生产构建及`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 14396、监听PID 40420），本机/局域网健康与首页均HTTP 200，线上`index-Bx6MgKpn.js`/`index-SxRVP2WU.css`与本地SHA-256一致，新根词扩展库未登录返回401。Python仅1条既有Starlette TestClient/httpx弃用提示；全部前端测试退出码为0，测试服务器关闭时出现一次瞬时Vite依赖扫描提示，独立生产构建随后通过。本次未调用真实模型、Takealot根词扩展/搜索、Seller Portal或正式业务写入，未执行登录态浏览器验收。

- 竞品雷达已隐藏顶栏重复的“数据范围 / 按自然月 / 开始—截止 / 本月”控件，页面仍以自己的北京时间观察区间和“按区间重算”为唯一周期入口，其他模块的全局范围控件保持不变。个人监控池的自有卡若在当前单店范围未返回详情，不再显示“等待首次采集”或要求重新粘贴链接，而会明确标注当前店铺权限范围不可见、提示切换上方全部已授权店铺，并说明个人监控关系和类型库归类仍保留；全部已授权范围仍无详情时提示核对权限或当前Offer数据，不泄露无权店铺名称和私有详情。日期视口专项`4 passed`、个人池专项`9 passed`、全部前端`101 passed`；全部测试后独立Vue TypeScript/生产构建和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 33208、监听PID 23384），本机/局域网健康和首页均HTTP 200，线上`index-Bx6MgKpn.js`/`index-SxRVP2WU.css`与本地SHA-256一致，线上包包含权限提示且全局日期控件条件排除竞品页。全测出现一次测试服务器关闭时的瞬时Vite依赖扫描提示但退出码为0；本次为前端展示调整，未运行Python回归，未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 竞品雷达“库存减少”现按所选北京时间区间内累计的周期销售件数排序，页面排序说明显示“区间内售出件数”；真正竞品、自有链接和顶部个人监控池共用该口径。“补货”和“库存减少且评论增加”仍按周期销售额，“库存变化大”仍按库存周转金额，缺失件数继续排在有值记录之后。列表排序专项`3 passed`、个人池专项`8 passed`、个人池区间指标专项`3 passed`、全部前端`100 passed`、既有后端区间库存算法`3 passed`；Vue TypeScript/生产构建在全部测试后独立复跑和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 33208、监听PID 32300），本机/局域网健康和首页均HTTP 200，线上`index-BdQPKRoS.js`/`index-Xpn4cf-o.css`与本地SHA-256一致，线上包包含“区间内售出件数”。未执行登录态浏览器验收、Takealot上游请求或正式业务写入。

- 竞品雷达卖家连续对比台的价格/精确库存/PLID共用评论三联图已移除重复曲线光晕和图框内阴影，保留高对比主线、缺失区间虚线桥接、固定时间点详情、鼠标/键盘查点及响应式无横向滚动。图表上方新增随当前卖家/报价切换的“区间内售出件数”：只在同报价来源时间线中按相邻同范围精确库存的下降分段累计，补货不抵扣，补货后的再次下降继续累计；有效点不足、中间报价缺失、非精确库存或SKU/卖家/变体/条件变化时显示“数据不足”，不会展示部分件数，并明确是库存观察而非Takealot实际订单。竞品报价历史专项`12 passed`、折线交互专项`6 passed`、全部前端`100 passed`、既有后端区间库存算法`3 passed`；Vue TypeScript/生产构建在测试后的独立复跑和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 16120、监听PID 35020），本机/局域网健康和首页均HTTP 200，线上`index-CUfZT9vO.js`/`index-Xpn4cf-o.css`与本地SHA-256一致，线上包包含新指标及数据不足文案且不含曲线光晕选择器或图框内阴影。未执行登录态浏览器验收、上游请求或正式业务写入。

- 搜索定位已重构为三段清晰证据链：隔离图片观察阶段完全不接收标题；服务器单独计算图题交叉验证，差异大只提示；图文融合阶段同时使用可信主图和当前Seller Offers主标题，Schema强制至少6个第一直觉词/词组及6个南非本地习惯完整搜索词。完整缓存绑定主图、规范化标题、模型链和提示版本，标题变化不再复用旧融合词。融合模型明确指出关键事实缺口时，批次把该商品记为待人工并跳过全部Takealot请求，后续商品继续；每个已分析商品仍有可选人工事实入口，不强制运营逐件确认。单商品预算扩为最多14词（6个融合直生成、6个平台根词扩展、1个相邻需求、条件式1个补救），根词扩展输入上限16。页面新增独立差异筛选、现标题分数筛选、2.5秒进度同步、暂停/恢复/停止/从队首新批次重启；标题评分按图文一致性、平台验证词覆盖、自然搜索可见性、首页同类相关性、结构可读性和事实证据六项逐条给依据，缺失证据从分母排除并显示覆盖率。完整Python`604 passed`、全部前端`101 passed`、Ruff、Mypy、六店数据库校验、Vue TypeScript/生产构建及正式ERP重启健康检查通过；未刷新六店最新费用/时长预估，也未启动真实批次、模型或Takealot请求。

- 搜索定位现对每个合格自有在售商品固定显示“决策参数人工确认”，不再由投影幕布专用规则或多模态模型自动决定。服务器从当前Seller Offers标题确定性提取功率、电压、电流、容量、尺寸、重量、数量/套装、分辨率及IP/IK防护等级等明确规格；运营逐项选择“是 决策参数”或“不是 保持后置”，无规格也可确认。完整判断按店铺、PLID和标题追加不可变审计，记录确认人和时间；标题变化后旧判断只留档并失效，每标题最多3项为决策参数。保存只写本地MySQL，不调用模型、Takealot或Seller Portal；确认“是”后仍须点击重新验证定位，系统用一个同商品族查询位实测完整搜索页，只有相关性通过且参数不冲突才允许前置，其余规格继续后置。投影幕布`100 Inch`仅保留系统重点核对提示，`300W`、`IP66`也可由运营判断，但没有人工确认和实时页面证据都不会前置。人工参数词等量替换5个核心槽中的平台补全位，3个南非模型长尾、1个相邻需求探针和条件式第10词预算不变。搜索定位专项`98 passed`、前端契约`19 passed`、完整Python`592 passed`、全部前端`92 passed`（Python仅1条既有Starlette TestClient/httpx弃用提示）；Ruff、68个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验及`git diff --check`通过。正式ERP最终经`restart_erp.ps1`重启（启动器PID 41464、监听PID 30172），本机/局域网健康、首页及`index-CrtgHcEU.js`/`index-Dgn8k9tb.css`均HTTP 200且线上线下SHA-256一致；正式MySQL新审计表存在且为0行，没有伪造运营判断，新GET/POST未登录均401。本次未调用模型、Takealot补全/搜索、Seller Portal或外部倒搜，未写正式决策参数或商品事实，也未执行浏览器自动化。

- 搜索定位现支持经过费用快照确认的动态多店一键批次，并把补全、第一页和游标公开请求节流延长为3至5秒。批次范围始终来自当前账号可访问、已启用且已接入的店铺，不写死六店；预览只读本地MySQL，按单商品资格、既有图片缓存、同店同批主图复用和本机历史usage计算商品数、Token、人民币与时长，64位快照不一致时拒绝启动。整批与单商品共用全局锁、最大并发固定为1；资格变化只跳过，供应商/网络错误在当前项后暂停，失败商品不自动重试，ERP重启不自动续跑。进度逐项检查点保存到Git忽略的`logs/search-ranking-batch.json`，页面可暂停、确认继续或停止；倒搜保持下线。本次正式库只读预览为6店、1587条当前Offer、541条合格，5条已有识别缓存、53条预计同批同图复用、483次预计新识别，历史中位约3329 Token/次，合计约160.79万Token；豆包主模型基准约¥1.78、常见约¥1.78–2.71、历史保守上界约¥5.60，3至5秒节流本身约14.1小时，含模型/网络建议预留约16.2–31.3小时。批次专项`4 passed`、带认证路由专项`1 passed`、搜索定位前端契约`18 passed`、全部前端`91 passed`、完整Python`588 passed`；Ruff、68个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP已重启为监听PID 39520，本机/局域网健康、首页及`index-BUDNh88x.js`/`index-dn6slDll.css`均HTTP 200且线上线下SHA-256一致，未登录批次预览401；没有生成正式搜索定位批次状态文件。本次只实现并验证入口，没有启动正式批次，没有模型Token、Takealot补全/搜索或正式业务写入，也未执行登录态浏览器验收。

- 搜索定位已经下线全部外部倒搜操作链路，只保留“人工确认商品事实”：前端不再显示倒搜入口或费用，HTTP倒搜路由不存在，后端倒搜客户端、协议、提示词、证据解析和费用配置均已删除。人工确认仍绑定最新分析、当前标题和可信主图，确认事实按店铺与PLID留痕，换主图暂停应用；历史非人工事实不删除数据库，但不会再进入当前档案、图片身份核对、候选词或标题建议。同期完善投影幕布规则：`projection/projector + screen/cloth/curtain`按受控双词商品族核对同类率，单独通用词不计同类；`100 Inch`只有在当前Seller Offers标题明确、商品身份匹配且参数查询通过真实页面验证后才可适度前置，冲突尺寸被拦截，普通自由文本人工事实不能直接提供参数值，`300W`、`IP66`等普通规格继续后置。参数商品仍使用最多10词的自适应预算，以4个核心补全路径加1个标题确认参数精准词替换普通5个补全位。搜索定位/ERP Web专项`139 passed`、完整Python`584 passed`、搜索定位前端契约`18 passed`、全部前端`91 passed`；Ruff、67个源码文件Mypy、Vue TypeScript、生产构建、六店数据库校验和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启为监听PID 20792，本机/局域网健康、首页及`index-BV4wdnfj.js`/`index-RTTwmDl2.css`均HTTP 200且线上线下SHA-256一致；正式包只保留人工确认文案，不含倒搜操作或旧路由字符串。本次未调用模型、外部图片搜索、Takealot补全或搜索页，也未修改Seller Portal、正式商品事实或历史分析。

- 搜索定位词卡已把“未采用”和扫描执行阶段拆开：零页请求显示“未进入扫描范围”，只完成第一页相关性筛选且未进入后续定位显示“仅完成首页筛选 未进入后续定位”，已找到本商品但未达推荐门槛仍保留具体页/行/列及跨页自然排名，并说明“已定位，但该搜索词未进入推荐词”；旧“已拦截”已移除。搜索定位页面契约`20 passed`、全部前端`93 passed`，Vue TypeScript/生产构建和`git diff --check`通过；本次前端状态表达没有重复运行上一轮已通过的完整Python`576 passed`。正式ERP经启动器PID 21892重启为监听PID 31504，本机/局域网健康、首页及`index-C8VkUtIa.js`/`index-OiDAQJ6X.css`均HTTP 200，在线资源SHA-256与本地一致，未登录搜索定位状态接口401。同一09:00批次从1081次尝试/899条结果续至1082/900，失败索引保持0。本次未调用模型、倒搜、Takealot补全/搜索、Seller Portal或正式业务写入。

- 搜索定位三种标题打法现共用确定性的最终规格后置层：商品类型和相关证据词在前、功能卖点居中，服务器从原始标题完整提取功率、电压、电流、容量、尺寸、重量、数量/套装、数值分辨率及IP/IK防护等级后按原顺序追加到末尾。截图商品会生成`Flood Lights Outdoor RGB LED Floodlight Smart App Control Waterproof 300W IP66`，不再把`300W`夹在类目词中；`220 V`等分离写法及同一数字用于不同规格组时保持完整，`S24`、`iPhone 15`、`5G`等身份词不误移。搜索定位专项`87 passed`、完整Python`576 passed`、全部前端`92 passed`，Ruff、Mypy、Vue TypeScript/生产构建和六店数据库校验通过。正式ERP已由启动器PID 34056重启为监听PID 33836，本机/局域网健康、首页及`index-ByNF6Pvy.js`/`index-C_rBhcG1.css`均HTTP 200；同一09:00批次从876次尝试/780条结果续至888/787且失败索引为0。本次无模型Token、倒搜、Takealot补全/搜索、Seller Portal或正式业务数据写入。

- 六店总销售额趋势已改为“已有店铺部分合计”：某业务日只要至少一家可见店铺存在`ordered_revenue`，接口就返回已有店铺金额之和并绘制点位；缺失店铺仍不补0，顶部摘要、无障碍标题和跟随指针的详情浮层同时披露覆盖/总店数、缺失店数和来源待核验状态，只有所有店铺都没有金额时才保留空值断点。正式库只读投影确认7月29日R88,722、7月31日R87,829、8月1日R112,466，三天均为5/6店、缺失`store-06 / AGG AUTOMAN (PTY) LTD`且`data_status=pending`。完整Python`571 passed`（1条既有第三方弃用提示）、全部前端`91 passed`，Ruff、67个源码文件Mypy、六店数据库校验、折线专项、Vue TypeScript/生产构建通过。正式ERP经`restart_erp.ps1`重启（启动器PID 31108、监听PID 24804），本机与`192.168.110.180:8501`健康均为`ok/takealot-erp`，首页及`index-BwE5bQbW.js`/`index-D4zgaX10.css`均HTTP 200且在线SHA-256与本地一致，范围接口未登录均401。本次未调用Takealot、未修改正式销售数据、未执行登录态浏览器验收。

- 搜索定位已修复`PLID101182825 / 300W Outdoor RGB LED Floodlight`暴露出的三项同源问题。正式库只读审计确认分析11的两家模型原始识别均为`RGB LED Flood Light with Remote`、原始置信度0.95，但旧整段商品名词元比例把标题中的`floodlight`与模型的`flood light`分开，得分0.3333后错误切备用并压到0.49，故两个南非模型直生成词都以`model_low_confidence`保存、`pages_scanned=0`，Takealot搜索页从未被请求；页面却把内部占位分数0显示成“首页同类占比0%”。现在标题交叉认证优先使用模型内部一致的商品主体/受控形态短语，有限归一`floodlight/flood light`、`backlight/back light`等闭合/开放复合写法，通用`light/bar`仍要求完整尾部双词，`RGB/outdoor`等修饰词不能单独放行；同一归一同时用于完整第一页标题同类率，因此竞品标题采用`Floodlight`也会命中`flood light`。未发起搜索页的历史/新候选统一显示“未验证”，不再把占位0冒充实测0%。分析12、13的两次人工倒搜均在约120秒网络阶段超时，`tool_call_count=0`且无已知usage/费用，并非返回了“0个匹配”；千问Responses图搜图现改用官方流式响应等待完整`response.completed`，仍严格保持一次人工确认一个上游调用、不自动重试。搜索定位专项`81 passed`、完整Python`570 passed`、全部前端`91 passed`（Python仅1条既有第三方弃用提示）；Ruff、搜索定位Mypy、Vue TypeScript/生产构建和`git diff --check`通过。部署期间本任务已重启通过，随后共享工作区另一项总销售额改动再次构建并重启；最终正式ERP为监听PID 24804，本机/局域网健康均为`ok/takealot-erp`，最终首页及`index-BwE5bQbW.js`/`index-D4zgaX10.css`均HTTP 200、在线SHA-256与本地一致，且最终JS静态确认包含本次“未验证”、受控`floodlight/flood light`和倒搜流式文案；未登录搜索定位与补全库接口均401。部署与验证未调用真实模型、倒搜、Takealot搜索或Seller Portal，也未修改历史分析；运营需对该商品再点击一次“重新验证定位”才会按新规则取得真实搜索页同类率与位置。

- 竞品雷达类目入口已从仅支持`/all?custom=...`扩展为同时支持Takealot SEO类目路径：路径每段必须是安全英文短横线格式，末段必须明确以1至12位数字类目ID结尾，例如`/camping-outdoor/family-tents-27895`；普通页面和无数字ID路径继续在任何外部请求前拒绝。规范化会保留原类目路径，剥离片段及非身份查询参数后只叠加受支持排序和价格筛选；浏览器仍捕获页面实际发出的产品列表请求与平台`after`游标，不猜造类目API参数。用户给出的三种帐篷类目路径已进入前后端回归；后端专项`15 passed`、来源前端专项`7 passed`、完整Python`566 passed`、全部前端`90 passed`（Python仅1条既有第三方弃用提示），Ruff、67个源码文件Mypy、Vue TypeScript/生产构建通过。正式ERP经启动器PID 31540重启，健康、首页和`index-CDLcX78h.js`/`index-ClqHAAZ-.css`均HTTP 200，在线资源与本地SHA-256一致；既有批次`scheduled-20260811-904ecda8db94`从504次尝试/435条结果续至505/436，失败索引仍为0，确认同一断点继续运行。本次未发起真实类目预览/确认、正式业务写入或登录态浏览器验收。

- 竞品雷达已增加按北京时间开始/截止日期重算的周期库存观察金额。真正竞品按相邻快照、同一SKU/卖家/变体范围的精确库存，自有商品按同一店铺Offer的完整刷新点，逐段累计“库存下降量×后点价格”为周期销售额；补货段另计补货量和补货货值。“库存减少”当前按周期销售件数（页面显示“区间内售出件数”）排序，“补货”和“库存减少且评论增加”按周期销售额排序，“库存变化大”按“周期销售额+补货货值”的库存周转金额排序。范围改变、非精确库存、有效点不足时不展示部分合计，变化点缺价只保留件数；页面明确这些是库存观察金额而非平台真实订单收入。个人池“按区间重算”复用同一次后端区间投影，真正竞品和自有商品都会更新。整页刷新另新增当前账号个人池/可见共享库PLID小投影，仍按当前/全部店铺授权过滤，并在返回前显示“正在恢复”而不再误报首次采集；正式库只读实测`kxx`可见18个个人池PLID，小投影0.097秒恢复3个已有快照商品，完整1209个商品投影为6.405秒。正式库当前真正竞品312条中152条可计算周期销售额、70条库存周转金额大于0，最高周期销售额R73,596；自有商品897条中896条可计算、311条周转金额大于0，最高周期销售额R21,248。完整Python`561 passed`（1条既有第三方弃用提示）、当前最新工作区全部前端`90 passed`，Ruff、67个源码文件Mypy、六店数据库校验、Vue TypeScript/生产构建和定向接口/算法契约通过。正式ERP经`restart_erp.ps1`重启（启动器PID 11164、监听PID 13428），本机/局域网健康、首页及最终`index-vCyK28UM.js`/`index-ClqHAAZ-.css`均HTTP 200且线上线下SHA-256一致，新个人池小投影和完整竞品接口未登录均401。09:00批次保持`scheduled-20260811-904ecda8db94`，重启后从已尝试416条/结果384条/终止32条推进到418条/385条/33条，失败索引0，确认同一断点继续运行。本次只读验收未调用Takealot、未执行登录态浏览器自动化或正式业务写入。

- 顶栏店铺数据控件已由单个“数据截止日期”更换为全局“开始日期—截止日期”视口：默认当前自然月月初至今天，上月/下月按完整自然月切换，本月不会越过今天，也允许直接修改任意历史范围。商品、关键词、经营坐标、竞品等只认单日截止的既有页面继续使用区间截止日期；经营总览的单店下单、周期末流量和六店总销售额读取整个区间；保留的运营日报后端流程仍使用独立10:00业务周期日期。六店收入接口已移除单店`tail(30)`和汇总层二次30日截断，显式范围会同时读取对应日期的Sales来源状态与修订。正式库只读投影确认`2026-08-01..2026-08-11`返回10个已结束日期，8月10日为R88,498、6/6店、6店来源已验证且4店共4条修订；`2026-07-01..2026-07-31`可返回31个日期，证明长于30日的视口不再被截短。完整Python`558 passed`（1条既有第三方弃用提示）、全部前端`87 passed`，Ruff、67个源码文件Mypy、六店数据库校验、日期视口/折线专项、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP经`restart_erp.ps1`最终重启（启动器PID 33132、监听PID 33004），本机与`192.168.110.180:8501`健康均为`ok/takealot-erp`，首页及`index-DcNVQyjf.js`/`index-DqMuHa1Q.css`均HTTP 200且在线SHA-256与本地一致，新范围接口未登录均401。本次页面/接口部署未再次调用Takealot、未执行登录态浏览器验收或正式业务写入。

- 搜索定位已把“事实是否支持图片身份”从任意词元比例改为商品主体词/受控别名规则：`floor sofa`与`compressed sofa`可由`sofa`确认同一商品家族，`floor sofa`与`floor lamp`不会再因`floor`误通过；`bar`、`stand`、`light`等通用主体还要求相同尾部双词类型。压缩、真空包装等隐藏事实仍只来自人工或倒搜证据，不会被说成图片直接识别，也不会替换图片模型首个可见物理形态；某条查询明确包含当前主图已确认的另一产品类型时，才仅对该查询使用最具体的确认类型核算完整第一页。查询方案同步升级为自适应最多10词：前9个基础位目标为5个不同直觉词根的Takealot真实补全路径、3个南非本地模型直生成长尾和1个相邻需求探针；完整第一页通过的不同平台核心词根少于3个时才执行第10个补救词，优先偏差页同形态标题学习后的真实补全，否则用未验证的次优补全，达到3个即省略。最多12个补全输入状态会优先保留5个核心词根的最短有意义输入和完整词根，并为条件式偏差页学习留1个状态。搜索定位后端专项`78 passed`、完整Python`558 passed`（仅1条既有第三方弃用提示）、全部前端`87 passed`；Ruff、搜索定位Mypy、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP已加载本次后端与当前首页引用的JS/CSS；本机/局域网首页和资源均HTTP 200且在线资源与本地SHA-256一致，搜索定位状态接口未登录为401。本次未执行真实多模态、倒搜、Takealot补全/搜索、Seller Portal或正式业务写入。

- 个人监控池新增关系现已由后端按当前登录账号的动态店铺授权硬拦截，覆盖直接加入、真正竞品/共享库卡片、单商品链接以及店铺/类目冻结候选确认。PLID若只属于账号无权查看的已接入店铺，接口返回不泄露店铺名称或私有详情的明确403，并在任何个人关系、类型库、全局目标、操作记录、审计和队列写入前整体拒绝；同一PLID横跨有权与无权店铺时允许加入但只投影有权店铺。整批命中禁止项时原冻结令牌不失效，可直接减少最终数量重试，无需重新扫描。专项`4 passed`、相关后端`45 passed`、完整Python`551 passed`、全部前端`79 passed`（Python仅1条既有第三方弃用提示）；Ruff、67个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 3112、监听PID 32060），本机/局域网健康、首页及`index-CoDHxBJj.js`/`index-CC2EGvpo.css`均HTTP 200且在线资源与本地SHA-256一致，相关接口未登录均401。09:00批次保持`scheduled-20260811-904ecda8db94`并在重启后从已尝试267条/结果265条推进到268条/266条，失败索引0、既有确认失效终止项2条。本次未用正式登录账号执行个人池或队列业务写入，未触发真实店铺/类目扫描、Takealot上游请求或浏览器自动化。

- 店铺/类目来源现把第一次预览得到的平衡名次融合、PLID去重候选一次性冻结在当前账号约20分钟有效的服务端令牌中，单次最多1000个。候选超过20个后可直接在现有预览修改最终总数并重新确认，只从冻结队列截取，不再访问Takealot或重新扫描；不超过20个仍强制全部纳入，超过冻结容量的确认会由前后端共同拒绝而不会静默少加。每次批量确认必须显式选择当前账号自己创建的个人类型库，默认库和共享库都不能替代；全部入选PLID都会加入个人池并归入该库，但只有全新或重新激活的真正竞品写入全局目标并追加活动批次，已有目标和自有商品只更新个人关系/归类而不重复入队。确认操作头保存所选库ID与名称快照，升级前旧记录保留空值。顶部个人监控池同时新增真正竞品/自有商品跟卖来源标签，以及商品/卖家、竞品店铺、库存、跟卖、类型库、经营信号、信号方向、每页数量和北京时间区间整套选择器；新增后定位会自动切来源并清除冲突筛选。店铺/类目专项后端`12 passed`，完整Python`549 passed`（仅1条既有第三方弃用提示），全部前端`79 passed`；Ruff、67个源码文件Mypy、Vue TypeScript/生产构建和六店数据库校验通过。正式ERP最终经`restart_erp.ps1`重启（启动器PID 32784、监听PID 20376），健康、本机与`192.168.110.180:8501`首页及`index-CoDHxBJj.js`/`index-CC2EGvpo.css`均HTTP 200，在线SHA-256与本地一致；目标、个人池、操作记录、预览与确认接口未登录均401。正式库已增加两个可空操作库快照列；现有1条升级前操作记录两列保持NULL，没有伪造归属。09:00批次保持同一`scheduled-20260811-904ecda8db94`运行，整个部署序列已尝试152→155、成功结果150→153、失败索引0，另有2条既有确认失效终止项；最终重启后从第154项/152条结果继续推进至第155项/153条结果，确认同一断点实际续跑。本次未执行真实店铺/类目预览或确认、Takealot上游请求、登录态浏览器验收或正式业务写入。

- 竞品雷达价格、精确库存和PLID共用评论三联图的共用详情框已恢复为图表上方固定展示：默认显示最新时间点，鼠标横向移动或左右方向键只切换北京时间、价格、库存证据和评论数，详情框不再跟随指针移动，也不会在移出图表时消失。经营总览六店总销售额、店铺滚动流量和关键词滚动流量仍保留跟随指针、靠边避让和移出隐藏的既有浮层；没有详情弹框的其他图表未新增弹框，响应式无横向滚动、缺失值断点和虚线桥接口径均未改变。折线交互专项`6 passed`、全部前端`80 passed`、Vue TypeScript/生产构建和`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 22728、监听PID 1412），本机/局域网健康均为`ok/takealot-erp`，首页及`index-7ys7_Of5.js`/`index-D5zBa0bg.css`均HTTP 200且在线SHA-256与本地一致，未登录竞品接口为401。09:00批次保持同一`scheduled-20260811-904ecda8db94`，重启后从已尝试301条/结果299条至少推进到303条/301条，失败索引0、既有终止项2条。本次为前端交互调整，未运行Python回归，未执行登录态浏览器验收、Takealot上游请求或数据库业务写入。

- 搜索定位的一键链路现已把“当前主图已确认商品类型事实”加入图片识别后的本地身份核对，但事实、主标题、SKU和PLID仍不会发给多模态模型。若图片身份与主标题偏差较大、同一主图绑定的有效商品类型事实支持图片判断，页面明确进入“事实化解冲突并继续公开验证”分支；若事实缺失或不支持，则继续跨厂商备用核对，仍冲突便降为低置信并停止平台搜索。默认单商品查询由原4个扩为最多8个：目标覆盖4个尽量来自不同第一直觉词根的Takealot真实补全路径词、3个由模型按南非英语购买习惯直接提出的已知长尾词、1个相邻需求补全探针；最多读取12个补全输入状态并按词根轮转，同词若具备两类来源会双重标明。所有候选仍须逐个读取完整第一页，最多36个自然`product_views`，排除显式`sponsored/promoted`后，以图片识别首个窄物理形态词做小写、标点和简单单复数归一的标题词元全集匹配，默认命中率至少60%才是核心词；这不是逐商品多模态分类，同义词省略会漏计、形态词过宽会误计。补全、第一页及游标请求在ERP实例内串行，默认相邻至少3秒并追加0至2秒随机抖动；搜索端点403、429或5xx不自动重试。匿名上下文不带Seller Portal、ERP或用户Chrome登录Cookie，但Takealot仍可看到服务器出口IP、TLS/浏览器特征及匿名站点Cookie，因此只能降低风险，不能称为零风险。三种标题打法随证据渠道调整：连续版优先已验证的已知长尾或最明确核心词，类目覆盖版只组合真实补全并优先不同直觉根，蓝海版维持真实补全、目标命中、前72自然位、首页扣己后标题词元直接同类不超过2个的门槛；所有建议仍待同词改后复采，不承诺前移。部署收口时搜索定位后端专项`75 passed`（含`floor sofa`图片身份与`compressed sofa`当前主图人工事实的专用回归）、前端页面契约`16 passed`，Ruff及搜索定位/公开客户端Mypy通过，Vue TypeScript与生产构建通过。正式ERP当前监听PID `37100`的启动时间晚于本模块后端文件，首页已加载`index-B_3VO66r.js`/`index-CC2EGvpo.css`；健康、首页和两项资源均HTTP 200，线上资源SHA-256与本地构建一致，未登录搜索定位列表、补全库及分析POST均为401。部署验收未访问真实Takealot、未调用多模态或倒搜、未执行Seller Portal/数据库业务写入；工作区其他模块仍有各自并行改动与非本模块测试状态，本条不据此宣称全仓干净或全量回归通过。

- 六店总销售额已完成周期末失败后的正式修订与来源审计。2026-08-11前一周期`pre_close`为5店失败、仅`store-06`成功；旧折线只看指标行存在，曾把失败前旧值误画成正常绿线。现在每次Sales成功都独立重拉最近30个SAST日并直接重建历史指标，Offers失败仍不冒充完整日报成功；改变值会写不可覆盖的前后金额/件数/差额/前后来源/运行ID/范围/时间审计和文件日志，无变化也更新来源验证。正式备份校验后六店真实重拉`2026-07-13..2026-08-11`全部成功，8月10日`store-02/03/04/05`发生4条修订，六店总额R37,344→R88,498、件数27→58；最新折线点为已结束的8月10日、R88,498、6/6店、蓝色修订状态，进行中的8月11日日内R1,995不进入折线。正式ERP最终由启动器PID 23924、监听PID 11808运行，健康、首页、现有JS/CSS与审计接口401边界通过。销售额专项、Ruff、67个源码文件Mypy和折线契约5项通过；本任务主改完成时全部前端73项通过，随后并行个人池改动使最终工作区前端为`72 passed, 1 failed`（无扩展名导入），最终全量Python为`545 passed, 2 failed`，其中Windows启动器时序项单独复跑通过，剩余1项及当前Vue全构建阻断均来自并行竞品`library_id`改动后旧调用/测试少传参数，均与本销售额改动无关。

- 店铺/类目来源继续保留“价格：从高到低”和“价格：从低到高”两个排序选项，但两者现在互斥：页面勾选任一方向会自动取消另一方向，粘贴链接自带的受支持价格排序也按同一规则替换相反方向；若绕过页面同时提交两项，后端会在任何Takealot请求前拒绝。评分最高与最新上架的默认双选、平衡名次融合、价格区间、最终去重数量、20个阈值和操作记录口径均未改变。店铺/类目后端专项及管理员门禁`11 passed`、来源前端专项`6 passed`，完整Python回归`542 passed`、全部前端`73 passed`（Python仅1条既有弃用提示）；Ruff、67个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP经`restart_erp.ps1`加载`index-BlZfuu4y.js`/`index-DJbynhRl.css`，健康、首页及资源均HTTP 200且在线SHA-256与本地一致，未登录预览和操作记录接口均为401。09:00批次保持同一`scheduled-20260811-904ecda8db94`并在重启后从已尝试94条/成功92条推进至105条/103条，失败索引0、另有2条既有确认失效终止项；本次未执行真实店铺/类目预览确认、Takealot上游请求、数据库业务写入或登录态浏览器验收。

- 个人监控池类型库现支持由创建者向任意系统用户逐人分享，并分别授予“只读”或“可编辑”。页面把“我的监控池 / 我的类型库 / 共享给我”分区展示；只有创建者能重命名、删除、设置默认和管理分享，只读接收者只能查看，可编辑接收者可把自己个人池中的卡片加入共享库并移除库内卡片。共享本身不会把商品加入接收者个人池，也不会改变全局清单、活动批次、每日采集或历史；共享只传递库信息与PLID，卡片详情继续按接收者原有店铺授权投影，无权Seller API私有数据不显示。撤销分享后库从接收者页面消失但其个人关系不变；库内增删后服务端数量与前端角标立即同步，最后一张卡移除后无需刷新即显示0。分享与既有个人池专项`4 passed`，完整Python回归`542 passed`、全部前端`73 passed`（Python仅1条既有第三方弃用提示）；修改后端源码Ruff/Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP经`restart_erp.ps1`加载`index-BlZfuu4y.js`/`index-DJbynhRl.css`，健康、首页及资源均HTTP 200且在线SHA-256与本地一致，分享候选与分享写接口未登录均为401；正式库新增分享表且保持0条，没有伪造分享关系，原有1个类型库和10条库内归类保持不变。09:00批次保持同一`scheduled-20260811-904ecda8db94`运行，重启前后活动索引106→108、成功结果104→106，另有2条既有确认失效终止项；本次未人工启停批次、未执行登录态浏览器验收、上游采集或正式业务写入。

- 搜索定位的商品事实缺口现在提供两条人工路径：运营可直接输入并确认短英文商品事实，也可先查看原因、范围、证据门槛和费用后再确认使用千问`image_search`倒搜。两条路径都绑定最新分析、当前标题和可信主图；事实按店铺与PLID保存来源、确认账号、时间、分析标题、主图及证据，撤销只改状态并保留历史，主图变化时旧事实留档但暂停应用。人工或跨站倒搜确认事实只补充图片模型看不出的候选，例如`compressed sofa`；仍须通过当时真实Takealot补全、完整第一页相关性、目标自然位和标题事实安全门槛，系统不会直接据此改标题或承诺排名前移。平台补全已建立全店共享有序库：输入首次实际命中才请求并保存，24小时内共享复用，严格超过24小时后只在下一次真实命中时刷新一次，不做定时刷新；刷新失败不让过期顺序进入当前标题。库页面只读MySQL，显示补全文案、最佳/平均补全顺序、输入状态及采集时间；顺序是意图参考而非公开搜索量，系统命中次数也不是买家搜索次数。正式库已从既有成功分析本地幂等回填19个输入状态、32条原时点快照和56个补全文案，其中11个已超过24小时并等待下次实际命中刷新；商品事实档案仍为0条，没有伪造人工确认。搜索定位专项`69 passed`、完整Python回归`538 passed`（1条既有弃用提示）、全部前端`70 passed`；Ruff、67个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP经`restart_erp.ps1`加载`index-D5SpHHh-.js`/`index-BP-NXlNg.css`，健康、首页及资源HTTP 200且在线SHA-256与本地一致，新接口未登录为401。本次未执行真实倒搜、模型调用、Takealot搜索定位、Seller Portal写入或登录态浏览器验收。

- 店铺/类目来源的最终多排序合并已从简单轮流取名次升级为可解释的平衡名次融合，页面默认同时选择“评分最高”和“最新上架”，链接自带的受支持`sort`只补入而不会覆盖默认双选。两榜都选择时先按是否同时进入两榜候选排序，再比较两榜较差名次与名次和；相关度、价格等额外排序只作后续覆盖数、较差名次、名次和、倒数名次分数和稳定顺序决胜。预览及确认明细显示每个PLID的实际榜单名次，最后按PLID去重并截取人工最终总数，因此优先“两边都靠前”而不是某一边极端靠前。店铺和类目入口还新增各自独立的管理员折叠操作记录：预览不留痕，确认时与目标/个人池变更同事务写入筛选条件、规则和全部有序商品；操作头10条/页，展开后链接明细20条/页，完整显示URL、PLID、标题、排序名次及新增、重新启用、已有或自有结果。正式库两张新表已建立但均为0行，没有伪造历史或执行真实候选确认。店铺/类目后端专项与管理员门禁共`11 passed`，来源、操作记录和店铺选择前端专项分别`5/2/5 passed`；完整Python回归`537 passed`、全部前端`70 passed`（Python仅1条既有弃用提示），Ruff、67个源码文件Mypy、Vue TypeScript/生产构建和六店数据库校验通过。正式ERP已重启并最终加载`index-D5SpHHh-.js`/`index-BP-NXlNg.css`，健康、首页和资源均HTTP 200，在线资源与本地构建SHA-256一致，新接口未登录为401。09:00批次保持`scheduled-20260811-904ecda8db94`并从重启前已尝试47条/成功46条续至核对时68条/67条，失败索引0、另有1条既有确认失效终止项；未执行登录态浏览器验收、真实店铺/类目预览确认或上游人工调用。

- “真正竞品”最新状态已增加独立“竞品店铺”搜索选择器。用户可输入数字`sellers`、公开报价中的`M`前缀ID或店铺名，也可从当前对比报价自动汇总的选项中选择；同一标准化`sellers` ID跨PLID只保留一个选项并显示唯一商品数，同名不同ID保持分开。店铺名可能改名或重名，因此只作搜索/显示，报价缺失ID时才回退为名称身份。筛选覆盖商品卡的全部当前报价，但只处理已读取的真正竞品列表，不改个人池、全局清单、数据库或采集批次。专项`5 passed`、全部前端`66 passed`，Vue TypeScript和生产构建通过；正式ERP未重启即提供`index-ukzH3_SA.js`/`index-oU3pelvg.css`，健康、首页及资源均HTTP 200，在线JS与本地构建SHA-256一致。09:00批次`scheduled-20260811-904ecda8db94`保持运行，核对时已尝试32条、成功31条、失败0条。本次未执行Python回归、数据库业务写入、上游采集或浏览器自动化。

- 竞品雷达店铺/类目筛选的数字输入异常已修复。根因是Vue数字输入把最低价`800`和最终数量`10`写成number，旧辅助函数却按string直接调用`.trim()`，因此在请求预览前报`d.trim is not a function`。可空整数现在集中接受number/string/null/undefined并统一转换后校验，最高价空白仍表示不限；20个阈值、多排序PLID去重及人工确认规则均未改变。用户原场景已加入回归测试：来源专项`5 passed`、全部前端`61 passed`，Vue TypeScript与生产构建通过。正式ERP无需后端重启即已加载`index-BGZGv6EB.js`/`index-oU3pelvg.css`，健康、首页和两项资源均HTTP 200；`scheduled-20260811-904ecda8db94`继续运行且核对时为已尝试5条、成功4条、失败0条。本次未执行真实店铺预览、候选确认、数据库写入、Python回归或浏览器自动化。

- 搜索定位已增加“商品事实倒搜”补证层，但普通一键识别和重新验证绝不会自动触发。系统先展示建议开启或暂不建议的具体原因、只发送可信主图 URL 的范围、证据门槛和费用快照；拥有 `search_ranking.run` 权限的运营还必须打开确认框，明确确认外部调用与预计费用。后端随后再次核对最新成功分析 ID、原因码、当前主标题和主图，前端绕过、旧页面以及标题或主图变化都会在调用前被拒绝。确认后固定使用千问 Responses `image_search`，一次确认最多一个上游请求且不自动重试；返回链接仅作为同图线索。事实词需置信度至少 0.70，并原词命中两个不同标题和两个独立站点域名，同站子域名不重复计数；弱事实和倒搜排除词不会进入搜索或标题。通过证据会重新执行原 Takealot 补全、类目页、自然位和三标题流程，仍不自动修改 Seller Portal 或保证前移。相同主图、模型和提示版本可缓存，但每次应用仍须重新人工确认并记录确认人、原因、时间、用量和费用。本次搜索定位专项 `65 passed`、完整 Python 回归 `534 passed`（仅 1 条既有 Starlette TestClient/httpx 弃用提示）、全部前端 `60 passed`；Ruff、67 个源码文件 Mypy、Vue TypeScript、生产构建、六店数据库校验和 `git diff --check` 通过。正式 ERP 已加载 `index-pyE3K6i6.js`/`index-oU3pelvg.css`，健康、首页和资源均为 HTTP 200，未登录搜索定位接口为 401。本次没有执行真实倒搜、模型调用、Takealot 搜索定位或 Seller Portal 写入。原 09:00 竞品批次保持 `scheduled-20260810-098235dcf697` 并在重启后从 1089 次尝试续至 1091 次；其间因 Takealot 网络不可达连续 2 条进入既有 10 分钟暂停，22:52 已按原机制自动恢复且成功结果 904→905、失败索引回落为 3、剩余队列 118，未被本任务人工启停。

- 竞品雷达已把新增来源分为“商品链接、店铺链接、类目链接”三个独立入口。店铺/类目入口支持可选价格上下限和平台排序多选，先预览再确认；该版最初按相同名次轮流合并并按PLID去重，当前已由本节首条的平衡名次融合替代，人工数量仍是最终唯一商品总数。筛选结果不超过20个时全部纳入，超过20个时必须填写1至1000后才签发当前账号专属、约20分钟有效的冻结令牌。确认时在一个事务中区分已有真正竞品、自有店铺PLID和全新/重新激活目标，已有项不重复建链或入队，自有项只进入当前个人池，全新/重新激活项才写审计并追加活动批次队尾；随后继续复用原商品链接的变体、主报价和跟卖采集。店铺/类目URL目前是人工来源扫描条件，不是持续卖家上新或类目历史监测。公开只读契约核对得到示例店铺默认25个、价格100–1000后18个，示例类目同价格和最新上架排序233个、首屏36个并有游标。专项后端`10 passed`、完整Python`530 passed`（1条既有弃用提示）、全部前端`57 passed`，Ruff、67个源码文件Mypy、Vue TypeScript/生产构建和六店数据库校验通过。正式ERP已由启动器PID 41452重启，健康、首页及`index-dVq07Zjx.js`/`index-Ak4afUdc.css`均HTTP 200，未登录新接口为401；原09:00批次`scheduled-20260810-098235dcf697`已尝试1083→1089条、成功结果898→904条、失败仍为2条，确认同一断点恢复推进。本次未执行登录态页面人工验收，未向正式库提交来源候选。

- 个人监控池类型库数量已改为随当前页面的真实成员关系即时同步：后端原本已在删除卡片时清理对应归类，前端现于本地新增、替换或删除卡片后按全部剩余成员的`library_ids`统一重算角标。删除“红光库”最后一张卡后无需刷新即从1变0，空类型库继续保留入口。个人池专项`6 passed`、全部前端`49 passed`，Vue TypeScript及生产构建通过；正式ERP已直接提供`index-hh5GrYOH.js`/`index-3mS5enAz.css`，健康、首页和两项资源均HTTP 200。09:00批次保持`scheduled-20260810-098235dcf697`运行并已到活动索引717；本次为纯前端修复，未重启后端、未执行Python回归、数据库写入、上游采集或浏览器自动化。

- `PLID58371612`并非六次采集都没拿到图片：正式MySQL有19次商品快照，19次`image_url`都保存为Takealot官方`media.takealot.com/covers_tsins/61498554/...`。旧同源缩略图代理只放行`/covers_images/`，因此在图片已入库的情况下仍返回来源校验错误，前端才显示“暂无图片”。代码现对同一两个官方主机额外放行精确`/covers_tsins/`路径，继续拒绝其他主机、其他路径、查询参数、重定向和超限资源；搜索定位模型主图仍只接受`/covers_images/`。真实原图已成功转换为192×192、5571字节JPEG，缩略图专项`19 passed`、本任务相关`78 passed`。修复已由`restart_erp.ps1`正式加载（启动器PID 21872、监听PID 27216），`/api/health=ok/takealot-erp`，首页及`index--BVK0en_.js`/`index-CToVd43m.css`均HTTP 200；原09:00批次`scheduled-20260810-098235dcf697`从活动索引604继续推进至607，未手工启停或执行浏览器自动化。已经打开过详情并缓存过失败状态的页面需刷新一次即可重新请求缩略图。

- 自有商品与真正竞品现统一为“一个PLID一张父卡、卡内切换变体/报价”的详情逻辑：自有Seller API Offer带出TSIN和各自图片，同一PLID跨店重复Offer按TSIN统计为同一变体；切换任一来源的具体变体/报价时，左上主图同步切换为所选项图片。截图中的`PLID102722716`和`PLID102722728`是两个不同PLID，正式只读投影各保留一张卡；每个PLID内部均识别4个唯一TSIN和4张对应图片，不用标题相似度跨PLID误合并。自有链接新增归属检查也改为目标PLID定向查询，命中后自动加入当前账号个人监控池且不再触发全量自有页面重载。个人池现同时容纳真正竞品与自有商品，并增加账号独立的类型库、多库归类、默认新增类型库、“不自动加入任何类型库”以及首次加入前设置；顶部可按全部、未分类和各类型库直接筛选，空库也可见。完整Python回归`519 passed`（仅1条既有Starlette TestClient弃用提示）、前端全部`48 passed`，Ruff、66个源码文件Mypy、Vue TypeScript、本功能提交对应生产构建、六店数据库`verify`和`git diff --check`通过。正式ERP已由启动器PID 21872重启为监听PID 27216，健康、首页及`index--BVK0en_.js`/`index-CToVd43m.css`均HTTP 200；4张新增表保持存在、原个人关系10条未改变。原09:00批次`scheduled-20260810-098235dcf697`从活动索引604继续推进至607，未手工启停，未执行浏览器自动化。

- 真正竞品页面已移除“可跟卖机会”选择器、统计说明区、“全部机会 / 全部报价售罄 / 暂无卖家报价”数字、卡片标签及隐藏字段搜索，后端保守机会判定和历史兼容字段继续保留，采集、快照与数据库未改变。商品详情慢的根因也已修复：正式接口原先每次为单个PLID加载全库5680条历史、6598条评论和7601条变体后再过滤，`PLID101163999`实际只需18条历史、1条评论和18条变体，读取约4.45秒；现在SQL直接按PLID限制三类竞品表及自有店铺观察点，冷启动约0.092秒、热态约0.026–0.031秒。前端只在弹窗打开时请求并使用24项有界缓存，观察区间、店铺范围或最新采集版本变化后自动换键。后端定向`2 passed`、完整Python回归`510 passed`（仅1条既有Starlette TestClient弃用提示）、前端全部`44 passed`，Ruff、66个源码文件Mypy和Vue TypeScript/生产构建通过；资源为`index-CEW0YlIp.js`与`index-C53X1MhK.css`。正式ERP经`restart_erp.ps1`重启（启动器PID 32440、监听PID 38860），健康、首页及两项资源均HTTP 200；09:00批次保持原`scheduled-20260810-098235dcf697`，从重启时第270条`PLID95929484`继续推进至第271条`PLID95943943`，已尝试269→270条、成功结果265→266条、待重试仍为2条。本次未手工启停批次或执行浏览器自动化。

- 个人监控池现已成为所有账号竞品雷达标题后的第一块独立商品区，不再通过“查看我的个人监控池”跳到下方自有店铺/真正竞品分区。区域每页6张卡：已有快照展示图片、商品、价格、库存和最近采集，尚无历史也显示PLID与“等待首次采集”；卡片可查看详情、从个人池移除，管理员另有“监控队列操作”。顶部按钮恢复并明确双重动作“加入监控队列和我的监控池”：全新链接同时加入两者，已有全局目标但不在个人池时先补入，已经属于个人池时直接定位；三种路径都自动切换个人池分页、滚动聚焦，并以暖色描边、四轮脉冲和“已定位到此商品”高亮4.5秒。管理员共享管理、全局指标和链接复核紧随个人池，仍在商品经营区之前。个人池专项`3 passed`、全部前端`42 passed`，后端账号隔离与自动加入契约`2 passed`，Vue TypeScript检查和生产构建通过；新资源为`index-DJfwEeRj.js`与`index-DzwcqwT9.css`。正式ERP经`restart_erp.ps1`重启（启动器PID 36776、监听PID 42844），健康、首页和两项资源均HTTP 200；09:00批次保持原`scheduled-20260810-098235dcf697`，从第141条续至第143条，已检查140→142、成功结果136→138、待重试仍为2。本次未执行完整Python回归或浏览器自动化。

- 管理员竞品雷达的“共享采集与管理”仍保持前置优先级，但当前位于独立个人监控池之后、商品经营区之前；其内部继续依次展示管理员核心工作区、全局指标和链接复核，并以橙色边框与暖色背景强化。普通运营不渲染或轮询任何管理员批次模块。该上一版调整的前端全部`40 passed`，Vue TypeScript检查与生产构建通过，资源为`index-DEcSbpd0.js`和`index-BY5gg2Au.css`。正式ERP当时经`restart_erp.ps1`重启（启动器PID 17572、监听PID 23672），健康、首页和两项资源均HTTP 200；09:00批次保持原`scheduled-20260810-098235dcf697`并连续运行。本次最新个人池独立区规则以上一条当前结论为准。

- 竞品雷达已按账号隔离真实任务：所有角色首先看到当前用户名、个人监控池数量、独立池内商品卡和“加入监控队列和我的监控池”；普通运营不渲染或轮询全局链接清单、操作审计、失效复核、批次说明、技术进度、浏览器开关和启停控制，管理员的完整共享管理区、全局指标与链接复核位于个人池之后。个人池继续由`user_id + plid`在后端隔离；全局修改、删除、插队、审计和失效复核也有管理员角色后端门禁，非管理员即使直接请求接口仍为403，只保留新增链接并自动加入自己个人池。`kxx`现可在人工与09:00自动批次中切换“显示检测浏览器”，自动批次设置写入服务端断点、ERP重启后恢复，当前链接不变而下一条生效。此前完整Python回归`508 passed`（1条既有第三方弃用提示），Ruff、66个源码文件Mypy、数据库`verify --date 2026-08-10 --all-stores`及正式部署均已通过；最新个人池前端验证与部署以上方当前结论为准。

- 已定位并修复彭晓桂、梁文轩等只获授权非`current`店铺账号在所有模块整店无图的问题：普通API会发送`X-Store-Code`，但原生`<img>`无法发送该头，旧缩略图URL因而被后端按默认`current`拒绝为403；黄越鸿恰有`current`授权，所以可能正常。前端现把店铺上下文集中管理并在每个同源缩略图URL显式附加真实`store_code`，后端缩略图路由也兼容无参数旧页面。浏览器仍只接收192/640像素JPEG，平台原图由ERP服务器按白名单读取、转换并缓存，未退回客户端直载高清原图；局域网其他电脑和无痕登录会话使用同一链路。六店839个唯一真实来源的192像素服务器侧生成全部成功，逐店640像素抽查全部成功；`store-05`另有3条正式Offer上游没有图片地址且公开PLID详情返回404，继续显示“暂无图片”。完整Python回归`507 passed`、全部前端专项`40 passed`，Ruff、Mypy、Vue生产构建与六店数据库`verify`通过。正式ERP已重启为监听PID 40092，本机及`192.168.110.180:8501`健康、首页、JS/CSS均通过，09:00自动批次从原持久断点恢复运行；未在三位运营人员的物理电脑执行浏览器自动化或人工验收。

- 人工竞品批次的退出登录/会话过期/切换账号边界已加固：旧页面在认证会话变化前同步安全脱离，只取消浏览器等待，不取消服务端已开始的当前链接；脱离后不再请求剩余链接，也不把401/403写成失败。运行中断点保留当前索引与请求编号，原账号`kxx`可恢复，其他账号仍不能接管；认证会话版本隔离还会忽略旧请求迟到的401，避免误踢新登录账号。该机制只保证当前链接继续并安全暂停剩余队列，人工批次没有改成完全后台运行；09:00自动批次仍由服务端独立驱动。生命周期专项`3 passed`，前端九组专项`36 passed`，关键后端边界`2 passed`，完整Python回归`504 passed`（1条既有弃用提示）；Ruff、66个源码文件Mypy、Vue TypeScript/生产构建、六店数据库`verify`及`git diff --check`通过。正式ERP已重启为PID 23316并加载`index-BDctEG8l.js`/`index-DOQno_56.css`，健康、首页和资源均HTTP 200，未登录批次状态返回401。本次未启动真实采集、未访问Takealot上游、未执行数据库业务写入或浏览器自动化。

- Vue ERP 已仅在前端移除旧“风险与质量”模块：侧边导航、页面组件、前端 API/类型和专用样式均已删除，后端风险接口、异常计算、数据库记录与报表口径未修改。2026-08-15新增的“异常商品”是独立只读规则、接口和页面，不恢复旧风险投影或旧前端。原删除任务的前端七组专项共`27 passed`，Vue TypeScript/生产构建、源码与产物残留扫描及`git diff --check`通过；正式ERP当时由PID 10472加载`index-DhnYSZk0.js`与`index-CujdTvT7.css`，健康、首页和两项资源均为HTTP 200。该次删除未执行Python回归、上游采集、数据库业务写入或浏览器自动化。

- Vue ERP 已仅在前端移除 NFT102 报表续写模块：侧边导航、页面组件、前端 API/类型、样式和用户权限展示均已删除，用户权限顺位前移；后端 NFT102 路由、辅助脚本、既有权限数据、输出目录和数据库未修改。前端七组专项共`27 passed`，Vue TypeScript/生产构建及前端残留扫描通过；正式ERP已重启并加载`index-CQKjStJ5.js`与`index-C8kVjLgU.css`，健康、首页和两项资源均为HTTP 200。

- 个人监控池已从原真正竞品专用关系扩展为真正竞品与自有商品两个独立来源关系，仍由账号隔离且同一PLID可同时属于多个账号。新建、重新激活、重复提交或命中自有商品都会自动/幂等加入提交账号；拥有竞品查看权限的账号也可在两类来源详情顶部通过醒目按钮增删。个人删除不停止全局每日采集、不移除活动批次、不改变自有全量巡检、不删历史；升级仍不猜测历史归属。个人池归属与默认库设置仍严格私有；类型库只有创建者和被明确分享的用户可读，管理员也不能凭角色读取未分享库。
- 重复监控链接原先定位下方“竞品最新状态”的行为已被顶部独立个人池替代：提交时先幂等确保当前账号个人池关系，然后自动切换个人池分页并高亮对应卡片；下方筛选、来源分区和观察区间保持不变。4.5秒暖色描边、四轮短脉冲和“已定位到此商品”反馈继续保留，但作用目标已改为顶部个人池卡片。
- “可跟卖机会”仍作为后端保守兼容字段保留，但正式Vue前端不再展示对应选择器、统计、数字、卡片标签或搜索命中。历史2026-08-07只读投影记录仅用于说明旧功能曾经存在，不代表当前页面仍有该入口；删除展示未修改后端判断、历史快照或采集行为。
- 竞品雷达共享“搜索商品”已修复完整链接触发的`Cannot read properties of undefined (reading 'toLocaleLowerCase')`：真正竞品专属机会字段在自有商品记录中缺失，以及历史报价空值或数字字段，都会先转换为安全字符串再匹配；完整链接继续提取PLID，目标不在列表首项时也能逐条扫描。新增专项3项，七组前端专项共27项及Vue TypeScript/生产构建通过；当前ERP已直接提供`index-Bo4stxm8.js`/`index-DjmGZBFn.css`，健康、首页和资源均HTTP 200，未重启后端、未触发采集或写库。
- 六店铺已完成全模块数据接入。官方`GET /v1/seller`确认显示名依次为`VoltTech ZA`、`VeldBox`、`YeboShop`、`T C STORE`、`INDLELA YEMIKHIQIZO PTY LTD`、`AGG AUTOMAN (PTY) LTD`，ERP不再使用“店铺1—6”占位名。忽略提交的`.env`使用`TAKEALOT_STORES`登记非敏感店铺代码/名称/密钥变量名，6个真实Key保持分开且启动时强制去重；原历史归入`current`。商品、销售、退货、指标、异常、质量、物流服务商快照、刷新状态和运营日报表均带`store_code`，SQLAlchemy会话统一自动过滤和写入所选店铺；前端以`X-Store-Code`切换，后端同时校验账号店铺授权和接入状态。手动刷新把所选店铺传给独立进程，四个运营日报计划任务使用`--all-stores`依次执行。首次真实采集六店商品数为410/242/218/209/348/97，每店均已生成销售、指标和手工日报版本；2个管理员账号已设为全部店铺范围。

- 经营总览与竞品雷达的顶栏现按账号提供“我的运营店铺 · N 店”（多店时标为合并），跨店可见范围大于运营范围时另保留“全部店铺 · N 店合并”；两者相同不重复显示。运营范围只取账号明确勾选、可访问、启用且已接入的店铺，全部范围取账号所有可访问已接入店铺，后端不会接受任意店铺ID扩大范围。经营总览切回具体店铺时按顶栏完整开始/截止日期视口读取该店下单与周期末流量折线、经营健康度和商品明细；多店范围不作为`store_code`进入其他单店接口。顶部按可解释信号汇总需关注、数据待补和当前正常店铺数，不生成黑箱健康分；逐店卡按风险优先排列，显示具体缺货/数据缺口、最新日下单件数/金额、近7日下单、在售商品、转化率、周期末近30天浏览量，以及平台可售、在途和收货中库存，并可直接切回该店明细。数据异常仍不在经营总览展示，也不参与健康判断或店铺排序，独立异常商品模块按当前单店查看。负责运营只取明确勾选该店、启用且拥有`store.view`的账号，管理员若明确勾选也可进入名单；全店可见本身不再自动推导运营责任。顶部物流从本地快照把共享W8海外仓只计一次，平台库存按合并范围最新Offer汇总并披露字段覆盖；锁定、已分配出库和各在途/收货阶段不相加成可能重复的总库存。跨店销售合计标明实际覆盖家数，不补0、不强行对齐不同指标日，也不跨店平均转化率中位数。普通查看不访问W8或Takealot上游。
- “全部 N 个店铺”老板盘面的总销售额已增加来源新鲜度和历史修订语义，并按顶栏完整日期视口返回，不再在单店和六店汇总层各截一次最近30日。2026-08-11只读诊断确认前一周期5店`pre_close`失败、仅`store-06`成功；旧接口只检查六店是否都有指标行，没有核对这些行是否已被失败后的新Sales验证，因此会把旧指标误画成完整绿色。现在每次Sales成功都重拉并重建最近30个SAST自然日；Offers失败仍继续尝试Sales修订，但该次运营日报/周期末完整采集保持失败。此前一次性全历史回补为更早人工视口提供本地历史，日常30日修订范围不再限制页面可查看长度。每日来源状态记录Sales运行ID、请求范围和验证时间；金额或件数变化直接更新正式指标，并向不可覆盖的`sales_revenue_revisions`追加前后值、差额、前后数据源和发现时间，同时写入脱敏文件日志。当前尚未结束的SAST日即使已有日内部分值也不进入折线，今天的拉取只先修订昨天及更早日期。折线用绿色表示已验证、橙色表示失败后待补验证、蓝色表示已由后续Sales修订；下方只读审计表默认跟随当前视口并可人工改期回查前后来源。任一店铺缺少正式金额时仍返回空值、保留断点且不补0，普通页面和审计查询均只读本地MySQL。
- Vue ERP 当前折线图已统一提升可读性：经营总览的六店总销售额、店铺滚动流量，关键词流量，以及竞品价格/精确库存/PLID共用评论三联图均使用加粗高对比曲线、清晰网格与坐标、放大命中区、活动点定位环和十字指示线。竞品自有链接选中账号可见店铺的精确Seller API报价时，从同一次`StoreOfferObservation`追加“近30天浏览量”第四组；每次完整Offers刷新会原样保存该滚动值和已拉取状态，四组共用同一北京时间横轴范围、左右绘图区边界和完全相同的Seller观察时间戳，不再把每日快照吸附到最近报价节点。历史只从明确关联同一次Offers采集的日报版本或完全同刻旧快照回填，无法证实的旧点保持缺口。流量场景下卖家导航位于图表上方，四联图独占整行宽度；指标说明栏、纵轴刻度栏和绘图区彼此独立，长标题、刻度与折线不重叠。商品名称相对前一有效标题变化时加粗对应流量节点，流量缺失时保留空心变更节点，详情显示同次刷新时间及变更前后名称。经营总览和关键词流量的既有详情浮层继续跟随鼠标，靠边自动换向或钳制，并在鼠标离开图表后隐藏；竞品三/四联图则不使用曲线或图框阴影，在图表上方固定显示当前报价区间内售出观察件数与共用详情框，鼠标和左右方向键只切换时间点内容。没有详情弹框的其他折线图不新增弹框。图表随容器宽度缩放且不产生横向滚动条。详情按各自口径披露完整日期/北京时间、数值、店铺或商品覆盖、缺失未补0、同日参考来源、库存证据和评论归属；缺失点自身仍为无值并用“×”区分真实0，只有缺口前后都存在真实值时才以虚线桥接，虚线不代表插值、补0或中间走势。

- 六店物流PO已按各店独立Key重新同步并隔离缓存：`VoltTech ZA`、`VeldBox`、`YeboShop`、`T C STORE`、`INDLELA YEMIKHIQIZO PTY LTD`、`AGG AUTOMAN (PTY) LTD`分别取得194/132/166/122/87/11个Shipment，其中193/132/166/122/85/11个带唯一PO，共712个Shipment、709个唯一PO；另外3个Shipment为官方响应本身未返回PO，系统保持缺失。六店均已分别保存Takealot与W8最近成功快照；Takealot工作线程凭据和内存缓存按`store_code`隔离，不再回落到默认店铺或跨店复用PO。

- Seller API 没有独立的店铺总浏览量字段；经营总览已增加“店铺商品近30天浏览量汇总趋势”。后端复用每天次日09:00成功冻结的 `pre_close` 商品观察，按周期末实际采集的北京日期减一天归档，例如8月6日09:00采集显示为8月5日，不直接复用运营日报的 `business_date`。同一归档日选择最新成功版本并汇总接口已返回的 `page_views_30_days`；同日重试不会重复相加。横轴日期按顶栏所选视口返回，明确显示覆盖/缺失商品数，缺失不补0。整次周期末刷新失败时正式值继续保持失败；若该北京时间自然日另有成功的早间、晚间或手动采集，则以最近一次成功值画橙色虚线参考并披露实际时间、来源和覆盖，绝不冒充09:00周期末数据；没有同日成功采集才保持完整断点。每个点仍是滚动30天窗口汇总，不是精确当天流量或独立访客数。
- “关键词流量”已成为零人工建档的正式店铺模块：每日趋势读取 `offer_snapshots.page_views_30_days`，标题关键词档案读取同一快照的官方`title`。实时410条Seller Offers确认没有独立关键词/搜索词字段，`product_label`均为数字商品标签，因此页面明确称“商品标题关键词”，不冒充平台搜索词。选中商品顶部固定双栏持续显示首次上架和最近补货：前者优先读取Offers `created_at`并按南非时间展示，旧记录明确标为“本库最早记录”；后者只记录后一条有效平台库存快照真实增加，显示该快照实际采集的北京时间和增加数量，未观察到增加时显示“暂无记录”。首个有效标题就是基线，标题词新增、移除、词序或写法变化时标记高对比节点；不提供人工建档、补录或修改入口。每个变化并列展示变更前最后有效值到变更后最新有效值的上升/下降、净变化与百分比，以及前后真实日期线性斜率的增强、减弱、反转或稳定；窗口可选3/7/14/30天并排除变更日，尚未结束时显示仍在积累，缺失保持缺失。该对比是节点附近的观察关联，不单独宣称标题变化造成流量变化。
- 正式店铺模块“搜索定位”继续只接受当前授权Seller Offers中`buyable`、正数可售库存、36小时内快照且主标题/数字PLID/官方主图完整的自有Offer，并在模型前和平台搜索前双重重验。多模态现为豆包Seed 2.0 Lite主用、千问`qwen3.7-plus`备用；模型请求只含640像素主图，不含标题或SKU，识别后才在本地用标题交叉核对，明显身份冲突会切备用或降为低置信度。千问字符串候选由适配层规范化，业务Schema仍严格验证。同一主图、模型链和提示版本复用时本轮图片Token/费用为0，标题变化只重做本地核对与排名；当前主图仍一致的人工/跨站倒搜确认事实可补充图片模型无法判断的产品类型，但不绕过平台验证。搜索词分图片精准词、Takealot真实补全词和相邻需求词：核心词要求完整第一页至少60%命中第一条窄物理形态；相邻需求词必须有真实补全、实际找到本商品、自然位不晚于72，并按窄形态标题词核算、首页扣除本商品后直接同类不超过2个才成为蓝海候选；该门槛是标题词证据，不等同于已排除所有同义竞品。通过后沿`after`游标保存页/行/列和跨页自然序位，普通查看仍只读本地MySQL。补全顺序不是搜索量，结果数只是供给规模；标题建议不写平台、不保证前移，只有改后同词复采才显示观察变化。
- 搜索定位图片缓存现在按当前实际启用且按调用顺序排列的提供商/模型链签名，签名不含Key值；增删供应商Key会使模型链变化，避免只启用千问时的旧识别在接入豆包后被错误复用。模型阶段成功后先持久化原始profile、usage和估算费用，再进行Offer重验与公开搜索；后续搜索失败时重试可复用这次已付费识别，最近失败尝试继续显示实际Token/费用，而主详情保留最后一次完整成功结果。同轮跨厂商切换会累计所有已知usage和费用，身份冲突后未采用的有效响应也不漏计；HTTP 200但Schema失败且返回usage时照实记账并标明不可缓存。两家均身份冲突时只缓存原始图片profile，本轮降置信不会永久污染缓存，标题纠正后可零Token恢复核对。缓存复用仍重新执行当前标题身份核对，明显异类时零Token降置信并停止平台搜索。
- 搜索定位结果卡把第几页、第几行、第几列作为主定位信息，跨页自然排名直接解释为自然商品序列中的第几个。第几页取Takealot实际`after`游标页，每个游标页最多纳入36个自然商品，页内从左到右每行四项；跨页自然位只按实际纳入项连续累计，含被排除记录时不承诺固定1–36、37–72区间。广告不是视觉猜测，后端只纳入`sections.products.results`中`type=product_views`且没有显式`sponsored/promoted`标记的记录。每张词卡同时显示来源、补全词根/顺序、共享缓存命中或刷新状态、补全证据采集时间、完整第一页窄形态标题词命中数、供给规模和排名采集时间。最终“未采用”与扫描阶段现已拆开：已定位但未达推荐门槛仍显示具体位置；零页请求显示“未进入扫描范围”；只完成第一页相关性筛选而未继续定位显示“仅完成首页筛选 未进入后续定位”，不再显示笼统的“已拦截”。
- 搜索定位标题建议继续由服务器确定性整理，只输出字母、数字和单个空格，并固定显示三种独立打法：完整连续词组版保留首个通过验证的核心短语完整连续；类目热词覆盖版只组合真实Takealot补全、核心相关性达标且获当前标题或当前主图绑定的已确认商品事实支持的表达，其中“热词”只是搜索意图代理而非公开搜索量；相邻需求蓝海版只在真实补全、目标前72位和按窄形态标题词核算的首页低竞争门槛同时成立时生成，否则明确暂无达标方案。三种打法现在共用同一最终排序：商品类型与相关证据词前置、功能卖点居中，原始标题中的功率、电压、电流、容量、尺寸、重量、数量/套装、分辨率和IP/IK防护等级等明确规格组完整提取后稳定追加到末尾；型号、代际和`5G`等连接身份不按普通数字规格处理。蓝海补全新增词必须来自原始相邻需求词根；未获当前标题或已确认事实支持的数字/单位、材质、颜色、受众、品牌兼容、功效、App、Smart、USB及模型区别属性不得写入。历史实际发出的建议文本保留用于判断是否采用，当前展示仍按新门槛投影；真实标题等于某一策略后，只对该打法证据词做完整同词复采，缺词或未找到一律报告证据不足，其他查询仅作次要观察，仍不保证前移。页面基线显示分析时标题；若当前Offer标题已变化则单独告警并要求重跑，避免把旧建议误解为基于新标题生成。
- 竞品经营信号已由数据库原始状态动态枚举收敛为固定十一类：降价、涨价、价格不变、补货、库存减少、库存数量不变、评论增加、好评增加、差评增加、库存减少且评论增加、新增跟卖卖家。“检测到补货、库存增加、恢复有货”统一映射为补货；商品总库存净流出或任一报价库存减少/转为没货映射为库存减少。后端只读投影按观察区间首尾新增PLID级好评、差评增量，不修改数据库结构；评论总数、4–5星好评数和1–2星差评数分别驱动三类评论信号，库存减少与评论总数同时增加时产生组合信号；系统全历史首次观察某卖家的日期落入所选区间时进入“新增跟卖卖家”。正式数据只读核对当前全区间有评论增加34个商品、好评增加28个、差评增加5个，严格商品总库存净流出且评论增加14个。待建立基线、价格/报价/库存不可比、库存状态不变和旧技术趋势名称不再进入筛选或醒目标签；底层原始信号、数值、判断说明与详情证据仍完整保留。
- 竞品详情已把卖家切换、排序和历史趋势合并为顶部“卖家连续对比台”：默认按所选区间首尾可比的精确库存净流出排序，也可切换价格、当前精确库存或原始顺序，并可使用上一个/下一个连续查看。基础历史快照是价格、精确库存和PLID共用评论数三组同时间轴折线；自有链接选中精确Seller API自有报价时追加同店同Offer的近30天浏览量第四组，公开跟卖报价不借用自有流量。第四组读取与价格/库存同一条逐次Seller Offer观察，只保留时间戳完全相同的节点，不做最近值吸附；无法证明的旧刷新点和上游实际空值均保留断点。流量场景下四联图使用整行宽度，卖家卡片紧凑排列在上方；指标区使用低干扰的独立浅色底，相邻指标间增加清晰实线分隔。鼠标悬浮或键盘左右移动显示同一Seller刷新时刻的北京时间、价格、库存状态/证据、评论数和真实流量记录。库存净流出明确只是观察信号，不等于实际出货或订单；`page_views_30_days`仍是滚动30天值，不是当天流量或访客数。

物流关联现按高/中/低三档候选展示：高置信要求完整SKU数量一致、双方唯一和30天窗口；中置信允许完整SKU集合相同但数量不同；低置信允许至少50%精确SKU重合，中低档使用60天窗口。页面展示数量差、日期差和一对多歧义，2至3个Shipment逐SKU合计完全等于一个长睿单时才提示拆批组合；所有候选仍需有权限用户逐对人工确认。
- “约平台仓”已接入Takealot Seller Portal当前BFF契约，并增加总开关与店铺代码允许列表双重闸门：页面只保留“直接创建发货单草稿”，点击后先用`whoami`校验当前店铺内存会话；有效则直接执行平台`shipments_review`和Task 22，无效则由服务器Windows凭据管理器读取该店铺邮箱/密码登录。只有Takealot登录响应要求2FA时才创建等待记录并弹出验证码，验证成功自动续接同一个幂等请求；ERP没有定期强制2FA。浏览器密码登录、本地冻结、单独预审和单独创建路由均已移除。固定主机`https://seller-api.takealot.com`，不经过星凯且不复用星凯JWT/签名；公开Marketplace客户端继续严格GET-only。密码不进入页面、项目`.env`、MySQL、日志或审计，OTP与Bearer只在进程内存按店铺隔离且OTP不信任设备。服务端逐Offer/区域完全一致校验继续拒绝`UNALLOCATED`、未知仓和数量变化；所有POST/PUT禁止自动重试并保留任务、Shipment与操作者审计。确认PO和归档保留人工确认；“确认已发货”独立开关继续默认关闭，待授权人工抓包确认。当前本机只允许`store-03 / YeboShop`使用约平台仓，并已将用户级本地环境变量中的账号密码一次性写入该店铺的Windows凭据管理器；其余五店由后端拒绝。配置与验证未执行真实Seller Portal登录、验证码或上游写入，也未实现或允许“不可约”绕过。

项目主体已完成数据接口、MySQL 存储、指标计算、异常识别、HTML/Excel/PNG 导出和统一 Vue 3 + TypeScript 小型 ERP。十个正式前端模块为经营总览、商品中心、关键词流量、搜索定位、经营坐标、异常商品、物流管理、约平台仓、竞品雷达和管理员可见的用户权限。NFT102、旧风险/质量与运营日报后台能力仍保留，但对应旧前端模块已移除。物流管理页面固定只读 MySQL 最近成功快照，打开页面、切店和普通查看均不访问上游；长睿 W8 与 Takealot Shipment/Shipment Items 仅由页面人工同步，或在10:05早间、18:00晚间、次日09:00周期末及右上角手动店铺采集完整成功后同步。六店同一轮采集共用一次长睿读取，Takealot Shipment仍按店铺独立读取和保存。两侧每次成功响应均保存为 MySQL 最近成功快照，上游失败时继续展示本地数据并标明同步时间；明确共享的头程号、箱唛、单号、Reference、Tracking Info 或 PO 才形成编号直连，完整 SKU 数量签名、双方唯一且30天窗口内只形成待人工确认的高置信候选，确认与撤销均持久化审计。正式服务监听 `0.0.0.0:8501` 供同一受信任局域网登录，MySQL 8.0 的 `takealot_ops` 数据库仍只允许本机连接；原 Streamlit 看板仅保留为 `dashboard-legacy` 兼容入口。保留的运营日报后端按北京时间每日10:00至次日10:00建立核对周期，将10:05早间、18:00晚间、次日09:00周期末定时版本、周期内每次手动刷新版本、人工候选值和确认最终值分层留痕，并在18:30形成未合并快照。日报事件接口继续保留给兼容客户端，但统一 Vue 前端不再订阅，也不再显示矩阵、待办或全局提醒。所有采集入口都使用请求级重试、整批新会话重试和绕过异常系统代理的直连备用，只有Offers与Sales完整获取才冻结日报；每次尝试和最终脱敏根因继续保存在文件日志和数据库。漏爬与真实数值冲突仍保持分离：漏爬自动采用本周期其他可用值并说明原因；近30天浏览量继续保存和导出但不参与冲突判断。同周期版本只比较销量，业务日D的库存采用D+1日北京时间10:05后首次完整成功快照；若早间漏爬或不完整，则由当天随后最早的完整成功库存补齐并锁定。

后台异常判定中的销量趋势已改为短长窗口均值比较：截至指标日的近3个自然日日均下单件数低于近15日均值时为“销量下降”，高于时为“销量上升”，相等时不触发；两个窗口均包含指标日，只有完整覆盖15日窗口才判断，已覆盖的空销量日按0件参与平均，历史不足保持未判定，旧的最低销量和涨跌百分比门槛已移除。正式库已按新规则重算2026-07-15至2026-07-29共3676条指标；7月29日是首个完整窗口，产生59条销量下降和21条销量上升，更早日期不保留旧趋势记录。

后台风险投影已按类型保存触发证据，普通提示级“不可购买”不增加重点证据区。销量趋势直接展示截至异常日的15天每日销量柱状图，并保留近3日/近15日日均和差值；高浏览低转化显示异常时近30天浏览量、当前记录销量、近30天转化率及当日分位阈值，低浏览高转化只保留浏览量、销量和转化率三项，不重复展示低浏览边界与高转化边界；本库销量不足30天时明确显示实际记录天数和起止日期；疑似断货显示异常时库存、状态和异常日前7日下单；停止更新显示最近Offer采集时间、实际间隔和阈值；未知销售状态显示具体未配置状态。销量证据使用异常发生日对应的历史逐日指标，不把当前商品指标作为历史判定证据，也不把不足30天的销量伪装为完整窗口。

用户权限页现提供查看、运营、选品、管理员四个等权展示的快速模板，不再把模板当成固定角色。管理员可对每个现有账号逐项增删店铺、竞品、刷新和用户管理等现有前端权限；账号卡片使用连续四边框，消除原用户表分隔线断开。运营日报权限键继续由后端兼容接口强制执行，但统一 Vue 用户权限页不再展示或编辑这些选项。无权限模块继续在导航中以“未开放”显示，点击锁定模块或页面内未授权操作统一提示“当前账号该功能权限未开放”。新前端遇到尚未重启的旧后端时会按旧账号模板补齐权限，避免管理员登录后空白；无法识别的登录响应、连接失败和页面脚本异常均显示带重新连接或重新加载入口的错误页。

店铺授权基础是无上限的账号—店铺多对多关系。每个账号可多选运营店铺；账号同时可独立开启“全部店铺（含未来新增）”，此时未勾选店铺仍可单店查看，但不会进入“我的运营店铺”或负责运营名单。关闭全部店铺后，运营店铺勾选同时定义可查看范围。首个管理员和升级前既有管理员保留全店范围。登录顶栏的店铺选择器已连接真实逐店数据上下文，六家已接入店均可读取独立数据；没有授权、尚未接入或伪造店铺代码都会由后端拒绝。竞品雷达仍为前端公共模块，不额外要求店铺分配；NFT102 仅保留后台兼容能力。

店铺授权不再作为公共模块的前置条件。竞品雷达按账号功能权限独立放行，0店铺账号登录后会自动进入第一个已开放公共模块；竞品查看、采集及其共用安全缩略图均不要求店铺范围。经营总览、商品、经营坐标、全量刷新和既有店铺报表继续同时校验功能权限与已接入店铺授权；保留的风险与运营日报后端接口执行原有权限及店铺校验。正式账号`pxg`已只读核对为启用、拥有竞品查看/采集权限、`all_stores=false`且0个店铺，本次不修改其权限或店铺关系。

竞品雷达现已把自有店铺与真正竞品分为两个来源标签页，默认只显示“真正竞品”，自有商品跟卖需切换后才显示。自有链接每次从`OfferCurrent.productline_id`自动生成，不写入人工竞品清单；每次点击“开始采集”都会重新读取最新真正竞品和账号可访问的全部已接入店铺自有PLID，先放真正竞品、再放全部自有链接，按PLID去重后复用同一个页面批次、断点与串行浏览器，顶栏当前店铺只影响查看而不缩小手动采集。每天09:00自动任务在实际取得采集权时读取全部活跃真正竞品和所有已接入店铺当前自有PLID，先真正竞品、再自有链接并跨店去重；每完成25条及收尾前重扫一次，Seller Offers完整刷新或监控清单新增暴露的新PLID自动追加队尾。自动任务只由Windows短命令持久登记并唤醒正式ERP，真正爬取与手动入口共用同一个服务端批次、请求协调器和串行浏览器，不存在00:30隐藏并行进程。触发处理时已有活动批次，或目标读取期间被人工批次先取得共享槽，当天自动触发会记录`skipped_busy`并视为已处理，不保持待开始、不跨日排队，也不在现有批次结束后补跑；ERP离线时仍保留尚未处理的触发文件，已经开始的未完成批次在退出或重启后从服务端断点恢复；同一北京触发日幂等只处理一次。

自有商品页面默认严格使用顶栏“当前查看店铺”范围；进入竞品雷达时同一个下拉框按账号提供“我的运营店铺”合并项，跨店可见范围更大时另有“全部店铺”，分别汇总明确勾选的运营店铺或全部可访问已接入店铺并跨店去重；这些选项不会进入经营总览以外的物流等单店前端模块，保留的日报后端仍按真实单店范围校验。自有分区另有六店日期区间查询，从不可变快照列出区间内曾被跟卖的PLID、具体发现日期和卖家；列表经营信号排序、Seller API`store_offer_baselines`每日首拉与`store_offer_observations`每次完整刷新口径保持不变。自有PLID读取全部公开报价后按所有已接入店铺当前Seller API Offer ID/SKU排除自有报价；剩余报价才是跟卖，公开变体本身不直接算跟卖。真正竞品、自有商品和个人监控池的跟卖状态已拆为互斥的“现在被跟卖 / 曾经被跟卖 / 未发现跟卖”：末次快照仍有跟卖报价才算“现在”，区间内曾出现但末次已无才算“曾经”，两者都没有才算“未发现”；不再保留含糊的“有被跟卖”。自有链接详情同时移除重复的“SELLER API REFRESH / 自有 Offer 最新完整刷新”逐Offer清单，但Seller API刷新、观察历史、PLID共享评论及卖家连续对比台中的自有报价继续保留。管理员能看到自动任务与手动任务相同的来源、进度、当前PLID、后台阶段和逐链接成功/待重试明细；普通运营不加载该技术模块。连续2条真实网络失败时自动批次持久暂停10分钟并在管理员模块显示倒计时后续爬。`kxx`可从任意管理员页面停止活动自动批次，即使本页没有本地断点；停止会取消当前请求并关闭库存与复用公开浏览器，同一触发日不再自动启动，次日09:00照常。停止后的服务端断点、批次ID、冻结顺序、统计和明细不会删除；管理员批次控制会显示专用继续按钮，由`kxx`显式恢复同一批次并只续爬失败/未执行项，ERP重启后仍可继续。

- 2026-08-09搜索定位三标题打法与成本稳定性闭环已完成：专项`55 passed`、完整Python回归`504 passed`（仅1条既有Starlette TestClient弃用提示），前端八组专项共`33 passed`，Ruff、66个源码文件Mypy、数据库校验、Vue TypeScript/生产构建及`git diff --check`通过。正式库只读确认PLID101397581旧`ambient lighting`自然位95已按新蓝海门槛降为不采用，当前Offer标题与分析时标题不同会在页面告警。正式ERP经`restart_erp.ps1`重启（启动器PID 37632），`/api/health=ok/takealot-erp`，首页及`index-C-Dbii81.js`/`index-BCcDTLcY.css`均HTTP 200，未登录搜索定位GET为401；本次未调用真实模型、未产生模型Token/费用、未执行Takealot上游搜索、Seller Portal写入或浏览器自动化。

- 2026-08-09已在本机实装`Takealot 竞品雷达每日采集`：Enabled/Ready、每日09:00，下一次运行2026-08-10 09:00；动作仅执行`trigger-competitor-collection`，启用错过补跑、忽略重叠实例和失败后每5分钟最多重试3次。旧`Takealot 自有商品跟卖自动追踪`00:30隐藏任务已删除。安装及正式ERP重启后均未生成2026-08-09触发文件或批次断点，没有误启动真实采集。最终完整Python回归`459 passed`，前端七组专项`27 passed`，Ruff、修改源码Mypy、数据库校验、Vue TypeScript/生产构建、PowerShell语法及`git diff --check`通过。ERP启动器PID 23156，`/api/health=ok`，首页及`index-D2NWZSN0.js`/`index-CujdTvT7.css`均HTTP 200；未执行浏览器自动化或Takealot上游采集。

- 2026-08-09已实装`Takealot ERP 登录后自动启动`：状态Ready，触发器为`DESKTOP-NTRMANG\Mayn`登录后延迟30秒，Principal为当前交互式用户、Limited权限，动作使用隐藏PowerShell和绝对`ensure_erp_started.ps1`路径，工作目录固定为项目根；`StartWhenAvailable=true`、`IgnoreNew`、允许电池供电，失败每分钟最多重试12次。任务列表三次手工启动均返回0；首次运行超过5分钟后，正式8501监听仍为PID 22320和`2026-08-09 19:45:50`启动时间，`/api/health=ok/takealot-erp`，首页及当前JS/CSS均HTTP 200，`erp-startup.log`明确记录“already healthy / no restart needed”，证明重复触发不会打断健康ERP且5分钟任务上限不会杀死脱离后的服务。完整Python回归`460 passed`，启动/任务专项`13 passed`，三个PowerShell脚本Parser、Mutex嵌套探针及`git diff --check`通过。本次没有重启电脑，真实关机—登录触发仍待下一次实际开机观察；电脑停在登录界面时不会启动，登录进入桌面后无需人工操作。

- 六店跟卖发现历史查询卡默认收起，标题区保留北京时间/公开快照口径与“展开记录”按钮；展开时才挂载日期表单、说明和结果，收起后保留已选日期与查询结果。该交互只改变前端展示，不自动请求平台、不写数据库，也不改变不可变快照的历史计算口径。

- 2026-08-05多报价主购买区修复：公开采集现在同时保存每个变体主购买区的全部单选报价和`other_offers`；变体自身选中主报价不算跟卖，同变体绿色未选报价及红色报价才算跟卖候选。真正竞品分区也提供“有被跟卖/未发现跟卖”筛选。真实`PLID100376924`由旧3条报价补齐为4条：R516选中、R513绿色未选、R518/R549红色；六店Offer ID/SKU交叉排除后，R516 VoltTech ZA和R518 YeboShop属于已接入自有店铺，保留R513卖家ID 29900272（平台未返回名称）及R549 Future Star为外部跟卖。该PLID已定向刷新写入正式库，最新投影为2条外部跟卖和4条统一对比报价；完整回归407 passed、四个前端专项19 passed，Ruff、Mypy、数据库校验和生产构建通过。正式ERP重启后8501 PID 33560，健康、首页及`index-D7wog3_4.js`/`index-DiDTPCH_.css`均HTTP 200；按既定偏好未执行浏览器自动化验收。

- 2026-08-05验证：跟卖/库存/存储/ERP专项`111 passed`，完整回归`406 passed`（仅1条第三方TestClient弃用提示），四个前端专项`18 passed`，Ruff、Mypy（59个源码文件）、`verify --date 2026-08-05`及Vue TypeScript/生产构建通过。正式库只读新投影为真正竞品288、自有PLID863，其中88个PLID有跟卖，369条跟卖变体/报价按卖家名归为351组；`PLID96909926`没有其他卖家，但同一对比台可展示4条自有Seller API报价。该次正式ERP经`restart_erp.ps1`重启，8501监听PID 27132，`/api/health=ok`，首页及`index-BlvJv1ik.js`/`index-DiDTPCH_.css`均HTTP 200；当时的00:30任务运行态已由2026-08-09的新09:00共享任务取代。

ERP 登录会话现采用7天滑动有效期：成功登录先获得7天，只要到期前继续发起有效请求，就把数据库到期时间和 HttpOnly Cookie 同步延长到最近使用后的7天。同一会话每1天最多续期一次，避免2秒状态轮询造成持续数据库写入；连续7天没有任何有效使用、主动退出或管理员修改账号权限、模板、密码、启用状态时仍需重新登录。

第 7、8 项的代码与合成数据验收已经完成；本机数据库和既有 NFT102 输出已包含真实只读采集结果。NFT102 前端入口现已移除，本次未主动再生成一份生产日报。

2026-07-28 曾增加运营日报登录态实时事件；该前端订阅、矩阵和全局提醒已在2026-08-15随模块删除，事件接口继续保留给兼容客户端。正式修复统一使用 `scripts/restart_erp.ps1` 重启并检查健康状态。同日已在备份 `backups/takealot-20260728-054911-517635.sql` 后删除两件演练商品的137条日报关联数据，直接引用及截止快照 JSON 引用复核均为0。

## 已完成范围

| 任务 | 状态 | 主要产出 |
|---|---|---|
| 1. 项目基础与配置 | 完成 | Python 项目、配置模型、领域类型、测试工具链 |
| 2. Takealot 只读 API 客户端 | 完成 | `/offers`、`/sales`、`/shipments`及可选`/returns`，分页、重试、错误脱敏 |
| 3. 数据持久化 | 完成 | SQLAlchemy + MySQL 8.0、幂等写入、事务与数据库约束、SQLite 全量迁移、两侧物流最近成功快照、平台仓草稿/明细/状态审计及人工关联审计 |
| 4. 采集与指标 | 完成 | Offer/Sales 采集、SAST 销售日、流量快照、异常规则 |
| 5. 可分享报表 | 完成 | 独立 HTML、Excel、PNG，均来自同一指标数据集 |
| 6. 小型 ERP | 完成 | 现有9个统一 Vue 中文模块、关键词变更与近30天浏览量前后趋势对比、长睿 W8 与 Takealot 货件只读物流总览、默认双闸门且当前仅YeboShop启用的平台仓预审/建单/人工状态动作、四个权限模板、账号级自定义功能权限、无上限账号—店铺多对多授权与全店动态范围、数据新鲜度、手动完整刷新、局域网单端口启动和 Windows 完整进程树清理；NFT102、风险/质量与运营日报仅保留后台兼容能力 |
| 7. 日常运行工具 | 完成 | 日报早间/晚间/周期末采集与截止快照 CLI、`.env` 自动加载、中国时间 10:05/18:00/次日09:00/18:30 四任务统一处理同一业务日、D盘完整备份与35天binlog连续归档、运营说明 |
| 8. 最终审计与打包 | 合成验收完成 | 跨输出一致性、安全、打包和覆盖率已通过；待真实只读联调 |

## 最近验证结果

- 2026-08-11搜索定位动态多店严格串行批次：公开请求默认3秒基线加0至2秒随机抖动；启动前只读预览并确认冻结快照，运行中全局单任务、逐项检查点、资格失效跳过、供应商/网络遇错暂停且不自动重试，ERP重启后也需人工确认继续。正式六店只读快照为541条合格商品、预计483次新识别、约160.79万Token，豆包主模型基准约¥1.78、常见约¥1.78–2.71、历史保守上界约¥5.60，预计16.2–31.3小时。批次专项`4 passed`、带认证只读/缺配置路由专项`1 passed`、搜索定位前端契约`18 passed`，Ruff、Vue TypeScript和生产构建通过；完整回归、Mypy、数据库校验和正式ERP部署待本任务结束前补录。未启动真实批次或调用任何上游。

- 2026-08-11搜索定位标题规格后置：三种打法统一先提取原始规格组、再去重商品词、最后追加规格；截图标题稳定输出`Flood Lights Outdoor RGB LED Floodlight Smart App Control Waterproof 300W IP66`。回归覆盖`300W/IP66`、功率/电压/电流、容量、尺寸、重量、数量/套装、4K/1080P、IP/IK、分离单位写法、重复数字及型号/连接身份保护。搜索定位专项`87 passed`、完整Python`576 passed`、全部前端`92 passed`（Python仅1条既有Starlette TestClient/httpx弃用提示）；Ruff、搜索定位Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式ERP当前监听PID 33836，本机/局域网健康、首页和新哈希资源均HTTP 200，未登录搜索定位状态401；既有自动竞品批次保持同一ID并实际续跑。本轮未调用模型、倒搜、Takealot补全/搜索或Seller Portal，也未写正式业务数据。

- 2026-08-11关键词流量商品时间固定栏：选中商品顶部持续显示首次上架与最近补货；首次上架优先读取Offers `created_at`并按南非时间展示，旧记录才标“本库最早记录”，最近补货只取后一条有效库存快照真实增加时的实际采集北京时间并显示增加数量，无证据显示“暂无记录”。关键词后端专项`7 passed`、固定栏前端契约`2 passed`、完整Python`561 passed`、全部前端`90 passed`（Python仅1条既有Starlette TestClient/httpx弃用提示）；Ruff、67个源码文件Mypy、Vue TypeScript/生产构建、六店数据库校验和`git diff --check`通过。正式库只读投影共1582个当前Offer，全部有平台首次上架时间，239个已观察到补货；合成回归另覆盖旧记录回退、缺失库存跳过和无补货。正式ERP最终监听PID 25828，本机/局域网健康和首页均HTTP 200，线上`index-vCyK28UM.js`/`index-ClqHAAZ-.css`与本地SHA-256一致，关键词接口未登录401。既有09:00批次保持同一`scheduled-20260811-904ecda8db94`并在部署期间从384条结果推进到385条，最终保留31条队列，没有从头重跑；未执行登录态浏览器验收、Seller Portal写入或人工上游采集。

- 2026-08-08搜索定位图片优先与双赛道实测：模型请求正文和回归断言均确认不含源主标题或SKU，服务器只在识别后做本地交叉核对，并删除标题未确认的高风险身份词。豆包Seed 2.0 Lite一次新图调用为2748输入/565输出Token、估算¥0.003683，正确识别PLID101397581为`RGB light bars`；千问兼容字符串候选后直连为1721输入/342输出Token、估算¥0.006178，但同图误判为`RGB Corner Floor Lamp`，因此调整为豆包主用、千问备用并增加身份冲突闸门。最终缓存复采Token为0：`rgb light bar`来自Takealot补全第2项，完整首页23/36同类，目标第1页第3行第3列/自然序位11；`gaming light bar`为补全第5项，24/36同类，目标第1页第2行第3列/自然序位7；`ambient lighting`为补全第3项，首页5/36直接同类，作为第二选择定位第3页第6行第3列/自然序位95。核心与第二选择标题均无标点且分开显示，未修改Seller Portal。搜索定位专项`11 passed`、完整Python回归`445 passed`（仅1条第三方TestClient弃用提示）、前端七组专项`27 passed`，Ruff、65个源码文件Mypy、Vue TypeScript与生产构建通过。正式ERP经脚本重启（启动器PID 32132），健康、首页和`index-DmO-bECY.js`/`index-BHb5t1lz.css`均HTTP 200，未登录搜索定位GET返回401。

- 2026-08-07搜索模型Key正式接入：Windows用户级`DASHSCOPE_API_KEY`与`ARK_API_KEY`均脱敏确认存在，原启动父进程与项目`.env`均未提供这两项。`restart_erp.ps1`现仅在进程变量缺失时补载这两个固定名称，显式进程变量继续优先；脚本语法与隔离继承探针通过，运行配置只输出千问`qwen3.7-plus`、豆包`doubao-seed-2-0-lite-260215`均已配置。搜索定位专项`7 passed`、完整Python回归`440 passed`（仅1条第三方TestClient弃用提示）。正式ERP经脚本重启（启动器PID 23864），`/api/health=ok`、首页和`index-B0LZMPGA.js`/`index-B1imY9lb.css`均HTTP 200，未登录搜索定位GET返回401；stdout/stderr没有变量名或Key值。未新增模型调用、Token费用、平台搜索、数据库业务写入、Seller Portal写入或浏览器自动化。

- 2026-08-07搜索定位标题与自然序列口径：建议标题由服务器确定性重排，通过Takealot相关性门槛的搜索词前置、卖点和参数后置，最终只含字母、数字和单个空格；新结果与旧缓存读取使用同一规则，未重新调用模型。页面排名规则改为“基于默认相关性排序，按36项分页并映射四列坐标”，跨页排名解释为自然商品序列中的第N个。解析器只接收`sections.products.results`中的`product_views`并排除显式赞助标记。搜索定位专项`7 passed`、完整Python回归`440 passed`（仅1条第三方TestClient弃用提示）、前端七组专项`27 passed`、Ruff、65个源码文件Mypy及Vue TypeScript/生产构建通过；公开搜索接口只读HTTP核对返回200、36条同类型结果且无Sponsored/Ad标记。正式ERP经`restart_erp.ps1`重启（启动器PID 21880），健康、首页及`index-jVGHq2P8.js`/`index-Bi0NYPCj.css`均HTTP 200，未登录搜索定位GET返回401。未调用模型、未新增Token/费用、未执行平台写入或浏览器自动化。

- 2026-08-07约平台仓单店启用：新增`TAKEALOT_PORTAL_ENABLED_STORES`后端允许列表，登录、OTP、预审、Task 22及确认PO/已发货/归档入口都会先核对当前`store_code`，非允许店铺不能仅靠总开关或直接调用接口绕过。正式本机只允许`store-03 / YeboShop`，从名称明确的Windows用户级环境变量读取账号密码并一次性写入该店铺的Windows凭据管理器，值未输出；逐店只读核对显示YeboShop为“功能启用/凭据已配置”，其余五店均未启用且未配置凭据。平台仓三组专项`12 passed`，最终完整回归`438 passed`（仅1条第三方TestClient弃用提示），Ruff、Mypy（65个源码文件）、Vue TypeScript/独立目录生产构建、数据库`verify --date 2026-08-07`及配置核对通过。正式ERP经最终`restart_erp.ps1`重启（启动器PID 19792），`/api/health=ok`、首页及`index-DBa5Rfvf.js`/`index-Ttaiuxzc.css`均HTTP 200。未执行真实Seller Portal登录、OTP、上游写入或浏览器自动化；`TAKEALOT_PORTAL_SHIPPED_WRITE_ENABLED=false`继续关闭。

- 2026-08-07搜索定位位置文案：命中结果现在直接显示“第X页·第X行·第X列”，其下显示“跨页自然排名 #N（排除广告后按序第N个）”；顶部规则改为每页最多36个自然商品、从左到右每行4个、广告不计，下方补充第1页1–36和第2页37–72的示例。前端七组专项共`27 passed`，Vue TypeScript检查与生产构建通过，最终资源为`index-DBa5Rfvf.js`和`index-Ttaiuxzc.css`。正式ERP经`restart_erp.ps1`重启（启动器PID 35536），健康、首页及两项资源均HTTP 200，未登录搜索定位GET返回401。未执行Python回归或浏览器自动化，未新增模型调用或平台写入。

- 2026-08-07搜索定位真实模型与完整链路：Windows用户级环境变量中的`DASHSCOPE_API_KEY`与`ARK_API_KEY`均已按脱敏方式确认存在，正式ERP重启时安全注入当前新进程，Key值未进入文件、日志或输出。千问`qwen3.7-plus`与豆包`doubao-seed-2-0-lite-260215`分别用同一条合格在售投影幕Offer完成真实图片输入和强制结构化输出；豆包直连使用2401输入/433输出Token，估算人民币0.002999元，返回4个商品类型一致的候选词。随后正式搜索定位完整链路由千问完成，使用1339输入/360输出Token，估算人民币0.005558元、置信度0.95；3个候选均通过Takealot前12个自然结果相关性门槛，其中`100 inch portable projection screen`定位第1页第1行第1列/自然位1，`high brightness outdoor movie screen`定位第1页第2行第4列/自然位8，另1词在实际返回的2页内未找到本商品。该结果只证明此样本链路和词义相关性，不代表全品类准确率；未修改Seller Portal标题。正式ERP经`restart_erp.ps1`重启（启动器PID 16880）并通过健康检查，未执行浏览器自动化。

- 2026-08-07搜索定位自有在售闸门与国内模型链：六店正式MySQL只读核对1557条当前授权Seller Offer，529条同时满足`buyable`、明确正数可售库存、36小时内快照和完整PLID/标题/官方主图，1028条在模型调用前排除，严格候选范围缩小约66.0%；页面普通读取仍不访问上游。多模态改为千问`qwen3.7-plus`主用、豆包Seed 2.0 Lite跨厂商备用，640像素缩略图Base64输入、强制Function Call、非思考模式、跨厂商失败切换、缓存Token归零及按配置单价费用估算均有专项覆盖。搜索定位专项`5 passed`、完整Python回归`437 passed`（仅1条第三方TestClient弃用提示）、前端六组专项`24 passed`、Ruff、65个源码文件Mypy及Vue TypeScript/生产构建通过。正式ERP经`restart_erp.ps1`重启（启动器PID 25884），`/api/health=ok`、首页和最终JS/CSS均HTTP 200，未登录搜索定位GET返回401。当前两家API Key均未配置，未发出真实模型请求、未产生费用、未执行真实Takealot搜索或Seller Portal写入，也未执行浏览器自动化。

- 2026-08-07真正竞品可跟卖机会：后端专项1项、完整Python回归435项、六组前端专项24项、Ruff、65个源码文件Mypy及Vue TypeScript/生产构建通过。正式MySQL只读投影297个真正竞品中108个命中，全部为“全部报价售罄”，当前“暂无卖家报价”为0；未知库存和非完整报价证据回归明确不命中。正式ERP经`restart_erp.ps1`重启（启动器PID 23856），`/api/health=ok`，首页及`index-t6hOwhGu.js`/`index-Dvn_pof-.css`均HTTP 200，未登录竞品接口返回401。未启动真实竞品采集、未访问平台、未写数据库、未执行浏览器自动化。

- 2026-08-07六店老板盘面：权限/库存/共享仓专项1项、完整回归435项、前端六组专项24项、Ruff、修改后端定向Mypy、Vue TypeScript/生产构建和`git diff --check`通过；完整回归仅有1条第三方TestClient弃用提示。正式MySQL只读核对6家店共1557个当前Offer，平台可售3146、平台在途1149、平台收货中50，三个字段覆盖均为1557/1557；共享W8海外仓库存8331、可用5078、锁定3253、已分配出库581、海外仓在途0。每店当前都能映射到1个启用且拥有店铺查看权限的非管理员账号，管理员未进入结果。正式ERP经`restart_erp.ps1`重启（启动器PID 29404），健康接口、首页、`index-t6hOwhGu.js`和`index-Dvn_pof-.css`均HTTP 200，多店接口未登录返回401；构建JS包含健康定位、库存物流、负责运营和进入店铺明细。核对只读本地MySQL，没有触发物流同步、平台采集、正式业务写入或浏览器自动化。
- 2026-08-07经营总览数据异常指标暂时撤下：六店顶部合计、逐店经营卡和单店顶层KPI均不再展示“数据异常/异常商品”，异常数量不再参与需关注状态或店铺排序；底层异常数据与既有判定保持不变，只在“风险与质量”模块查看。ERP Web专项`30 passed`、完整回归`440 passed`（仅1条第三方TestClient弃用提示）、前端七组专项`27 passed`、Ruff、65个源码文件Mypy、Vue TypeScript/生产构建及`git diff --check`通过；数据库校验命令退出0并明确提示`current`当日无日报记录，异常数999的逆序夹具确认异常不再影响健康原因和排序。正式ERP经`restart_erp.ps1`重启（启动器PID 28128），`/api/health=ok`、首页和`index-B0LZMPGA.js`/`index-B1imY9lb.css`均HTTP 200，多店接口未登录返回401；运行JS与本地构建字节一致，包含新口径且不存在旧合计文案。未触发上游采集或数据库写入，未执行浏览器自动化。
- 2026-08-07搜索定位模块：新增图像识别候选、低置信闸门、Takealot自然结果相关性验收、`after`游标分页定位、页/行/列与自然总位、模型缓存/token用量、标题建议后续观察验证和`search_ranking.run`权限。搜索定位/权限专项9项、完整回归434项、Ruff、65个源码文件Mypy、前端22项专项及Vue生产构建通过。公开搜索只读契约实测当前为`v-1-18-0`、36个自然结果/页和1365×900桌面4列，赞助卡来自独立接口且不计自然位。当前`.env`未配置`OPENAI_API_KEY`，未调用真实OpenAI、未产生模型费用、未改Seller Portal标题；只使用隐藏浏览器核对公开搜索契约，未执行登录态ERP浏览器验收。
- 2026-08-07周期末流量兜底参考：09:00正式周期末失败仍保持`status=failed`与合计为空；同一北京时间自然日另有可用成功采集时，接口另行返回来源、实际时间、覆盖与缺失，前端以橙色虚线显示且绿色正式线不跨越失败。正式MySQL只读核对07-30/07-31同日晚间参考为`18908`（170/409覆盖）与`18883`（169/410覆盖）。周期末专项2项及完整回归430项通过，相关Ruff和Vue生产构建通过；全量Mypy仅命中当前并行搜索排名模块2条既有可空类型错误。ERP重启后健康、首页和最终JS/CSS均HTTP 200；未触发真实采集或浏览器自动化。
- 2026-08-06经营信号排序冲突修正：移除独立排序指标选择器，升序/降序现在直接按当前经营信号对应的价格、库存、评论或新增卖家变化值排列；“全部”保持原顺序并禁用方向，缺失值置后。五组前端专项共22 passed，Vue TypeScript/生产构建及`git diff --check`通过；正式ERP重启后`/api/health=ok`，首页与`index-m1pR4Cl4.js`/`index-bvMeG95d.css`均HTTP 200，未登录竞品接口返回401；未执行浏览器自动化或真实采集。
- 2026-08-06竞品全量手动队列和跟卖历史：每次开始都会实时重读真正竞品与全部有权店铺自有PLID，当前正式只读投影为293+869；顶栏单店只改变查看范围。自有分区可按北京时间区间回溯曾被跟卖的PLID、卖家和具体发现日期，匿名且无卖家名/ID/Offer ID的主报价不生成卖家事件；07-23至08-06共有139个自有PLID保存到可识别的非自有卖家。两个分区新增“新增跟卖卖家”经营信号；排序方向直接跟随当前经营信号对应的价格、库存、评论或新增卖家变化值，不再放重复的指标选择器。竞品存储/ERP专项12 passed、五组前端专项22 passed，Ruff、Mypy、数据库校验、Vue生产构建及`git diff --check`通过；完整428项中任务相关均通过，另有并行约平台仓模块2条旧测试期望未同步（426 passed, 2 failed），本次未改该模块。ERP重启后8501健康、首页与`index-BEkDIlht.js`/`index-bvMeG95d.css`均HTTP 200；未执行浏览器自动化或真实采集。

- 2026-08-06约平台仓直接创建流程：专项`11 passed`，完整回归`427 passed`（均仅1条第三方TestClient弃用提示），四组前端专项`20 passed`，Ruff、Mypy（63个源码文件）、Vue TypeScript/生产构建和数据库`verify --date 2026-08-06`通过。正式变更前备份`backups/takealot-20260806-023257-678008.sql.gz`，SHA-256为`3b6398945499da647b931e623096b76352b90f1b9c601f04684c7af0b5a46783`；正式MySQL无损增加`client_request_id`和店铺级唯一索引，迁移后保留原有1条`local_only/draft`历史记录，平台Shipment仍为0。Mock覆盖有效会话直接创建、平台拒绝旧会话后后台重登、平台2FA暂停并续接同一草稿、请求幂等以及旧密码/冻结/二段接口404。每店铺凭据改由服务器交互式CLI保存到Windows凭据管理器；按用户明确要求，建单总开关`TAKEALOT_PORTAL_BFF_ENABLED=true`已开启，页面浏览和服务启动本身不会访问Seller Portal，只有有权限用户点击直接创建才进入上游流程。当前尚未配置或读取真实店铺凭据，`TAKEALOT_PORTAL_SHIPPED_WRITE_ENABLED=false`继续关闭；截至开启时未登录Seller Portal、未请求验证码、未发出任何真实上游写入。

- 关键词流量模块正式变更前的一致性备份`backups/takealot-20260803-142249-386128.sql.gz`已校验，SHA-256为`b8bb7c3f01c31886f0b7a3cb82b62d91dbc07fac6254ef5e09d65ac2e3736195`；增量创建`product_keyword_snapshots`后核对8列/0行，现有410个Offer中167个在2026-08-03有近30天浏览量，关键词档案与变更均为0，没有替运营伪造历史。关键词算法专项`4 passed`、关键词/权限/路由组合专项`12 passed`、完整回归`394 passed`（1条第三方TestClient弃用提示）；Ruff、Mypy（56个源码文件）、数据库`verify --date 2026-08-03`、Vue TypeScript/生产构建通过。正式ERP经`restart_erp.ps1`重启，`/api/health=ok`，首页与`index-CxKEDrEH.js`/`index-BkWMJUrx.css`均HTTP 200，未登录关键词读写均返回401；按既定偏好未执行浏览器自动化。

在当前工作区已执行：

- 关键词流量建档只读核对：截至2026-08-04，410个现有商品全部可从既有每日Offer标题快照建档，识别出1个真实标题变化节点；SKU`9902337192346`在2026-07-28从标题中移除`NEXOHOGA`，系统标签为“变化｜移除1词”。本次读取未写正式库。当前完整工作区专项`13 passed`、完整回归`394 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（56个源码文件）、数据库`verify --date 2026-08-04`、Vue TypeScript/生产构建与`git diff --check`通过。正式ERP经`restart_erp.ps1`重启（启动器PID 1224），`/api/health`、首页与`index-BvXRdvMf.js`/`index-CstUQ5xo.css`均HTTP 200；未登录关键词GET/旧POST均返回401，登录态路由测试确认旧POST为405；按既定偏好未执行浏览器自动化。

- 经营总览店铺流量折线已用正式MySQL只读核对：2026-07-30与07-31的09:00周期末整批失败仍保留为正式失败；两天同日最近成功晚间采集分别为北京时间18:00:34与18:01:01，虚线参考合计`18908`、`18883`，商品覆盖`170/409`、`169/410`，其余缺失未补0。相邻成功周期末点07-29与08-01分别为`18702`和`18851`。参考值只用于图表辅助判断，不改写正式失败状态。
- 物流分级候选已用正式本地快照核对：1条高置信、4条中置信、1条低置信、0个精确拆批组合。4条中置信分别展示100→70、100→93、30→5、32→14的数量差及日期差，其中100件长睿单存在多个同SKU平台候选并明确标为歧义；低置信为SKU部分重合，只作为人工排查入口。候选生成专项、权限接口测试及Vue生产构建通过，未自动确认任何关系。
- 物流管理已从进程内临时展示升级为本地持久化闭环，并进一步取消页面打开触发上游：普通GET只读取MySQL，人工按钮才带刷新标记；既有早间、晚间、周期末和右上角手动店铺采集成功后自动同步物流快照。全店定时进程复用一次W8读取，Takealot按店独立同步；某一上游失败时按服务商独立保留最近成功快照。明确编号直连与高/中/低候选、人工确认和撤销审计规则保持不变。
- 竞品报价历史已从PLID主快照拆开并显示为卖家专属折线：选中任一卖家报价后，趋势只使用包含该卖家兼容报价的时间点，没有该卖家的日期直接过滤，不创建“未识别”点或复用主报价。Offer ID继续保留为最强身份证据；缺失或变化时，页面按卖家ID/名称并校验SKU、变体、条件，只有候选唯一才连接，同卖家多SKU不猜测。前端主视觉保留卖家、SKU、变体、价格和库存，Offer ID只在存在时以较小浅色辅助文字出现，未返回时不重复提示。同一PLID评论继续共用，因为Takealot公开评论未提供卖家或Offer ID归属。匿名变体主报价的历史兼容层已补齐：只有同一PLID内存在唯一变体明细时才用“变体键 + SKU”生成匿名报价身份，并采用该变体已保存的精确库存；SKU或明确卖家不同的时间段继续保持另一报价。真实`PLID96909926`只读投影已验证07/23–07/29与08/03的Left Hand SKU `98109849`均为R529/精确库存0，07/30–08/02的T C STORE SKU `231601538`仍独立。
- 跟卖库存探测已适配Takealot新报价区和新购物车：不再依赖已消失的`other-buying-option-<SKU>` DOM ID，而是主动展开延迟加载的报价区，按卖家ID/名称、SKU和价格找到唯一报价行及行内购买按钮。已打开的条款对话框只关闭对话框本身，不触发`T&Cs Apply`；Braze遮罩在精确按钮点击前再次清理。新购物车不再渲染卖家名时，只有商品页报价行身份、购物车PLID和精确价格全部一致才取量；若购物车显示了不同卖家则继续拒绝。真实隔离购物车样本`PLID50067762 / Station Vibration / SKU 69484211 / R 4,595`取得精确库存2，测试购物车已清理。
- 本次跟卖适配已通过专项`37 passed`和完整回归`376 passed`（只有1条第三方TestClient弃用提示），Ruff、Mypy（53个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过，资源保持`index-qel-mXFA.js`与`index-BX9mMrRT.css`。正式ERP重启为启动器PID 4660，健康、首页和资源均HTTP 200；原327条批次从断点继续，重启后`PLID92938527`已成功记录1个变体和12个跟卖报价并继续后续任务，未从头重跑。
- 竞品批次控制已支持同账号安全接管：新 `kxx` 页面读取同源浏览器断点后申请转移，服务端在当前商品完成时才交接客户端租约，旧页不能继续启动下一条；服务重启后也可从保存的失败/未完成断点继续。显示库存浏览器开关改为服务端批次设置，从任意 `kxx` 页面修改后供下一条采集请求读取，不再只是当前页面的本地假开关。共享状态把固定数字明确标为“未解决”，并单独显示当前库存复探或普通自动重试次数及后端阶段。批次/ERP Web定向`40 passed`、完整回归`368 passed`（均只有1条第三方TestClient弃用提示），前端重试队列`3 passed`，Ruff、Mypy（53个源码文件）、数据库`verify`和Vue TypeScript/生产构建通过，资源为`index-qel-mXFA.js`与`index-BX9mMrRT.css`。正式ERP已重启为启动器PID 10992，健康、首页及最终资源均为HTTP 200；原批次重启后继续37条未解决链接，已先后重新发起`PLID94973716`和`PLID91303868`，没有重跑279条成功链接。

- 新增只读“物流管理”正式模块：长睿 W8 客户接口只允许7个审核查询路径，正式基础地址为 `https://crgyl.w8soft.net/prod-api/w8`，授权码仅保存在 Git 忽略 `.env`；页面展示南非仓、渠道、产品/库存、入出库、退货实际记录与状态，以及 Takealot Shipment、PO、目的仓和 Shipment Items 的发送/实收/破损数量，不展示长睿收件人信息。实时联调长睿读取399个产品档案、326条库存、238个入库单、166个出库单和29条实际退货记录；退货接口自报`total=136`但未返回对应明细，页面明确告警。Takealot读取187个货件；当前两边没有明确共享编号，自动匹配为0，不按日期或数量猜测。专项`73 passed`、完整回归`361 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（53个源码文件）、数据库`verify`、Vue TypeScript/生产构建和`git diff --check`通过；正式ERP经`restart_erp.ps1`重启为PID 9900，`/api/health`和首页均HTTP 200，资源为`index-C0mwayX-.js`与`index-DteeVW28.css`，未登录物流接口返回401。按既定偏好未执行浏览器自动化
- 竞品详情弹窗中的全部卖家报价可点击选择，并在顶部连续对比台支持排序和上一个/下一个：当前选择以绿色描边和“正在查看”标识，弹窗标题、顶部价格与当前卖家库存、报价级价格/库存信号、经营信号证据、Offer ID、SKU、变体、条件、折线及经营信号卡/底部商品外链统一切换到该报价。当前报价趋势同步按报价键或`offer_id`过滤，只展示该卖家报价自己的价格和库存；历史时间点没有该报价时保持缺失，不再回退到PLID主报价或其他卖家。评论结构与公开评论继续按同一PLID商品共用，页面明确说明Takealot未返回卖家/Offer ID归属；商品变体按PLID共用，监控队列按PLID去重，切换报价不会重复入队。
- “竞品最新状态”保持每个PLID一张商品父卡片，未点开的列表卡继续只显示商品身份、卖家报价数、报价区间、主报价库存、经营信号和评论评分摘要，不挂载全部卖家报价行；点击或键盘打开详情弹窗后，顶部“卖家连续对比台”集中显示排序后的卖家列表、当前报价信息和历史折线。没有数量的公开库存明确显示“数量未返回”，不会补0或伪装成精确库存；完整监控队列操作、经营信号、变体和评论仍在同一弹窗。卖家搜索和报价级经营信号筛选保持。
- 同一PLID下的竞品卖家报价现已独立保存与展示库存：公开`stock_availability`按`offer_id`/稳定回退键进入报价快照，匿名购物车数量仅绑定实际探测且变体键、SKU、卖家一致的变体主报价，其他跟卖报价数量保持缺失、不补0。所选区间首尾范围一致且数量均精确时显示库存增加/减少，否则只比较明确的有货/没货状态；下拉明细同时显示每个报价的价格、库存与各自变化。报价库存专项`35 passed`、隔离完整回归`349 passed`（1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）、数据库`verify`和隔离的Vue TypeScript/生产构建通过，资源为`index-BZYXThCu.js`与`index-CcvSGXDb.css`；现有快照JSON兼容，无数据库迁移，并发出现的运营日报/GZip源码和测试改动未混入构建和本次提交。正式ERP使用本次隔离源码经`restart_erp.ps1`重启（启动器PID 36620），`/api/health`返回`ok`，首页和两项资源均HTTP 200；按既定偏好未执行浏览器自动化
- 竞品待重试队列已改为按“间隔任务数”指数退避：普通可恢复失败的三次自动重试前分别至少执行1、2、4条其他任务，库存未探测的两次自动复探前分别执行1条和2条；当轮剩余任务不足以满足间隔时保留待重试，不再退化为队尾立即重试。任务爬取详情中的成功和待重试链接全部可点击或用键盘打开监控队列操作页；待重试入口提供“人工重试并插队”，服务端以独立`manual_retry`来源在批次运行记录和MySQL链接审计中保存操作人、北京时间、PLID与URL，尚未消费的重复请求幂等，上一条开始执行后可再次发起。指数退避前端单测`3 passed`，批次与ERP Web专项`37 passed`，完整工作区回归`340 passed`；仅含本次竞品改动的独立暂存快照回归`320 passed`，Ruff、Mypy（50个源码文件）及Vue TypeScript/生产构建通过，独立构建资源为`index-DdFQXC6f.js`与`index-Di9cvrHx.css`（两次回归均只有1条第三方TestClient弃用提示）。数据库`verify`通过。正式ERP已通过`restart_erp.ps1`重启为PID 2276，`/api/health`返回`ok`，首页及当前完整工作区资源`index-Byk7B3lK.js`与`index-vzGieKaS.css`均为HTTP 200；按既定偏好未执行浏览器自动化
- 竞品雷达“链接复核状态”已改为默认收起，关闭时不挂载完整复核表；展开后的疑似/确认失效行可直接点击，即使没有成功快照也会打开对应 PLID 的独立监控队列操作页，可修改、删除、活动批次插队或重新加入监控清单。“竞品最新状态”商品点击后仍进入原详情弹窗，并显示同一套对应 PLID 队列操作卡。批次逐商品结果统一放入一个“任务爬取详情”折叠区，内部再分成功任务与待重试任务。Vue TypeScript检查和生产构建通过，最终资源为`index-BeGvTnHg.js`与`index-2mbfkEtn.css`；静态断言与`git diff --check`通过。正式ERP已重启，`/api/health`返回`ok`，首页及最终JS/CSS均为HTTP 200。按既定偏好未执行浏览器自动化；本次纯前端与文档调整未运行Python测试
- 竞品采集已改为服务端统一的保守随机节流：第一条直接启动，从第二条起每条公开数据浏览器租约前随机等待5至10秒，网络失败导致浏览器重建时也不能绕过；公开浏览器首次预热后等待4至7秒，变体请求间隔3至6秒，额外评论分页间隔2至5秒。前端1秒只负责界面队列交接，403、429、超时和5xx退避重试、10分钟VPN恢复及最多25条串行复用浏览器的规则均保持不变。节流/竞品专项`25 passed`，完整回归`338 passed`（均只有1条第三方TestClient弃用提示），Ruff、Mypy（50个源码文件）及数据库`verify`通过。正式ERP已重启为PID 2780，`/api/health`返回`ok`且首页为HTTP 200；原326条批次仍保持人工暂停断点，已完成75条、待继续251条，未擅自恢复。按既定偏好未执行浏览器自动化
- 运营日报顶部“同周期销量版本差异”和“库存不平”已改为分别统计截至当前日报日仍未处理的历史版本、库存待办，前者已去掉“个商品”单位；“已确认”仍只统计当前日报日。数据完整性说明现在每条均带数据日期，默认读取当前日报日及前2天，并可通过开始、结束日期查询任意范围；所选范围全部说明在固定高度区域内滚动展示，不再截断前8条。Vue TypeScript检查与生产构建通过，最终资源为`index-2tpE0X7g.js`与`index-D2CN5B-7.css`；日报及ERP接口专项`61 passed`（1条第三方弃用提示），Ruff、Mypy及数据库`verify`通过。正式ERP已重启，`/api/health`返回`ok`，首页和新JS/CSS均为HTTP 200；按既定偏好未执行浏览器自动化
- 竞品雷达长列表已完成轻量化：监控链接管理与链接操作记录均使用固定视口弹层，关闭时不创建明细DOM；链接管理可选择每页20/50/100条并只创建当前页链接节点，搜索或切换数量后回到第1页；操作记录首次打开才请求且服务端每页20条。竞品最新状态同样提供上一页/下一页和每页20/50/100条选择，只创建当前页商品行；重复目标跳转会先切到目标页。监控链接与竞品最新状态两个SKU搜索框均可粘贴完整Takealot链接，并通过正则提取PLID匹配。Vue TypeScript检查和生产构建通过，正式ERP已重启并加载`index-7CuIbBd1.js`与`index-Dch9i85E.css`，健康、首页及两项资源均为HTTP 200；数据库`verify`通过。当前326条竞品批次由用户手动暂停，停止前已开始的`PLID70540744`在重启前保存成功，断点保留。按既定偏好未执行浏览器自动化；本次纯前端调整未运行Python测试
- 统一 ERP、运营日报、竞品采集、MySQL 存储与迁移、本机 API、页面导航及原有业务完整测试：`331 passed`（1条第三方 TestClient 弃用提示，不影响结果）
- 竞品可见库存窗口之间的准备链路已压缩：前端正常队列交接由5秒降为1秒；ERP隐藏公开数据浏览器在服务器单任务内串行复用，最多25条主动回收，网络失败、浏览器异常、请求取消和服务退出立即关闭，下条重建；商品变体和评论的正常请求间隔缩短，但403/429/超时/5xx退避保持不变。可见匿名库存浏览器仍逐链接隔离。公开数据/ERP专项`44 passed`、完整回归`331 passed`，Ruff、Mypy（50个源码文件）、数据库`verify`和Vue TypeScript/生产构建通过。正式ERP已重启为PID 30220，`/api/health`返回`ok`并加载`index-DZxR9VZZ.js`与`index-9vYMQDyp.css`；运行批次第215条首次预热约7.3秒，第216条明确记录“正在复用后台数据浏览器”，从链接开始到进入库存浏览器阶段约0.9秒。重启前已打开的旧前端标签仍保留原5秒交接，刷新加载新资源后才使用1秒交接
- Ruff：通过
- Mypy strict：通过（50 个源码文件）
- Vue TypeScript 类型检查和 Vite 生产构建：通过
- `takealot_ops.cli verify`：通过，2026-07-29 未发现数据质量事件
- 店铺授权、迁移和 ERP Web 专项`22 passed`；正式 MySQL 已新增`erp_stores`、`erp_user_stores`及`erp_users.store_access_all`，升级前3个账号全部保留为全店账号，当前数据登记为唯一`current`已接入店铺。变更前一致性备份`takealot-20260730-025306-812858.sql.gz`二次校验通过；升级后数据库`verify`确认2026-07-30无质量事件。正式ERP已重启并加载`index-DkGT1Ka-.js`与`index-FHzvI_VO.css`，健康、首页、JS、CSS及认证状态接口均为HTTP 200；构建JS已核对包含顶栏店铺选择、待接入安全占位、店铺目录、账号店铺范围、全店动态范围和`/api/auth/stores`
- 公共模块与店铺门禁分离专项随 ERP Web 文件共`20 passed`：0店铺账号可查看/采集竞品、请求共用缩略图并使用 NFT102，店铺经营与新鲜度接口仍返回明确的店铺授权403；完整回归`326 passed`，Ruff、Mypy（50个源码文件）、数据库`verify`和Vue TypeScript/生产构建通过。正式ERP已重启并加载`index-H_hYjjNj.js`与`index-CA6_R_Rk.css`，健康、首页、JS、CSS及认证状态接口均为HTTP 200
- 商品缩略图缓存键锁已改为活跃使用人数计数并在最后一个使用者退出后删除；同键并发仍只转换一次，成功、失败和异常路径都不会滞留历史锁。专项`18 passed`，正式ERP重启后健康接口返回`ok`；新进程启动基线工作集150.1MB、私有内存475.4MB、341个句柄，项目Playwright/Chrome/Node残留为0
- 竞品链接清单变更前已生成并复核一致性备份`backups/takealot-20260729-101244-504064.sql.gz`，SHA-256为`97113ed49165234d069e87af20d737595a47941eb2cb32b583932f78ef24056f`；正式MySQL已无损新增`competitor_target_audits`表。链接管理现默认归纳为总数/已有历史/待首次采集汇总，关闭时不创建明细DOM；点击“打开管理”后使用固定视口弹层搜索并每页最多渲染20条，不再推动下方竞品长列表整体重排。操作留痕也使用固定视口弹层，首次打开时才按北京时间日期条件由服务端每页返回20条。正式ERP加载`index-Cb7sjgnP.js`与`index-BKyAdKPP.css`，健康接口返回`ok`；在第221条成功后切换，切换窗口刚启动的第230条没有结果并继续保留在待续爬队列，原326条批次恢复后已继续到第269条，待重试由11降至9。按既定偏好未执行浏览器自动化
- MySQL 迁移：12张表4204行与旧 SQLite 逐表数量一致；ERP 只读事务拒绝 UPDATE，五个核心 API 为 HTTP 200；`CHECK TABLE`、HTML/Excel/PNG 读取及 `mysqldump` 备份通过。本地备份现使用 `.sql.gz + .json + .sha256` 文件组，落盘后重新校验完整压缩内容，并按14天全量、90天每日、1年每周、7年每月分层保留；旧 `.sql` 不自动清理
- 全部备份产物固定在 `D:\南非店铺数据抓取\backups\`；已创建专用 `takealot_backup@localhost` 账号并启用 `mysqlbinlog --raw --stop-never` 连续归档、失败后每分钟重启、每日闭合文件校验与SHA-256、35天清理和同步差距检查。当前Windows账号无任务计划注册权限，已自动降级为当前用户登录启动项，重启后需登录Windows才会继续。正式归档4份共72520481字节、同步差距0字节，3份闭合文件校验通过；完整备份`takealot-20260729-103411-686192.sql.gz`已记录`DESKTOP-NTRMANG-bin.000004:72519292`。MySQL位于C盘数据目录的活动binlog只是运行源，不作为备份副本；隔离库PITR恢复演练仍未执行
- PowerShell 日报脚本语法检查：通过；连续日期文件名后缀归一化样例通过
- CodeGraph 1.5.0 已安装并接入 Codex MCP；排除可重建前端`dist`产物后的初始图谱识别110个源码/测试/配置文件、2801个节点和8211条关系，其中包含88个Python文件、11个Vue组件、8个TypeScript文件和62个FastAPI路由。初次唯一读取错误来自工作区已删除的旧构建文件，应用`codegraph.json`后重建索引已消除；本地索引保存在`.codegraph/`且只提交其忽略规则，项目专用查询和验证边界记录在`docs/CODEGRAPH_AI_GUIDE.md`
- 真实 MySQL 当前包含6张运营日报业务表，其中`daily_inventory_snapshots`按实际北京时间日期和实际成功时间保存不可覆盖的早间首次成功库存；正式 `8501` 前端不再显示“运营日报”模块。四个 Windows 任务固定安装为中国时间10:05、18:00、次日09:00和18:30，其中三个采集时刻都只是计划触发时间。恢复7月27日原始早间库存时创建的`recovery_daily_report_observations_20260727`暂存表已清空为0行且不被程序读取；受限应用账号没有`DROP`权限，需在后续数据库管理员维护窗口删除该空表。
- ERP 正式绑定 `0.0.0.0:8501` 供受信任局域网访问；MySQL 仍只连接 `127.0.0.1`。首个管理员只能从服务器本机初始化；查看、运营、选品、管理员仅为快速模板，实际授权读取账号级权限集合。管理员可逐项修改现有前端权限，变更后既有会话失效；至少保留一个启用的用户权限管理账号。日报权限键及接口鉴权保留在后端兼容流程，但不在统一 Vue 用户权限页显示
- 登录与自动恢复会话的 Cookie `Max-Age` 为604800秒；有效请求会在距离上次续期至少1天时把 MySQL `expires_at` 和浏览器 Cookie 同步顺延至最近使用后的7天，1天内重复请求不再次写入或发送 Cookie。连续7天无有效使用才自然过期，账号安全变更仍立即撤销
- 侧边栏最近成功采集时间和最新指标日期会在可见页每15秒及页面重新可见时自动读取本机只读状态，无需手工刷新浏览器；隐藏页暂停定时请求，短暂服务异常保留最后一次成功值。运营日报事件不再触发前端重读。自动读取不触发Takealot、采集任务或数据库业务写入；“立即刷新看板数据”仍复用完整 `daily-run`，错误提示不会回显子进程输出或 API Key
- 最近采集及竞品历史快照固定按北京时间（Asia/Shanghai）显示；最新指标日期继续按 SAST 业务日口径，不混用时区
- 商品中心的404条真实商品栏均直接展示平台缩略图或“暂无图片”占位；点击或键盘激活商品栏后只在当前视口打开唯一详情弹窗，页面底部不再重复渲染详情。弹窗包含商品身份、6项经营指标和唯一的每日销售件数快照，历史区不再展示流量快照；销售柱图纵轴使用整数数字刻度。弹窗继续支持遮罩、顶部/底部关闭入口、Escape关闭、背景滚动锁定及焦点恢复。正式 Chrome 以真实商品 `WOVIBO RGB Gaming Light Bars` 验收：销售快照标题1处、两个旧流量快照标题均为0处，10根历史柱的纵轴显示“件数”及整数刻度`1、0`，控制台错误为0
- ERP 前端已删除“报表工作台”、NFT102、“风险与质量”和“运营日报”的导航、页面、专用前端权限项及调用代码；对应后台接口、数据、计划任务和命令行/导出兼容流程未删除
- “经营四象限”已改为不带分类限制的“经营坐标”：页面只保留近30天浏览量相对排名与近30日下单件数相对排名两个维度，移除宽松/标准/严格分位选择、五类统计筛选卡、四象限区域名称、分类配色和十字分界
- 经营坐标只绘制同时具备两个真实指标的商品，缺失浏览量或下单指标的商品不按0放入图中，仍保留在“全部商品”区域；页面明确显示已定位和缺少坐标的商品数
- 两轴0、25、50、75、100关键排名刻度已移到散点图框外，使用高对比白色数字标记，图内只保留25/50/75轻量网格；桌面与390像素窄屏逐刻度核对均未与散点重叠
- 经营坐标散点统一使用高对比蓝色；无等待悬浮卡继续显示商品图片、平台 SKU、近30天浏览量、近30日下单、平台库存、首次上架/最早记录、最近补货和数据截止日期，点击小点直接复制 SKU；“全部商品”使用自适应卡片完整展示上述全部字段，名称和值自动换行、整页无需左右滑动，并提供按浏览量、下单、平台库存或商品名称排序的选择器，以及忽略大小写、支持部分匹配的平台 SKU 即时搜索框；页面持续显示当前结果数与总数，无匹配时显示空态，缺图显示“暂无图片”
- Vue ERP 现存商品图片入口已统一为登录态同源多尺寸 JPEG 缩略图：商品中心、经营坐标、竞品雷达及其弹窗/历史区不再直载平台高清原图。列表、卡片、表格和小型头图使用192像素，商品中心大图使用640像素；后端只允许读取`takealot.s3.amazonaws.com`和`media.takealot.com`两个官方主机的`/covers_images/`及旧竞品仍会返回的`/covers_tsins/`路径，禁止任意重定向，限制原图字节、像素及可选尺寸，并按尺寸缓存到Git忽略运行目录。搜索定位的Seller Offer模型主图资格仍只接受`/covers_images/`，没有因缩略图兼容而扩大。`PLID58371612`正式19次商品快照实际全部保存`media.takealot.com/covers_tsins/61498554/...`图片，旧代理因只放行`covers_images`而显示“暂无图片”；修复后服务器真实下载并转换为192×192、5571字节JPEG。同一缓存键的并发首次请求继续串行且只转换一次，键锁按正在使用人数计数，成功、失败或异常退出后由最后一个使用者立即移除，不再随历史图片数量长期积累。全部图片声明固有尺寸并异步解码，长列表懒加载且跳过离屏布局/绘制，商品/竞品重复项使用Vue记忆化降低无关状态更新。
- 单品分析和店铺总览的每日下单件数图仅保留整数柱形，不再绘制会产生小数的7日移动平均线；四象限真实件数和分界也均按整数显示
- 竞品雷达商品级区域继续展示商品快照主图；非默认变体公开详情的 gallery 图片已按变体、按快照独立保存，自有Seller API Offer也投影自身TSIN与图片。当前变体表和每个区间历史快照均展开当时全部变体的可读名称、库存、限购标记及独立图片；在详情对比台切换真正竞品或自有商品的具体变体/报价时，左上主图同步切换为所选项图片。默认款或所选报价缺少独立变体图时回退到所属商品快照主图；颜色、尺寸等非默认旧变体缺图才显示“暂无图片”，不拿其他变体主图冒充，API 字典/JSON 变体值也不会直接出现在名称中
- ERP 前端已彻底移除报表工作台、NFT102 上传续写入口、风险与质量及运营日报模块；既有 NFT102、风险/质量与运营日报后台能力继续保留用于兼容流程。下列日报页面交互条目是删除前的历史验收记录，不代表当前统一 Vue 前端仍有入口；其中数据、调度、审计与导出规则仍适用于保留的后端流程
- 运营日报按北京时间每日10:00至次日10:00归集同一日报日，保存不可覆盖的10:05早间、18:00晚间以及周期内任意次数的手动刷新销量版本；例如7月25日10:00至7月26日09:59全部归入7月24日。每个自然日10:05后首次完整成功取得的库存另存到`daily_inventory_snapshots`并按实际北京时间日期固定；业务日D的日报统一采用D日整日销量与D+1日库存。候选可以来自早间、手动或晚间采集；早间库存漏爬或不完整时采用当天随后最早的完整成功库存，一旦完整即固定，不再被任何更晚拉取覆盖。早间或恢复库存完整后若相邻日报值齐全且库存连续性公式不平，立即持久化并推送库存待办，不再只动态红标而等待晚间；后续销量版本差异优先接管，公式恢复时自动解除。普通HTML/Excel/PNG也按该业务日生成；页面不再提供日报业务日期选择器，按10:00边界自动读取当前周期，加载最近30个有数据日并通过内部纵向滚动查看，视窗一次最多完整显示4日且默认定位最新日期。人工候选值与采集版本并存，可重复修改并回填上次值和原因，人工修改无需填写操作备注，每次保存均追加前后值审计；版本差异待办中的人工订单和库存若已使相邻日报日公式相符，保存时即自动采用人工值、写入可撤销确认留痕并关闭整条待办，无需再点击“消除差异”，只有公式仍不相符或暂不可计算时才继续待处理。其他最终确认仍可选择最新拉取、早间、晚间或人工值且强制填写备注。已确认项可从日期备注行的确认记录撤销，原确认不删除，撤销原因追加审计并恢复待核对；相邻下一日报日保留“撤销确认后库存待重算”待办，重新确认后按新值重算并转为真实冲突或自动解除。近30天浏览量保留全部版本与底层数据但不参与合并判定。同周期只比较销量，任意成功版本销量不同才推送待办；销量一致则不推送版本待办。销量版本确认后再用相邻两行已对齐的期末库存计算连续性。顶部手动刷新次数只统计当前周期。
- 次日09:00周期末任务自北京时间2026-07-30起运行，仍归入10:00结束前的同一核对周期并参与全部销量版本比较；其库存只留痕，不替换D+1日早间首次成功库存。更早业务日返回“尚未启用”，不产生周期末漏采问题。
- 运营日报采集保护分四层：HTTP请求对网络异常、429和5xx有限退避重试；整批失败后用新客户端再次采集；第三次切换为`trust_env=False`直连以绕开错误代理；计划任务收到非零退出码后每5分钟重启、最多3次。每次整批尝试的策略、时点、阶段结果和脱敏原因写入日志与`daily_report_runs.counts`，页面可展开查看；全部路径失败才生成带最终根因的漏爬记录，不允许使用陈旧缓存伪装采集成功。
- 早间10:05、晚间18:00和次日09:00周期末更新只表示计划触发时间；`daily_report_runs.captured_at`、早间库存快照、页面状态、版本名称和导出均使用最终完整成功的实际北京时间。内部重试或Windows任务第三次重启后才成功时，必须显示实际成功时间，不能仍把版本写成计划时刻。
- 2026-08-01 起采用库存恢复规则，2026-08-03 起将恢复选择进一步固定为D+1当天10:05后首次完整成功值。早间漏爬或不完整时，随后最早的晚间/手动完整成功库存作为恢复值并立即锁定，更晚拉取不再覆盖。补齐后清除商品级漏爬占位，页面与 Excel 库存行统一改称“平台库存数量（次日实采）”，在库存/日期总格备注“早间库存漏爬已解决”、来源、精确到秒的实际北京时间和“周期内首次完整库存”；原失败原因继续保留在运行记录和 Excel“漏爬说明”中。当天结束仍没有完整恢复值时才保持库存为空。
- 正式库已用北京时间2026-08-01 11:16:03的手动完整拉取补齐业务日2026-07-31所需库存：`daily_inventory_snapshots`共410条、库存空值0条、库存合计582，日报商品级`missing_capture`为0；早间和此前手动批次的HTTP 403原因仍作为整批说明保留。`Cancelled by Takealot - DC Stock Inquiry`已按取消单排除，2026-07-28以来未知销售状态质量事件复核为0。
- 日报页面和 Excel 主表保留原工作表的商品横向布局，页面读取最近30个有数据业务日并纵向堆叠；每个日期固定显示近30天浏览量、当天订单数、平台库存数量（次日实采）、备注四行，已经完全移除“当天访客数”。页面日期列在订单行和库存行分别显示该日报日全部商品的订单合计与次日实采库存合计，合计只显示数字，不附加“单”或“件”，统计不受搜索、筛选或横向滚动位置影响；任何商品对应值缺失时不显示部分合计，而是显示“—”及缺失商品数。商品列标题下方只显示 SKU 值，不添加“平台 SKU”前缀。页面左侧固定“指标”列宽170px，“日期”列固定偏移同步为170px，两列无空白缝隙。独立备注按通用、版本、库存类型以括号展示；当前有效的确认合并备注显示为“（确认：备注内容）”，确认合并后继续确认库存差异时，再并列显示“（库存差异已确认：备注内容）”，页面与 Excel 备注行保持一致。撤销相应确认后只移除对应的当前有效备注，审计历史继续保留。拥有“处理运营日报待办”权限的账号可点击备注行管理。页面表格内部支持上下滑动，视窗最多完整展示4日数据并在打开时定位到最新日期；商品名称与纯 SKU 表头保持吸顶，并通过矩阵滚动边界隔离避免查看较早日期时把滚动串到外层页面。筛选后的商品全部一次放入同一横向矩阵，不再按24列翻组，也没有“上一组/下一组”，直接使用矩阵底部横向滚动条查看到最后一列；鼠标位于该滚动条时，普通滚轮改为横向滚动并阻止纵向页面移动，离开滚动条后恢复正常纵向滚动。页面默认收起大段漏爬原因，商品表头用细色条表达状态，日期块、销量、库存不平和字段缺失使用相互独立的底色。计划执行时间未到的早晚批次显示“等待计划时间”，不会提前误报成漏爬。
- 日报宽表已限制在矩阵内部滚动，不再撑宽下方内容；“只处理真实差异”固定为全宽单列卡片，右侧人工修改、确认合并和加备注按钮在桌面及窄屏布局中均保持可见。待办状态不再笼统显示“数据有差异”：同周期只显示“销量版本有差异”，跨日显示“前后日报库存不平”，人工确认后触发时增加对应前缀；版本问题与库存问题采用不同颜色。
- 商品漏爬只看本周期最终采用值：当天订单数或平台库存任一为空才标记，近30天浏览量为空不算漏爬，采集整批失败或商品未出现在某一批次但订单与库存已由其他成功版本补齐时也不打商品漏爬标识。业务日D所需的D+1日早间库存漏爬或不完整时先记录实际快照日期和原因；D+1当天10:05后的首次完整成功拉取会补写库存、清除已补齐商品的漏爬占位，并在库存/日期总格保留恢复来源和实际北京时间；此后更晚采集不覆盖该库存，当天结束仍没有完整恢复值时才保持为空。页面顶部“同周期销量版本差异”不带单位，统计截至当前日报日仍未处理、带`capture_difference`问题的历史待办；“库存不平”统计同一截止范围内带`stock_continuity`问题的历史待办，“已确认”只统计当前日报日。近30天浏览量缺失仍保持缺失并在矩阵中显示。具体采集失败、商品缺席和字段空值原因仍在运行日志及 Excel“漏爬说明”工作表中保留；页面说明逐条显示数据日期，默认近3天并支持任意起止范围。漏爬不进入确认清单、不形成历史待办、不阻止Excel导出；只有同周期任意成功版本的非空销量不一致才是需确认的版本差异。
- 已先生成MySQL一致性备份，再将首次运行误归7月25日的1个晚间批次、404个商品状态、1条截止审计和1条截止快照整体改标到7月24日；修正后7月25日运营日报为空，商品原值和采集时间未改动。
- 库存核对的系统值固定采用每个业务日次日10:05期末库存快照，人工确认库存值仍可优先覆盖；`前一日报日正确库存 - 当前日报日正确订单 != 当前日报日正确平台仓可售库存` 时红色提示。按自然日展开就是“D日早间库存 - D日整日销量 = D+1日早间库存”。确认合并只确定正确销量版本或明确人工值，不自动豁免库存连续性；确认后公式仍不平会保留或新建库存待办。库存待办期间若出现新的销量版本差异，先暂停跨日核对并展示确认基线与新销量版本，重新确认后再计算。纯库存待办二次打开人工修改时，订单输入固定重置为当前正确销量，不再沿用旧人工候选销量；版本差异未解决时页面明确提示先确认合并，库存操作遇到服务端状态变化会自动刷新并展示当前正确按钮。撤销前一日报确认时，前一日报日恢复确认待办，后一日报日同时保留带完整撤销上下文的“撤销确认后库存待重算”待办；重新确认后立即转成真实库存连续性待办或自动解除。具备权限的运营账号填写原因后可把库存异常确认为“差异属实”，但只关闭待办，不清除页面矩阵、库存筛选、异常统计或 Excel 的红标。“待办处理留痕”现覆盖最近100次全部人工操作：人工候选、备注新增/修改/删除、确认合并、库存差异确认及两类撤销均独立成行，每条明确展示商品、SKU、日报日、操作人和北京时间；人工候选补充前后值且允许操作备注为空，确认合并和库存差异确认继续强制填写备注，确认记录补充公式或采用来源，备注修改/删除补充修改前后或被删除内容。当前有效确认可直接撤销，撤销本身单独留痕，原处理记录继续保留撤销人、时间和备注；库存确认撤销后公式仍不平会自动恢复待办。列表可按北京时间处理日期和日报数据日期独立或组合筛选。历史商品已不在当前商品表时，名称和SKU从对应日报日采集快照恢复，并优先补充当前或历史快照商品图。备注支持新增、修改、删除并按通用、同周期差异或库存连续性关联展示；所有人工操作统一检查“处理运营日报待办”权限。修改通用备注只填写新正文，删除通用备注无需填写原因但仍保留被删除内容、操作人和时间；删除版本/库存问题备注仍须填写原因，任何备注操作均不改变待办状态。
- 库存连续性待办的最终动作按人工候选值重算结果强制互斥：修改后公式相符时只允许“消除差异”，采用人工值、关闭待办并去除红标；修改后仍不相符时只允许“确认库存差异”，采用人工值并关闭待办，但继续保留库存异常红标。两个接口都会独立重算并拒绝错误动作，不能通过绕开页面误处理。“消除差异”单独进入待办处理留痕并可按确认值撤销；再次修改已确认库存差异的商品会自动重新打开核对状态。
- 人工核对待办卡和待办处理留痕卡的一键定位均完成“SKU筛选 + 数据日期跳转”：点击卡片非操作区域后自动筛出对应平台SKU，把页面带回日报矩阵，并在矩阵内部滚动到记录自己的业务日期，日期起始行以绿色描边突出；卡片操作按钮继续互不干扰。目标日期超出当前日报表读取范围时页面返回明确提示。两类卡片及其操作/备注弹窗的商品身份区显示商品图，缺图显示“暂无图片”；日报横向数据表不增加图片。
- 日报顶部“历史未合并”提醒只统计当前日报日之前的数据；页面现在明确显示“不含当前日报日”，并同时列出当前日报日待办数和下方历史加当前的合计数，避免与完整待办列表数量混淆。
- 2026-07-28 已在一致性备份`backups/takealot-20260728-040343-062804.sql`后修正正式历史中“库存挂早一天”的问题：业务日D的全部日报观察统一改接D+1日10:05期末库存，共更新3646条观察、重算662条状态。7月24日因缺少7月25日快照仍保留404条明确漏爬；7月25日改接7月26日库存，7月26日改接7月27日库存，7月27日改接7月28日库存。随后按用户要求在备份`backups/takealot-20260728-054911-517635.sql`后删除两件演练商品在日报库存、观察、处理、审计及截止快照中的全部137条关联数据；直接引用和快照JSON引用复核均为0。真实404个商品中，7月26和27两个自然日区间均有403个公式成立，所有发生销量的商品分别16/16和15/15成立。
- 18:30 对真实未确认差异形成 MySQL 持久快照；旧 ERP 全局提醒已随运营日报前端模块移除，漏爬仍不计入待办。任何截止日期及之前的真实未合并项会阻止旧表布局 Excel 导出，并明确返回日期、SKU 和商品。
- 异常口径已统一：2026-07-23 最新指标日为289个去重异常商品、299条异常记录；历史累计340个商品、986条记录继续保存在数据库并由后台风险投影提供，Excel、HTML、PNG 默认不混入历史日期
- 风险后台接口继续提供异常类型、历史发生日期和具体触发证据，既有判定规则与数据库记录不变；当前 ERP 前端不再提供风险卡片、筛选或详情弹窗
- 2026-07-23 的 HTML、Excel、PNG 已按新口径重新生成；Excel 总览为289个异常商品，异常明细299行且日期仅为2026-07-23，HTML嵌入数据核对结果一致
- Windows计划任务安装脚本现会建立早间采集、晚间复核、周期末更新、截止待办、竞品雷达每日采集及ERP登录自启动六个任务。前五项默认中国时间分别为10:05、18:00、次日09:00、18:30、09:00；两个09:00任务分别处理运营日报周期末与竞品共享批次。`Takealot ERP 登录后自动启动`以当前交互式用户、有限权限在登录后延迟30秒运行，先等待`MySQL80`并检查`/api/health`，健康时不重启，未运行时才调用正式`restart_erp.ps1`，使用跨进程互斥避免重复拉起，失败每分钟最多重试12次且允许电池供电。使用当前用户而非SYSTEM是为了保留用户级环境变量、Windows凭据和可见浏览器能力；因此开机后需要正常登录Windows，进入桌面后无需手工启动。竞品动作只运行`trigger-competitor-collection`，按日写持久触发并从loopback唤醒ERP；错过后补跑、忽略同一任务并发，动作失败每5分钟最多重试3次。安装时删除旧“Takealot 自有商品跟卖自动追踪”00:30隐藏任务；旧CLI只保留人工兼容，不再允许与页面批次并行。
- 页面固定控件、字段、状态和说明均使用中文；平台原始商品名称、店铺代码、商品编码及南非货币符号保持真实原值
- 旧版“NFT102 日报更新”“风险与质量”和“运营日报”页面的前端验收记录已失效；当前 ERP 不再显示对应导航、上传按钮、续写页面、风险页面或日报页面
- 统一 Vue ERP 生产构建通过；正式单服务已在 `0.0.0.0:8501` 运行，本机与 `192.168.110.180:8501` 均返回 HTTP 200；本机初始化页显示首个管理员表单且无控制台错误，局域网地址的认证状态明确禁止初始化。当前尚未代用户创建管理员，需由用户本人输入首个账号和密码后再进入业务模块
- 当前11个竞品链接保存在 MySQL，重新采集后已有13条商品快照、14条变体结果和425条共享去重评论；`PLID70540744` 最新精确库存为4，`PLID99275672` 最新精确库存为10
- 2026-08-04只读快照中，六个已接入店铺共有1524条Offer、1241个店铺内自有PLID、跨店去重后859个；逐店去重数为VoltTech ZA 327、VeldBox 220、YeboShop 187、T C STORE 165、INDLELA YEMIKHIQIZO PTY LTD 303、AGG AUTOMAN (PTY) LTD 39。历史人工竞品清单中47个实际属于六店的自有PLID已全局排除出真正竞品。当前手动和09:00自动批次都包含真正竞品与自有PLID统一队列；页面顶栏范围只改变查看投影，不缩小采集。859只是该日快照而不是永久目标数，以后Offers完整刷新出现的新PLID会在批次开始时自动进入，运行中重扫发现时追加队尾。`PLID96909926`正式库已有6条PLID共用评论和2个公开变体；无论是否存在跟卖都展示已取得的评论、评分、好中差评结构和公开变体。
- 浏览器竞品批次断点已升级为版本9，首次加载会主动删除版本1至8的旧断点，因此历史647条混合“待爬取”不会再被恢复；新手动批次按当前规则冻结真正竞品和账号可访问的全部自有PLID。09:00自动批次另用服务端持久断点保存冻结目标、当前项、已尝试/失败/终态、重试队列和逐链接结果，不依赖浏览器页面。新增真正竞品继续正常写入、审计并进入活动队尾或下一批；任一店铺自有PLID不写人工清单，接口返回真实店铺并提示已自动纳入统一每日批次。
- 已修复刷新/切店后把当前标签页误标为“本账号另一页面”的客户端身份竞态：页面先从未完成断点恢复客户端编号，卸载时主动关闭同源广播；只有另一实例连续回答两轮探测才换发编号。刷新旧实例只短暂回答首轮时仍归属本页面，真实复制标签页继续保持隔离。新增3个前端回归覆盖瞬时旧页、真实第二页和无占用三种情况。
- 竞品雷达组件在ERP导航切换时保持人工任务实例；采集中切换页面不会清空输入、进度和逐条结果。自动任务不要求页面预先打开，任何账号中途进入竞品雷达都会从服务端看到同一当前进度与明细。
- 竞品监控链接已改为 MySQL 持久化清单。具备竞品采集权限的普通运营只可新增链接，新增前按目标PLID定向检查自有Offers并自动进入提交账号的个人监控池；命中真正竞品时才写全局目标与活动批次队尾，命中自有商品时不重复写真正竞品，但继续沿用每日自有全量巡检。管理员才可修改、删除、插队、查看操作审计和失效复核，后端同步执行角色限制。修改不能改变PLID，删除只停用后续真正竞品监控并保留全部历史快照。重复目标无论是否已有快照都幂等确保个人池关系并定位顶部独立个人池卡片；自有命中只更新当前个人卡片、不再加载整页自有投影，无快照显示等待首次采集，有快照显示完整业务摘要，不改变下方筛选或跳到真正竞品分页。管理员链接区默认只显示总数、已有历史和待首次采集汇总，关闭时不创建全部明细节点；点击“打开管理”后使用固定视口弹层，支持名称/PLID/完整链接搜索并每页只渲染20条，完整Takealot链接自动提取PLID匹配，不再推动下方数百条竞品行重排。新增、修改、删除和从待重试详情发起的人工重试都追加操作人、时间、PLID和URL审计，其中人工重试使用独立`manual_retry`动作；管理员“链接操作记录”同样使用固定视口弹层，首次打开才按北京时间开始/结束日期请求数据，服务端只返回当前20条分页记录。新增真正竞品链接若遇到活动批次会按新增顺序写入服务端待执行队尾；修改和删除只影响未来新批次。
- 竞品商品采集现在枚举全部公开变体的主报价和`other_offers`。PLID只作为商品线和爬取目标，同一PLID只入队及抓取一次；该次公开数据返回的全部卖家报价保存在同一快照中，优先按公开`offer_id`或`other-buying-option-<SKU>`购买卡片标识区分，缺失时以卖家、SKU、变体和条件组成稳定回退键。只有明确指向不同Takealot URL/PLID的报价才形成另一个目标；原始提交链接始终保留，SKU、卖家ID、纯数字商品字段不会被猜造成链接。不同PLID新目标按PLID幂等写入并以`auto_discover`审计，活动批次只追加新建或重新激活的成员。监控链接弹层按原始/跟卖关系分页和下拉折叠，关闭组不创建成员DOM，展开后显示原始链接及全部报价的Offer ID、卖家、价格、报价级涨跌和各报价自己的库存状态/变化。启用库存探测时，同一商品隔离会话逐个加购变体主报价与可购买跟卖报价，跟卖同时通过购买卡片ID/SKU、卡内卖家、购物车PLID和购物车卖家校验，数量只绑定对应报价；供应商调货或明确不可加购记0，校验失败则保持缺失并进入库存复探。报价库存按同一报价身份比较所选区间首尾，范围或证据不完整时明确不可比
- 活动批次中新保存的监控链接由服务端按保存先后进入待执行队尾，不中断当前Playwright请求，也不改变断点中已经冻结的原任务顺序；连续新增按先进先出排序，刷新和断点恢复继续使用服务端队尾顺序。管理员监控链接明细提供手动“插队”：服务端登记目标PLID、请求人和时间，持有页面轮询后在当前商品之后额外增加一次优先探测；目标在原队列中的位置继续保留，走到原位置时仍按原顺序再探测一次。待重试任务的管理员链接操作页提供“人工重试并插队”，使用独立`manual_retry`来源记录操作者和时间；同一指令尚未消费时重复请求幂等，前一条已经开始后可再次人工重试。未消费的队尾追加项和手动插队指令持久化到`logs/competitor-batch-queue.json`，ERP重启后同批恢复继续执行，开始目标后移除待执行项，批次无剩余项完成后清理
- 页面人工批次仍仅用户名`kxx`可开始、继续、自动续爬和停止，其他账号即使使用管理员模板也被后端拒绝；唯一自动开始例外是服务器本机loopback的09:00持久触发。自动批次的人工停止和停止后的显式断点继续同样只允许`kxx`。具备竞品采集权限的管理员可管理全局链接、插队、审计和失效复核，非管理员只可新增链接并管理自己的个人监控池。
- `kxx`可在人工或09:00自动竞品队列执行期间切换“显示检测浏览器”；自动批次虽由`scheduled-task`持有也允许`kxx`修改，其他账号仍由后端拒绝。当前链接保持启动时模式，下一条链接读取最新选择；人工批次同步写入本地断点，自动批次写入服务端运行断点并在ERP重启后恢复。
- 活动竞品批次的来源、进度条、成功、待重试、确认失效、已检查、待续爬、当前PLID、后台阶段和逐链接`results/errors`统一使用服务端共享状态，管理员页面显示同一口径；普通运营不渲染或轮询该技术模块。09:00自动批次结束或停止后继续在管理员模块展示该批最终共享结果，非持有页面不混入旧本地断点；ERP重启导致进程内共享投影清空时，停止批次的最终统计和可续标志从服务端定时断点重建。
- 竞品雷达使用服务器级单批次互斥和单活动链接租约：人工批次由一个账号的一个浏览器会话持有，09:00批次由正式ERP服务持有且不依赖页面心跳；两者共用相同registry、请求编号协调和Playwright配置，不能并行。人工页面刷新可用相同请求编号接回，任何不同请求编号在上一条运行时返回423。管理员页面每2秒同步来源、操作者、已检查/总数、成功、待重试、确认失效、待续爬、当前序号、PLID和后台阶段；运营新增目标仍由服务端自动追加队尾。人工页面每10秒心跳且空闲2分钟可释放；自动批次由持久断点和服务端心跳维持，连续2条网络失败暂停10分钟时仍保持管理员可见和可停止。`kxx`从任意管理员页面按共享批次编号停止后，当日触发记为已处理，不会自动重启；当前Playwright请求取消清理完成后才释放，避免残留浏览器并发。停止清理完成后可由`kxx`调用专用恢复接口复用同一`batch_id`；服务端按冻结索引重建失败和未执行队列，跳过本批成功与确认失效项，前端存在该可续断点时隐藏新建人工批次入口，避免误点“开始采集”改变续爬语义。
- 09:00竞品自动触发按北京自然日只尝试一次取得共享批次槽：触发时已有活动批次，或读取目标期间被人工批次先取得槽，都会把当天触发持久标记为`skipped_busy`和已处理；ERP重启时持久断点中的未完成自动批次即使尚未重新登记到内存注册表，也按活动批次处理新触发。自动任务不再等待手动批次结束后延迟启动，不会停止、接管或改写现有批次，次日09:00再正常尝试。已经成功启动的自动批次内部网络暂停、延时重试和显式断点继续仍沿用原`batch_id`，不受这条启动冲突规则影响。
- 普通待重试项使用按任务数计算的指数退避：404疑似失效、409页面不确定、单条连接失败及5xx等可恢复失败的三次自动重试前分别至少执行1、2、4条其他待执行链接；当轮任务不足以满足间隔时不退化为队尾立即重试，继续保留给人工插队或后续继续。库存未探测总计最多3次探测，两次自动复探前分别间隔1条和2条其他任务。续爬队列优先保留明确失败索引，不会再因同一PLID曾有成功/插队结果而提前归零，任一次后续成功会清除旧失败状态。历史日志确认2026-07-27批次的`PLID101534220`和`PLID99141891`曾在21:00以`pending=2`结束，直到21:03人工继续后才完成复核，本次在保留失败索引基础上进一步加固了复排策略
- “刷新全部数据”增加跨用户全局状态和MySQL持久化的1小时冷却：任何成功刷新都会让所有非管理员共享同一倒计时，管理员可越过冷却但不能与正在运行的刷新并发；所有页面同步刷新人、运行态、最近成功与剩余时间，服务重启不清空冷却，失败刷新不启动冷却
- 竞品库存探测中任何快捷数量或自定义数量校验只要出现购物车 `current stock = N` 明确提示，就立即把 `N` 保存为精确平台仓库存并终止该变体的后续翻倍与二分；检测到 `Limited to N per customer` 时会实际测试 `N` 件，能够加入则只记录“至少 N”并持久化每位客户限购标记，不把促销限购误判为精确库存，无法加入才在限购值下方继续校验。数量控件先显示 `Qty N` 时会等待3秒提示结算窗口，避免异步仓库警告稍晚渲染后仍被误判为成功。9 件可加入后先验证 10 件，出现 `We currently do not have 10 in stock.` 或 10 件被明确拒绝时记录为精确 9 件；输入框未出现时仍检查页面弹窗，但单纯打不开 `10+` 且没有明确缺少 10 件提示时继续保守记录“至少 9 件”。仅当 10 件被明确接受时才继续超量测试和二分。首次数量菜单现在等待异步动画并最多重开3次，仍无数字选项时刷新购物车一次并重新按同一 PLID 定位；最终失败仍保留当前空库存，但 SKU、卖家和变体集合一致时页面显示上次成功库存及北京时间并标记为过期参考，参考值不参与库存净流出和销量计算。隐藏的 `aria-hidden` 数量节点不再被误认成已打开编辑器，临时购物车更新错误会在重新加载后重试，阻挡目标控件的 Braze 营销遮罩会被定向清理。此前真实隔离会话已验证 `PLID70540744=4`、`PLID99275672=10` 且均为精确值；真实 MySQL 的 `PLID95565512` 当前失败快照已只读验证可显示上次成功精确9件及时间，同时本次库存净流出仍为空。公开数据标记为供应商调货/长时效到货的商品统一显示“没货”，不计有效平台仓库存且不参与库存净流出销量推断
- 勾选库存探测后，可购买变体最终返回`failed`时仍保存已取得的商品、评论和变体快照，但整条链接返回独立424并进入待重试，不计连接失败；每个失败变体按名称、SKU、失败阶段和精简技术原因保存并写入424结果，例如`打开购物车失败：连接在页面加载时被关闭（ERR_CONNECTION_CLOSED）`，前端运行提示和批次日志因此可直接说明未探测原因。两次自动复探前分别至少执行1条和2条其他待执行链接；若当轮剩余任务不足，不立即复探而继续保留待重试。主动关闭库存探测、供应商调货和明确不可加购不进入复探
- 竞品购物车定位已限制为当前目标 PLID 商品页的主购买区，明确排除推荐商品卡片；购物车中必须再次按相同 PLID 定位数量控件，不再以“唯一购物车商品”作为目标身份回退。4个真实链接的 DOM 核对通过，`PLID95526981` 真实隔离购物车验证取得精确库存90并完成清理；`PLID99275672` 的异步加购等待后能够稳定找到同一 PLID
- 平台拒绝普通 HTTP 客户端后，竞品公开商品/评论采集已切换为 Playwright 驱动本机 Chrome/Edge，在一个会话中逐地址导航公开 JSON；采集器、ERP 接口和 CLI 已完成异步改造。公开商品数据会递归枚举尺寸、颜色等选择器组合并清洗嵌套对象值，MySQL 按商品快照保存每个变体的 SKU、卖家、价格、独立图片、库存结果和客户限购标记；自有Seller API当前Offer补充TSIN和图片，页面按TSIN优先识别跨店重复的同一变体。库存探测复用另一个隔离会话逐个变体执行，评论仍只按 PLID 抓取和去重一次。只有前后变体键、SKU 与卖家集合一致时才允许比较汇总库存，避免变体范围变化制造虚假销量信号
- 竞品 Playwright 生命周期已覆盖梯子/代理断线、启动预热失败、页面超时和请求取消：隐藏公开数据浏览器在ERP服务器单任务内串行复用且最多25条主动回收，网络/浏览器异常后立即失效并在下一条重建；可见匿名库存浏览器仍逐链接隔离。所有浏览器对象在创建后立即纳入回收，退出时依次关闭 context、browser 和 driver；Takealot公开API或商品页403、真实导航失败、429及Takealot 5xx使用HTTP 503并计入连续连接失败，页面内容复核不确定使用409保留重试但不触发断网暂停。已清理历史遗留的57个项目 Playwright Node 驱动和274个Chrome子进程，系统物理内存占用由87.9%回落到69.1%、可用虚拟内存由1.5GB回升到10.6GB；真实Chrome启停验证前后项目进程数未增长
- 竞品批量采集在浏览器本地保存当前批次断点：冻结的链接顺序、已检查/失败索引、带原始URL的成功结果、结构化失败PLID/URL/原因、选项、停止原因、运行态、批次编号、标签页客户端编号和当前请求编号均可恢复。采集中整页刷新会自动回到竞品雷达；当服务端活动批次编号与本地断点一致时，当前标签页自动重连，先把服务端仍在执行的当前链接放到恢复队列首位并复用同一请求编号等待原探测任务，再回到最早失败项，避免旧请求与恢复队列并发争用Playwright；运行中后续新增的服务端队尾项按新增顺序追加进同一断点，不改变既有链接索引和顺序。人工停止及正常完成不会自动重启；ERP 服务重启后仍保留“继续失败/未完成”。恢复从最早失败或首个未执行位置开始，按当前批次成功 PLID 跳过中间已经成功的链接，只重试失败及未执行项；监控清单后续修改或删除不篡改本批已冻结队列。数据库旧快照不参与本批跳过判断。批次生命周期、逐链接阶段、结果、复用和异常写入`logs/competitor-collection.log`
- 竞品网络类暂停增加10分钟循环自动续爬：Takealot公开API及商品页403与导航失败、429、5xx统一视为可恢复连接异常；单链接先有限退避重试，任一成功立即清零连续失败次数，连续2条连接类失败后保存断点及下一次尝试时间，页面显示逐秒倒计时。暂停原因同时记录触发熔断的两个PLID、浏览器连接失败或HTTP状态及各自原因；新增链接、插队和库存轮后复探普通提示不参与连接失败计数。到时从失败/未完成项继续，VPN恢复后自动继续，仍为403或同类失败则重新安排10分钟。刷新页面继续原倒计时，关闭后重新打开且时间已到会立即尝试；其他批次占用时顺延10分钟。人工停止、链接校验/权限问题、确认失效和正常完成均清除或不创建自动计划，倒计时期间仍可人工提前继续
- 竞品首次或疑似失效链接的单次 API 404 不直接判为永久错误：同一 Playwright 浏览器检查目标商品页和最近成功商品对照，只有“目标明确404且对照正常”才累计一次有效证据；至少间隔10分钟累计3次才首次确认失效，内容不确定返回409保留重试。历史已持久确认为失效的链接在后续批次只需一次公开商品数据复核，再次返回404就立即维持确认失效并成为本批终态，不重复三次页面和对照检查；403、导航失败、429、5xx等连接异常仍进入待重试，后续采集成功自动恢复正常。MySQL继续保存复核次数、对照PLID和检查时间；前端“链接复核状态”默认收起且只保留疑似/确认数量，需要时才展开完整复核表，展开后的状态行可以直接打开该 PLID 的监控队列操作页
- 竞品雷达公开评论区支持评价分类与起止日期组合筛选，提供最新评论、最早评论、评分升序和评分降序四种展示排序，并显示当前结果数/评论总数；日期缺失评论只在未限定时间时保留
- 竞品最新状态已移除基于2%–5%假设评论率反推的“累计销量估算”列及说明，只保留评论数、库存变化、新增评论和观察期信号；历史数据库字段暂时保留用于兼容旧快照，但只读 API 不再投影给前端
- 竞品最新状态已增加按北京时间自然日的观察日期区间，首次默认覆盖全部可用快照日期。列表和详情接口按区间筛选每个商品的原始快照，以区间内最旧与最新快照实时重算商品主价格及同一报价身份的涨价/降价、价格差额，并重算库存净变化、库存净流出/补货、新增评论、观察期销量区间和趋势说明，不再复用采集写库时基于相邻两次快照保存的旧信号；商品价格信号可参与经营信号筛选，报价级信号在链接管理下拉中逐项展示，两者均不换算销量。价格、报价集合、库存和评论结构取区间内最新快照，详情时间线展示区间内每个原始快照及其全部变体明细。库存比较继续要求首尾变体键、SKU、卖家集合一致且两端库存精确，库存不可比不会阻止商品维度的新增评论信号
- 竞品最新状态现对真正竞品和自有商品都按PLID每个商品只创建一张父卡片，不把同一PLID的卖家报价或跨店重复Offer拆卡，也不按标题相似度跨PLID猜测合并；自有卡片按TSIN优先统计真实变体数。卡片内统一展示当前主报价及全部跟卖，逐条保留卖家、Offer ID、SKU、变体、图片、价格涨跌与独立库存状态/数量变化；卡片头汇总全部报价区间、主报价库存、评论评分及商品经营信号，旧快照没有可区分报价时保留主报价与原链接并明确不猜身份。搜索可匹配商品名称、PLID、完整Takealot链接及卡内任一卖家/Offer ID/SKU/变体；真正竞品另有按当前全部报价汇总的店铺选择器，可用标准化`sellers` ID或店铺名搜索，同一ID跨PLID去重并计数、同名不同ID不合并。经营信号可匹配商品或任一报价的价格/库存信号，并可叠加有货/没货/未探测库存状态；页面实时显示结果数/总数并提供一键清除，过期库存参考归入本次“未探测”。明细提供上一页/下一页和每页20/50/100张卡片，只创建当前页父卡片；筛选或切换每页数量自动回到第1页，“查看对应商品”会先切到目标所在页。筛选和分页只处理已读取列表且不触发外部采集或数据库写入
- 竞品最新状态的每张商品卡可直接打开唯一的完整详情弹窗，在当前视口集中展示商品价格、平台仓库存/过期参考、采集时间、经营信号、评论结构、完整变体库存与探测说明、库存及评论历史快照，并在弹窗内提供评论分类、起止日期组合筛选、日期/评分排序和评论全文；弹窗新增当前 PLID 的监控队列操作卡，复用链接管理的修改、删除、插队和重新加入能力。任务爬取详情中的全部成功与待重试链接同样可点击或用键盘打开该操作页，待重试来源提供“人工重试并插队”。原页面下方重复详情已删除，长内容仅在弹窗内部滚动，支持鼠标、键盘及Escape关闭
- 竞品链接新增和修改由前端与服务端双重检查 URL 格式、Takealot 域名和 PLID；外部平台链接无法写入。修改现有项必须保持原PLID，不同商品通过删除旧项并新增处理，避免历史商品身份被改写
- 旧 `data/takealot.db` 的12张表4204行已全量迁移到 MySQL 并逐表核对一致；MySQL 读写账号为受限应用账号，密码仅存在于 Git 忽略的本机 `.env`。ERP 只读连接已实测拒绝 UPDATE；每日备份使用 `mysqldump --single-transaction`，压缩为本机 `.sql.gz` 并配套 JSON 清单和 SHA-256 校验，不接入云端或异地
- 搜索定位已完成图片候选词、Takealot相关性验收和自然位闭环；类目榜单、卖家上新、Buy Box 历史占有率和六工作表 Excel 仍是后续范围，未在当前页面中标记为已完成

Windows 回归测试会启动真实的官方看板启动器、终止外层启动进程，并确认所有后代进程和监听端口同时消失。测试后未发现相关后台进程残留。

## 配置和安全状态

- `.env` 应位于项目根目录：`D:\南非店铺数据抓取\.env`。
- `.env` 已被 Git 忽略，CLI 和看板会从项目根目录自动加载，且不会覆盖已经存在的系统环境变量。
- 真实 Key 只能写入 Git 忽略的 `.env` 或受控环境变量，不能写入 `.env.example`、日志、报表、测试夹具、文档或版本库；搜索定位的两个 Windows 用户级 Key 由正式重启脚本按固定白名单缺失补载，且不回显值。
- API Key 由本机项目根目录 `.env` 提供；网页不会显示、保存或回传 Key。只有点击“立即刷新看板数据”、运行保留的 NFT102 命令行流程或每日计划任务时才触发对应只读采集。
- 当前正式数据库是本机 MySQL 8.0；自有店铺、指标和竞品共用 `takealot_ops`。SQLite 文件只作为迁移源和历史留档，不再被运行时读取或自动回退。
- 流量指标只能称为“近30天浏览量”，不能称为精确日流量或访客数。

## NFT102 兼容流程

统一 ERP 前端已移除 NFT102 续写模块。后台实现、`更新NFT102日报.bat` 和相关命令行能力仍保留；如需继续使用，由服务器本机执行兼容脚本。该流程不覆盖源文件，既有基准保存在 `data/nft102-baselines/`，生成结果保存在 `outputs/nft102-daily/YYYY-MM-DD/`。

## 正式文档与样例

- 产品需求：`docs/requirements/takealot-operations-dashboard-prd.md`
- 实施清单：`docs/implementation/implementation-plan.md`
- 合成数据 QA 样例：`artifacts/`

`artifacts/` 中所有内容均由合成测试数据生成，不包含真实店铺信息或 API Key。
