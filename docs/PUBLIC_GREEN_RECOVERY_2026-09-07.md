# 公网绿版502与雷达慢加载排障（2026-09-07）

后续状态：同日已迁移为SYSTEM开机回源18503，并部署性能监测/异常通知；下文18502及HKCU启动描述是首轮恢复历史。当前运维方式与未完成的DHCP地址保留见 [稳定性加固记录](PUBLIC_GREEN_STABILITY_2026-09-07.md)。

当前状态（11:28）：两条用户逐项批准的公司出口UDP规则均已应用，但Tailscale仍出现DERP(lax)/海外代理端点，不能声称UDP国内直连成功。绿版已改用经物理路由的加密SSH回源，公网大资源从25–43秒降至0.085–1.953秒，健康与鉴权检查通过。用户刷新公网后明确确认“已明显变快，两个分区都正常”，公网自有店铺、真正竞品体验已由用户验收。

## 已验证原因

- 正式地址为 `https://119.91.117.232/`。主机本地8501健康与账号准备接口200，MySQL正常，ERP错误日志为空。
- 腾讯Nginx在10:30–10:35记录主机 `100.70.103.11:8501` 连接超时，随后尝试已停用的笔记本 `100.122.102.37:8501`，最终报 `no live upstreams`/502。
- 主机经雪莲境外出口208.68.180.11建立Tailscale连接，实测325–500ms且有超时；绑定物理网卡直连腾讯公网的TCP连接约9ms。只绕过curl环境代理并不能绕过TUN路由。
- 腾讯控制台显示的7条规则没有UDP41641；云机tailscaled实际监听UDP41641。改走公司物理路由后，Tailscale仍经DERP(lax)，约0.4–1.8秒。一次端点重新发现和UDP重新绑定未建立直连；没有停止或重启Tailscale服务。
- 新建已登录局域网浏览器标签页实测：真正竞品GET约526ms、1115094字节；六店自有链接GET约2954ms、592980字节（日期2026-07-23至2026-09-07）。这只是本轮浏览器请求样本，不是全场景性能保证。

## 已实施

1. 经一次Windows UAC授权，使用 `scripts/ha/repair_main_tencent_route.ps1` 为 `119.91.117.232/32` 添加持久路由：物理以太网接口18、网关192.168.110.1。脚本执行前核验机器名、网卡、地址192.168.110.180、网关及已有冲突路由；有WhatIf模式，不改变默认路由。
2. 腾讯 `/etc/nginx/conf.d/takealot-erp-tailnet.conf` 只保留主机绿版上游，移除退役笔记本8501备用项；对应模板为 `scripts/ha/takealot_erp_tailnet_nginx.conf`。
3. 两份绿版Nginx配置增加 `proxy_set_header Connection ""`，使既有keepalive连接池能够复用连接；连接超时由3秒改为10秒。通过 `nginx -t` 后平滑reload，没有重启ERP。公网模板为 `scripts/ha/takealot_erp_public_nginx.conf`。
4. 第一轮腾讯蓝8443及两个原有网站配置在应用前后SHA-256一致。未改证书、认证、账号、MySQL角色或采集配置。
5. 用户真实登录腾讯控制台后，依次新增来源 `14.155.151.192/32` 和 `113.90.82.43/32`、UDP41641、允许规则，备注分别为 `Takealot Tailscale office UDP` 和 `Takealot Tailscale office UDP 2`。第二来源另行取得明确同意；控制台确认总计9条，原7条保留。直连SSH服务端观测公司TCP出口确实交替使用两IP。两规则只放行加密Tailscale传输端口，不公开ERP、数据库或SSH中继环回端口。
6. 规则生效后主机仍经DERP(lax)，反向探测曾指向海外代理208.68.180.11；Windows已有tailscaled进程UDP入站允许规则。未继续改动雪莲、系统默认路由或停止Tailscale。
7. 临时SSH回源测试：云机127.0.0.1:18502到主机127.0.0.1:8501，云端健康约0.05秒、299296字节雷达脚本约0.17秒。随后新增 `scripts/ha/run_main_green_public_tunnel.ps1` 和 `install_main_green_public_tunnel.ps1`：复用现有受控SSH密钥，严格校验主机身份、已知主机密钥及腾讯/32物理路由，绑定本机物理地址；云端仅监听127.0.0.1:18502。10秒心跳、连续3次失联退出、3秒后重连，互斥锁避免重复隧道。`status.json` 的 ssh_running 仅代表进程存活，不能代替ERP健康检查。
8. 11:22腾讯绿版上游优先使用127.0.0.1:18502，原主机100.70.103.11:8501只作备用（两条路径均指向同一正式ERP，非主备数据库切换）。nginx -t通过后平滑reload。未打开新的公网TCP业务端口；未重启ERP/MySQL/雪莲。

云端备份：`/var/backups/takealot-green-20260907-0242/`（最初tailnet.conf、public.conf；SSH切换前tailnet.before-ssh.conf）。运行证据：`logs/public-green-repair-20260907/`。

## 验证与边界

- 10:42:20后首批53条相关入口访问为49条200、3条正常未登录401、1条304，没有5xx；其中已包含实际登录后的雷达、关键词、版本检查和刷新状态读取。此为时段样本，不能当作长期稳定保证。
- 公网首页、health和auth/status均200，匿名auth/session和两个受保护业务GET为401；初轮小响应约0.49–2.34秒。浏览器已渲染公网登录页，但本任务尚未完成公网登录态雷达视觉验收。
- 公网入口、主JS、CSS和雷达分包最终均200且与本地SHA-256一致，JS/CSS类型正确；但129863字节主JS耗时25.361秒、173151字节CSS耗时43.492秒、299296字节雷达分包耗时33.537秒，另一次HEAD超时。用户反馈“能打开但是选品雷达模块的自有店铺与真正竞品打开时间超级久”。故不能仅凭小接口200关闭排障。
- 原采集批次 `scheduled-20260907-6acf1514f466`、修订40、第15轮和2467个目标顺序保持一致，记录从226增加至255，之前全部结果逐项保留；ERP PID13996、MySQL PID24628保持。没有新建、恢复、停止或重试采集批次。
- SSH切换后公网采样：首页0.089秒、health0.091秒、auth/status0.126秒；主JS0.091秒、CSS0.085秒、雷达分包1.953秒，全部200、资源SHA与本地一致、MIME正确。匿名session与两个业务GET均401。证书验证保持启用。证据 `public-resources-direct.json`；结果是同一公司网络样本，不代表所有客户端网络。
- SSH尚未接入正式入口时，仅终止本任务SSH子进程3896进行断线测试，监督进程18772自动拉起新子进程1588，云端健康恢复200；没有终止任何ERP或采集进程。重复启动脚本后确认仍只有一个隧道SSH。
- HKCU Run `TakealotGreenPublicTunnel` 已安装并核对，当前用户登录后启动；用户注销后的运行和整机重启恢复未实测。现有ERP亦依赖主机可用，本次不构成高可用切换。
- PowerShell语法、WhatIf、实机重连/单实例及Nginx语法通过；未修改Python/Vue/数据库，未执行完整应用回归，未提交推送。

## 验收与运维

- 两条已授权云防火墙规则均完成，不再等待控制台登录。用户已刷新公网并确认“已明显变快，两个分区都正常”。服务器11:27:32、11:27:33分别记录真正竞品和自有店铺GET200，返回1114522/592804字节；切换后74条采样为67条200、4条正常匿名401、3条304，无5xx。体验验收来自用户，本任务自身浏览器仅渲染到公网登录页。
- 当前直连回源依赖主机物理IP192.168.110.180、接口18、网关192.168.110.1及腾讯119.91.117.232/32持久路由。网络迁移后必须重新核验；不得直接扩大云防火墙来源。
- 隧道状态在 `%LOCALAPPDATA%\TakealotGreenPublicTunnel\status.json`，SSH诊断在同目录 `ssh.stderr.log`。重新安装先使用 `powershell.exe -NoProfile -File scripts/ha/install_main_green_public_tunnel.ps1 -WhatIf`，再执行不带WhatIf的同一命令。脚本不写入或输出密钥内容。
- 若要撤销SSH回源，先恢复云端 `tailnet.before-ssh.conf` 到绿版上游配置并经nginx -t后平滑reload，确认备用旧路径可用，再删除HKCU Run中精确的 `TakealotGreenPublicTunnel` 值，在上述状态目录创建名为 `stop` 的文件；监督进程会关闭自己创建的SSH子进程后退出。解除停用必须人工确认后移除该精确stop文件。不得批量终止ssh/powershell进程。
- 若要撤销最初网络修复，只移除119.91.117.232/32对应接口18/网关192.168.110.1的精确ActiveStore和PersistentStore路由，并恢复最初两份Nginx备份；必须先确认当前业务连接依赖。云防火墙仅按精确来源+UDP41641删除本次两条规则。不要改变默认网关、重启采集服务或恢复已退役笔记本8501作为正式备用。

SSH转发语义参照 [OpenSSH官方手册](https://man.openbsd.org/ssh)，与其他VPN共存的排障边界参照 [Tailscale官方说明](https://tailscale.com/docs/reference/faq/other-vpns)；本报告的速度及路径结论均来自本机实测。
