# 竞品雷达商品小卡紧凑化

## 22:59 追加调整：销售第二列与查询按钮上移

- 宽卡顺序统一为价格、销售情况、库存、类目、周期销售额、评论；类目仍为第四列。自有销售对比与竞品五窗口销售占同一第二列，窄屏继续按实际卡片宽度折行。
- 查询按钮利用价格下方空位，与其他指标共用卡片行，不再独占底部一整行；支持 subgrid 的浏览器让按钮跟随价格实际高度，兼容回退保留预留空间，避免遮挡内容。个人池查询按钮放到图片下方，右侧内容保持完整。
- 进一步收紧卡片边距、指标间距和五窗口行距，给固定第四列的类目更多宽度，减少长类目反复折行撑高卡片；未隐藏字段或缩短类目路径。
- 使用实际竞品卡及销售对比组件生成隔离 HTML 样例，Chromium 核对六种宽度（1200/1000/820/700/480/320px）、两类卡共12组调整前后布局；全部样例高度下降，列顺序、容器边界和按钮不遮挡价格/其他指标断言通过。另核对两种个人池宽度的图片/按钮/正文不重叠，并查看桌面截图。1200px样例竞品216.56→164.97px，自有265.34→224.34px；这些是合成数据样例，不代表全部真实商品或登录态浏览器验收。
- 最终35项相关回归、Vue类型与隔离构建通过。22:59仅替换8501紧凑样式链接，主JS及旧资源保留；新版CSS为 `radar-cards-compact-626e2b172e83-css-mime-v2.css`（8194字节），SHA-256 `626e2b172e831eede44c8828aa4868fd098d57f2685e56fdd5a2b255abee48fb`。本机/LAN首页、健康及CSS哈希/MIME共6项通过。证据、截图与回退首页保存在 `logs/compact-radar-cards-20260905-v2/`。

## 22:32 初版记录

- 新增独立的 `frontend/competitor/src/radarCardsCompact.css`，由 `main.ts` 在既有样式后引入，覆盖真正竞品、自有链接、个人监控池/共享库及复用卡片的目录、查询结果。
- 宽卡上下内边距从 10px 调至 8px，区块间距从 7px 调至 4px，图片从 52px 调至 44px；首次监控标签与日期横向排列，空间不足时自然换行。
- 库存观察的五个时间窗口改为横排，实际指标容器不足 210px 时按三列换行；整项移动，数据不足仍明确显示。自有官方/跟卖五行对比表仅压缩行距，不改变结构与数据。
- 个人池图片从 88px 调至 64px，缩小卡片、类目、指标及按钮区间距；卡片按各自内容高度排列，避免被同排长卡拉高。保留全部标题、类目层级、日期、指标和动作；触屏保留独立的按钮点击尺寸。
- 35 项既有卡片、类目、指标、详情回归通过，Vue 类型检查与隔离生产构建通过，CSS 压缩无警告。本次未新增复述 CSS 数值的测试；未执行浏览器渲染或真实手机验收，未声称具体整卡高度降幅。
- 2026-09-05 22:32 发布到本机/LAN 8501。仅在当前生产首页末尾追加独立内容哈希样式，保留原 JS 入口和全部旧资源；共享工作区其他未发布源码未随本次样式发布。后续常规构建会通过 `main.ts` 自动合并本样式。
- 发布文件 `radar-cards-compact-b40e6c8b32ae-css-mime-v2.css`，4354 字节，SHA-256 `b40e6c8b32ae9bf2b06fb3d67e492339bc1728b226bd2b8a833a7fb55fb5f9fe`。本机与 `192.168.110.180:8501` 首页、健康、样式 HTTP/MIME/哈希共 6 项检查通过，服务 PID 保持 22172，无后端重启。
- 构建、测试、发布清单、HTTP 核验和发布前首页保存在 `logs/compact-radar-cards-20260905/`。未操作独立蓝环境、数据库、采集或业务逻辑，未提交或推送共享工作区。
