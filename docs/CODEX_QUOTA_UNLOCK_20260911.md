# 标题分析解除系统额外周额度上限

2026-09-11，按用户要求取消标题优化中的“基线 + 10个百分点”硬上限。

- 根因：本地状态保留基线31%、旧上限41%，账号周用量91%时被ERP自设规则拦截。该规则比较的是共享账号整体周用量差，不能认定差值全部由ERP消耗。
- 新规则：标题识图、图题融合、竞品标题复核均使用原Sol high客户端和官方Codex额度。ERP不再附加10个百分点预算；官方七天窗口达到100%仍预检停止，官方其他限额和账号/模型可用性仍有效。
- 迁移：同一窗口保留baseline、started_at、reset时间及历史用量，忽略旧41%阈值，将`budget_percent`记为null、`system_budget_enforced`记为false，剩余量取100减官方已用百分比。原分析、失败记录和模型缓存不改写。
- BLUE共用同一个基础规则，双节点共享状态迁移已有回归覆盖；本次现场报错来自GREEN本地状态，本次发布范围为GREEN，未同步或重启BLUE。
- 已停用Terra自动全量重跑的历史授权字段保持兼容，不启动旧批次；未消费额度重置权益，不改模型，不新增推理请求。

## 验证

- 相关后端236项通过，包含旧状态91%/99%放行、100%拦截、周窗口更换、共享节点基线、模型切换、识图/复核和批次逻辑；Ruff、该源文件Mypy、Vue类型及隔离构建通过。
- 真实Sol客户端只执行模型可用性及额度预检：北京时间18:28，官方周用量92%、剩余8%，`status=active`；原31%基线、开始时间和2026-09-15T01:49:02Z窗口重置时间保留。预检通过不等于已重新完成商品分析。
- 页面仅替换三处额度说明，从当前GREEN部署图派生28文件范围包；14项改名JS语法检查通过，其他代码仅替换资源引用，旧资源保留。隔离构建验证了三处新文本确实来自当前源码。
- 首次测试命令误填不存在的标题测试文件，未执行任何测试；更正为`test_title_optimization.py`后236项通过。
- 重载前检查共享目录的另一个雷达任务改动：101项通过，1项因当时尚未写完整的测试样例缺少URL失败；该任务随后补齐样例，单独1项和所在文件2项复测通过。三个运行源文件与冻结清单相同，Ruff通过；本任务没有改写或提交这些雷达改动。
- GREEN在`active_item=null`的持久断点通过`scripts/restart_erp.ps1`正式重载。原批次`scheduled-20260911-30ea3b9ad1ab`、revision45、round18、目标及612条既有结果全部保留，状态仍running；原队列已留备份。
- 本机、局域网、公网三入口84项当前资源HTTP/MIME/SHA及健康检查通过；本机和公网的授权标题详情GET均200，原成功分析89仍可读取。未触发模型重分析，未做浏览器实点验收。

证据：`logs/codex-quota-unlock-20260911/`中的`backend-tests.log`、`live-preflight.json`、`typecheck.log`、`build.log`、`transform.json`、`source-manifest.json`、发布包和原始状态备份。

持久技能已补充本次授权的适用范围及迁移、核验要求：`C:/Users/Mayn/.codex/skills/takealot-erp-verified-release/SKILL.md`。
