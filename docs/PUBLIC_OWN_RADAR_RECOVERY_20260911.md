# 2026-09-11 公网选品雷达自有链接超时

## 故障证据

用户明确指选品雷达的自有链接列表。正式 GREEN 公网入口为 `https://119.91.117.232`，与 `:8443` BLUE 区分。

- 原始公网自有列表请求 75.138 秒超时，本机同一路径 25.572 秒超时；同批真正竞品预览 1.366 秒返回。健康接口约 0.24 秒不代表业务正常。
- 腾讯 Nginx 记录自有列表 102.939 秒完成、64.777/77.622/208.608 秒客户端中断；真正竞品也有一次严格读取 228.312 秒。上游指向 `127.0.0.1:18503`，不是只看客户端猜测公网拥堵。
- 只读线程快照显示请求等待物化列表的 Future；后台在读取按 PLID 分批历史。原始权限摘要相同，但晚间店铺刷新改变全局 Offer 身份摘要，产生多个新缓存键；旧键在五分钟活跃期内继续排队计算，新的自有请求没有完整预览。
- 默认日期原来加载完整 Offer 后按每家店重复查询历史范围，一次实测 38.583 秒。合并窄查询后实测 0.698 秒；两次测量时的整机负载不同，不能把比例归因于单条 SQL。
- 18:00 正常六店刷新于 18:24:36 结束，exit=4；其中第一店质量校验失败，其余五店成功。没有重跑或更改该流程。运行中的竞品采集单独按持久化安全边界处理。

## 修复范围

- 自有列表的完整旧快照按有效用户权限、选中店铺和店铺目录隔离。普通 Offer 身份变化进入数据版本及卡片指纹，更新后台数据，不再使合法的完整预览失效；保留生成时间及正在刷新标志。
- 真正竞品继续使用全局自有身份作为预览隔离边界，防止新自有商品被旧公共快照误当作竞品；旧非分页接口也有独立的回归用例。
- 日期范围查询保留实际授权店铺及当前 PLID 约束，改为目录、窄身份和三次日期聚合，共五次数据库读取；不改变历史区间、销量或缺失值口径。
- 不增加线程、历史批量、缓存范围和磁盘上限；不修改业务数据、监控目标、前端文件或代理配置。

## 验证与发布

83 项相关后端检查通过，Ruff 和三个源码文件 Mypy 通过。第一次重载后的 diff 审查发现非分页边界改动落在相似的公共处理块，已恢复公共边界、将变更限定到自有处理块，并新增真实 API 回归检查；第二次正式安全重载以最终源码为准。首次重载证据保留在 `logs/public-own-links-20260911/first-reload/`。

最终正式重载和三入口验证通过。当前入口图为28个文件，共84次资源SHA/MIME检查及3次健康检查；并行任务发布了配额提示修复，最初沿用旧标题版本的静态清单因此出现index SHA不符，已保留旧清单并重新冻结、核对实际完整资源图，本任务没有发布前端。

| 公网业务范围 | 数量 | 预览耗时 | 严格读取耗时 |
| --- | ---: | ---: | ---: |
| 全部自有链接，后台生成推进后 | 1090 | 1.608秒 | 0.720秒 |
| 受限账号经营店铺，后台生成推进后 | 517 | 0.400秒 | 0.274秒 |
| 真正竞品，后台生成推进后 | 1502 | 0.197秒 | 0.180秒 |

全店、受限经营店铺、真正竞品的生成时间均较首次验收推进，非仅复用同一个准备好的版本。受限账号报价只含store-02/store-05，单店仅current；本机/LAN全店预览及严格读取0.510–1.055秒。未进行浏览器实点。

发布后的首次全店完整重建57.930秒，随后一次严格请求还等待71.188秒；这些首轮耗时没有删除。修复解决正常刷新时反复丢失完整自有预览，首次新权限/新范围或发布后的完整计算仍可能需要约一分钟，尚未优化为任意冷查询秒开。

原scheduled-20260911-30ea3b9ad1ab/revision45/round18、目标、615条既有结果和队列全部核对保留，交付前推进至623条且running；正常排队目标41条不变。只加载正式GREEN，BLUE本轮未同步。

已将自有完整预览与公开身份隔离、分页/旧接口同时验收及默认日期查询计时规则补充到持久技能 `C:/Users/Mayn/.codex/skills/takealot-erp-verified-release/SKILL.md` 并核对。

证据目录：`logs/public-own-links-20260911/`。临时接口核验会话只保留哈希并在脚本 finally 中删除；不记录 Cookie 或上游凭据。
