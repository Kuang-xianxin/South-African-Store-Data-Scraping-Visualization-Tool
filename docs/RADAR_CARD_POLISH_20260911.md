# 雷达小卡渐进优化

2026-09-11 14:57，正式 GREEN 已静态发布。

用户要求先 push 当前项目，再逐步完善截图中的小卡，并保护正在使用的用户。修改前的完整项目基线已提交、推送并核对远端 main：`3a1ed4229dd3703d327d5a95f52765a0b1bcfc74`。基线包含1367个文件变更，441项前端测试通过；凭据、数据库、断点、日志、备份和依赖仍按忽略规则排除。

## 本次行为

- 自有主列表复用公共宽卡片；主列表和竞品查询中的公共卡片取消整卡点击及键盘伪按钮。
- “查看详情”和“竞品查询”放在卡片底部并排，保留原有详情新标签页/叠加弹窗、查询和类目入口。
- 标题可在新标签页打开接口保存的完整平台地址。只接受 HTTPS、官方域名及匹配 PLID 的路径；缺失时保留文字并提示，不猜造链接。
- 价格下增加“单件预计利润”。本阶段点击“查看利润”读取现有鉴权详情接口，在卡内显示 NF 运营表模型的单件预计利润、利润率、计算售价和可核算报价数量。
- 多店报价分别计算并显示范围，不相加。缺失成本、旧服务缺少模型输出、负利润、零利润和部分覆盖均保留，不能回退为毛利或视为实际结算净利润。
- 普通列表首屏、排序和翻页不自动请求所有商品详情。重复点击合并；切换店铺/商品/报价、退出及卸载取消旧读取。更新失败保留上次结果并说明，支持重试。
- 局部增大标题及正文、行高、间距和按钮，保留销量表、库存、类目、评论、时间和数据缺失依据。提供窄屏重排、键盘焦点和触屏点击尺寸。

本次范围是宽卡片渐进优化，未切换此前预览的整页表格，也未改个人监控池的小网格和独立类目结果卡片。BLUE 的后端成本模型及历史前端版本与 GREEN 不同，本次只发布正式 GREEN，不把整套新产物直接覆盖双 BLUE。

## 验证

- 前端448项通过，Vue类型检查和隔离生产构建通过，Git差异检查通过。
- 新增7项测试涵盖平台地址、负利润/零值/部分覆盖、缺失模型、独立按钮、首次无请求、重复点击、取消旧请求、失败保留结果及重试；共享组件重构同步调整原有契约，Windows测试读取统一换行。
- 本机及公网8次鉴权详情读取成功，覆盖两款截图商品的全店和单店范围；每个可见 Offer 的运营表结果与现有权威计算器逐项一致。单店 `current` 范围没有这两款商品时，接口返回0个报价，没有越界混入其他店铺。
- PLID100149090：全店范围1个报价，售价R514，单件预计利润R205.455810192，利润率39.971947508%。
- PLID100204852：全店范围2个报价，售价R513–640，单件预计利润R204.672981392–304.092238992，利润率39.897267328–47.514412343%。这些是本次核对时模型估算，不是结算利润。
- 两款商品列表接口保存的标题地址分别是官方 `/p/PLID100149090` 和 `/p/PLID100204852`。域名及标识已核对；自动HTTP访问均403，未确认浏览器实际打开成功。
- 正式发布前源文件哈希与上次NF发布记录完全一致，没有夹带其他前端变更；本次在独立worktree构建，28个当前入口/JS/CSS文件冻结清单后原子切换入口。
- 本机8501、局域网8501、公网443共87项当前资源HTTP/MIME/SHA及健康检查通过；27个旧JS/CSS在三入口另81项保留核验通过。
- ERP和MySQL监听进程PID保持，未重启服务、强制刷新客户端或触发额外业务采集。原批次 `scheduled-20260911-30ea3b9ad1ab`、revision45、round18、目标、405条既有结果及队列完整保留，核对时已推进至407条且running。

本轮没有浏览器实点、屏幕截图或真实用户帧率测量；组件交互和HTTP验证不能等同于这些验收。

## 发布与回退依据

- 证据目录：`logs/radar-card-polish-20260911/`，包括原始失败测试、最终测试、隔离构建、业务核对、平台地址、资源清单、进程和采集连续性。
- 当前入口：`assets/index-B5wq9rGp.js`；雷达页面：`assets/CompetitorsPage-CCZCu14D.js`。
- `green.zip`：28文件，SHA256 `2582d86de031decb0171c9fdc750bfb6ef59a82065c81ded58867002c8792b9f`。
- 原入口与发布清单：`green-backup/index.html`、`green-backup/manifest.json`。所有旧哈希资源保留；如需回退，先核对现用入口仍是本次版本，再原子恢复备份入口，避免覆盖后续发布。
- 持久发布技能已同步渐进卡片、明确详情按钮、平台原址、利润依据与按需读取、先基线push及静态发布保留旧资源的规则：`C:/Users/Mayn/.codex/skills/takealot-erp-verified-release/SKILL.md`。
