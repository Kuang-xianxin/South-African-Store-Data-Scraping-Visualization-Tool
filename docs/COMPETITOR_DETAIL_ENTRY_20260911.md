# 竞品查询结果详情入口修复

2026-09-11 11:55，GREEN、主机BLUE及笔记本BLUE已发布。

## 原因与修改

截图商品为PLID95895158。竞品查询按独立轻量目录和当前页完整卡片返回结果，但普通雷达入口的openCategoryProductDetail原先只在独立详情/详情内查询时打开catalogDetailPlid叠加详情；普通查询回落到openProductDetail。后者的selected只从当前主列表、个人池和既有嵌入商品中取值，结果卡不在这些集合时，detailModalOpen虽然开启，selected仍为空，v-if阻止弹窗显示。

现在竞品查询和类目结果中的真正竞品一律使用既有按PLID读取的叠加完整详情，覆盖页外且不在个人池的商品，保留底层来源商品、搜索、页码及关闭/焦点逻辑。自有商品仍走既有授权新标签页路径；接口权限、计算口径、采集与数据写入没有改动。

## 验证

- 新增两项真实入口回归：普通竞品查询和类目查询中的页外商品。修复前两项均失败，修复后前端全量425项通过；Vue类型检查和隔离生产构建通过。
- 对两版实际发布产物的原/新函数运行8项行为检查，验证页外竞品从旧列表回落切换到按PLID叠加详情，自有链接授权路径保持。新JS语法解析通过。
- GREEN管理员和现有受限账号分别在本机/公网读取截图商品及下方PLID98503922的卡片与详情，共12次GET200。95895158有18条历史快照，最近2026-09-10 12:01:26北京时间；98503922有2条历史。测试临时会话在finally删除，没有修改账号权限。
- BLUE三个入口均返回95895158卡片及13条详情历史；其独立快照最近2026-09-04 04:29:21北京时间。首次同时查询两商品返回404，后核实98503922不在BLUE目录；保留初次失败日志，仅对BLUE实际可见商品验证，不导入GREEN业务数据或冒充蓝数据已更新。
- GREEN本机8501、LAN8501、公网443和BLUE主机8503、笔记本8502、公网8443，各28个HTML/JS/CSS文件的HTTP状态、MIME、SHA及health通过，共174项。没有执行浏览器实点、外部Takealot页面打开或无关后端全套验证。

## 发布与连续性

工作区存在未提交的共享依赖，隔离构建用于源码验证。运行发布从每版当前完整资源图出发，只通过语法树删除详情入口的一段错误条件；其余修改仅传播新文件名引用，反向恢复引用后其余内容逐字节不变。三节点均28文件，新增资源先写入校验，检查旧index哈希未被并发改动后原子发布；保留旧哈希资源，无ERP/MySQL/worker重启。

原批次scheduled-20260911-30ea3b9ad1ab、revision45、round18与全部目标保持；257条既有结果完整保留，核对时260条且running，原队列与已尝试集合保持。未触发平台采集、模型、刷新或数据库角色操作。

证据目录为logs/competitor-detail-entry-20260911/，含before、源码补丁、回归/构建日志、业务接口结果、compiled-behavior.json、两版transform/zip清单、六入口检查和batch-continuity.json。回退HTML位于green-backup、blue-main-backup，以及笔记本D:/TakealotBlue/staging/pre-detail-entry-20260911-1153。

## 文档、技能与Git

AGENT.md最后更新、相关状态、修改日志及README/PROJECT_STATUS同步。持久发布技能C:/Users/Mayn/.codex/skills/takealot-erp-verified-release/SKILL.md补充“查询结果详情不依赖主列表页和个人池，须验证页外结果”的规则。

当前HEAD的openCategoryProductDetail尚不包含本次所复用的叠加详情实现，且现有回归测试文件未跟踪；单独提交本次条件删除无法形成可独立构建的提交。已保留本任务开始基线和task-source.patch，未混合提交或推送其他任务工作。
