# NF主表映射优先规则

2026-09-11用户明确要求解决截图中的对应冲突，所有公司SKU与990对应以 `D:/doc/NF毛利计算.xlsx` 的「成本&在库统计」为准。该要求覆盖之前按其他子表差异暂停计算的规则。原文件SHA256仍为 `d457c453c64307d623543892ddfacfefda58f4bb632e1f3087d45ec75e7018c1`。

## 核对结果

主表B列公司SKU、D列平台SKU共976个有效去重990，没有主表内部矛盾；与正式MySQL现有976项映射全部一致，没有需要重新分配的数据库记录。此次同步的是主表裁决依据和计算资格，没有将正确映射做无意义重写。

原17项中，15项主表有明确对应，解除其他子表造成的冲突限制。截图 `9902245237368` 以主表第199行为准，对应 `NFT101-CWXQ-ORANGE`。`9902307554501` 和 `9902271464806` 不在主表，改为“主表未收录”，不从其他子表猜配。对应关系清楚不代表每项都具有有效售价或完整费用参数，其他缺失仍如实提示。

编译器只读取主表明确的SKU字段；重复同一对应合并保留行号，多SKU单元格按分隔符拆分，主表内部一对多、混合备注或列结构变化拒绝编译。模型快照升级为版本2，保留全部976项主表对应及来源行号。运行时逐Offer核对主表和系统公司SKU，主表未收录/系统不一致明确返回原因；不再维护固定的历史冲突编号名单。

741条利润记录的公式参数、输入、费用覆盖值、源结果和64条源异常完全未变。主表裁决仅改变SKU对应依据，不改汇率、箱规、费用或历史成本版本。

## 验证及覆盖

- 724项NF利润、旧利润兼容、配柜及主档相关回归通过，包含677条有效源行复算；新增主表裁决、过时冲突名单不再阻断、缺失主表、主表内部冲突及多SKU解析检查。沿用本任务上一轮全量2014项通过的基线，本次新增分支做针对性回归。
- 新快照与原快照741条profiles逐项完全相同，只有映射依据及版本变化。Ruff/Mypy通过。
- 全店审核改用明确的Core表查询，避免默认ORM店铺范围缩小PLID集合。当前1056个PLID/1848个Offer、1195个正售价Offer，其中822个能唯一匹配并计算。前次675个Offer记录属于当时默认店铺PLID集合，不能称全系统覆盖。
- 截图准确Offer `seller-api:current:224523736`、PLID99235899：售价R352、采购及头程¥63.80、表内总成本¥112.12379385、人民币利润¥19.19928308、利润率13.75584998%。同PLID其他店铺不借用这条990的参数。

## 发布

正式GREEN已生效。持久化active_item=null后原子替换已验证配置，通过正式restart_erp.ps1加载并健康。原scheduled-20260911-30ea3b9ad1ab、revision45、round18、targets及441条已有结果完全保留，原队列已备份，采集running继续。

本机和公网的全店/当前店铺四次鉴权详情请求全部逐Offer复算一致；截图商品的公司SKU、主表第199行来源、R352对应人民币利润19.19928308及利润率13.75584998%均验证通过。三入口28项当前HTML/JS/CSS逐项HTTP/MIME/SHA及健康检查共87项通过。核验会话已清理；原前端未改动，未执行本轮浏览器实点或同步BLUE。

部署源码/配置哈希、旧配置、主表预检、全店Core覆盖、接口结果及连续性保存在 `logs/nf-authority-20260911/`。只提交本任务明确文件，保留并行BLUE任务改动。

持久ERP技能和用户电子表格规则已更新：明确指定主表即构成跨表冲突裁决授权，不再重复确认或以旧冲突名单阻断正确对应。
