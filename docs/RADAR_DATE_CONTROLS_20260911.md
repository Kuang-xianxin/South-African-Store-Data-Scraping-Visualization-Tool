# 雷达列表日期控件精简

2026-09-11 11:42，已发布GREEN、主机BLUE及笔记本BLUE。

按用户截图，移除个人监控池和雷达主列表里的开始日期、结束日期及“按区间重算”。源码同时移除不再使用的草稿日期状态和重算操作。继续使用原默认观察范围，保留实际范围说明、分区数量、“清除筛选”、销量周期排序，以及商品详情图表和跟卖历史的独立日期选择；不改变库存观察、官方销量、覆盖缺失和截止日口径。

## 验证与发布

- 前端423项测试通过；只调整既有日期重算测试的过时断言，保留两分区使用同一日期请求的检查。Vue类型检查及隔离生产构建通过，CodeGraph最终up to date，任务相关git diff --check通过。
- 工作区存在其他任务改动，隔离构建只用于源码验证。部署从每版当时正在运行的完整资源图出发，按JavaScript语法树只删除两组日期输入、分隔符和重算按钮节点，保留清除筛选节点。其他资源仅传播新文件名引用，反向还原引用后与原代码逐字节一致；所有新JavaScript语法解析通过。
- 三节点入口在本次冻结时一致，各为28文件。原资源保留；新增资源先写入并校验，检查旧入口SHA未被并发修改后原子切换。各版完整SHA清单位于`logs/radar-date-controls-20260911/*-transform.json`及两个发布包内的`manifest.json`。
- GREEN本机8501、LAN8501、公网443，BLUE主机8503、笔记本8502、公网8443，各28项HTML/JS/CSS HTTP状态、MIME、SHA校验及health均通过，总计174项。未做本轮浏览器实点、认证业务接口或无关后端全套验证。
- 原`scheduled-20260911-30ea3b9ad1ab`、revision45、round18、2625个目标保持；254条既有结果逐条保留，检查时255条且running，原已尝试与排队集合保持。未重启服务、改动数据库/复制身份、控制采集或启动模型。
- 准备阶段修正过输出日志相对路径、Node依赖相对路径和SSH PowerShell引号问题；这些失败未执行服务发布。正式三节点安装及六入口验证均通过。

## 交付记录

证据目录：`logs/radar-date-controls-20260911/`。包括任务前源码/文档快照、`task-source.patch`、423项前端日志、隔离构建日志、发布前资源图、两版发布包、逐入口核验记录和`batch-continuity.json`。

GREEN与主机BLUE旧入口分别保存在该证据目录的`green-backup/`和`blue-main-backup/`；笔记本位于`D:/TakealotBlue/staging/pre-radar-dates-20260911-1140/`。旧哈希资源未删除，回退仅需在核对当前入口后恢复对应旧HTML，不回滚数据库或采集断点。

持久发布技能`C:/Users/Mayn/.codex/skills/takealot-erp-verified-release/SKILL.md`已补充本次列表日期精简要求和详情日期独立边界。项目`AGENT.md`、`README.md`及`docs/PROJECT_STATUS.md`同步记录。

Git交付边界：源码与文档包含大量其他任务尚未提交的修改和依赖，不能将本轮删除作为可独立构建的HEAD补丁；保留工作区与独立源码差异，未混合提交或推送。
