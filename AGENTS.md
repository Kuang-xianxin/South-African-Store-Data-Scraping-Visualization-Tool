# Codex 项目入口

在对本项目进行分析、实现、修复、重构或文档修改前，必须完整阅读项目根目录的 `AGENT.md`。

强制要求：只要本任务修改了本项目的源码、测试、配置、脚本、依赖、数据库结构、业务规则或项目文档，就必须在同一个任务中同步更新 `AGENT.md` 的“最后更新”、相关状态信息和“修改日志”。结束前使用 `git diff` 核对，不得在缺少 `AGENT.md` 更新时宣称任务完成。

具体业务口径、安全边界、验证命令和历史决策均以 `AGENT.md` 为准。

本项目已初始化 CodeGraph。当前会话提供 `codegraph_explore` 时，分析架构、调用链、业务流程或改动影响应先用一次图谱查询取得相关源码、关系和影响摘要，再精读目标文件；CLI 回退和项目专用查询示例见 `docs/CODEGRAPH_AI_GUIDE.md`。CodeGraph 只辅助导航，不替代 `AGENT.md`、测试、数据库核对、浏览器验收或正式 ERP 健康检查。
